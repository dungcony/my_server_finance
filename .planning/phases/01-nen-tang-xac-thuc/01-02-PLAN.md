---
phase: 01-nen-tang-xac-thuc
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/main/java/com/datn/financeapp/common/response/ApiResponse.java
  - src/main/java/com/datn/financeapp/common/response/ErrorResponse.java
  - src/main/java/com/datn/financeapp/common/response/PageMeta.java
  - src/main/java/com/datn/financeapp/common/response/PageRequestParams.java
  - src/main/java/com/datn/financeapp/common/exception/BusinessException.java
  - src/main/java/com/datn/financeapp/common/exception/GlobalExceptionHandler.java
  - src/main/resources/messages_vi.properties
  - src/main/java/com/datn/financeapp/common/security/JwtService.java
  - src/main/java/com/datn/financeapp/common/security/JwtAuthFilter.java
  - src/main/java/com/datn/financeapp/common/security/SecurityConfig.java
  - src/main/java/com/datn/financeapp/common/security/SecurityContextUtil.java
  - src/test/java/com/datn/financeapp/common/security/JwtServiceTest.java
  - src/test/java/com/datn/financeapp/common/exception/GlobalExceptionHandlerTest.java
autonomous: true
requirements: [CORE-01, CORE-02, AUTH-08]

must_haves:
  truths:
    - "Gọi bất kỳ endpoint lỗi validate nào trả về đúng khung {success:false, error:{code:'VALIDATION_ERROR', fields:[...]}} với TẤT CẢ lỗi trường trong một lần, không dừng ở lỗi đầu tiên"
    - "JWT access token sinh ra chỉ chứa sub (user id), claim plan, và exp — không có trường nhạy cảm khác"
    - "Token hết hạn hoặc chữ ký sai bị JwtAuthFilter từ chối, không set SecurityContext"
  artifacts:
    - path: "src/main/java/com/datn/financeapp/common/response/ApiResponse.java"
      provides: "Response wrapper thành công {success:true, data}"
      contains: "record ApiResponse"
    - path: "src/main/java/com/datn/financeapp/common/exception/GlobalExceptionHandler.java"
      provides: "@RestControllerAdvice chuyển mọi exception thành {success:false, error}"
      contains: "@RestControllerAdvice"
    - path: "src/main/java/com/datn/financeapp/common/security/JwtService.java"
      provides: "Sinh/verify JWT bằng jjwt 0.13.x API mới"
      contains: "Jwts.parser()"
    - path: "src/main/java/com/datn/financeapp/common/security/SecurityConfig.java"
      provides: "SecurityFilterChain STATELESS, đăng ký JwtAuthFilter"
      contains: "SessionCreationPolicy.STATELESS"
  key_links:
    - from: "GlobalExceptionHandler.handleValidation"
      to: "MethodArgumentNotValidException"
      via: "@ExceptionHandler convert sang error.fields"
      pattern: "MethodArgumentNotValidException"
    - from: "JwtAuthFilter"
      to: "SecurityContextHolder"
      via: "set Authentication sau khi verify token hợp lệ"
      pattern: "SecurityContextHolder.getContext\\(\\).setAuthentication"
---

<objective>
Dựng tầng `common/` dùng chung cho toàn bộ dự án: response wrapper thống nhất `{success,data}`/`{success,error}` (CORE-01), khung phân trang dùng lại từ Phase 2 (CORE-02), và hạ tầng bảo mật JWT (JwtService sinh/verify token theo đúng API jjwt 0.13.x, JwtAuthFilter, SecurityConfig STATELESS) làm nền cho AUTH-08 và mọi endpoint có `Authorization: Bearer`.

Purpose: Đây là tầng mà MỌI controller (Plan 04, 05, và toàn bộ Phase 2-5 sau này) import trực tiếp — sai ở đây sẽ lan ra toàn dự án.
Output: `common/response/`, `common/exception/`, `common/security/` hoàn chỉnh, có unit test cho JwtService và GlobalExceptionHandler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-CONTEXT.md
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-RESEARCH.md
@D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-01-SUMMARY.md

<interfaces>
<!-- pom.xml (Plan 01) đã có: jjwt-api/impl/jackson 0.13.0, spring-boot-starter-security,
     spring-boot-starter-validation, lombok. application.yml có jwt.secret=${JWT_SECRET}. -->

Khung response chuẩn theo `api/00-QUY-UOC-CHUNG.md` mục 4:
```json
// Thành công
{ "success": true, "data": { } }
// Thất bại
{ "success": false, "error": { "code": "...", "message": "...", "detail": {} } }
// Thất bại nhiều lỗi validate
{ "success": false, "error": { "code": "VALIDATION_ERROR", "message": "...", "fields": [{"field":"amount","message":"..."}] } }
```

Mã lỗi dùng chung (mục 6, api/00) — GlobalExceptionHandler phải map đúng các mã này:
```
VALIDATION_ERROR   400
UNAUTHENTICATED    401
TOKEN_EXPIRED      401
TOKEN_INVALID      401
FORBIDDEN          403
NOT_FOUND          404
DUPLICATE          409
RATE_LIMIT_EXCEEDED 429
INTERNAL_ERROR     500
REQUEST_IN_PROGRESS 409   -- MỚI, thêm ở Plan 03 (D-14) — GlobalExceptionHandler Plan này chỉ cần cấu trúc BusinessException tổng quát đủ để Plan 03 ném mã này mà không cần sửa lại handler
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Response wrapper + GlobalExceptionHandler + message tiếng Việt</name>
  <read_first>
    D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md (mục 4, 5, 6)
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-RESEARCH.md (Pattern 1)
  </read_first>
  <files>
    src/main/java/com/datn/financeapp/common/response/ApiResponse.java, src/main/java/com/datn/financeapp/common/response/ErrorResponse.java, src/main/java/com/datn/financeapp/common/response/PageMeta.java, src/main/java/com/datn/financeapp/common/response/PageRequestParams.java, src/main/java/com/datn/financeapp/common/exception/BusinessException.java, src/main/java/com/datn/financeapp/common/exception/GlobalExceptionHandler.java, src/main/resources/messages_vi.properties, src/test/java/com/datn/financeapp/common/exception/GlobalExceptionHandlerTest.java
  </files>
  <behavior>
    - Test: `POST` tới controller giả lập (hoặc `@WebMvcTest` với controller test-only) gửi DTO thiếu 2 trường bắt buộc → response JSON có `success=false`, `error.code=VALIDATION_ERROR`, `error.fields` chứa đúng 2 phần tử.
    - Test: ném `BusinessException("EMAIL_ALREADY_EXISTS", 409, "Email đã có người dùng.")` từ controller giả lập → response HTTP 409, `error.code=EMAIL_ALREADY_EXISTS`.
    - Test: exception không xác định (`RuntimeException` bất kỳ) → response HTTP 500, `error.code=INTERNAL_ERROR`, KHÔNG lộ message/stack trace gốc ra `error.message` (dùng message tiếng Việt chung chung).
  </behavior>
  <action>
**`ApiResponse.java`** (package `common.response`):
```java
public record ApiResponse<T>(boolean success, T data) {
    public static <T> ApiResponse<T> of(T data) {
        return new ApiResponse<>(true, data);
    }
}
```

**`PageMeta.java`**:
```java
public record PageMeta(int page, int pageSize, long totalItems, int totalPages) {}
```

**`PageRequestParams.java`** — dùng lại từ Phase 2 (CORE-02), chưa có endpoint dùng ở Phase 1 nhưng phải dựng sẵn:
```java
public record PageRequestParams(int page, int pageSize, String sortBy, String sortOrder) {
    public static final int DEFAULT_PAGE = 1;
    public static final int DEFAULT_PAGE_SIZE = 20;
    public static final int MAX_PAGE_SIZE = 100;

    public static PageRequestParams of(Integer page, Integer pageSize, String sortBy, String sortOrder) {
        int p = (page == null || page < 1) ? DEFAULT_PAGE : page;
        int ps = (pageSize == null || pageSize < 1) ? DEFAULT_PAGE_SIZE : Math.min(pageSize, MAX_PAGE_SIZE);
        String order = "asc".equalsIgnoreCase(sortOrder) ? "asc" : "desc";
        return new PageRequestParams(p, ps, sortBy, order);
    }
}
```

**`ErrorResponse.java`**:
```java
public record ErrorResponse(boolean success, ErrorBody error) {
    public record ErrorBody(String code, String message, List<FieldError> fields, Object detail) {
        public ErrorBody(String code, String message) { this(code, message, null, null); }
        public ErrorBody(String code, String message, Object detail) { this(code, message, null, detail); }
    }
    public record FieldError(String field, String message) {}
}
```

**`BusinessException.java`** (`RuntimeException`, dùng cho mọi lỗi nghiệp vụ có mã + HTTP status xác định):
```java
package com.datn.financeapp.common.exception;

import lombok.Getter;

@Getter
public class BusinessException extends RuntimeException {
    private final String code;
    private final int httpStatus;
    private final Object detail;

    public BusinessException(String code, int httpStatus, String message) {
        this(code, httpStatus, message, null);
    }

    public BusinessException(String code, int httpStatus, String message, Object detail) {
        super(message);
        this.code = code;
        this.httpStatus = httpStatus;
        this.detail = detail;
    }
}
```

**`GlobalExceptionHandler.java`** (`@RestControllerAdvice`):
- `@ExceptionHandler(MethodArgumentNotValidException.class)` → HTTP 400, `error.code="VALIDATION_ERROR"`, `error.message="Dữ liệu gửi lên không hợp lệ."`, `error.fields` = map từ `ex.getBindingResult().getFieldErrors()` — LẤY HẾT MỌI FIELD ERROR (dùng `.stream().map(...).toList()`, không `.findFirst()`).
- `@ExceptionHandler(BusinessException.class)` → `ResponseEntity.status(ex.getHttpStatus())`, `error.code=ex.getCode()`, `error.message=ex.getMessage()`, `error.detail=ex.getDetail()`.
- `@ExceptionHandler(AuthenticationException.class)` (Spring Security) → HTTP 401, `error.code="UNAUTHENTICATED"`.
- `@ExceptionHandler(AccessDeniedException.class)` → HTTP 403, `error.code="FORBIDDEN"`.
- `@ExceptionHandler(Exception.class)` (catch-all cuối cùng) → HTTP 500, `error.code="INTERNAL_ERROR"`, `error.message="Đã có lỗi xảy ra, vui lòng thử lại sau."` — **log đầy đủ exception thật ở tầng log (SLF4J `log.error(...)`) nhưng KHÔNG đưa message/stack gốc vào response** (chống rò rỉ thông tin nội bộ — V7 ASVS Error Handling).

**`messages_vi.properties`** (src/main/resources) — placeholder ban đầu cho các message dùng chung, ví dụ:
```properties
error.validation=Dữ liệu gửi lên không hợp lệ.
error.internal=Đã có lỗi xảy ra, vui lòng thử lại sau.
error.unauthenticated=Vui lòng đăng nhập để tiếp tục.
error.forbidden=Bạn không có quyền truy cập tài nguyên này.
```
Không bắt buộc dùng `MessageSource` đầy đủ ở Phase 1 (không có yêu cầu đa ngôn ngữ thật) — chỉ cần file tồn tại làm chỗ chứa cho các message dùng chung, GlobalExceptionHandler có thể hardcode chuỗi tiếng Việt trực tiếp trong Java, miễn nội dung khớp file này.

**`GlobalExceptionHandlerTest.java`** — dùng `@WebMvcTest` với một `@RestController` test-only inner class định nghĩa ngay trong file test (không đụng tới controller thật nào), expose 3 endpoint giả: 1 endpoint nhận `@Valid` DTO có 2 trường `@NotBlank`, 1 endpoint ném `BusinessException`, 1 endpoint ném `RuntimeException` trần. Dùng `MockMvc` gửi request và assert JSON response bằng `jsonPath`.
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server &amp;&amp; mvn -q test -Dtest=GlobalExceptionHandlerTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q test -Dtest=GlobalExceptionHandlerTest` exit code 0
    - `GlobalExceptionHandler.java` chứa `@RestControllerAdvice` và ba `@ExceptionHandler` cho `MethodArgumentNotValidException`, `BusinessException`, `Exception`
    - Test case validate nhiều trường xác nhận `error.fields` có đúng 2 phần tử (không phải 1)
    - Test case `RuntimeException` xác nhận response KHÔNG chứa message gốc của exception (chỉ chứa message tiếng Việt chung)
  </acceptance_criteria>
  <done>`common/response/` và `common/exception/` hoàn chỉnh, mọi lỗi validate trả hết một lượt trong `error.fields`, không rò rỉ chi tiết lỗi hạ tầng ra response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JwtService + JwtAuthFilter + SecurityConfig</name>
  <read_first>
    D:\PTIT\DATN\api\01-XAC-THUC.md (mục "Ghi chú triển khai" — nội dung JWT)
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-RESEARCH.md (Pattern 2, "Anti-Patterns to Avoid")
  </read_first>
  <files>
    src/main/java/com/datn/financeapp/common/security/JwtService.java, src/main/java/com/datn/financeapp/common/security/JwtAuthFilter.java, src/main/java/com/datn/financeapp/common/security/SecurityConfig.java, src/main/java/com/datn/financeapp/common/security/SecurityContextUtil.java, src/test/java/com/datn/financeapp/common/security/JwtServiceTest.java
  </files>
  <behavior>
    - Test: `generateAccessToken(userId, "free")` sinh chuỗi JWT hợp lệ; `parseAndValidate` trên chuỗi đó trả về `Claims` có `subject == userId.toString()`, claim `plan == "free"`.
    - Test: token hết hạn (tạo với `expiration` trong quá khứ bằng cách gọi biến thể test-only hoặc mock `Instant`) ném `ExpiredJwtException` khi parse.
    - Test: token bị sửa 1 ký tự trong phần chữ ký ném `SignatureException`.
    - Test: `parseAndValidate` KHÔNG chấp nhận token ký bằng thuật toán `none`/HS384 khác key — chỉ HS256 với đúng secret cấu hình (chống JWT algorithm confusion).
  </behavior>
  <action>
**`JwtService.java`** (package `common.security`) — dùng ĐÚNG API jjwt 0.13.x (KHÔNG `Jwts.parserBuilder()`/`setSigningKey()` — đã lỗi thời):
```java
package com.datn.financeapp.common.security;

import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.security.Keys;
import io.jsonwebtoken.security.MacAlgorithm;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;

import javax.crypto.SecretKey;
import java.time.Instant;
import java.util.Base64;
import java.util.Date;
import java.util.UUID;

@Service
public class JwtService {

    private static final MacAlgorithm ALG = Jwts.SIG.HS256;

    private final SecretKey key;
    private final long accessTokenExpirySeconds;

    public JwtService(
            @Value("${jwt.secret}") String base64Secret,
            @Value("${jwt.access-token-expiry-seconds:3600}") long accessTokenExpirySeconds) {
        // D-01: JWT_SECRET bắt buộc qua application.yml -> ${JWT_SECRET}, không default ở đây cho giá trị secret
        this.key = Keys.hmacShaKeyFor(Base64.getDecoder().decode(base64Secret));
        this.accessTokenExpirySeconds = accessTokenExpirySeconds;
    }

    public String generateAccessToken(UUID userId, String plan) {
        Instant now = Instant.now();
        return Jwts.builder()
                .subject(userId.toString())
                .claim("plan", plan)
                .issuedAt(Date.from(now))
                .expiration(Date.from(now.plusSeconds(accessTokenExpirySeconds)))
                .signWith(key, ALG)
                .compact();
    }

    public Claims parseAndValidate(String token) {
        return Jwts.parser()
                .verifyWith(key)
                .build()
                .parseSignedClaims(token)
                .getPayload();
    }
}
```
Nếu `JWT_SECRET` trong `.env` chưa đủ độ dài base64 cho HS256 (>=32 byte sau decode), `Keys.hmacShaKeyFor` ném `WeakKeyException` ngay lúc khởi động bean — đây là hành vi ĐÚNG (fail-fast, không cần try/catch nuốt lỗi).

**`SecurityContextUtil.java`** — helper lấy `userId` hiện tại từ `SecurityContextHolder`, dùng lại ở mọi Service/Aspect cần biết "current user":
```java
package com.datn.financeapp.common.security;

import org.springframework.security.core.context.SecurityContextHolder;
import java.util.UUID;

public final class SecurityContextUtil {
    private SecurityContextUtil() {}

    public static UUID currentUserId() {
        Object principal = SecurityContextHolder.getContext().getAuthentication().getPrincipal();
        return UUID.fromString(principal.toString());
    }
}
```

**`JwtAuthFilter.java`** (`OncePerRequestFilter`) — đọc header `Authorization: Bearer <token>`, verify bằng `JwtService`, set `SecurityContextHolder` với principal = userId (String), authorities rỗng (Phase 1 chưa cần role-based, chỉ cần biết "đã đăng nhập"). Nếu thiếu header hoặc token không hợp lệ → KHÔNG set SecurityContext, để `chain.doFilter` tiếp tục (Spring Security tự trả 401 ở entry point cho endpoint yêu cầu auth). Bắt riêng `ExpiredJwtException` để (tuỳ chọn) set request attribute phân biệt `TOKEN_EXPIRED` vs `TOKEN_INVALID` cho entry point dùng sau — GlobalExceptionHandler Task 1 đã có `@ExceptionHandler(AuthenticationException.class)`.

**`SecurityConfig.java`** — `SecurityFilterChain` STATELESS:
```java
http.csrf(csrf -> csrf.disable())
    .sessionManagement(sm -> sm.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
    .authorizeHttpRequests(auth -> auth
        .requestMatchers("/auth/register", "/auth/login", "/auth/refresh",
                          "/auth/forgot-password", "/auth/reset-password").permitAll()
        .anyRequest().authenticated())
    .addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class);
```
Bean `PasswordEncoder` = `new BCryptPasswordEncoder(12)` (D-01 tài liệu yêu cầu cost ≥12) đặt trong `SecurityConfig` để Plan 04 (AuthService) inject dùng ngay.

**`JwtServiceTest.java`** — unit test thuần (không cần Spring context đầy đủ, khởi tạo `new JwtService(testSecret, 3600)` trực tiếp), test theo 4 behavior đã liệt kê. Với test "token hết hạn", tạo `JwtService` phụ với `accessTokenExpirySeconds = -10` (đã hết hạn ngay lúc sinh) thay vì mock `Instant`.
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server &amp;&amp; mvn -q test -Dtest=JwtServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q test -Dtest=JwtServiceTest` exit code 0
    - `JwtService.java` chứa chuỗi `Jwts.parser()` và `.verifyWith(key)`, KHÔNG chứa `parserBuilder` hay `setSigningKey`
    - `SecurityConfig.java` chứa `SessionCreationPolicy.STATELESS`
    - `SecurityConfig.java` chứa `BCryptPasswordEncoder(12)` (cost factor ≥12 theo `api/01` "Ghi chú triển khai")
    - Test xác nhận token với claim `plan` giải mã đúng giá trị đã set lúc sinh
  </acceptance_criteria>
  <done>JWT sinh/verify đúng API jjwt 0.13.x, access token chỉ chứa id/plan/exp theo AUTH-08, SecurityFilterChain STATELESS sẵn sàng cho Plan 03/04 gắn thêm filter và controller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → `Authorization: Bearer` header | Input không tin cậy — token có thể giả mạo, hết hạn, hoặc bị sửa |
| `GlobalExceptionHandler` → response JSON | Ranh giới rò rỉ thông tin nội bộ — mọi exception nội bộ phải được lọc trước khi ra ngoài |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | JWT algorithm confusion (kẻ tấn công đổi `alg` sang `none` hoặc thuật toán yếu hơn) | mitigate | `Jwts.parser().verifyWith(key)` của jjwt 0.13.x mặc định từ chối `alg=none` và ép đúng thuật toán đã ký (HS256) — không tự ý parse claims trước khi verify chữ ký |
| T-02-02 | Information Disclosure | `GlobalExceptionHandler` lộ stack trace/message nội bộ qua response 500 | mitigate | Task 1: catch-all `Exception.class` trả message tiếng Việt chung chung cố định, log chi tiết thật ở SLF4J server-side only |
| T-02-03 | Spoofing | JWT secret yếu (ngắn, đoán được) cho phép giả mạo token | mitigate | D-01 + `Keys.hmacShaKeyFor` ném `WeakKeyException` fail-fast nếu secret <32 byte sau decode base64 — không có fallback âm thầm |
| T-02-04 | Elevation of Privilege | Access token nhét thêm claim nhạy cảm (role admin giả mạo) do dev vô tình thêm sau này | mitigate | AUTH-08: `generateAccessToken` chỉ nhận đúng 2 tham số `(userId, plan)`, không có cơ chế thêm claim tuỳ ý — buộc mọi thay đổi sau này phải sửa method signature, dễ review |

</threat_model>

<verification>
- `mvn -q test -Dtest=GlobalExceptionHandlerTest,JwtServiceTest` xanh
- `mvn -q -DskipTests compile` không lỗi (đảm bảo SecurityConfig/JwtAuthFilter compile đúng với dependency Plan 01 đã thêm)
</verification>

<success_criteria>
1. Mọi response lỗi (validate, business, lỗi hệ thống) đều đi qua GlobalExceptionHandler và đúng khung CORE-01
2. JWT sinh/verify đúng chuẩn AUTH-08, không dùng API jjwt cũ
3. SecurityFilterChain STATELESS, endpoint auth công khai đã liệt kê đúng permitAll, còn lại yêu cầu Bearer token
</success_criteria>

<output>
After completion, create `.planning/phases/01-nen-tang-xac-thuc/01-02-SUMMARY.md`
</output>
