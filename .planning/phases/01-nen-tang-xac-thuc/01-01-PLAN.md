---
phase: 01-nen-tang-xac-thuc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/java/com/datn/financeapp/FinanceAppApplication.java
  - src/main/resources/application.yml
  - src/main/resources/application-dev.yml
  - src/main/resources/application-test.yml
  - docker-compose.yml
  - .env.example
  - .env
  - .gitignore
  - README.md
  - src/main/java/com/datn/financeapp/auth/entity/User.java
  - src/main/java/com/datn/financeapp/auth/entity/RefreshToken.java
  - src/main/java/com/datn/financeapp/auth/entity/PasswordResetToken.java
  - src/main/java/com/datn/financeapp/auth/entity/LoginAttempt.java
  - src/main/java/com/datn/financeapp/common/idempotency/IdempotencyKeyEntity.java
  - src/main/java/com/datn/financeapp/common/wallet/WalletMinimal.java
  - src/test/java/com/datn/financeapp/SchemaSmokeTest.java
autonomous: true
requirements: [CORE-09, CORE-10, CORE-11]

must_haves:
  truths:
    - "Chạy `mvn -q spring-boot:run` (hoặc test context) khởi động thành công, Flyway áp dụng đủ V1 đến V7 lên PostgreSQL sạch mà không lỗi checksum/thứ tự"
    - "`mvn -q test -Dtest=SchemaSmokeTest` xanh — context Spring khởi động được với `ddl-auto=validate`, nghĩa là entity JPA khớp đúng cột thật trong V1/V7 (kể cả kiểu INET, JSONB)"
    - "Dev mới clone repo đọc README.md làm theo đúng thứ tự lệnh là chạy được app, không cần đoán"
  artifacts:
    - path: "pom.xml"
      provides: "Khai báo Maven groupId/artifactId, toàn bộ dependency đã verify version"
      contains: "bucket4j_jdk17-core"
    - path: "src/main/resources/application.yml"
      provides: "Cấu hình chung — datasource/flyway/jwt đọc từ biến môi trường bắt buộc, không default"
      contains: "filesystem:../../db/migration"
    - path: "docker-compose.yml"
      provides: "Postgres dev container đọc .env"
    - path: ".env.example"
      provides: "Mẫu biến môi trường commit vào repo"
    - path: "README.md"
      provides: "Trình tự khởi động: copy .env.example, docker compose up, mvn spring-boot:run"
    - path: "src/main/java/com/datn/financeapp/auth/entity/User.java"
      provides: "Entity users khớp V1 (email lowercase, plan check, password_hash)"
    - path: "src/main/java/com/datn/financeapp/auth/entity/RefreshToken.java"
      provides: "Entity refresh_tokens khớp V7 (token_hash unique, revoked_at nullable)"
    - path: "src/main/java/com/datn/financeapp/auth/entity/PasswordResetToken.java"
      provides: "Entity password_reset_tokens khớp V7"
    - path: "src/main/java/com/datn/financeapp/auth/entity/LoginAttempt.java"
      provides: "Entity login_attempts khớp V7 (ip_address INET, không FK user)"
    - path: "src/main/java/com/datn/financeapp/common/idempotency/IdempotencyKeyEntity.java"
      provides: "Entity idempotency_keys khớp V7 (response_body JSONB, composite unique)"
  key_links:
    - from: "src/main/resources/application.yml"
      to: "../../db/migration"
      via: "spring.flyway.locations=filesystem:../../db/migration"
      pattern: "filesystem:\\.\\./\\.\\./db/migration"
    - from: "src/test/java/.../SchemaSmokeTest.java"
      to: "src/main/resources/application-test.yml"
      via: "Testcontainers @ServiceConnection + @SpringBootTest khởi động context, buộc Hibernate validate schema"
      pattern: "@SpringBootTest"
---

<objective>
Dựng khung project Spring Boot từ số không (greenfield — `source/server/` hiện chỉ có `.planning/`, `CLAUDE.md`, `.git`), cấu hình môi trường theo D-01 đến D-05, xác minh migration V1-V7 chạy sạch trên PostgreSQL thật (D-06/D-07), và viết đủ entity JPA tối thiểu để chứng minh `ddl-auto=validate` không crash lúc khởi động — đặc biệt các cột kiểu đặc biệt (`INET`, `JSONB`) đã cảnh báo trong RESEARCH.md (Assumption A5).

Purpose: Đây là nền móng bắt buộc trước khi viết bất kỳ business logic nào — Plan 02+ sẽ import trực tiếp các entity/cấu hình tạo ở đây.
Output: `pom.xml` chạy được, 2 profile `dev`/`test`, Docker Compose Postgres, entity `User`/`RefreshToken`/`PasswordResetToken`/`LoginAttempt`/`IdempotencyKeyEntity` khớp đúng schema thật, test smoke xanh, README hướng dẫn khởi động.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-CONTEXT.md
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-RESEARCH.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\db\README.md

<interfaces>
<!-- Cột thật trong V1 (users, wallets) và V7 (4 bảng hạ tầng) — executor PHẢI dùng đúng tên/kiểu này, không suy đoán -->

Từ `db/migration/V1__nen_tang.sql` — bảng `users`:
```sql
CREATE TABLE users (
    id              UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    email           VARCHAR(255) NOT NULL,           -- UNIQUE, CHECK (email = lower(email))
    password_hash   VARCHAR(255) NOT NULL,
    full_name       VARCHAR(100) NOT NULL,
    avatar_url      TEXT,
    plan            VARCHAR(20)  NOT NULL DEFAULT 'free',  -- CHECK IN ('free','premium')
    created_at      TIMESTAMPTZ  NOT NULL DEFAULT now(),
    last_login_at   TIMESTAMPTZ
);
```

Từ `db/migration/V1__nen_tang.sql` — bảng `wallets` (chỉ dùng tối thiểu ở AUTH-01, KHÔNG dựng package `wallet/`):
```sql
CREATE TABLE wallets (
    id               UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id          UUID,        -- FK users, CASCADE
    group_id         UUID,        -- FK groups, CASCADE — Phase 1 luôn NULL
    name             VARCHAR(50)  NOT NULL,
    type             VARCHAR(20)  NOT NULL,           -- CHECK IN ('cash','bank','e_wallet','credit_card')
    initial_balance  BIGINT       NOT NULL DEFAULT 0,
    current_balance  BIGINT       NOT NULL DEFAULT 0,
    include_in_total BOOLEAN      NOT NULL DEFAULT TRUE,
    icon             VARCHAR(50),
    color            CHAR(7),
    sort_order       INTEGER      NOT NULL DEFAULT 0,
    is_deleted       BOOLEAN      NOT NULL DEFAULT FALSE,
    created_at       TIMESTAMPTZ  NOT NULL DEFAULT now()
    -- CHECK ck_wallets_owner: đúng 1 trong user_id/group_id có giá trị
);
```

Từ `db/migration/V7__ha_tang_xac_thuc.sql` — 4 bảng đầy đủ:
```sql
CREATE TABLE refresh_tokens (
    id          UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id     UUID         NOT NULL,                -- FK users, CASCADE
    token_hash  VARCHAR(64)  NOT NULL,                -- UNIQUE (uq_rt_hash)
    expires_at  TIMESTAMPTZ  NOT NULL,
    revoked_at  TIMESTAMPTZ,                          -- NULL = còn hiệu lực
    device_info VARCHAR(255),
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT now()
);

CREATE TABLE idempotency_keys (
    id              UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    idempotency_key VARCHAR(255) NOT NULL,
    user_id         UUID         NOT NULL,            -- FK users, CASCADE
    endpoint        VARCHAR(255) NOT NULL,
    status          VARCHAR(20)  NOT NULL DEFAULT 'processing',  -- CHECK IN ('processing','completed')
    response_status INTEGER,
    response_body   JSONB,
    created_at      TIMESTAMPTZ  NOT NULL DEFAULT now()
    -- UNIQUE uq_idem_scope (idempotency_key, user_id, endpoint)
);

CREATE TABLE password_reset_tokens (
    id         UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id    UUID        NOT NULL,                  -- FK users, CASCADE
    token_hash VARCHAR(64) NOT NULL,                  -- UNIQUE (uq_prt_hash)
    expires_at TIMESTAMPTZ NOT NULL,
    used_at    TIMESTAMPTZ,                            -- NULL = chưa dùng
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE login_attempts (
    id           UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    email        VARCHAR(255) NOT NULL,               -- KHÔNG FK tới users
    ip_address   INET,
    succeeded    BOOLEAN      NOT NULL,
    user_agent   VARCHAR(255),
    attempted_at TIMESTAMPTZ  NOT NULL DEFAULT now()
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Khởi tạo Maven project + cấu hình môi trường + Docker Compose</name>
  <read_first>
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-RESEARCH.md (mục "Standard Stack — Installation", "Cấu hình .env & Flyway (D-01, D-02, D-09)")
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-CONTEXT.md (D-01 đến D-09)
    D:\PTIT\DATN\db\README.md (mục "Cách chạy" — xác nhận cú pháp Flyway locations)
  </read_first>
  <files>
    pom.xml, src/main/java/com/datn/financeapp/FinanceAppApplication.java, src/main/resources/application.yml, src/main/resources/application-dev.yml, src/main/resources/application-test.yml, docker-compose.yml, .env.example, .env, .gitignore, README.md
  </files>
  <action>
Tạo Maven project mới bằng tay (không dùng Spring Initializr CLI vì có thể không có mạng ổn định — viết trực tiếp `pom.xml`):

**`pom.xml`** — `groupId=com.datn`, `artifactId=financeapp`, `packaging=jar`, `java.version=17`. Parent `org.springframework.boot:spring-boot-starter-parent:3.4.13`. Dependencies:
```xml
<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-data-jpa</artifactId></dependency>
<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-security</artifactId></dependency>
<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-validation</artifactId></dependency>
<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-aop</artifactId></dependency>
<dependency><groupId>org.postgresql</groupId><artifactId>postgresql</artifactId><scope>runtime</scope></dependency>
<dependency><groupId>org.flywaydb</groupId><artifactId>flyway-core</artifactId></dependency>
<dependency><groupId>org.flywaydb</groupId><artifactId>flyway-database-postgresql</artifactId></dependency>
<dependency><groupId>io.jsonwebtoken</groupId><artifactId>jjwt-api</artifactId><version>0.13.0</version></dependency>
<dependency><groupId>io.jsonwebtoken</groupId><artifactId>jjwt-impl</artifactId><version>0.13.0</version><scope>runtime</scope></dependency>
<dependency><groupId>io.jsonwebtoken</groupId><artifactId>jjwt-jackson</artifactId><version>0.13.0</version><scope>runtime</scope></dependency>
<dependency><groupId>com.bucket4j</groupId><artifactId>bucket4j_jdk17-core</artifactId><version>8.19.0</version></dependency>
<dependency><groupId>com.github.ben-manes.caffeine</groupId><artifactId>caffeine</artifactId></dependency>
<dependency><groupId>me.paulschwarz</groupId><artifactId>springboot3-dotenv</artifactId><version>4.0.0</version></dependency>
<dependency><groupId>org.mapstruct</groupId><artifactId>mapstruct</artifactId><version>1.6.3</version></dependency>
<dependency><groupId>org.projectlombok</groupId><artifactId>lombok</artifactId><optional>true</optional></dependency>
<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
<dependency><groupId>org.springframework.security</groupId><artifactId>spring-security-test</artifactId><scope>test</scope></dependency>
<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-testcontainers</artifactId><scope>test</scope></dependency>
<dependency><groupId>org.testcontainers</groupId><artifactId>junit-jupiter</artifactId><scope>test</scope></dependency>
<dependency><groupId>org.testcontainers</groupId><artifactId>postgresql</artifactId><scope>test</scope></dependency>
```
`dependencyManagement` thêm BOM `org.testcontainers:testcontainers-bom:2.0.5` (scope import). `annotationProcessorPaths` trong `maven-compiler-plugin` phải có cả `lombok` VÀ `mapstruct-processor:1.6.3` (thứ tự: lombok trước mapstruct-processor, theo tài liệu MapStruct để tránh lỗi builder không sinh).

**QUAN TRỌNG — verify tên artifact `springboot3-dotenv`:** trước khi ghi vào `pom.xml`, chạy `mvn dependency:get -Dartifact=me.paulschwarz:springboot3-dotenv:4.0.0` (hoặc kiểm tra qua `curl https://repo1.maven.org/maven2/me/paulschwarz/springboot3-dotenv/4.0.0/`) để xác nhận artifact tồn tại đúng tên (RESEARCH.md đánh dấu `[ASSUMED]` — A1). Nếu không tồn tại, thử `spring-dotenv` gốc hoặc dùng cơ chế `EnvironmentPostProcessor` tự viết đọc file `.env` bằng `java.util.Properties`/dotenv thủ công thay thế — chọn phương án chạy được, không dừng lại hỏi.

**`FinanceAppApplication.java`** — class `@SpringBootApplication` chuẩn, package `com.datn.financeapp`.

**`application.yml`** (chung, dùng biến môi trường bắt buộc — KHÔNG viết `${VAR:default}`):
```yaml
spring:
  application:
    name: financeapp
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    locations: filesystem:../../db/migration
    encoding: UTF-8
    baseline-on-migrate: false

jwt:
  secret: ${JWT_SECRET}
  access-token-expiry-seconds: 3600
  refresh-token-expiry-days: 30
```
`application-dev.yml`: không cần override gì đặc biệt ngoài `spring.profiles.active` mặc định — chỉ cần file tồn tại (dùng cho log level `DEBUG` cho package `com.datn.financeapp` nếu muốn, không bắt buộc).
`application-test.yml`: cấu hình rỗng hoặc tối giản — Testcontainers dùng `@ServiceConnection` tự động override `spring.datasource.*`, nhưng vẫn phải giữ `spring.flyway.locations: filesystem:../../db/migration` (kế thừa từ `application.yml` là đủ, không cần lặp lại trừ khi cần override).

**Đường dẫn Flyway `filesystem:../../db/migration`:** đường dẫn tương đối này đúng khi working directory là `source/server/`. Executor phải verify bằng cách chạy thật cả `mvn spring-boot:run` (Task 2) VÀ `mvn test` (Task 3) — nếu 1 trong 2 kịch bản lỗi "location does not exist", chuyển sang đường dẫn tuyệt đối tính từ `System.getProperty("user.dir")` qua một `EnvironmentPostProcessor`, hoặc dùng biến môi trường `DB_MIGRATION_PATH` do `.env` cấp — chọn phương án chạy ổn định cho cả 2 kịch bản, ghi lại lựa chọn cuối trong SUMMARY.

**`docker-compose.yml`**:
```yaml
services:
  postgres:
    image: postgres:16
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
```

**`.env.example`** (commit):
```
DB_URL=jdbc:postgresql://localhost:5432/finance_app
DB_USERNAME=postgres
DB_PASSWORD=changeme
JWT_SECRET=
POSTGRES_DB=finance_app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
```

**QUAN TRỌNG — thứ tự bắt buộc: tạo `.gitignore` TRƯỚC, ghi `.env` SAU.** Không được để tồn tại bất kỳ khoảnh khắc nào mà `.env` nằm trên đĩa mà `.gitignore` chưa chặn nó — nếu chạy `git status`/`git add` giữa 2 bước, `.env` có thể vô tình bị staged.

**`.gitignore`** (tạo mới trong `source/server/`, file này hiện chưa tồn tại — TẠO TRƯỚC `.env`):
```
target/
.env
*.class
.idea/
*.iml
```

**`.env`** (tạo SAU `.gitignore` ở trên; KHÔNG commit — dùng để dev chạy thử ngay trong task này): copy từ `.env.example`, điền `JWT_SECRET` bằng giá trị sinh từ `openssl rand -base64 32` (hoặc tương đương PowerShell nếu `openssl` không có: `[Convert]::ToBase64String((1..32|%{Get-Random -Max 256}))`). Ngay sau khi tạo, chạy `git status --short` xác nhận `.env` KHÔNG xuất hiện trong output (đã bị `.gitignore` chặn) trước khi tiếp tục sang bước khác.

**`README.md`** (D-05 — bắt buộc, tiêu chí "backend chạy được" chưa đạt nếu thiếu): mô tả đúng trình tự — (1) `git clone` xong đứng ở `source/server/`, (2) `cp .env.example .env` rồi điền `JWT_SECRET` (gợi ý lệnh sinh), (3) `docker compose up -d`, (4) `mvn spring-boot:run` (hoặc `mvn spring-boot:run -Dspring-boot.run.profiles=dev`), (5) cách chạy test `mvn test` (tự dùng Testcontainers, cần Docker chạy sẵn). Ghi rõ yêu cầu môi trường: Java 17, Maven, Docker Desktop.
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server &amp;&amp; mvn -q -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>
    - `pom.xml` tồn tại, chứa chuỗi `bucket4j_jdk17-core` và `flyway-database-postgresql`
    - `mvn -q -DskipTests compile` exit code 0
    - `application.yml` chứa chuỗi `filesystem:../../db/migration` và không chứa bất kỳ `${DB_URL:` hay `${JWT_SECRET:` nào (không có giá trị default)
    - `.env` tồn tại nhưng KHÔNG nằm trong `git status --short` output (đã bị `.gitignore` chặn)
    - `.env.example` tồn tại và nằm trong `git status --short` (chưa add, nhưng không bị ignore)
    - `README.md` chứa cả 4 cụm từ: `.env.example`, `docker compose up`, `mvn spring-boot:run`, `mvn test`
  </acceptance_criteria>
  <done>Project Maven biên dịch được, cấu hình môi trường đầy đủ theo D-01 đến D-05, Docker Compose sẵn sàng, README hướng dẫn đủ để dev mới clone chạy được ngay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Viết entity tối thiểu khớp schema thật + verify migration V1-V7 qua Testcontainers</name>
  <read_first>
    D:\PTIT\DATN\db\migration\V1__nen_tang.sql
    D:\PTIT\DATN\db\migration\V6__va_loi_bao_mat.sql
    D:\PTIT\DATN\db\migration\V7__ha_tang_xac_thuc.sql
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-RESEARCH.md (mục "Entity ↔ schema mapping", "Pitfall 4")
  </read_first>
  <files>
    src/main/java/com/datn/financeapp/auth/entity/User.java, src/main/java/com/datn/financeapp/auth/entity/RefreshToken.java, src/main/java/com/datn/financeapp/auth/entity/PasswordResetToken.java, src/main/java/com/datn/financeapp/auth/entity/LoginAttempt.java, src/main/java/com/datn/financeapp/common/idempotency/IdempotencyKeyEntity.java, src/main/java/com/datn/financeapp/common/wallet/WalletMinimal.java, src/test/java/com/datn/financeapp/SchemaSmokeTest.java
  </files>
  <behavior>
    - Test: `SchemaSmokeTest` khởi động `@SpringBootTest` context với Testcontainers PostgreSQL 16, Flyway tự chạy V1→V7 lên DB rỗng của container, Hibernate `ddl-auto=validate` phải PASS (không ném `SchemaManagementException`) — nghĩa là mọi entity khớp đúng cột thật.
    - Test: context load thành công (`applicationContext != null`) chứng minh không có lỗi cấu hình Flyway locations/bean nào chặn khởi động.
  </behavior>
  <action>
Viết entity JPA tối thiểu (chỉ field cần cho Phase 1, không thêm quan hệ `@OneToMany` không cần thiết) cho 5 bảng, đối chiếu chính xác với SQL đã đọc:

**`User.java`** (`@Entity @Table(name = "users")`, package `auth.entity`): `id` (UUID, `@Id`, không `@GeneratedValue` — để DB tự sinh qua `gen_random_uuid()`, dùng `@GeneratedValue(strategy = GenerationType.UUID)` CHỈ áp dụng khi insert từ Java không truyền id — quyết định: set `id` bằng `UUID.randomUUID()` ở tầng Service trước khi save, để Hibernate không phải đoán chiến lược sinh, tránh xung đột với `DEFAULT gen_random_uuid()` của cột), `email` (`String`, `nullable=false`, `unique=true` — nhưng annotate `@Column(name="email")`, KHÔNG thêm `@Email` validation ở entity, việc đó thuộc DTO), `passwordHash` (`@Column(name="password_hash")` String), `fullName` (`@Column(name="full_name")` String), `avatarUrl` (`@Column(name="avatar_url", columnDefinition="TEXT")` String, nullable), `plan` (String, default không cần set ở Java vì DB có default), `createdAt` (`Instant`, `@Column(name="created_at")`), `lastLoginAt` (`@Column(name="last_login_at")` Instant, nullable). Dùng Lombok `@Getter @Setter @Builder @NoArgsConstructor @AllArgsConstructor`.

**`RefreshToken.java`** (`@Table(name = "refresh_tokens")`): `id` (UUID), `userId` (`@Column(name="user_id")` UUID — dùng UUID trần thay vì `@ManyToOne` để tránh lazy-loading phức tạp không cần thiết ở Phase 1), `tokenHash` (`@Column(name="token_hash")` String), `expiresAt` (`@Column(name="expires_at")` Instant), `revokedAt` (`@Column(name="revoked_at")` Instant, nullable), `deviceInfo` (`@Column(name="device_info")` String, nullable), `createdAt` (Instant).

**`PasswordResetToken.java`** (`@Table(name = "password_reset_tokens")`): `id`, `userId`, `tokenHash`, `expiresAt`, `usedAt` (`@Column(name="used_at")` Instant nullable), `createdAt`.

**`LoginAttempt.java`** (`@Table(name = "login_attempts")`): `id`, `email` (String, KHÔNG FK), `ipAddress` — cột Postgres kiểu `INET`, Hibernate không có type chuẩn cho nó. Dùng `@JdbcTypeCode(SqlTypes.INET)` (Hibernate 6.2+ hỗ trợ trực tiếp `SqlTypes.INET` map sang `String` field) — import `org.hibernate.annotations.JdbcTypeCode` và `org.hibernate.type.SqlTypes`, field kiểu `String ipAddress`. Nếu `SqlTypes.INET` không tồn tại trong version Hibernate mà Spring Boot 3.4.13 kéo theo (verify bằng cách thử compile), dùng fallback `@Column(name="ip_address", columnDefinition = "inet")` kèm `@JdbcTypeCode(SqlTypes.OTHER)`. `succeeded` (`Boolean`, nullable=false), `userAgent` (`@Column(name="user_agent")` String, nullable), `attemptedAt` (`@Column(name="attempted_at")` Instant).

**`IdempotencyKeyEntity.java`** (`@Table(name = "idempotency_keys")`, package `common.idempotency`): `id`, `idempotencyKey` (`@Column(name="idempotency_key")` String), `userId` (UUID), `endpoint` (String), `status` (String), `responseStatus` (`@Column(name="response_status")` Integer, nullable), `responseBody` — cột `JSONB`. Dùng `@JdbcTypeCode(SqlTypes.JSON)` (Hibernate 6+ built-in, KHÔNG cần thư viện `hibernate-types` ngoài) trên field `String responseBody` (lưu JSON dạng chuỗi, serialize/deserialize bằng Jackson ở tầng Service — Plan 03 sẽ dùng). `createdAt` (Instant).

**`WalletMinimal.java`** (package `common.wallet` — theo Architectural Responsibility Map RESEARCH.md, KHÔNG tạo package `wallet/` đầy đủ ở Phase 1, chỉ entity tối thiểu để AUTH-01 insert được 1 dòng): `@Entity @Table(name = "wallets")`, field: `id`, `userId` (`@Column(name="user_id")` UUID, nullable — vì `group_id` cũng có thể set nhưng Phase 1 luôn dùng nhánh `user_id`), `groupId` (`@Column(name="group_id")` UUID, nullable, luôn để `null` khi tạo ví Tiền mặt), `name` (String), `type` (String — set `"cash"` khi tạo), `initialBalance` (`@Column(name="initial_balance")` Long), `currentBalance` (`@Column(name="current_balance")` Long), `includeInTotal` (`@Column(name="include_in_total")` Boolean), `icon`, `color` (`@Column(columnDefinition="char(7)")` String, nullable), `sortOrder` (`@Column(name="sort_order")` Integer), `isDeleted` (`@Column(name="is_deleted")` Boolean), `createdAt` (Instant). Ghi rõ Javadoc class-level: "Entity tối thiểu chỉ phục vụ AUTH-01 (tạo ví Tiền mặt lúc đăng ký). Module wallet/ đầy đủ thuộc Phase 2 — không mở rộng entity này ở Phase 1."

**`SchemaSmokeTest.java`** (package gốc `com.datn.financeapp`, thư mục test):
```java
package com.datn.financeapp;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
import org.springframework.context.ApplicationContext;
import org.springframework.test.context.ActiveProfiles;
import org.testcontainers.containers.PostgreSQLContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;

import static org.assertj.core.api.Assertions.assertThat;

@Testcontainers
@SpringBootTest
@ActiveProfiles("test")
class SchemaSmokeTest {

    @Container
    @ServiceConnection
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16");

    @Test
    void contextLoadsAndSchemaValidates(ApplicationContext ctx) {
        assertThat(ctx).isNotNull();
    }
}
```
Nếu `@ServiceConnection` yêu cầu thêm cấu hình JWT_SECRET/DB_* (vì `application.yml` không có default), thêm `@TestPropertySource` hoặc set biến môi trường test qua `@DynamicPropertySource` cho `jwt.secret` (giá trị test cố định, ví dụ Base64 32 byte bất kỳ) — vì Testcontainers tự override `spring.datasource.*` nhưng KHÔNG tự cấp `JWT_SECRET`.
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server &amp;&amp; mvn -q test -Dtest=SchemaSmokeTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q test -Dtest=SchemaSmokeTest` exit code 0 (yêu cầu Docker chạy sẵn trên máy)
    - Không xuất hiện `SchemaManagementException` hoặc `Schema-validation` trong output test
    - `User.java` chứa `@Table(name = "users")` và field `passwordHash` map `@Column(name = "password_hash")`
    - `LoginAttempt.java` chứa `ipAddress` với annotation `@JdbcTypeCode` (map kiểu INET)
    - `IdempotencyKeyEntity.java` chứa `responseBody` với annotation `@JdbcTypeCode(SqlTypes.JSON)`
    - `WalletMinimal.java` có Javadoc ghi rõ "Phase 2" — xác nhận không mở rộng quá phạm vi
  </acceptance_criteria>
  <done>Toàn bộ entity JPA khớp đúng cột thật của V1/V7, `ddl-auto=validate` không lỗi, Flyway đã chứng minh chạy sạch V1→V7 trên PostgreSQL thật qua Testcontainers — đóng CORE-09/CORE-10/CORE-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `.env` / biến môi trường → ứng dụng | Secret nạp từ file cục bộ không commit — ranh giới giữa máy dev và mã nguồn |
| Dev machine → PostgreSQL container | Kết nối local qua Docker network, chưa có TLS (chấp nhận được cho môi trường dev) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` chứa `JWT_SECRET`/`DB_PASSWORD` bị commit nhầm vào git | mitigate | `.gitignore` chặn `.env` (chỉ `.env.example` được commit) — verify bằng `git status --short` không liệt kê `.env` |
| T-01-02 | Tampering | Secret có giá trị mặc định yếu bị quên đổi ở môi trường thật | mitigate | D-01: không viết `${VAR:default}` ở bất kỳ đâu trong `application.yml` — app fail-fast nếu thiếu biến, buộc dev phải tự cấp giá trị thật |
| T-01-03 | Information Disclosure | `SchemaManagementException` hoặc stack trace lộ chi tiết kết nối DB khi khởi động thất bại | accept | Rủi ro thấp — chỉ log cục bộ trong môi trường dev, chưa có endpoint public nào ở Plan này |

</threat_model>

<verification>
- `mvn -q -DskipTests compile` chạy sạch từ thư mục `source/server/`
- `mvn -q test -Dtest=SchemaSmokeTest` xanh (yêu cầu Docker Desktop đang chạy)
- `docker compose up -d` khởi động container Postgres không lỗi, `docker compose down` dọn sạch
- README.md đọc từ đầu tới cuối là đủ để chạy `mvn spring-boot:run` thành công lần đầu tiên trên máy sạch
</verification>

<success_criteria>
1. Project Maven biên dịch và chạy được (`mvn spring-boot:run` khởi động Tomcat, Flyway migrate V1-V7 không lỗi)
2. `ddl-auto=validate` không ném exception cho bất kỳ entity nào trong 5 entity đã viết
3. `.env` không lọt vào git, `.env.example` có đủ mọi biến cần thiết
4. README.md mô tả đúng trình tự khởi động, không thiếu bước nào
</success_criteria>

<output>
After completion, create `.planning/phases/01-nen-tang-xac-thuc/01-01-SUMMARY.md`
</output>
