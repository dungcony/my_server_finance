---
phase: 01-nen-tang-xac-thuc
plan: 06
type: execute
wave: 6
depends_on: ["01-03", "01-05"]
files_modified:
  - D:\PTIT\DATN\source\server\.planning\ROADMAP.md
  - src/test/java/com/datn/financeapp/auth/AuthIdempotencyRateLimitEndToEndTest.java
  - README.md
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-07, CORE-08, CORE-09, CORE-10, CORE-11]

must_haves:
  truths:
    - "Toàn bộ test suite Phase 1 (mvn test, chạy tất cả) xanh trong 1 lần chạy liên tục — không có test nào phụ thuộc thứ tự chạy trước đó gây flaky"
    - "ROADMAP.md Phase 1 mô tả đúng thực tế: V6/V7 đã tồn tại từ trước, việc đầu tiên là verify chứ không phải viết mới (D-08)"
    - "CORE-05 (kiểm tra quyền trong SQL, 404 không phải 403): Phase 1 chưa có tài nguyên đa người dùng thật (chỉ `/auth/me` — GET/PATCH luôn tự giới hạn theo `SecurityContextUtil.currentUserId()` lấy từ JWT subject, không nhận `user_id` từ query/path). Xác nhận bằng code review `AuthController.me()`/`updateProfile()` (Plan 05) không có tham số `userId` truyền từ client — không có bề mặt để áp dụng 404 vs 403 ở Phase 1; CORE-05 sẽ áp dụng đầy đủ từ Phase 2 khi có tài nguyên ví/giao dịch multi-user. Verify: `grep -n \"currentUserId\" AuthController.java` — không có endpoint nào nhận `userId` làm input"
    - "CORE-06 (xoá mềm, DELETE idempotent): Phase 1 không có bất kỳ endpoint DELETE nào (9 endpoint auth đều là GET/POST/PATCH). Xác nhận bằng `grep -rn \"@DeleteMapping\" src/main/java/com/datn/financeapp/auth/` trả về rỗng — CORE-06 không có bề mặt kiểm chứng ở Phase 1, sẽ áp dụng khi Phase 2 có endpoint xoá ví/danh mục (đã có sẵn cột `is_deleted` trong V1 cho các bảng đó, schema đã chuẩn bị trước)"
    - "CORE-07 (timestamp UTC): toàn bộ cột thời gian của 4 bảng hạ tầng V7 (`refresh_tokens.expires_at/revoked_at/created_at`, `password_reset_tokens.expires_at/used_at/created_at`, `login_attempts.attempted_at`, `idempotency_keys.created_at`) đều kiểu `TIMESTAMPTZ`, map qua JPA `Instant` (Plan 01 entity đã dùng đúng kiểu này, không dùng `LocalDateTime`). Verify: `grep -rn \"Instant\" src/main/java/com/datn/financeapp/auth/entity/*.java src/main/java/com/datn/financeapp/common/idempotency/IdempotencyKeyEntity.java` xác nhận không có `LocalDateTime` nào được dùng cho cột `TIMESTAMPTZ`"
    - "CORE-08 (amount là số nguyên VND): Phase 1 không có trường `amount` nào trong toàn bộ DTO/entity 9 endpoint auth. Verify: `grep -rn \"amount\|BigDecimal\|double \|float \" src/main/java/com/datn/financeapp/auth/` trả về rỗng — không có bề mặt kiểm chứng ở Phase 1, sẽ áp dụng khi Phase 2 có `wallets.initial_balance`/`transactions.amount` (schema V1 đã dùng `BIGINT`, đúng chuẩn số nguyên)"
  artifacts:
    - path: "D:\\PTIT\\DATN\\source\\server\\.planning\\ROADMAP.md"
      provides: "Phase 1 description cập nhật đúng D-06/D-07/D-08, không còn ghi 'phải viết V6/V7 trước'"
      contains: "verify"
  key_links:
    - from: "AuthIdempotencyRateLimitEndToEndTest"
      to: "JwtAuthFilter -> RateLimitFilter -> IdempotencyAspect -> AuthController"
      via: "gọi qua HTTP thật (TestRestTemplate), xác nhận toàn bộ chuỗi filter/AOP phối hợp đúng, không riêng lẻ từng phần"
      pattern: "TestRestTemplate"
---

<objective>
Đóng phase bằng việc chạy toàn bộ test suite liên tục để bắt lỗi flaky/thứ tự phụ thuộc giữa các Plan trước, viết 1 test end-to-end xác nhận Idempotency + Rate limit + Auth phối hợp đúng qua HTTP thật (không phải từng mảnh riêng lẻ như các Plan trước), và cập nhật `ROADMAP.md` Phase 1 theo D-08 để tài liệu khớp thực tế (V6/V7 đã tồn tại từ trước, không phải "viết trước khi code Java").

Purpose: Đây là điểm nghiệm thu cuối phase — nếu test suite tổng thể đỏ dù từng Plan riêng lẻ xanh, nghĩa là có xung đột ẩn (ví dụ tranh chấp Testcontainers, thứ tự Flyway, cấu hình chung bị Plan sau ghi đè Plan trước).
Output: `ROADMAP.md` khớp thực tế, 1 test end-to-end mới, README xác nhận cuối cùng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-CONTEXT.md
@D:\PTIT\DATN\source\server\.planning\ROADMAP.md
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-03-SUMMARY.md
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cập nhật ROADMAP.md Phase 1 theo D-08 + chạy toàn bộ test suite liên tục</name>
  <read_first>
    D:\PTIT\DATN\source\server\.planning\ROADMAP.md (mục "Phase 1: Nền tảng & Xác thực" đầy đủ)
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <files>
    D:\PTIT\DATN\source\server\.planning\ROADMAP.md
  </files>
  <action>
Sửa đúng đoạn trong `ROADMAP.md` mục "### Phase 1: Nền tảng & Xác thực" → "**Kỹ thuật (từ SUMMARY.md)**" — dòng hiện tại:

```
  - **Việc đầu tiên của phase — viết migration trước khi viết code Java:** V1–V5 hiện có **15 bảng**, thiếu toàn bộ bảng hạ tầng cho Auth/Idempotency. Phải viết `V6__va_loi_bao_mat.sql` (CORE-09) và `V7__ha_tang_xac_thuc.sql` (CORE-10) trước, nếu không AUTH-03 chết ngay task đầu vì không có chỗ lưu refresh token
```

Thay bằng (theo D-06/D-07/D-08 — V6/V7 đã tồn tại từ commit 533e95b trước khi phase này bắt đầu lập kế hoạch):

```
  - **Việc đầu tiên của phase — verify migration đã có, không viết mới:** `V6__va_loi_bao_mat.sql` (CORE-09) và `V7__ha_tang_xac_thuc.sql` (CORE-10) đã tồn tại sẵn trong `db/migration/` (commit 533e95b) trước khi Phase 1 bắt đầu lập kế hoạch chi tiết. Việc đầu tiên là dựng project trỏ Flyway vào `db/migration/` gốc và chạy thật V1→V7 trên PostgreSQL sạch để xác nhận không lỗi checksum/thứ tự, đúng cột/kiểu dữ liệu cho 4 bảng hạ tầng Auth/Idempotency — không phải viết migration mới
```

Không sửa bất kỳ đoạn nào khác trong file (Success Criteria, Requirements, Depends on giữ nguyên — đã đúng thực tế). Cập nhật bảng "Progress" ở cuối file: đổi dòng `| 1. Nền tảng & Xác thực | 0/TBD | Not started | - |` thành `| 1. Nền tảng & Xác thực | 0/6 | Not started | - |` (6 plan đã lập ở phase này). Cập nhật dòng `**Plans**: TBD` trong mục Phase 1 detail thành danh sách 6 plan với mô tả ngắn:
```
**Plans**:
- [ ] 01-01-PLAN.md — Khung Maven, cấu hình môi trường, verify migration V1-V7, entity tối thiểu
- [ ] 01-02-PLAN.md — common/ (response wrapper, exception handler, JWT service, SecurityConfig)
- [ ] 01-03-PLAN.md — Idempotency AOP + Rate limiting 3 tầng
- [ ] 01-04-PLAN.md — Auth lõi: register/login/refresh/logout
- [ ] 01-05-PLAN.md — Auth còn lại: profile, đổi/quên/đặt lại mật khẩu, test khoá đăng nhập
- [ ] 01-06-PLAN.md — Đóng phase: cập nhật ROADMAP, test end-to-end, verify toàn bộ suite
```

**Xác nhận CORE-05/06/07/08 (thoả mãn thụ động ở Phase 1, không phải bỏ sót):** RESEARCH.md mục "Architectural Responsibility Map" đã ghi rõ 4 mã này chưa có bề mặt kiểm chứng đầy đủ ở Phase 1 vì thiếu tài nguyên multi-user thật (CORE-05), thiếu endpoint DELETE (CORE-06), và thiếu trường `amount` (CORE-08); riêng CORE-07 (timestamp UTC) áp dụng được ngay vì 4 bảng hạ tầng V7 đều dùng `TIMESTAMPTZ`. Chạy các lệnh sau và ghi kết quả vào SUMMARY.md (không phải chỉ đọc bằng mắt):
```
grep -n "currentUserId" src/main/java/com/datn/financeapp/auth/controller/AuthController.java
grep -rn "@DeleteMapping" src/main/java/com/datn/financeapp/auth/
grep -rn "Instant" src/main/java/com/datn/financeapp/auth/entity/*.java src/main/java/com/datn/financeapp/common/idempotency/IdempotencyKeyEntity.java
grep -rn "amount\|BigDecimal\|double \|float " src/main/java/com/datn/financeapp/auth/
```
Kết quả mong đợi: lệnh 1 xác nhận không endpoint nào nhận `userId` từ client (CORE-05 chưa có bề mặt); lệnh 2 trả rỗng (CORE-06 chưa có bề mặt); lệnh 3 xác nhận toàn bộ timestamp entity dùng `Instant` (CORE-07 đã thoả mãn); lệnh 4 trả rỗng (CORE-08 chưa có bề mặt). Đây KHÔNG phải giảm phạm vi yêu cầu — là xác nhận tường minh rằng 3/4 mã (05/06/08) sẽ được implement đầy đủ khi Phase 2 có tài nguyên tương ứng, còn CORE-07 đã hoàn thành ngay từ Phase 1.

Sau khi sửa file và xác nhận CORE-05/06/07/08 xong, chạy toàn bộ test suite của `source/server/` liên tục 1 lần (`mvn test`, KHÔNG filter `-Dtest=`) để phát hiện xung đột ẩn giữa các Plan — ví dụ 2 test cùng dùng chung 1 Testcontainers container static có thể xung đột dữ liệu nếu không dọn giữa các test class, hoặc `@DirtiesContext` thiếu sót làm bean cache sai giữa test class. Nếu phát hiện lỗi, sửa tại chỗ (thường là thêm `@Transactional` + rollback tự động cho test, hoặc `@DirtiesContext` cho test class thay đổi Security config).
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server &amp;&amp; mvn -q test</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q test` (toàn bộ suite, không filter) exit code 0
    - `ROADMAP.md` mục Phase 1 KHÔNG còn chứa chuỗi "Phải viết `V6__va_loi_bao_mat.sql`" hoặc "trước khi viết code Java"
    - `ROADMAP.md` mục Phase 1 chứa chuỗi "đã tồn tại sẵn" hoặc "verify" liên quan tới V6/V7
    - SUMMARY.md ghi rõ kết quả 4 lệnh grep xác nhận CORE-05/06/07/08 (như mô tả trong action) — không chỉ liệt kê ID suông trong requirements
    - `ROADMAP.md` bảng Progress dòng Phase 1 hiển thị `0/6` thay vì `0/TBD`
  </acceptance_criteria>
  <done>ROADMAP.md khớp đúng thực tế đã xảy ra (V6/V7 có sẵn, việc đầu là verify), toàn bộ test suite Phase 1 xanh khi chạy liên tục không filter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test end-to-end Idempotency + Rate limit + Auth phối hợp qua HTTP thật + README cuối cùng</name>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\auth\controller\AuthController.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\security\SecurityConfig.java
    D:\PTIT\DATN\source\server\README.md
  </read_first>
  <files>
    src/test/java/com/datn/financeapp/auth/AuthIdempotencyRateLimitEndToEndTest.java, README.md
  </files>
  <behavior>
    - Test: gửi `POST /auth/register` 6 lần liên tiếp trong vòng 1 phút từ cùng "IP" giả lập (test không dễ giả lập IP thật qua `TestRestTemplate` nội bộ — dùng cách khác: verify quota rate limit đã áp dụng đúng cho path `/auth/register` bằng cách kiểm tra header `X-RateLimit-Limit=5` xuất hiện đúng trên response, và request thứ 6 trả 429 nếu môi trường test cho phép kiểm soát địa chỉ nguồn giống nhau — TestRestTemplate mặc định luôn cùng 1 kết nối/IP loopback nên đủ điều kiện test thật).
    - Test: gửi `POST /auth/register` với `Idempotency-Key: e2e-test-key-001` — LƯU Ý D-11 đã cấm gắn `@Idempotent` lên `/auth/register`, nên test này phải xác nhận NGƯỢC LẠI: header `Idempotency-Key` bị BỎ QUA hoàn toàn (2 lần gọi với email khác nhau nhưng cùng key vẫn tạo 2 user riêng biệt, không bị cache) — đây là bằng chứng sống rằng D-11 được tuân thủ đúng trong code thật, không chỉ đọc bằng mắt.
    - Test: response `POST /auth/register` hợp lệ có đủ 3 header `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`.
  </behavior>
  <action>
`AuthIdempotencyRateLimitEndToEndTest.java` — Testcontainers + `@SpringBootTest(webEnvironment = RANDOM_PORT)` + `TestRestTemplate` (bean tự inject sẵn khi dùng `webEnvironment`), test 3 behavior ở trên. Vì `RateLimitFilter` (Plan 03) tính theo `req.getRemoteAddr()` cho nhóm "auth", và mọi request từ `TestRestTemplate` trong cùng test JVM tới `localhost` sẽ có cùng địa chỉ nguồn (127.0.0.1), 6 request liên tiếp tới `/auth/register` (mỗi lần dùng email khác nhau để tránh `EMAIL_ALREADY_EXISTS` che lấp kết quả rate limit) sẽ đúng kịch bản kiểm tra quota theo IP.

**Xử lý thứ tự test:** vì `RateLimitFilter` dùng Caffeine cache bucket theo key `"auth:" + ip`, và bucket này SỐNG XUYÊN SUỐT class test (không tự reset giữa các `@Test` method trong cùng class) — viết CẢ 3 behavior trong CÙNG 1 `@Test` method theo đúng thứ tự (đăng ký lần 1-5 trước, request thứ 6 kiểm 429, RỒI mới test Idempotency-Key ở email/request riêng — chấp nhận request đó cũng tính vào quota nếu còn hạn mức, hoặc verify ở khoảng thời gian mới sau khi bucket refill) để tránh trạng thái rate limit rò rỉ chéo giữa các `@Test` method độc lập, gây flaky.

Cập nhật `README.md` — thêm mục cuối "Trạng thái Phase 1" xác nhận: 9 endpoint auth hoạt động, hạ tầng idempotency/rate limit sẵn sàng cho Phase 2, cách chạy toàn bộ test suite (`mvn test`, cần Docker), và nhắc lại đúng 1 dòng: "Xem `.planning/ROADMAP.md` Phase 2 cho bước tiếp theo (Ví & Danh mục)".
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server &amp;&amp; mvn -q test -Dtest=AuthIdempotencyRateLimitEndToEndTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q test -Dtest=AuthIdempotencyRateLimitEndToEndTest` exit code 0 (yêu cầu Docker)
    - Test xác nhận request thứ 6 tới `/auth/register` trong 1 phút từ cùng nguồn trả 429
    - Test xác nhận 2 lần gọi `/auth/register` với cùng `Idempotency-Key` nhưng email khác nhau tạo ra 2 user_id khác nhau (chứng minh D-11 hoạt động đúng trong runtime thật)
    - `README.md` chứa mục "Trạng thái Phase 1" hoặc tương đương xác nhận 9 endpoint auth
  </acceptance_criteria>
  <done>Test end-to-end xác nhận 3 mảnh hạ tầng (auth, idempotency, rate limit) phối hợp đúng qua HTTP thật, không chỉ đúng riêng lẻ từng đơn vị — đóng toàn bộ Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Toàn bộ hệ thống Phase 1 dưới góc nhìn tích hợp | Ranh giới cuối cùng — nơi các lớp bảo vệ riêng lẻ (JWT, rate limit, idempotency, lockout) phải phối hợp đúng thứ tự, không lớp nào vô tình vô hiệu hoá lớp khác |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `@Idempotent` bị gắn nhầm vào `/auth/register` ở lần sửa code sau này (regression), khiến response token cũ bị cache và trả lại cho request đăng ký khác | mitigate | Test end-to-end Task 2 xác nhận sống động hành vi D-11 đúng — nếu ai đó vô tình thêm `@Idempotent` sau này, test này sẽ đỏ ngay (2 email khác nhau nhưng nhận cùng 1 user_id) |
| T-06-02 | Denial of Service | Filter chain sai thứ tự khiến rate limit không áp dụng được cho endpoint auth (ví dụ SecurityConfig bị sửa nhầm ở Plan sau làm mất `addFilterAfter`) | mitigate | Test end-to-end verify trực tiếp status 429 xuất hiện đúng ở request thứ 6 — bắt được regression về thứ tự filter |

</threat_model>

<verification>
- `mvn -q test` (toàn bộ suite Phase 1) xanh, không filter, chạy liên tục 1 lần
- `git diff D:\PTIT\DATN\source\server\.planning\ROADMAP.md` chỉ sửa đúng đoạn Phase 1 đã mô tả, không đụng phase khác
</verification>

<success_criteria>
1. Toàn bộ 6 Plan của Phase 1 hoạt động cùng nhau không xung đột khi chạy test suite đầy đủ
2. ROADMAP.md khớp thực tế đã triển khai (D-08)
3. Test end-to-end chứng minh 3 mảnh hạ tầng lõi (auth, idempotency, rate limit) phối hợp đúng — đây là bằng chứng cuối cùng cho Success Criteria #3, #4, #5, #6 của ROADMAP Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/01-nen-tang-xac-thuc/01-06-SUMMARY.md`
</output>
