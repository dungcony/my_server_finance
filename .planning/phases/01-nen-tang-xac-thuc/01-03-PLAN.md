---
phase: 01-nen-tang-xac-thuc
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/main/java/com/datn/financeapp/common/idempotency/Idempotent.java
  - src/main/java/com/datn/financeapp/common/idempotency/IdempotencyAspect.java
  - src/main/java/com/datn/financeapp/common/idempotency/IdempotencyKeyRepository.java
  - src/main/java/com/datn/financeapp/scheduler/IdempotencyCleanupJob.java
  - src/main/java/com/datn/financeapp/FinanceAppApplication.java
  - src/main/java/com/datn/financeapp/common/ratelimit/RateLimitRule.java
  - src/main/java/com/datn/financeapp/common/ratelimit/RateLimitProperties.java
  - src/main/java/com/datn/financeapp/common/ratelimit/RateLimitFilter.java
  - src/main/java/com/datn/financeapp/common/security/SecurityConfig.java
  - src/test/java/com/datn/financeapp/common/idempotency/IdempotencyAspectIntegrationTest.java
  - src/test/java/com/datn/financeapp/common/ratelimit/RateLimitFilterTest.java
  - D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md
autonomous: true
requirements: [CORE-03, CORE-04]

must_haves:
  truths:
    - "Gọi 2 lần liên tiếp cùng POST kèm cùng Idempotency-Key trong 24h nhận lại đúng response_body của lần đầu, không chạy lại nghiệp vụ lần 2"
    - "Gọi lại khi request đầu còn đang xử lý (status=processing) nhận 409 REQUEST_IN_PROGRESS ngay lập tức"
    - "Response 429 do RateLimitFilter tự ghi (KHÔNG đi qua GlobalExceptionHandler vì filter chạy trước DispatcherServlet) vẫn khớp đúng khung chuẩn {success:false, error:{code, message}} của toàn hệ thống — verify bằng grep chuỗi JSON tĩnh trong RateLimitFilter.java chứa đủ 3 khoá success/error/code, VÀ RateLimitFilterTest assert response body 429 parse được thành field success=false, error.code=RATE_LIMIT_EXCEEDED"
    - "Gọi endpoint /auth/** quá 5 lần/phút từ cùng 1 IP bị chặn 429 kèm header X-RateLimit-*; gọi endpoint thường quá 120 lần/phút từ cùng 1 user cũng bị chặn tương tự, độc lập theo user_id"
    - "Mọi response (kể cả không bị chặn) đều có header X-RateLimit-Limit/Remaining/Reset"
  artifacts:
    - path: "src/main/java/com/datn/financeapp/common/idempotency/IdempotencyAspect.java"
      provides: "AOP chặn method có @Idempotent, tra/ghi idempotency_keys"
      contains: "@Around"
    - path: "src/main/java/com/datn/financeapp/common/ratelimit/RateLimitFilter.java"
      provides: "Filter Bucket4j đặt sau JwtAuthFilter, set header X-RateLimit-*"
      contains: "X-RateLimit-Remaining"
    - path: "src/main/java/com/datn/financeapp/scheduler/IdempotencyCleanupJob.java"
      provides: "@Scheduled dọn idempotency_keys quá 24h"
      contains: "@Scheduled"
    - path: "D:\\PTIT\\DATN\\api\\00-QUY-UOC-CHUNG.md"
      provides: "Bổ sung mã REQUEST_IN_PROGRESS vào bảng mã lỗi dùng chung mục 6 (D-14)"
      contains: "REQUEST_IN_PROGRESS"
  key_links:
    - from: "RateLimitFilter"
      to: "SecurityConfig filter chain"
      via: "addFilterAfter(rateLimitFilter, JwtAuthFilter.class)"
      pattern: "addFilterAfter"
    - from: "IdempotencyAspect"
      to: "idempotency_keys"
      via: "INSERT ... ON CONFLICT DO NOTHING native query qua IdempotencyKeyRepository"
      pattern: "ON CONFLICT"
---

<objective>
Dựng hạ tầng Idempotency-Key (CORE-03, qua AOP `@Idempotent` theo D-10 đến D-15) và Rate limiting 3 tầng (CORE-04, Bucket4j + Caffeine theo D-16 đến D-21) — hai mảnh hạ tầng dùng lại nguyên vẹn cho mọi phase sau. Đồng thời thêm mã lỗi mới `REQUEST_IN_PROGRESS` (409) vào `api/00-QUY-UOC-CHUNG.md` mục 6 theo D-14 (việc tài liệu bắt buộc, không chỉ code).

Purpose: Phase 2 (`POST /wallets`) sẽ gắn `@Idempotent` ngay lập tức mà không cần dựng lại gì; rate limit áp dụng ngay cho `/auth/**` ở Plan 04/05.
Output: `common/idempotency/`, `common/ratelimit/`, job dọn dẹp trong `scheduler/`, tài liệu `api/00` đã cập nhật.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-CONTEXT.md
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-RESEARCH.md
@D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md
@D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-02-SUMMARY.md

<interfaces>
<!-- Từ Plan 02 (common/): -->
<!-- common/response/ApiResponse<T>, ErrorResponse — dùng để build response cache cho idempotency -->
<!-- common/exception/BusinessException(code, httpStatus, message) — dùng để ném REQUEST_IN_PROGRESS -->
<!-- common/security/SecurityContextUtil.currentUserId() -> UUID — dùng để lấy user_id cho cả 2 cơ chế -->
<!-- common/security/SecurityConfig — chứa SecurityFilterChain, addFilterBefore(jwtAuthFilter, ...) đã có sẵn, Plan này thêm addFilterAfter(rateLimitFilter, ...) -->

<!-- IdempotencyKeyEntity đã viết ở Plan 01 (common/idempotency/IdempotencyKeyEntity.java):
     field: id, idempotencyKey, userId, endpoint, status, responseStatus, responseBody (JSONB via @JdbcTypeCode(SqlTypes.JSON)), createdAt -->

Bảng cột `idempotency_keys` (V7, nhắc lại để executor không phải mở file khác):
```
idempotency_key VARCHAR(255) NOT NULL
user_id         UUID NOT NULL
endpoint        VARCHAR(255) NOT NULL
status          VARCHAR(20) DEFAULT 'processing'  -- CHECK IN ('processing','completed')
response_status INTEGER
response_body   JSONB
UNIQUE (idempotency_key, user_id, endpoint)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Idempotency AOP (@Idempotent) + job dọn dẹp 24h</name>
  <read_first>
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-CONTEXT.md (D-10 đến D-15)
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-RESEARCH.md (Pattern 4, Pitfall 6)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\idempotency\IdempotencyKeyEntity.java
  </read_first>
  <files>
    src/main/java/com/datn/financeapp/common/idempotency/Idempotent.java, src/main/java/com/datn/financeapp/common/idempotency/IdempotencyAspect.java, src/main/java/com/datn/financeapp/common/idempotency/IdempotencyKeyRepository.java, src/main/java/com/datn/financeapp/scheduler/IdempotencyCleanupJob.java, src/main/java/com/datn/financeapp/FinanceAppApplication.java, src/test/java/com/datn/financeapp/common/idempotency/IdempotencyAspectIntegrationTest.java
  </files>
  <behavior>
    - Test (Testcontainers): controller test-only có method `@Idempotent`, gọi 2 lần liên tiếp với cùng `Idempotency-Key` header sau khi lần đầu đã `completed` → lần 2 trả đúng `response_body` đã lưu, đếm số lần method nghiệp vụ thật sự chạy chỉ 1 (dùng `AtomicInteger` đếm trong controller test-only).
    - Test: giả lập request đang `status=processing` (insert thẳng bằng repository trước khi gọi) → request thứ 2 nhận HTTP 409, `error.code=REQUEST_IN_PROGRESS`.
    - Test: method nghiệp vụ ném exception → bản ghi `idempotency_keys` với `status=processing` bị xoá (không cache lỗi), gọi lại với cùng key chạy lại nghiệp vụ bình thường (không bị 409 mãi mãi).
    - Test: request KHÔNG có header `Idempotency-Key` → method nghiệp vụ chạy bình thường, không đụng tới bảng `idempotency_keys`.
  </behavior>
  <action>
**`Idempotent.java`**:
```java
package com.datn.financeapp.common.idempotency;

import java.lang.annotation.*;

@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface Idempotent {}
```

**`IdempotencyKeyRepository.java`** (`JpaRepository<IdempotencyKeyEntity, UUID>` + native query):
```java
public interface IdempotencyKeyRepository extends JpaRepository<IdempotencyKeyEntity, UUID> {

    @Modifying
    @Query(value = """
        INSERT INTO idempotency_keys (idempotency_key, user_id, endpoint, status)
        VALUES (:key, :userId, :endpoint, 'processing')
        ON CONFLICT (idempotency_key, user_id, endpoint) DO NOTHING
        """, nativeQuery = true)
    int tryInsertProcessing(@Param("key") String key, @Param("userId") UUID userId, @Param("endpoint") String endpoint);

    Optional<IdempotencyKeyEntity> findByIdempotencyKeyAndUserIdAndEndpoint(String idempotencyKey, UUID userId, String endpoint);

    @Modifying
    void deleteByIdempotencyKeyAndUserIdAndEndpoint(String idempotencyKey, UUID userId, String endpoint);

    @Modifying
    @Query(value = "DELETE FROM idempotency_keys WHERE created_at < :cutoff", nativeQuery = true)
    int deleteOlderThan(@Param("cutoff") java.time.Instant cutoff);
}
```
`@Modifying` cần đi kèm `@Transactional` ở tầng gọi (Aspect/Job) — Spring Data JPA không tự mở transaction cho native modifying query khi gọi từ ngoài `@Transactional` context.

**`IdempotencyAspect.java`** (`@Aspect @Component`) — bám sát D-12/D-13, dùng ĐÚNG luồng: chưa có → insert `processing` → chạy nghiệp vụ thật → `completed` (lưu `responseStatus`/`responseBody` serialize bằng Jackson `ObjectMapper`); có rồi và `completed` → trả cache; có rồi và `processing` → ném `BusinessException("REQUEST_IN_PROGRESS", 409, "Yêu cầu trước đó đang được xử lý, vui lòng thử lại sau.")`. Bọc `pjp.proceed()` trong try/catch: catch mọi `Exception` (không chỉ RuntimeException) → gọi `deleteByIdempotencyKeyAndUserIdAndEndpoint` rồi `throw` lại nguyên exception gốc (để `GlobalExceptionHandler` xử lý tiếp bình thường).

Cách lấy `HttpServletRequest` trong Aspect: dùng `RequestContextHolder.currentRequestAttributes()` cast `ServletRequestAttributes`, lấy `.getRequest()` — KHÔNG inject `HttpServletRequest` trực tiếp vào field của `@Aspect @Component` (bean singleton) vì sẽ lỗi nếu không có request-scoped proxy đúng cấu hình; dùng `RequestContextHolder` là cách an toàn hơn trong `@Around` advice.

`endpoint` dùng để phân biệt scope: `request.getMethod() + " " + request.getRequestURI()`.

`extractHttpStatus`/`extractBody` từ kết quả `pjp.proceed()`: nếu return type là `ResponseEntity<?>`, lấy `.getStatusCode().value()` và serialize `.getBody()` bằng `ObjectMapper.writeValueAsString(...)`. Khi trả cache lại (`buildResponseFromCache`), deserialize `response_body` JSON string thành `Object` (dùng `objectMapper.readValue(json, Object.class)`) rồi bọc lại `ResponseEntity.status(cachedStatus).body(parsed)`.

**`IdempotencyCleanupJob.java`** (package `scheduler`, mới — package này hiện rỗng theo ROADMAP "khởi tạo rỗng cho sau"):
```java
@Component
@RequiredArgsConstructor
@Slf4j
public class IdempotencyCleanupJob {
    private final IdempotencyKeyRepository repo;

    @Scheduled(cron = "0 0 3 * * *", zone = "Asia/Ho_Chi_Minh")
    @Transactional
    public void cleanup() {
        try {
            int deleted = repo.deleteOlderThan(Instant.now().minus(24, ChronoUnit.HOURS));
            log.info("Đã dọn {} bản ghi idempotency_keys quá 24 giờ", deleted);
        } catch (Exception e) {
            log.error("Lỗi khi dọn idempotency_keys", e);
        }
    }
}
```
Bọc try/catch để không crash scheduler pool (theo SUMMARY.md pitfall checklist).

**`FinanceAppApplication.java`** — thêm `@EnableScheduling` và `@EnableAspectJAutoProxy` (nếu chưa tự động bật qua `spring-boot-starter-aop`) lên class chính.

**`IdempotencyAspectIntegrationTest.java`** — dùng Testcontainers (tương tự `SchemaSmokeTest`), định nghĩa 1 `@RestController` test-only trong file test với method `@Idempotent` trả `ResponseEntity<Map<String,Object>>`, dùng `AtomicInteger` static đếm số lần method thật chạy, verify 4 behavior đã liệt kê bằng `MockMvc` hoặc `TestRestTemplate`. Cần set `Authorization` giả (mock `SecurityContext` bằng `@WithMockUser` hoặc set principal thủ công) để `SecurityContextUtil.currentUserId()` không NPE — dùng UUID cố định giả lập user đã đăng nhập.
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server &amp;&amp; mvn -q test -Dtest=IdempotencyAspectIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q test -Dtest=IdempotencyAspectIntegrationTest` exit code 0 (yêu cầu Docker)
    - `IdempotencyAspect.java` chứa `@Around` và xử lý cả 2 nhánh `completed`/`processing`
    - `IdempotencyKeyRepository.java` chứa chuỗi `ON CONFLICT` và `DO NOTHING`
    - Test xác nhận method nghiệp vụ thật chỉ chạy đúng 1 lần khi gọi 2 lần với cùng key
    - Test xác nhận exception khiến bản ghi `processing` bị xoá (retry sau đó không bị kẹt 409 vĩnh viễn)
    - `IdempotencyCleanupJob.java` chứa `@Scheduled` với `zone = "Asia/Ho_Chi_Minh"`
  </acceptance_criteria>
  <done>Idempotency hoạt động đúng luồng D-12/D-13, tự dọn dẹp sau 24h, sẵn sàng cho Phase 2 gắn `@Idempotent` vào `POST /wallets` mà không cần sửa gì thêm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rate limiting 3 tầng (Bucket4j + Caffeine) + cập nhật api/00 (D-14)</name>
  <read_first>
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-CONTEXT.md (D-16 đến D-21)
    D:\PTIT\DATN\source\server\.planning\phases\01-nen-tang-xac-thuc\01-RESEARCH.md (Pattern 5, Pitfall 5)
    D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md (mục 6, mục 10)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\security\SecurityConfig.java
  </read_first>
  <files>
    src/main/java/com/datn/financeapp/common/ratelimit/RateLimitRule.java, src/main/java/com/datn/financeapp/common/ratelimit/RateLimitProperties.java, src/main/java/com/datn/financeapp/common/ratelimit/RateLimitFilter.java, src/main/java/com/datn/financeapp/common/security/SecurityConfig.java, src/test/java/com/datn/financeapp/common/ratelimit/RateLimitFilterTest.java, D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md
  </files>
  <behavior>
    - Test: gửi 6 request liên tiếp tới path giả lập khớp prefix `/auth/` từ cùng IP (mock `request.getRemoteAddr()`) → 5 request đầu status khác 429, request thứ 6 status 429 kèm `error.code=RATE_LIMIT_EXCEEDED`.
    - Test: gửi request tới path không khớp `/auth/`/`/ai/` (rơi vào default 120/phút) với 2 `user_id` khác nhau cùng 1 IP giả lập → quota tính riêng theo từng user (không bị gộp theo IP) — xác nhận đúng D-18/D-21 và pitfall 5.
    - Test: mọi response (kể cả không bị chặn) đều có 3 header `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`.
  </behavior>
  <action>
**`RateLimitRule.java`**:
```java
package com.datn.financeapp.common.ratelimit;

import java.time.Duration;

public record RateLimitRule(String group, String pathPrefix, int limit, Duration window, boolean keyedByIp) {}
```

**`RateLimitProperties.java`** — bảng cấu hình tĩnh D-16 (không cần đọc từ `application.yml`, hardcode theo đúng bảng đã chốt, nhưng đặt trong 1 `@Component` để dễ test/mock sau này):
```java
@Component
public class RateLimitProperties {
    public static final List<RateLimitRule> RULES = List.of(
        new RateLimitRule("auth", "/auth/", 5, Duration.ofMinutes(1), true),
        new RateLimitRule("ai",   "/ai/",   30, Duration.ofMinutes(1), false),
        new RateLimitRule("default", "/",  120, Duration.ofMinutes(1), false)
    );

    public RateLimitRule resolve(String requestUri) {
        return RULES.stream()
            .filter(r -> !r.pathPrefix().equals("/") && requestUri.startsWith(r.pathPrefix()))
            .findFirst()
            .orElseGet(() -> RULES.stream().filter(r -> r.pathPrefix().equals("/")).findFirst().orElseThrow());
    }
}
```

**`RateLimitFilter.java`** (`OncePerRequestFilter`, `@Component`):
```java
@Component
@RequiredArgsConstructor
public class RateLimitFilter extends OncePerRequestFilter {

    private final RateLimitProperties properties;
    private final Cache<String, Bucket> bucketCache = Caffeine.newBuilder()
        .expireAfterAccess(Duration.ofMinutes(5))
        .maximumSize(100_000)
        .build();

    @Override
    protected void doFilterInternal(HttpServletRequest req, HttpServletResponse res, FilterChain chain)
            throws ServletException, IOException {
        RateLimitRule rule = properties.resolve(req.getRequestURI());
        String bucketKey = rule.keyedByIp()
            ? rule.group() + ":" + req.getRemoteAddr()
            : rule.group() + ":" + resolveUserOrIp(req);

        Bucket bucket = bucketCache.get(bucketKey, k -> Bucket.builder()
            .addLimit(l -> l.capacity(rule.limit()).refillGreedy(rule.limit(), rule.window()))
            .build());

        ConsumptionProbe probe = bucket.tryConsumeAndReturnRemaining(1);
        res.setHeader("X-RateLimit-Limit", String.valueOf(rule.limit()));
        res.setHeader("X-RateLimit-Remaining", String.valueOf(probe.getRemainingTokens()));
        res.setHeader("X-RateLimit-Reset", String.valueOf(
            Instant.now().plusNanos(probe.getNanosToWaitForRefill()).getEpochSecond()));

        if (!probe.isConsumed()) {
            res.setStatus(429);
            res.setContentType("application/json");
            res.getWriter().write("""
                {"success":false,"error":{"code":"RATE_LIMIT_EXCEEDED","message":"Bạn đã gọi quá nhiều lần, vui lòng thử lại sau."}}
                """);
            return;
        }
        chain.doFilter(req, res);
    }

    private String resolveUserOrIp(HttpServletRequest req) {
        var auth = SecurityContextHolder.getContext().getAuthentication();
        if (auth != null && auth.isAuthenticated() && auth.getPrincipal() != null
                && !"anonymousUser".equals(auth.getPrincipal())) {
            return auth.getPrincipal().toString();
        }
        return req.getRemoteAddr(); // chưa đăng nhập -> rơi về IP (hiếm khi xảy ra cho nhóm "default"/"ai" vì các endpoint đó yêu cầu auth)
    }
}
```
Header trả JSON thô bằng `res.getWriter().write(...)` (không qua `GlobalExceptionHandler` vì filter chạy TRƯỚC `DispatcherServlet`) — chấp nhận hardcode chuỗi JSON tĩnh ở tầng filter, đây là điểm duy nhất trong hệ thống không đi qua response wrapper chuẩn do giới hạn kỹ thuật của Filter (không thể ném exception để Controller Advice bắt).

**Sửa `SecurityConfig.java`** — thêm `rateLimitFilter` vào bean:
```java
http.addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class)
    .addFilterAfter(rateLimitFilter, JwtAuthFilter.class);  // D-18: SAU JwtAuthFilter
```
**Cảnh báo từ RESEARCH.md `[ASSUMED]`:** `addFilterAfter(filter, JwtAuthFilter.class)` dùng class filter đứng trước làm mốc — verify bằng cách chạy test thực tế (không chỉ đọc code) rằng `RateLimitFilter` chạy sau `JwtAuthFilter` trong chuỗi thật (ví dụ set breakpoint/log thứ tự, hoặc suy luận từ kết quả test "quota tính theo user_id" ở behavior thứ 2 — nếu test đó xanh thì thứ tự filter đã đúng vì `SecurityContext` phải có Authentication trước khi `RateLimitFilter` đọc được `user_id`).

**Cập nhật `D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md` mục 6 (D-14)** — thêm đúng 1 dòng vào bảng mã lỗi dùng chung, ngay sau dòng `RATE_LIMIT_EXCEEDED`:
```markdown
| `REQUEST_IN_PROGRESS` | 409 | Yêu cầu trước đó với cùng Idempotency-Key đang được xử lý, thử lại sau vài giây |
```
Giữ nguyên format bảng markdown hiện có, không đổi thứ tự các dòng khác.

**`RateLimitFilterTest.java`** — `@WebMvcTest` hoặc unit test trực tiếp gọi `doFilterInternal` với `MockHttpServletRequest`/`MockHttpServletResponse` (không cần Testcontainers vì không chạm DB), verify 3 behavior bằng vòng lặp gọi filter nhiều lần và assert response status/header.
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server &amp;&amp; mvn -q test -Dtest=RateLimitFilterTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q test -Dtest=RateLimitFilterTest` exit code 0
    - `RateLimitFilter.java` chứa cả 3 header `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset` được set TRƯỚC nhánh `if (!probe.isConsumed())` (nghĩa là set trên mọi response, không chỉ khi bị chặn — verify bằng đọc code thứ tự dòng)
    - `SecurityConfig.java` chứa `addFilterAfter(rateLimitFilter`
    - `api/00-QUY-UOC-CHUNG.md` chứa dòng `REQUEST_IN_PROGRESS` trong bảng mã lỗi mục 6, kèm mã HTTP `409`
    - Test xác nhận request thứ 6 trong vòng 1 phút tới `/auth/**` từ cùng IP bị 429
    - Test xác nhận 2 user khác nhau cùng IP có quota độc lập cho nhóm default
    - `RateLimitFilter.java` chứa chuỗi JSON tĩnh có đủ 3 khoá `"success":false`, `"error"`, `"code":"RATE_LIMIT_EXCEEDED"` — verify bằng grep; `RateLimitFilterTest` assert response body 429 parse đúng field `success=false`/`error.code=RATE_LIMIT_EXCEEDED`
  </acceptance_criteria>
  <done>Rate limit 3 tầng hoạt động đúng D-16 đến D-21, header luôn xuất hiện, tài liệu `api/00` đã cập nhật mã lỗi mới theo D-14 — CORE-04 hoàn tất cho Phase 1 và tái sử dụng nguyên vẹn ở mọi phase sau.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → `Idempotency-Key` header | Input không tin cậy — client có thể gửi key trùng cố ý để thăm dò hành vi cache |
| Client → tần suất request | Vector DoS/brute-force nếu không giới hạn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | Không giới hạn tần suất request → brute-force đăng nhập hoặc spam server | mitigate | CORE-04: Bucket4j 3 tầng, đặc biệt `/auth/**` giới hạn chặt 5/phút/IP — lớp phòng thủ độc lập với AUTH-07 (D-21) |
| T-03-02 | Tampering | Idempotency-Key cache trả nhầm response của user khác nếu thiếu điều kiện `user_id` trong scope | mitigate | Composite UNIQUE `(idempotency_key, user_id, endpoint)` ở DB (V7) + luôn truyền `userId` thật từ `SecurityContextUtil.currentUserId()` khi tra/ghi — không bao giờ tra chỉ theo `idempotency_key` một mình |
| T-03-03 | Information Disclosure | Response cache trong `idempotency_keys.response_body` chứa dữ liệu nhạy cảm lưu ở dạng JSONB không mã hoá | accept | Rủi ro thấp — dữ liệu vẫn nằm trong DB nội bộ đã kiểm soát quyền truy cập ở tầng hạ tầng, chỉ user sở hữu key mới tra được đúng bản ghi (đã ràng buộc theo `user_id`) |
| T-03-04 | Denial of Service | Caffeine cache bucket phình to vô hạn nếu kẻ tấn công sinh nhiều IP giả để tạo bucket mới liên tục | mitigate | `maximumSize(100_000)` + `expireAfterAccess(5 phút)` tự evict — chấp nhận đánh đổi độ chính xác quota khi bucket bị evict sớm, ưu tiên chống memory leak |

</threat_model>

<verification>
- `mvn -q test -Dtest=IdempotencyAspectIntegrationTest,RateLimitFilterTest` xanh
- `git diff D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md` chỉ thêm đúng 1 dòng mới, không sửa nội dung khác
</verification>

<success_criteria>
1. Idempotency-Key hoạt động đúng luồng D-12/D-13, tự dọn sau 24h
2. Rate limit 3 tầng đúng quota D-16, header luôn xuất hiện, 429 kèm đúng mã lỗi
3. `api/00-QUY-UOC-CHUNG.md` đã bổ sung `REQUEST_IN_PROGRESS` — tài liệu và code không trôi dạt
</success_criteria>

<output>
After completion, create `.planning/phases/01-nen-tang-xac-thuc/01-03-SUMMARY.md`
</output>
