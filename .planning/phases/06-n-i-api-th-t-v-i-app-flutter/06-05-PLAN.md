---
phase: 06-n-i-api-th-t-v-i-app-flutter
plan: 05
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - D:\PTIT\DATN\source\app\lib\core\config\app_config.dart
  - D:\PTIT\DATN\source\app\CHUYEN-SANG-API-THAT.md
  - D:\PTIT\DATN\api\04-GIAO-DICH.md
  - D:\PTIT\DATN\source\server\.planning\ROADMAP.md
  - D:\PTIT\DATN\source\server\.planning\STATE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "AppConfig.useMock mặc định false — chạy dữ liệu mẫu phải truyền --dart-define=USE_MOCK=true"
    - "core/network/mock/ vẫn chạy được với --dart-define=USE_MOCK=true (SC#7) — không bị xoá"
    - "CHUYEN-SANG-API-THAT.md cột Backend phải làm gì đã cập nhật thành ghi chú chỗ thực tế đã vấp (D-23)"
    - "ROADMAP.md bảng Progress phản ánh đúng trạng thái Phase 3/4/5/6 (D-25)"
    - "SC#6 (AI drafts + user_corrections) được ghi rõ là DỜI sang phase nối ai sau Phase 5, không đánh dấu đạt (D-03)"
    - "Bốn màn chính (Tổng quan, Sổ giao dịch, Ngân sách, Thêm giao dịch) hiển thị đúng dữ liệu backend thật, số dư/tiến độ ngân sách đổi ngay sau khi ghi giao dịch (SC3) — xác nhận thủ công qua checkpoint"
    - "Đúng 1 lượt /auth/refresh được ghi nhận (log backend hoặc bảng refresh_tokens) trong cửa sổ chạy test SC5 — xác nhận D-14 qua checkpoint Task 4"
  artifacts:
    - path: "D:\\PTIT\\DATN\\source\\app\\lib\\core\\config\\app_config.dart"
      provides: "useMock defaultValue: false"
    - path: "D:\\PTIT\\DATN\\source\\server\\.planning\\ROADMAP.md"
      provides: "Bảng Progress cập nhật đúng, Phase 6 đánh dấu hoàn thành với ghi chú SC#6 dời"
  key_links:
    - from: "app_config.dart useMock=false"
      to: "core/network/mock/ vẫn giữ nguyên, chạy được qua --dart-define=USE_MOCK=true"
      via: "cờ build-time, không xoá code mock"
      pattern: "defaultValue: false"
---

<objective>
Đóng Phase 6: đổi cờ mặc định `USE_MOCK` sang `false` (D-21) trong khi giữ nguyên `core/network/mock/` (D-22, SC#7), cập nhật tài liệu phản ánh thực tế đã vấp (D-23/D-24), sửa bảng Progress `ROADMAP.md` lỗi thời (D-25), và ghi rõ SC#6 (AI drafts) bị dời — không âm thầm đánh dấu đạt (D-03).

Purpose: Đây là bước chốt phase — không thêm code nghiệp vụ mới, chỉ chuyển trạng thái mặc định và đồng bộ tài liệu với thực tế đã triển khai ở 4 plan trước.
Output: App mặc định chạy backend thật khi build không truyền cờ, tài liệu (`CHUYEN-SANG-API-THAT.md`, `api/04-GIAO-DICH.md`, `ROADMAP.md`, `STATE.md`) khớp thực tế.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\source\server\.planning\phases\06-n-i-api-th-t-v-i-app-flutter\06-CONTEXT.md
@D:\PTIT\DATN\source\server\.planning\phases\06-n-i-api-th-t-v-i-app-flutter\06-01-PLAN.md
@D:\PTIT\DATN\source\server\.planning\phases\06-n-i-api-th-t-v-i-app-flutter\06-02-PLAN.md
@D:\PTIT\DATN\source\server\.planning\ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Đổi cờ useMock mặc định (D-21), xác nhận mock vẫn chạy được (SC#7)</name>
  <files>D:\PTIT\DATN\source\app\lib\core\config\app_config.dart</files>
  <read_first>
    D:\PTIT\DATN\source\app\lib\core\config\app_config.dart
    D:\PTIT\DATN\source\app\lib\core\network\api_client.dart
    D:\PTIT\DATN\source\app\lib\core\network\mock\mock_interceptor.dart
  </read_first>
  <action>
1. Trong `app_config.dart` dòng chứa `useMock`, đổi:
```dart
static const useMock = bool.fromEnvironment('USE_MOCK', defaultValue: true);
```
thành:
```dart
static const useMock = bool.fromEnvironment('USE_MOCK', defaultValue: false);
```
2. Sửa comment phía trên (dòng 18-20 hiện ghi "Mặc định bật vì `source/backend/` chưa dựng") thành phản ánh đúng trạng thái sau Phase 6:
```dart
  /// Bật thì mọi yêu cầu được chặn lại và trả dữ liệu mẫu, không ra mạng.
  ///
  /// Mặc định **tắt** — backend Spring Boot đã nối xong từ Phase 6. Muốn chạy
  /// dữ liệu mẫu (không cần bật backend) thì truyền `--dart-define=USE_MOCK=true`.
  /// Dữ liệu mẫu đi qua đúng lớp Dio nên tầng repository và provider không biết
  /// khác biệt — xem `core/network/mock/`, giữ nguyên không xoá (D-22).
```
3. KHÔNG xoá bất kỳ file nào trong `core/network/mock/` — xác nhận thư mục này còn nguyên bằng cách liệt kê lại sau khi sửa.
  </action>
  <verify>
    <automated>grep -c "defaultValue: false" D:\PTIT\DATN\source\app\lib\core\config\app_config.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep "defaultValue: false" D:\PTIT\DATN\source\app\lib\core\config\app_config.dart` khớp
    - `grep "defaultValue: true" D:\PTIT\DATN\source\app\lib\core\config\app_config.dart` KHÔNG còn khớp
    - `find D:\PTIT\DATN\source\app\lib\core\network\mock -type f | wc -l` > 0 (thư mục mock còn nguyên file)
    - `cd D:\PTIT\DATN\source\app && flutter test integration_test/add_transaction_flow_test.dart` (không truyền dart-define, chạy mặc định — giờ mặc định là `false` nên PHẢI truyền `--dart-define=USE_MOCK=true` để test này tiếp tục pass qua mock) — chạy `flutter test integration_test/add_transaction_flow_test.dart --dart-define=USE_MOCK=true` pass
  </acceptance_criteria>
  <done>useMock mặc định false, mock vẫn chạy được khi ép cờ true, thư mục mock không bị xoá.</done>
</task>

<task type="auto">
  <name>Task 2: Cập nhật CHUYEN-SANG-API-THAT.md và api/04-GIAO-DICH.md (D-23/D-24)</name>
  <files>
    D:\PTIT\DATN\source\app\CHUYEN-SANG-API-THAT.md
    D:\PTIT\DATN\api\04-GIAO-DICH.md
  </files>
  <read_first>
    D:\PTIT\DATN\source\app\CHUYEN-SANG-API-THAT.md
    D:\PTIT\DATN\api\04-GIAO-DICH.md
  </read_first>
  <action>
1. Trong `CHUYEN-SANG-API-THAT.md`, cập nhật dòng cuối file (hiện ghi *"Tài liệu này viết khi backend chưa dựng. Khi nối xong, cập nhật lại cột 'Backend phải làm gì' thành ghi chú về chỗ thực tế đã vấp."*) thành ghi chú tổng kết Phase 6, và sửa từng dòng trong bảng đối chiếu §3 mà thực tế đã vấp trong quá trình triển khai:
   - Dòng `POST /transactions`: từ "Ghi giao dịch, cập nhật số dư ví thật, trả ngân sách bị ảnh hưởng" thêm ghi chú: *"Đã vấp: `affected_budgets` ban đầu luôn trả rỗng dù `BudgetService` đã tồn tại — comment cũ trong code ghi 'Phase 4 chưa có' nhưng không được dọn lại sau khi Phase 4 xong. Sửa ở 06-01-PLAN.md."*
   - Dòng `PUT /transactions/{id}` và `DELETE /transactions/{id}`: xoá chữ "Chưa có tuyến" — backend đã có đầy đủ từ Phase 3, mock chưa có tuyến chứ không phải backend.
   - Dòng `POST /budgets`: xoá chữ "Chưa có tuyến" — tương tự, backend đã có từ Phase 4.
   - Thêm ghi chú cuối §3: *"D-11 (06-CONTEXT.md) từng nghi ngờ `/budgets/suggestion` và `/reports/by-category-group` thiếu — đã xác minh cả hai TỒN TẠI và hoạt động đúng khi nối (RESEARCH.md Phase 6)."*
2. Cập nhật dòng cuối file thành:
   *"Cập nhật 29/08/2026 sau khi nối xong Phase 6 — cột 'Backend phải làm gì' ở trên đã ghi chú lại các điểm chênh thực tế đã vấp khi triển khai (chủ yếu là dữ liệu tài liệu bị lỗi thời so với mã đã hoàn thiện ở các phase trước, không phải backend thiếu chức năng)."*
3. Trong `api/04-GIAO-DICH.md`, xác nhận đặc tả `affected_budgets` (dòng 249-264) khớp đúng với hiện thực mới ở `06-01-PLAN.md` (field `id`, `category`, `limit_amount`, `spent_amount`, `ratio`, `status`, `alert`) — nếu khớp, không cần sửa gì (đặc tả tài liệu vốn đúng, chỉ backend từng lệch). Nếu phát hiện lệch tên field trong lúc đọc `AffectedBudgetResponse.java` đã tạo ở 06-01, sửa lại `api/04-GIAO-DICH.md` cho khớp CODE THẬT (D-24: sai lệch nào phát hiện lúc nối phải sửa tài liệu trong cùng phase).
  </action>
  <verify>
    <automated>grep -n "D-11" D:\PTIT\DATN\source\app\CHUYEN-SANG-API-THAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Chưa có tuyến" D:\PTIT\DATN\source\app\CHUYEN-SANG-API-THAT.md` — số lần khớp giảm so với trước (không còn ở dòng PUT/DELETE transactions, POST budgets)
    - `grep "affected_budgets" D:\PTIT\DATN\source\app\CHUYEN-SANG-API-THAT.md` vẫn khớp (dòng ghi chú mới đã thêm)
    - `grep "D-11" D:\PTIT\DATN\source\app\CHUYEN-SANG-API-THAT.md` khớp (ghi chú đã xác minh endpoint tồn tại)
  </acceptance_criteria>
  <done>Tài liệu app phản ánh đúng thực tế backend đã có sẵn 6 nhóm API, không còn mô tả sai "chưa có tuyến" cho các endpoint backend thật sự đã có.</done>
</task>

<task type="auto">
  <name>Task 3: Sửa bảng Progress ROADMAP.md (D-25) và ghi chú SC#6 dời (D-03)</name>
  <files>
    D:\PTIT\DATN\source\server\.planning\ROADMAP.md
    D:\PTIT\DATN\source\server\.planning\STATE.md
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\.planning\ROADMAP.md
    D:\PTIT\DATN\source\server\.planning\STATE.md
  </read_first>
  <action>
1. Trong `ROADMAP.md`, tìm bảng ở mục "## Progress" (cột `Phase | Plans Complete | Status | Completed`), sửa các dòng đang lỗi thời:
```
| 3. Giao dịch | 4/4 | Complete | 2026-08-2x |
| 4. Nghiệp vụ phái sinh & Báo cáo | 7/7 | Complete | 2026-08-2x |
| 5. Nhóm gia đình & Trợ lý AI | 0/TBD | Not started | - |
| 6. Nối API thật với app Flutter | 5/5 | Complete (SC#6 dời) | 2026-08-29 |
```
Điều chỉnh ngày tháng thật theo lịch sử commit thực tế (`git log --oneline -- .planning/phases/03-*` và tương tự cho Phase 4) thay vì ghi cứng `2026-08-2x` — tra cứu ngày commit gần nhất của mỗi phase trước khi điền.
2. Trong mục "### Phase 6" của `ROADMAP.md`, sau dòng "**Success Criteria**", thêm 1 dòng ghi chú NGAY SAU danh sách 8 tiêu chí:
```
**Ghi chú khi đóng phase:** Success Criteria #6 (duyệt bản nháp AI có sửa → `user_corrections`) KHÔNG kiểm chứng được trong phase này — phụ thuộc Phase 5 (`GroupController`/`AiController`) chưa code (D-01/D-03, 06-CONTEXT.md). Dời sang phase nối nhóm `ai` sau khi Phase 5 hoàn thành. 7/8 tiêu chí còn lại đã kiểm chứng qua `integration_test/real_backend/` + thử tay trên máy ảo Pixel 7.
```
3. Đánh dấu ô Plans của Phase 6 trong mục "Plans:" (danh sách gạch đầu dòng) thành `[x]` cho cả 5 plan (06-01 đến 06-05) SAU KHI xác nhận cả 5 đã thực thi xong (chỉ thực hiện bước này khi review cuối, không tick trước khi verify).
4. Trong `STATE.md`, cập nhật `progress.completed_phases` trong frontmatter và mục "Current Position" phản ánh Phase 6 đã hoàn thành (số liệu chính xác lấy từ số plan thật đã tồn tại trong `.planning/phases/06-*/`).
  </action>
  <verify>
    <automated>grep -n "Success Criteria #6" D:\PTIT\DATN\source\server\.planning\ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SC#6 dời\|Success Criteria #6" D:\PTIT\DATN\source\server\.planning\ROADMAP.md` khớp
    - `grep "3. Giao dịch | 0/TBD" D:\PTIT\DATN\source\server\.planning\ROADMAP.md` KHÔNG còn khớp (đã sửa từ Not started)
    - `grep "Nhóm gia đình" D:\PTIT\DATN\source\server\.planning\ROADMAP.md` — dòng Progress Phase 5 vẫn đúng "Not started" (chưa code, không sửa nhầm thành Complete)
  </acceptance_criteria>
  <done>ROADMAP.md và STATE.md phản ánh đúng trạng thái thật của từng phase, SC#6 ghi rõ bị dời không đánh dấu đạt.</done>
</task>


<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Xác nhận thủ công 4 màn chính hiển thị đúng dữ liệu backend thật (SC3)</name>
  <files>Không sửa file — chỉ chạy app và quan sát</files>
  <action>Chạy app trên máy ảo Pixel 7 với backend thật đã seed, đăng nhập, và xem lần lượt 4 màn chính để xác nhận dữ liệu đến từ backend chứ không phải mock.</action>
  <what-built>Toàn bộ 6 nhóm API (auth, wallets, categories, transactions, budgets, reports) đã nối xong qua 06-01 đến 06-04. App build với USE_MOCK=false (mặc định mới sau Task 1 của plan này) sẽ gọi thẳng backend thật cho mọi màn hình.</what-built>
  <how-to-verify>
    1. Đảm bảo backend đang chạy thật (`mvn spring-boot:run` profile `dev` từ `D:\PTIT\DATN\source\server`, đã seed dữ liệu qua `DevDataSeeder`).
    2. Chạy app trên máy ảo Pixel 7: `flutter run --dart-define=USE_MOCK=false --dart-define=API_BASE_URL=http://10.0.2.2:8080/v1` (từ `D:\PTIT\DATN\source\app`).
    3. Đăng nhập bằng `seed.user@datn.local` / `SeedPass123!`.
    4. Kiểm 4 màn chính:
       - **Tổng quan:** số dư tổng, biểu đồ xu hướng hiển thị dữ liệu thật (không phải số cố định của mock).
       - **Sổ giao dịch:** danh sách giao dịch thật từ CSDL, phân trang hoạt động.
       - **Ngân sách:** ngân sách seed "Ăn uống" hiện đúng tiến độ tính động.
       - **Thêm giao dịch:** ghi 1 khoản chi mới — quay lại Tổng quan/Ngân sách, xác nhận số dư ví và tiến độ ngân sách đổi NGAY (không cần refresh thủ công/khởi động lại app).
    5. Xác nhận cả 4 màn không còn hiện dữ liệu mẫu cố định nào của `MockRouter` (ví dụ tên ví/giao dịch mẫu đặc trưng của mock).
    6. **D-14 (06-CONTEXT.md): đếm lượt `/auth/refresh` thật của SC5.** Để tránh mốc thời gian ghi tay sai lệch vài giây, chạy lệnh `date` (in ra terminal, ví dụ `date -u +%Y-%m-%dT%H:%M:%SZ`) NGAY TRƯỚC và NGAY SAU lệnh `flutter test integration_test/real_backend/refresh_token_concurrency_test.dart ...` (06-04 Task 2) — dùng đúng 2 giá trị `date` in ra đó (không phải ước lượng bằng mắt) làm `<thoi_diem_bat_dau_test>`/`<thoi_diem_ket_thuc_test>` cho truy vấn `psql` bên dưới. Sau đó chạy một trong hai cách:
       - Đọc log backend (console `mvn spring-boot:run`): đếm số dòng log request `POST /v1/auth/refresh` xuất hiện trong đúng cửa sổ thời gian giữa 2 mốc `date` vừa ghi — PHẢI đúng 1 dòng (không phải 3, ứng với 3 request song song đã gộp lại).
       - Hoặc, nếu có quyền truy cập CSDL dev, chạy: `psql -d <ten_csdl_dev> -c "SELECT COUNT(*) FROM refresh_tokens WHERE user_id = (SELECT id FROM users WHERE email = 'seed.user@datn.local') AND revoked_at BETWEEN '<thoi_diem_bat_dau_test>' AND '<thoi_diem_ket_thuc_test>';"` (thay 2 giá trị bằng đúng output lệnh `date` đã ghi ở trên) — kết quả PHẢI = 1 (đúng 1 refresh_token bị thu hồi do rotation trong cửa sổ test, xác nhận đúng 1 lần `/auth/refresh` thành công, không phải 3 lần rời rạc).
       - Ghi kết quả đếm được (số dòng log hoặc số bản ghi) vào `06-05-SUMMARY.md`.
  </how-to-verify>
  <verify>
    <automated>MISSING — không thể tự động hoá xác nhận UI hiển thị đúng bằng mắt; đây là checkpoint thủ công theo thiết kế</automated>
  </verify>
  <resume-signal>Type "approved" nếu cả 4 màn đúng và đã xác nhận đúng 1 lượt /auth/refresh (D-14), hoặc mô tả màn nào sai/còn hiện dữ liệu mẫu/số lượt refresh không đúng 1</resume-signal>
  <done>Người dùng xác nhận cả 4 màn chính hiển thị đúng dữ liệu backend thật, cập nhật ngay sau khi ghi giao dịch, và đúng 1 lượt /auth/refresh được ghi nhận cho test SC5 (D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Build release app | `useMock=false` mặc định nghĩa là bản build không truyền cờ sẽ gọi thẳng backend thật — sai `API_BASE_URL` lúc build release sẽ khiến app không hoạt động thay vì âm thầm dùng mock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-06-08 | Repudiation (tài liệu sai lệch, không phải tấn công) | `ROADMAP.md`/`CHUYEN-SANG-API-THAT.md` tiếp tục lỗi thời sau phase này nếu không ai cập nhật | accept | Đã sửa trong Task 2/3 của plan này; rủi ro còn lại là quy trình (không cập nhật tài liệu ở phase sau), nằm ngoài phạm vi kỹ thuật của Phase 6 |
</threat_model>

<verification>
1. `cd D:\PTIT\DATN\source\app && flutter analyze` không có lỗi mới
2. `flutter test integration_test/add_transaction_flow_test.dart --dart-define=USE_MOCK=true` — pass (SC#7: mock vẫn chạy được)
3. `grep -c "defaultValue: false" D:\PTIT\DATN\source\app\lib\core\config\app_config.dart` = 1
4. `grep "Success Criteria #6" D:\PTIT\DATN\source\server\.planning\ROADMAP.md` khớp, xác nhận ghi rõ "dời", không phải "đạt"
</verification>

<success_criteria>
- App build mặc định gọi backend thật, mock vẫn chạy được qua cờ `USE_MOCK=true`
- `CHUYEN-SANG-API-THAT.md` và `api/04-GIAO-DICH.md` phản ánh đúng thực tế backend đã hoàn thiện
- `ROADMAP.md`/`STATE.md` không còn dữ liệu Progress lỗi thời, SC#6 ghi rõ bị dời sang phase sau
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/06-n-i-api-th-t-v-i-app-flutter/06-05-SUMMARY.md`
</output>
