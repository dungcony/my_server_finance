---
phase: 06-n-i-api-th-t-v-i-app-flutter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - D:\PTIT\DATN\source\server\src\main\resources\application.yml
  - D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\ratelimit\RateLimitProperties.java
  - D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\exception\GlobalExceptionHandler.java
  - D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\auth\AuthIdempotencyRateLimitEndToEndTest.java
  - D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\dto\CreateTransactionResponse.java
  - D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\dto\AffectedBudgetResponse.java
  - D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionService.java
  - D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\seed\DevDataSeeder.java
  - D:\PTIT\DATN\source\server\.planning\STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "App gọi http://10.0.2.2:8080/v1/auth/login (context-path /v1) và nhận đúng response thay vì 404"
    - "Sai mật khẩu /v1/auth/login quá 5 lần/phút/IP vẫn bị khoá 429 — quota auth không rơi về rule default 120/phút"
    - "POST /transactions trả affected_budgets khác rỗng khi khoản chi làm ngân sách chạm near_limit/over_limit"
    - "Chạy lại DevDataSeeder nhiều lần không tạo trùng user/ví/ngân sách thử nghiệm"
    - "STATE.md không còn ghi '2 gap Phase 4 cần đóng' — phản ánh đúng DebtReminderWorker/ExportAsyncRunner đã hoàn thiện"
    - "401 do access token hết hạn trả đúng error.code=TOKEN_EXPIRED (khác TOKEN_INVALID cho chữ ký sai/định dạng sai) — AuthInterceptor phía app phân biệt được hai trường hợp"
  artifacts:
    - path: "D:\\PTIT\\DATN\\source\\server\\src\\main\\resources\\application.yml"
      provides: "server.servlet.context-path: /v1"
    - path: "D:\\PTIT\\DATN\\source\\server\\src\\main\\java\\com\\datn\\financeapp\\transaction\\dto\\AffectedBudgetResponse.java"
      provides: "DTO map từ BudgetProgressProjection sang JSON affected_budgets"
    - path: "D:\\PTIT\\DATN\\source\\server\\src\\main\\java\\com\\datn\\financeapp\\common\\seed\\DevDataSeeder.java"
      provides: "CommandLineRunner @Profile(dev) dựng user thử, 2 ví, 1 ngân sách — chạy lại được (D-16)"
  key_links:
    - from: "RateLimitProperties.RULES"
      to: "RateLimitFilter (getRequestURI trả /v1/auth/...)"
      via: "pathPrefix startsWith so khớp URI đầy đủ gồm context-path"
      pattern: "\"/v1/auth/\""
    - from: "TransactionService.create/update/duplicate"
      to: "BudgetProgressRepository.findActiveByUserAndCategoryInTree"
      via: "gọi đồng bộ trong cùng @Transactional, không qua event listener"
      pattern: "findActiveByUserAndCategoryInTree"
    - from: "JwtAuthFilter.ATTR_TOKEN_ERROR (request attribute TOKEN_EXPIRED/TOKEN_INVALID)"
      to: "GlobalExceptionHandler.handleAuthentication (error.code trong response 401)"
      via: "đọc request.getAttribute(JwtAuthFilter.ATTR_TOKEN_ERROR) thay vì hardcode UNAUTHENTICATED"
      pattern: "ATTR_TOKEN_ERROR"
---

<objective>
Sửa 2 lỗ hổng kỹ thuật mà việc thêm `server.servlet.context-path: /v1` (D-05) sẽ kéo theo âm thầm nếu làm riêng lẻ (Task 1), sửa lỗi có sẵn khiến mọi lỗi xác thực 401 bị làm phẳng thành `UNAUTHENTICATED` thay vì phân biệt `TOKEN_EXPIRED`/`TOKEN_INVALID` (Task 2), điền đúng `affected_budgets` trong response `POST /transactions` (lệch hợp đồng "không lỗi nào hiện ra" mà RESEARCH.md phát hiện, Task 3), dựng script seed dữ liệu thử chạy lại được và sửa lại `STATE.md` cho đúng thực tế (Task 4).

Purpose: Đây là nền tảng bắt buộc trước khi bất kỳ plan nào khác của Phase 6 chạy — mọi test/kiểm chứng sau này đều gọi qua `/v1` và cần dữ liệu seed ổn định.
Output: Backend chạy trên context-path `/v1` với rate-limit đúng quota, 401 trả đúng error.code theo nguyên nhân thật, `affected_budgets` trả dữ liệu thật, `DevDataSeeder` idempotent, tài liệu trạng thái Phase 4 chính xác.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\source\server\.planning\phases\06-n-i-api-th-t-v-i-app-flutter\06-CONTEXT.md
@D:\PTIT\DATN\source\server\.planning\phases\06-n-i-api-th-t-v-i-app-flutter\06-RESEARCH.md
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\api\04-GIAO-DICH.md

<interfaces>
Từ `BudgetProgressRepository.java` (projection dùng lại để dựng `AffectedBudgetResponse`):
```java
interface BudgetProgressProjection {
    UUID getId();
    UUID getCategoryId();
    UUID getWalletId();
    Long getLimitAmount();
    Long getSpentAmount();
    Long getRemaining();
    BigDecimal getRatio();
    String getStatus();          // normal | near_limit | over_limit
    Integer getDaysRemaining();
    String getPeriodType();
    LocalDate getStartDate();
}

List<BudgetProgressProjection> findActiveByUserAndCategoryInTree(
        UUID currentUser, UUID categoryId, LocalDate transactionDate);
```

Từ `CreateTransactionResponse.java` hiện tại (toàn văn):
```java
public record CreateTransactionResponse(TransactionResponse transaction, NewBalance newBalance, List<Object> affectedBudgets) {
    public CreateTransactionResponse(TransactionResponse transaction, NewBalance newBalance) {
        this(transaction, newBalance, List.of());
    }
    public record NewBalance(UUID walletId, Long balance) {}
}
```

Hợp đồng JSON kỳ vọng ở `api/04-GIAO-DICH.md` dòng 249-262 (mỗi phần tử `affected_budgets`):
```json
{
  "id": "b1000000-...",
  "category": "Ăn uống",
  "limit_amount": 3000000,
  "spent_amount": 2498000,
  "ratio": 0.83,
  "status": "near_limit",
  "alert": "Còn 502.000 đ cho 23 ngày còn lại."
}
```

Từ `AsyncConfig.java` (mẫu cấu trúc `@Configuration`/`@Bean` theo profile, dùng làm khung style cho `DevDataSeeder`):
```java
@Configuration
public class AsyncConfig {
    @Bean(name = "exportTaskExecutor")
    public Executor exportTaskExecutor() { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thêm context-path /v1, vá 2 điểm vỡ âm thầm (rate-limit + test path)</name>
  <files>
    D:\PTIT\DATN\source\server\src\main\resources\application.yml
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\ratelimit\RateLimitProperties.java
    D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\auth\AuthIdempotencyRateLimitEndToEndTest.java
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\resources\application.yml
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\ratelimit\RateLimitProperties.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\ratelimit\RateLimitFilter.java
    D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\auth\AuthIdempotencyRateLimitEndToEndTest.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\security\SecurityConfig.java
  </read_first>
  <action>
1. Trong `application.yml`, thêm khối cấp cao nhất (KHÔNG lồng dưới `spring:`) — hiện chưa có khối `server:` nào:
```yaml
server:
  servlet:
    context-path: /v1
```
2. Trong `RateLimitProperties.java`, đổi `pathPrefix` của 2 rule đầu (D-04/D-05 đã chốt prefix cứng `/v1`, không đọc động qua `@Value` vì context-path không đổi trong dự án này):
```java
public static final List<RateLimitRule> RULES = List.of(
        new RateLimitRule("auth", "/v1/auth/", 5, Duration.ofMinutes(1), true),
        new RateLimitRule("ai", "/v1/ai/", 30, Duration.ofMinutes(1), false),
        new RateLimitRule("default", "/", 120, Duration.ofMinutes(1), false));
```
Rule `"default"` giữ nguyên `pathPrefix = "/"` — đây là fallback catch-all, `resolve()` không so `startsWith` cho nó (đã loại trừ tường minh trong code hiện tại).
3. Trong `AuthIdempotencyRateLimitEndToEndTest.java` dòng 91, đổi path literal:
```java
return restTemplate.exchange(
        "/v1/auth/register",
        org.springframework.http.HttpMethod.POST,
        ...
```
Đây là **duy nhất** chỗ trong file dùng path tuyệt đối cần sửa (`TestRestTemplate` không tự cộng context-path, khác `MockMvc`). Kiểm lại toàn văn file để tìm mọi occurrence khác của `"/auth/` nếu có (RESEARCH.md xác nhận chỉ có 1 dòng, nhưng verify lại bằng grep trước khi coi là xong).
4. KHÔNG sửa `SecurityConfig.java` — `requestMatchers("/auth/register", ...)` tự động loại trừ context-path theo cơ chế Spring Security, giữ nguyên path tương đối không có `/v1`.
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server && mvn -q -o compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep "context-path: /v1" D:\PTIT\DATN\source\server\src\main\resources\application.yml` khớp
    - `grep "/v1/auth/" D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\ratelimit\RateLimitProperties.java` khớp 1 dòng, `grep "/v1/ai/"` khớp 1 dòng
    - `grep "\"/auth/register\"" D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\auth\AuthIdempotencyRateLimitEndToEndTest.java` KHÔNG còn khớp (đã đổi thành `/v1/auth/register`)
    - `grep "requestMatchers" D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\security\SecurityConfig.java` vẫn còn `/auth/register` KHÔNG có `/v1` (file này không sửa)
    - `mvn -q -o compile` (từ `D:\PTIT\DATN\source\server`) thoát mã 0
  </acceptance_criteria>
  <done>context-path /v1 hoạt động, rate-limit auth/ai không bị fallback về default, test end-to-end path đúng.</done>
</task>

<task type="auto">
  <name>Task 2: Sửa lỗi 401 luôn trả UNAUTHENTICATED thay vì TOKEN_EXPIRED/TOKEN_INVALID</name>
  <files>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\exception\GlobalExceptionHandler.java
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\security\JwtAuthFilter.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\exception\GlobalExceptionHandler.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\response\ErrorResponse.java
  </read_first>
  <action>
**Lỗi phát hiện khi review (đã đọc mã trực tiếp, xác nhận thật — không phải suy đoán):** `JwtAuthFilter.doFilterInternal` đã set `request.setAttribute(ATTR_TOKEN_ERROR, "TOKEN_EXPIRED"/"TOKEN_INVALID")` khi bắt `ExpiredJwtException`/`JwtException`, nhưng **không nơi nào đọc lại attribute này** — `GlobalExceptionHandler.handleAuthentication(AuthenticationException ex)` luôn trả cứng `error.code = "UNAUTHENTICATED"` bất kể lý do thật. Hậu quả: mọi request tới endpoint cần xác thực với access token hết hạn HOẶC sai chữ ký HOẶC thiếu hẳn Bearer header đều trả cùng một `UNAUTHENTICATED`, khiến `AuthInterceptor` phía app (chỉ phản ứng với `TOKEN_EXPIRED` theo `api/00-QUY-UOC-CHUNG.md` mục 8) không bao giờ tự động làm mới thẻ — phá vỡ toàn bộ luồng refresh ngầm (SC5), không chỉ ảnh hưởng lúc viết test ở 06-04 mà là bug thật trong luồng nghiệp vụ.

Sửa `handleAuthentication` đọc lại attribute:
```java
@ExceptionHandler(AuthenticationException.class)
public ResponseEntity<ErrorResponse> handleAuthentication(AuthenticationException ex, HttpServletRequest request) {
    Object tokenError = request.getAttribute(JwtAuthFilter.ATTR_TOKEN_ERROR);
    String code = tokenError instanceof String s ? s : "UNAUTHENTICATED";
    String message = "TOKEN_EXPIRED".equals(code)
            ? "Thẻ truy cập đã hết hạn."
            : "TOKEN_INVALID".equals(code)
                    ? "Thẻ truy cập không hợp lệ."
                    : "Vui lòng đăng nhập để tiếp tục.";
    var body = new ErrorResponse(false, new ErrorResponse.ErrorBody(code, message));
    return ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(body);
}
```
Thêm import `com.datn.financeapp.common.security.JwtAuthFilter` vào `GlobalExceptionHandler.java` (đã có sẵn import `HttpServletRequest` từ `handleUnexpected`, dùng lại). Không đổi `JwtAuthFilter.java` — nó đã set đúng attribute từ trước, chỉ thiếu phía đọc.
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server && mvn -q -o compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ATTR_TOKEN_ERROR" D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\exception\GlobalExceptionHandler.java` khớp
    - `grep "request.getAttribute" D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\exception\GlobalExceptionHandler.java` khớp
    - `mvn -q -o compile` thoát mã 0
    - `curl -s -X GET http://localhost:8080/v1/auth/me -H "Authorization: Bearer sai-chu-ky.gia.mao"` (server đang chạy) trả 401 với `error.code = "TOKEN_INVALID"` (không phải `UNAUTHENTICATED`)
  </acceptance_criteria>
  <done>401 trả đúng error.code TOKEN_EXPIRED/TOKEN_INVALID/UNAUTHENTICATED theo nguyên nhân thật thay vì luôn UNAUTHENTICATED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Điền affected_budgets thật trong POST/PUT/duplicate transactions</name>
  <files>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\dto\AffectedBudgetResponse.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\dto\CreateTransactionResponse.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionService.java
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\dto\CreateTransactionResponse.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionService.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\budget\service\BudgetAlertListener.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\budget\repository\BudgetProgressRepository.java
    D:\PTIT\DATN\api\04-GIAO-DICH.md
  </read_first>
  <behavior>
    - Test 1: `POST /transactions` với `type=expense`, `categoryId` gán vào danh mục "Cà phê" (con của "Ăn uống"), khi ngân sách "Ăn uống" đã chi vượt 80% hạn mức → response `affected_budgets` chứa đúng 1 phần tử với `status` là `near_limit`/`over_limit` và `category` là tên danh mục GỐC của ngân sách (không phải "Cà phê")
    - Test 2: `POST /transactions` với `type=income` → `affected_budgets` LUÔN rỗng bất kể ngân sách nào đang gần hạn (income không tính vào ngân sách)
    - Test 3: `POST /transactions` với `type=expense` nhưng `categoryId=null` (không hợp lệ theo validateShape nhưng nếu qua được) → không NPE, `affected_budgets` rỗng
    - Test 4: Ngân sách đang `status=normal` (chưa chạm 80%) → KHÔNG xuất hiện trong `affected_budgets` (chỉ báo khi cần chú ý, giống điều kiện `BudgetAlertListener` dòng 56 `if ("normal".equals(...)) continue;`)
  </behavior>
  <action>
1. Tạo file mới `AffectedBudgetResponse.java` (record), map trực tiếp từ `BudgetProgressProjection`:
```java
package com.datn.financeapp.transaction.dto;

import java.math.BigDecimal;
import java.util.UUID;

/**
 * Phần tử của {@code affected_budgets} trong response POST/PUT/duplicate transactions
 * (api/04-GIAO-DICH.md mục 4, dòng 249-262). Map trực tiếp từ
 * {@code BudgetProgressRepository.BudgetProgressProjection}.
 */
public record AffectedBudgetResponse(
        UUID id,
        String category,
        Long limitAmount,
        Long spentAmount,
        BigDecimal ratio,
        String status,
        String alert) {
}
```
2. Trong `CreateTransactionResponse.java`, đổi kiểu `List<Object>` thành `List<AffectedBudgetResponse>`, XOÁ constructor 2-tham số (buộc mọi call site truyền tường minh danh sách — kể cả rỗng — để không lặp lại lỗi "quên truyền nên luôn rỗng"):
```java
public record CreateTransactionResponse(
        TransactionResponse transaction, NewBalance newBalance, List<AffectedBudgetResponse> affectedBudgets) {

    public record NewBalance(UUID walletId, Long balance) {
    }
}
```
3. Trong `TransactionService.java`, thêm field `private final BudgetProgressRepository budgetProgressRepository;` vào danh sách field (constructor injection qua `@RequiredArgsConstructor` đã có sẵn), import:
```java
import com.datn.financeapp.budget.repository.BudgetProgressRepository;
import com.datn.financeapp.budget.repository.BudgetProgressRepository.BudgetProgressProjection;
import com.datn.financeapp.transaction.dto.AffectedBudgetResponse;
```
4. Thêm method private dùng chung:
```java
/**
 * Tính affected_budgets cho response POST/PUT/duplicate — gọi ĐỒNG BỘ trong cùng
 * @Transactional (khác BudgetAlertListener chạy AFTER_COMMIT bất đồng bộ), vì response
 * phải trả ngay trong request này (api/04-GIAO-DICH.md dòng 264: "không phải gọi thêm lần nữa").
 * Chỉ tính khi type=expense và categoryId != null — cùng điều kiện BudgetAlertListener áp dụng.
 */
private List<AffectedBudgetResponse> computeAffectedBudgets(UUID userId, String type, UUID categoryId, LocalDate date) {
    if (!"expense".equals(type) || categoryId == null) {
        return List.of();
    }
    List<BudgetProgressProjection> affected =
            budgetProgressRepository.findActiveByUserAndCategoryInTree(userId, categoryId, date);
    List<AffectedBudgetResponse> result = new ArrayList<>();
    for (BudgetProgressProjection budget : affected) {
        if ("normal".equals(budget.getStatus())) {
            continue;
        }
        Category root = categoryRepository.findById(budget.getCategoryId()).orElse(null);
        String categoryName = root != null ? root.getName() : "";
        long remaining = budget.getRemaining() == null ? 0L : budget.getRemaining();
        int daysRemaining = budget.getDaysRemaining() == null ? 0 : budget.getDaysRemaining();
        String alert = "over_limit".equals(budget.getStatus())
                ? "Vượt " + Math.abs(remaining) + " đ khi kỳ còn " + daysRemaining + " ngày."
                : "Còn " + remaining + " đ cho " + daysRemaining + " ngày còn lại của kỳ.";
        result.add(new AffectedBudgetResponse(
                budget.getId(), categoryName, budget.getLimitAmount(), budget.getSpentAmount(),
                budget.getRatio(), budget.getStatus(), alert));
    }
    return result;
}
```
5. Sửa 3 chỗ dựng `CreateTransactionResponse` trong `TransactionService.java`:
   - `create()` (khoảng dòng 121): thay
     ```java
     return new CreateTransactionResponse(
             toResponse(saved),
             new CreateTransactionResponse.NewBalance(req.walletId(), result.walletNewBalance()));
     ```
     thành
     ```java
     return new CreateTransactionResponse(
             toResponse(saved),
             new CreateTransactionResponse.NewBalance(req.walletId(), result.walletNewBalance()),
             computeAffectedBudgets(userId, req.type(), req.categoryId(), date));
     ```
   - `update()` (khoảng dòng 197): thay tương tự, dùng `req.type()`, `req.categoryId()`, `old.getDate()` (giá trị đã ghi ở bước 2 của 3-bước, biến `old` đã có sẵn trong scope)
   - `duplicate()` (khoảng dòng 283): dùng `original.getType()`, `original.getCategoryId()`, `date` (biến local đã tính ở đầu method)
6. `delete()` không đổi — `DeleteTransactionResponse` không có `affectedBudgets`, không thuộc phạm vi task này.
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server && mvn -q -o compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep "List<AffectedBudgetResponse> affectedBudgets" D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\dto\CreateTransactionResponse.java` khớp
    - `grep "computeAffectedBudgets" D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionService.java` khớp ít nhất 4 lần (định nghĩa + 3 call site)
    - `grep "public CreateTransactionResponse(TransactionResponse transaction, NewBalance newBalance)" D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\dto\CreateTransactionResponse.java` KHÔNG còn khớp (constructor 2-tham số đã xoá)
    - `mvn -q -o compile` thoát mã 0
  </acceptance_criteria>
  <done>3 call site POST/PUT/duplicate transactions trả affected_budgets thật, biên dịch sạch.</done>
</task>

<task type="auto">
  <name>Task 4: DevDataSeeder idempotent (D-15/D-16) + sửa STATE.md phản ánh đúng Phase 4</name>
  <files>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\seed\DevDataSeeder.java
    D:\PTIT\DATN\source\server\.planning\STATE.md
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\report\config\AsyncConfig.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\auth\service\AuthService.java
    D:\PTIT\DATN\db\migration\V1__nen_tang.sql
    D:\PTIT\DATN\db\migration\V3__ngan_sach.sql
    D:\PTIT\DATN\db\migration\V5__du_lieu_he_thong.sql
    D:\PTIT\DATN\source\server\.planning\STATE.md
  </read_first>
  <action>
1. Tạo package mới `common/seed/` và file `DevDataSeeder.java`:
```java
package com.datn.financeapp.common.seed;

import java.time.LocalDate;
import java.util.UUID;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Profile;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Component;

/**
 * Dựng dữ liệu nền cố định cho phép thử Phase 6 (D-15): 1 user thử, 2 ví, 1 ngân sách gắn danh
 * mục hệ thống "Ăn uống" (đã có sẵn từ V5, KHÔNG tạo lại cây danh mục). Chạy lại được (D-16) —
 * mọi bước đều SELECT-tồn-tại-trước-khi-INSERT hoặc ON CONFLICT DO NOTHING.
 *
 * <p>Chỉ chạy khi Spring profile là {@code dev} — KHÔNG chạy ở profile mặc định/test/prod.
 */
@Component
@Profile("dev")
@RequiredArgsConstructor
@Slf4j
public class DevDataSeeder implements CommandLineRunner {

    private static final String SEED_EMAIL = "seed.user@datn.local";
    private static final String SEED_PASSWORD = "SeedPass123!";

    private final JdbcTemplate jdbcTemplate;
    private final PasswordEncoder passwordEncoder;

    @Override
    public void run(String... args) {
        UUID userId = ensureUser();
        UUID walletA = ensureWallet(userId, "Ví A", "cash");
        UUID walletB = ensureWallet(userId, "Ví B", "bank");
        UUID anUongCategoryId = findSystemCategoryId("Ăn uống", "expense");
        ensureBudget(userId, anUongCategoryId);
        log.info("DevDataSeeder hoàn tất: user={}, walletA={}, walletB={}, category(Ăn uống)={}",
                userId, walletA, walletB, anUongCategoryId);
    }

    /** users.email có UNIQUE (uq_users_email, V1) — ON CONFLICT DO NOTHING an toàn. */
    private UUID ensureUser() {
        jdbcTemplate.update(
                "INSERT INTO users (email, password_hash, full_name, plan) VALUES (?, ?, ?, 'free') "
                        + "ON CONFLICT (email) DO NOTHING",
                SEED_EMAIL, passwordEncoder.encode(SEED_PASSWORD), "Người dùng thử Phase 6");
        return jdbcTemplate.queryForObject(
                "SELECT id FROM users WHERE email = ?", UUID.class, SEED_EMAIL);
    }

    /**
     * wallets có UNIQUE INDEX uq_wallets_user_name trên (user_id, lower(name)) WHERE user_id IS
     * NOT NULL AND NOT is_deleted (V1) — dùng ON CONFLICT theo đúng biểu thức index đó.
     */
    private UUID ensureWallet(UUID userId, String name, String type) {
        jdbcTemplate.update(
                "INSERT INTO wallets (user_id, name, type, initial_balance, current_balance) "
                        + "VALUES (?, ?, ?, 1000000, 1000000) "
                        + "ON CONFLICT (user_id, lower(name)) WHERE user_id IS NOT NULL AND NOT is_deleted DO NOTHING",
                userId, name, type);
        return jdbcTemplate.queryForObject(
                "SELECT id FROM wallets WHERE user_id = ? AND lower(name) = lower(?) AND NOT is_deleted",
                UUID.class, userId, name);
    }

    /** Danh mục hệ thống "Ăn uống"/"Cà phê" đã có sẵn từ V5 (user_id IS NULL) — chỉ đọc, không tạo. */
    private UUID findSystemCategoryId(String name, String type) {
        return jdbcTemplate.queryForObject(
                "SELECT id FROM categories WHERE user_id IS NULL AND parent_category_id IS NULL "
                        + "AND type = ? AND name = ? AND NOT is_deleted",
                UUID.class, type, name);
    }

    /**
     * budgets KHÔNG có UNIQUE constraint nào ngoài PK (V3__ngan_sach.sql đã xác nhận) — PHẢI
     * SELECT-tồn-tại-trước-khi-INSERT, không dùng được ON CONFLICT.
     */
    private void ensureBudget(UUID userId, UUID categoryId) {
        Integer existing = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM budgets WHERE user_id = ? AND category_id = ? AND is_active = TRUE",
                Integer.class, userId, categoryId);
        if (existing != null && existing > 0) {
            log.info("Ngân sách seed đã tồn tại cho user={} category={}, bỏ qua.", userId, categoryId);
            return;
        }
        LocalDate start = LocalDate.now().withDayOfMonth(1);
        LocalDate end = start.plusMonths(1).minusDays(1);
        jdbcTemplate.update(
                "INSERT INTO budgets (user_id, category_id, limit_amount, period_type, start_date, end_date, auto_renew, is_active) "
                        + "VALUES (?, ?, 3000000, 'month', ?, ?, TRUE, TRUE)",
                userId, categoryId, start, end);
    }
}
```
2. Kiểm tra `PasswordEncoder` là bean có sẵn (đã dùng ở `AuthService`) — nếu tên bean khác, điều chỉnh import cho khớp package thật đọc được ở `AuthService.java`.
3. Trong `STATE.md`, sửa dòng frontmatter `status:` (dòng 5) từ:
   `"7/7 plan xong; kiểm chứng 5/7 success criteria — cần đóng 2 gap (DebtReminderWorker, ExportAsyncRunner)"`
   thành:
   `"Phase 4: 7/7 plan xong, 7/7 success criteria đạt (DebtReminderWorker/ExportAsyncRunner đã hiện thực đầy đủ, STATE.md trước đó ghi lỗi thời) — Phase 6 đang triển khai"`
   Sửa tương tự dòng 30 (`Status:` trong mục "Current Position") và dòng 24 (`Current focus:`) cho khớp — không còn nhắc "cần đóng gap".
   Thêm 1 dòng mới vào "Decisions" (mục `### Decisions`) ghi nhận phát hiện của RESEARCH.md:
   `- 06-01: STATE.md sửa lại — DebtReminderWorker/ExportAsyncRunner ĐÃ hiện thực đầy đủ kèm test tích hợp (DebtReminderJobIntegrationTest, ExportJobIntegrationTest); dòng "cần đóng 2 gap" trước đó lỗi thời, không phải gap thật`
  </action>
  <verify>
    <automated>cd D:\PTIT\DATN\source\server && mvn -q -o compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class DevDataSeeder" D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\seed\DevDataSeeder.java` khớp
    - `grep "@Profile(\"dev\")" D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\seed\DevDataSeeder.java` khớp
    - `grep "cần đóng 2 gap" D:\PTIT\DATN\source\server\.planning\STATE.md` KHÔNG còn khớp
    - `mvn -q -o compile` thoát mã 0
  </acceptance_criteria>
  <done>DevDataSeeder biên dịch sạch, chỉ chạy ở profile dev, mọi INSERT idempotent; STATE.md không còn nhắc gap Phase 4 lỗi thời.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client (app/Postman) → backend HTTP | Request đi qua `context-path /v1` rồi `RateLimitFilter` → `JwtAuthFilter` → controller |
| DevDataSeeder → CSDL dev | Chạy tự động lúc khởi động ở profile `dev`, ghi dữ liệu thật vào bảng `users`/`wallets`/`budgets` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-06-01 | Elevation of Privilege | `RateLimitProperties` sau khi thêm context-path | mitigate | Sửa `pathPrefix` thành `/v1/auth/`, `/v1/ai/` đồng thời với D-05 (Task 1), verify lại bằng `AuthIdempotencyRateLimitEndToEndTest` — không để quota brute-force rơi về rule default |
| T-06-02 | Information Disclosure | `DevDataSeeder` tạo user với mật khẩu cố định `SeedPass123!` | accept | Chỉ chạy ở profile `dev`, không bao giờ bật ở `prod`/CI mặc định (`@Profile("dev")`); mật khẩu là dữ liệu thử nội bộ, không phải tài khoản thật |
| T-06-03 | Tampering | `idempotency_keys` cũ trên CSDL dev không khớp `endpoint` mới sau khi thêm context-path | accept | Không phải lỗ hổng bảo mật — chỉ gây nhầm lẫn khi debug thủ công; ghi chú lại trong RESEARCH.md, không cần code sửa (dữ liệu dev có thể xoá tay khi cần) |
| T-06-09 | Information Disclosure | `GlobalExceptionHandler.handleAuthentication` sau khi đọc `ATTR_TOKEN_ERROR` | mitigate | Chỉ trả 3 mã cố định (`TOKEN_EXPIRED`/`TOKEN_INVALID`/`UNAUTHENTICATED`) và thông điệp tiếng Việt chung chung — KHÔNG lộ chi tiết kỹ thuật (nguyên nhân JwtException cụ thể, stack trace); giữ đúng khung `ErrorResponse` hiện có, không thêm trường mới |
</threat_model>

<verification>
1. `cd D:\PTIT\DATN\source\server && mvn -q -o compile` — thoát mã 0
2. `mvn -q -o test -Dtest='!*IntegrationTest,!*EndToEndTest'` — toàn bộ xanh (không cần Docker)
3. Nếu Docker daemon sẵn sàng: `mvn test -Dtest=AuthIdempotencyRateLimitEndToEndTest` — xanh, xác nhận context-path + rate-limit đúng
4. `grep -c "context-path: /v1" src/main/resources/application.yml` = 1
5. `curl -s -X GET http://localhost:8080/v1/auth/me -H "Authorization: Bearer sai-chu-ky.gia.mao"` — trả 401 với `error.code = "TOKEN_INVALID"` (không phải `UNAUTHENTICATED`)
</verification>

<success_criteria>
- 401 do access token hết hạn trả `error.code = TOKEN_EXPIRED`, sai chữ ký/định dạng trả `TOKEN_INVALID` — không còn hardcode `UNAUTHENTICATED` cho mọi trường hợp
- Backend biên dịch và chạy được với context-path `/v1`, rate-limit `auth`/`ai` giữ đúng quota 5/phút và 30/phút
- `POST /transactions` trả `affected_budgets` thật, không còn luôn rỗng
- `DevDataSeeder` chạy lại nhiều lần không tạo trùng bản ghi
- `STATE.md` phản ánh đúng: Phase 4 đã hoàn thành đầy đủ, không còn gap treo
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/06-n-i-api-th-t-v-i-app-flutter/06-01-SUMMARY.md`
</output>
