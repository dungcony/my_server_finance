---
phase: 03-giao-dich
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/main/java/com/datn/financeapp/transaction/dto/TransactionListItemResponse.java
  - src/main/java/com/datn/financeapp/transaction/dto/TransactionDetailResponse.java
  - src/main/java/com/datn/financeapp/transaction/dto/TransactionByDateResponse.java
  - src/main/java/com/datn/financeapp/transaction/dto/TransactionSummaryDto.java
  - src/main/java/com/datn/financeapp/transaction/repository/TransactionRepository.java
  - src/main/java/com/datn/financeapp/transaction/service/TransactionService.java
  - src/main/java/com/datn/financeapp/transaction/controller/TransactionController.java
  - src/test/java/com/datn/financeapp/transaction/TransactionCategoryTreeFilterIntegrationTest.java
  - src/test/java/com/datn/financeapp/transaction/TransactionListQueryIntegrationTest.java
autonomous: true
requirements: [TXN-01, TXN-02, TXN-08]

must_haves:
  truths:
    - "User xem danh sách giao dịch có phân trang, lọc đủ tham số (ngày/loại/ví/danh mục/nguồn/từ khoá/khoảng tiền/include_transfers), summary loại trừ transfer"
    - "User xem giao dịch gom theo ngày (by-date) và chi tiết một giao dịch"
    - "Lọc theo category_id là danh mục cha tự động cộng gộp giao dịch của mọi danh mục con, không lặp điều kiện lọc ở nhiều nơi"
  artifacts:
    - path: "src/main/java/com/datn/financeapp/transaction/repository/TransactionRepository.java"
      provides: "Query native list/summary gọi fn_category_tree qua CategoryRepository, loại type=transfer khỏi summary"
    - path: "src/main/java/com/datn/financeapp/transaction/controller/TransactionController.java"
      provides: "GET /transactions, GET /transactions/by-date, GET /transactions/{id}"
  key_links:
    - from: "src/main/java/com/datn/financeapp/transaction/service/TransactionService.java"
      to: "src/main/java/com/datn/financeapp/category/repository/CategoryRepository.java"
      via: "list()/summary() gọi categoryRepository.findCategoryTree(categoryId) trước khi build điều kiện category_id IN (...)"
      pattern: "findCategoryTree"
---

<objective>
Xây 3 endpoint đọc giao dịch: danh sách có lọc/phân trang/summary (TXN-01), gom theo ngày (TXN-02),
chi tiết (TXN-02), và đảm bảo cộng gộp danh mục con dùng đúng một hàm chung `fn_category_tree` cho mọi
nơi lọc/thống kê (TXN-08) — không lặp điều kiện lọc ở nhiều chỗ khác nhau.

Đặt SAU 03-02 (cùng wave với 03-02 sẽ đụng file `TransactionController.java`/`TransactionService.java`
cùng lúc — theo quy tắc không được trùng `files_modified` trong cùng wave) để tránh xung đột merge.

Purpose: hoàn thiện luồng đọc giao dịch, chứng minh TXN-08 (lỗi khó phát hiện nhất của hệ thống —
quên cộng gộp danh mục con) không xảy ra.
Output: `GET /transactions`, `GET /transactions/by-date`, `GET /transactions/{id}`, test lọc theo
danh mục cha cộng gộp con + loại transfer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-CONTEXT.md
@D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-PATTERNS.md
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\api\04-GIAO-DICH.md
@D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md

<interfaces>
`CategoryRepository.findCategoryTree` (đã tồn tại từ Phase 2, Javadoc ghi thẳng "Phase 3" — DÙNG LẠI,
KHÔNG viết lại điều kiện lọc cây danh mục ở nơi khác):
```java
@Query(value = "SELECT category_id FROM fn_category_tree(:categoryId)", nativeQuery = true)
List<UUID> findCategoryTree(@Param("categoryId") UUID categoryId);
```

`PageRequestParams`/`PageMeta` (đã có từ Phase 1, dùng lại nguyên):
```java
public record PageRequestParams(int page, int pageSize, String sortBy, String sortOrder) {
    public static PageRequestParams of(Integer page, Integer pageSize, String sortBy, String sortOrder);
}
public record PageMeta(int page, int pageSize, long totalItems, int totalPages) {}
```

Tham số lọc `GET /transactions` (api/04-GIAO-DICH.md mục 1, đầy đủ):
`from_date, to_date, period(week|month|quarter|year — ưu tiên hơn from/to_date), type, wallet_id,
category_id, source, counts_in_report, search(display_name+note), min_amount, max_amount,
include_transfers(mặc định true), page, page_size, sort_by(date|amount|created_at, mặc định date),
sort_order(asc|desc, mặc định desc)`.

`summary` tính trên TOÀN BỘ kết quả lọc (không chỉ trang hiện tại) và LOẠI TRỪ `type=transfer` — CLAUDE.md
§2 bất biến: "Mọi truy vấn báo cáo/thống kê thu-chi phải có điều kiện `type != 'transfer'`".

Response `GET /transactions` (api/04-GIAO-DICH.md mục 1, cấu trúc mẫu):
```json
{
  "success": true,
  "data": [ { "id":"...", "type":"expense", "amount":68000, "date":"2026-08-06",
      "display_name":"Highlands Coffee", "note":"...", "source":"text",
      "wallet": {"id":"...","name":"Momo","type":"e_wallet"}, "destination_wallet": null,
      "category": {"id":"...","name":"Cà phê","type":"expense",
          "icon":{"code":"ca_phe","path_data":"..."},"color":"#3d6b7d",
          "parent":{"id":"...","name":"Ăn uống"}},
      "receipt_url": null, "recurring_id": null, "draft_id":"...",
      "created_at":"...","updated_at":"..." } ],
  "pagination": {"page":1,"page_size":20,"total_items":137,"total_pages":7},
  "summary": {"total_income":18500000,"total_expense":6687000,"difference":11813000}
}
```

`GET /transactions/by-date` response (api/04-GIAO-DICH.md mục 2) — `day_label` quy tắc: hôm nay →
`"Hôm nay"`, hôm qua → `"Hôm qua"`, còn lại → thứ trong tuần tiếng Việt. **Tính theo giờ Việt Nam,
không UTC** — nhưng vì `transactions.date` là cột `DATE` thuần (không timezone, theo CORE-07), việc
so sánh "hôm nay/hôm qua" chỉ cần so `LocalDate.now()` ở JVM đã ép `-Duser.timezone=UTC` (xem
`pom.xml`) — PHẢI dùng `LocalDate.now(ZoneId.of("Asia/Ho_Chi_Minh"))` khi tính "hôm nay" cho mục đích
NHÃN hiển thị (`day_label`), không dùng `LocalDate.now()` mặc định (sẽ lệch múi giờ JVM UTC so với
người dùng Việt Nam) — đây là điểm khác với so sánh ngày tương lai ở D-36 (D-36 dùng CURRENT_DATE của
Postgres, không liên quan tới nhãn hiển thị này).

`GET /transactions/{id}` (mục 3) — thêm so với list: `related_debt` (null nếu không gắn debt — Phase
4 mới có nghiệp vụ debt, Phase 3 luôn trả `null`), `recurring` (null — Phase 4), `ai_drafts` (object
rút gọn nếu `draft_id != null`, lấy `id/method/raw_input/confidence` — bảng `ai_drafts` đã tồn tại từ
V2, chỉ cần SELECT, không cần nghiệp vụ AI thật).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TransactionRepository query list/summary cộng gộp danh mục con (TXN-08) + GET /transactions + GET /{id}</name>
  <files>
    src/main/java/com/datn/financeapp/transaction/dto/TransactionListItemResponse.java
    src/main/java/com/datn/financeapp/transaction/dto/TransactionDetailResponse.java
    src/main/java/com/datn/financeapp/transaction/dto/TransactionSummaryDto.java
    src/main/java/com/datn/financeapp/transaction/repository/TransactionRepository.java
    src/main/java/com/datn/financeapp/transaction/service/TransactionService.java
    src/main/java/com/datn/financeapp/transaction/controller/TransactionController.java
    src/test/java/com/datn/financeapp/transaction/TransactionCategoryTreeFilterIntegrationTest.java
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\repository\TransactionRepository.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\category\repository\CategoryRepository.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\repository\WalletRepository.java
    D:\PTIT\DATN\api\04-GIAO-DICH.md
    D:\PTIT\DATN\CLAUDE.md
  </read_first>
  <action>
1. Thêm vào `TransactionRepository` (đã tồn tại từ 03-01, chỉ thêm method, không xoá gì đã có):
   ```java
   @Query(value = "SELECT t.* FROM transactions t "
           + "WHERE t.user_id = :userId AND NOT t.is_deleted "
           + "AND (:fromDate IS NULL OR t.date >= :fromDate) "
           + "AND (:toDate IS NULL OR t.date <= :toDate) "
           + "AND (:type IS NULL OR t.type = :type) "
           + "AND (:walletId IS NULL OR t.wallet_id = :walletId OR t.destination_wallet_id = :walletId) "
           + "AND (:categoryTree IS NULL OR t.category_id = ANY(:categoryTree)) "
           + "AND (:source IS NULL OR t.source = :source) "
           + "AND (:countsInReport IS NULL OR t.counts_in_report = :countsInReport) "
           + "AND (:search IS NULL OR t.display_name ILIKE CONCAT('%',:search,'%') OR t.note ILIKE CONCAT('%',:search,'%')) "
           + "AND (:minAmount IS NULL OR t.amount >= :minAmount) "
           + "AND (:maxAmount IS NULL OR t.amount <= :maxAmount) "
           + "AND (:includeTransfers = TRUE OR t.type <> 'transfer') "
           + "ORDER BY "
           + "CASE WHEN :sortBy = 'amount' AND :sortOrder = 'asc' THEN t.amount END ASC, "
           + "CASE WHEN :sortBy = 'amount' AND :sortOrder = 'desc' THEN t.amount END DESC, "
           + "CASE WHEN :sortBy = 'created_at' AND :sortOrder = 'asc' THEN t.created_at END ASC, "
           + "CASE WHEN :sortBy = 'created_at' AND :sortOrder = 'desc' THEN t.created_at END DESC, "
           + "CASE WHEN (:sortBy IS NULL OR :sortBy = 'date') AND :sortOrder = 'asc' THEN t.date END ASC, "
           + "CASE WHEN (:sortBy IS NULL OR :sortBy = 'date') AND :sortOrder = 'desc' THEN t.date END DESC "
           + "LIMIT :limit OFFSET :offset",
           nativeQuery = true)
   List<Transaction> search(@Param("userId") UUID userId, @Param("fromDate") LocalDate fromDate,
           @Param("toDate") LocalDate toDate, @Param("type") String type, @Param("walletId") UUID walletId,
           @Param("categoryTree") UUID[] categoryTree, @Param("source") String source,
           @Param("countsInReport") Boolean countsInReport, @Param("search") String search,
           @Param("minAmount") Long minAmount, @Param("maxAmount") Long maxAmount,
           @Param("includeTransfers") boolean includeTransfers, @Param("sortBy") String sortBy,
           @Param("sortOrder") String sortOrder, @Param("limit") int limit, @Param("offset") int offset);
   ```
   (Native query dài — đây là mẫu tham khảo, có thể tách bớt điều kiện bằng Specification/Criteria API
   nếu thấy JPQL/native khó bảo trì hơn, miễn giữ đúng ngữ nghĩa từng tham số lọc. Nếu đổi cách tiếp
   cận, PHẢI vẫn dùng `category_id = ANY(:categoryTree)` nơi `categoryTree` là mảng UUID lấy từ
   `categoryRepository.findCategoryTree(categoryId)` — KHÔNG được tự viết lại điều kiện lọc cây danh
   mục theo cách khác.)
   Thêm 1 method đếm tổng cho phân trang (cùng điều kiện WHERE, `SELECT COUNT(*)`), và 1 method
   summary (cùng điều kiện WHERE trừ phân trang, cộng thêm luôn `AND t.type <> 'transfer'` — CLAUDE.md
   §2 bất biến, dùng SQL: `SELECT COALESCE(SUM(CASE WHEN type='income' THEN amount END),0) AS
   total_income, COALESCE(SUM(CASE WHEN type='expense' THEN amount END),0) AS total_expense FROM
   transactions WHERE ... AND type <> 'transfer'`).

2. Trong `TransactionService`, viết method `list(UUID userId, TransactionFilterParams filters, PageRequestParams page)`:
   - NẾU `filters.categoryId() != null`: gọi `UUID[] categoryTree =
     categoryRepository.findCategoryTree(filters.categoryId()).toArray(new UUID[0]);` rồi truyền
     `categoryTree` vào query — **đây chính là điểm bắt buộc TXN-08, KHÔNG được tự viết
     `category_id = :categoryId` đơn thuần**.
   - NẾU `filters.period() != null` (week/month/quarter/year): tính `fromDate`/`toDate` từ
     `LocalDate.now()` (ưu tiên hơn `fromDate`/`toDate` client gửi trực tiếp theo đặc tả) — dùng
     `java.time.temporal.TemporalAdjusters` cho tuần/tháng, cộng `Period.ofMonths(3)` cho quý.
   - Trả `TransactionListItemResponse` list + `PageMeta` + `TransactionSummaryDto` (map từ query
     summary, tính `difference = total_income - total_expense`).

3. Viết `detail(UUID userId, UUID transactionId)`: `findByIdAndUserIdAndIsDeletedFalse` (404 nếu
   không thấy — không phải 403). Build `TransactionDetailResponse` gồm field như list + `relatedDebt =
   null`, `recurring = null` (Phase 4 sẽ điền, Phase 3 luôn null), `aiDrafts`: nếu `draftId != null`
   thì `jdbcTemplate.queryForObject` lấy `id, method, raw_input, confidence` từ `ai_drafts` theo
   `draft_id`, ngược lại `null`.

4. Thêm vào `TransactionController`:
   ```java
   @GetMapping
   public ApiResponse<List<TransactionListItemResponse>> list(... @RequestParam đầy đủ theo mục 1 ...) { ... }

   @GetMapping("/{id}")
   public ApiResponse<TransactionDetailResponse> detail(@PathVariable UUID id) { ... }
   ```
   Response `list` cần trả cả `pagination`/`summary` ở cùng cấp JSON với `data` (không lồng trong
   `data`) — theo đúng cấu trúc mẫu ở `<interfaces>`. Nếu `ApiResponse<T>` hiện tại chỉ có `{success,
   data}` (không có `pagination`/`summary`), KHÔNG sửa `ApiResponse` dùng chung (ảnh hưởng mọi
   endpoint khác) — thay vào đó tạo response wrapper riêng cho endpoint này hoặc kiểm tra xem
   `api/00-QUY-UOC-CHUNG.md` mục 7 (phân trang) đã định nghĩa sẵn cấu trúc gộp `pagination` ngoài
   `data` chưa; nếu có, dùng đúng cấu trúc đó thay vì tự bịa.

5. Viết `TransactionCategoryTreeFilterIntegrationTest`: tạo danh mục cha "Ăn uống" + 2 danh mục con
   "Cà phê", "Nhà hàng" (dùng repository Category có sẵn từ Phase 2). Ghi 3 giao dịch: 1 gán thẳng
   "Ăn uống", 1 gán "Cà phê", 1 gán "Nhà hàng", cộng thêm 1 giao dịch `type=transfer` không liên quan
   danh mục. Gọi `GET /transactions?category_id={id của Ăn uống}` → kỳ vọng trả về đúng 3 giao dịch
   (cộng gộp cả 2 con), KHÔNG có giao dịch transfer nào lọt vào nếu `include_transfers=false`. Test
   thêm `summary` của `GET /transactions` (không filter) loại trừ đúng giao dịch transfer khỏi
   `total_income`/`total_expense`.
  </action>
  <verify>
    <automated>mvn test -Dtest=TransactionCategoryTreeFilterIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "findCategoryTree" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` có kết quả
    - `grep -n "type <> 'transfer'\|type != 'transfer'" src/main/java/com/datn/financeapp/transaction/repository/TransactionRepository.java` có kết quả (summary loại transfer)
    - `mvn test -Dtest=TransactionCategoryTreeFilterIntegrationTest` thoát mã 0
  </acceptance_criteria>
  <done>GET /transactions lọc đủ tham số, cộng gộp danh mục con qua fn_category_tree, summary loại transfer; GET /transactions/{id} trả chi tiết kèm related_debt/recurring/ai_drafts.</done>
</task>

<task type="auto">
  <name>Task 2: GET /transactions/by-date (TXN-02)</name>
  <files>
    src/main/java/com/datn/financeapp/transaction/dto/TransactionByDateResponse.java
    src/main/java/com/datn/financeapp/transaction/service/TransactionService.java
    src/main/java/com/datn/financeapp/transaction/controller/TransactionController.java
    src/test/java/com/datn/financeapp/transaction/TransactionListQueryIntegrationTest.java
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionService.java
    D:\PTIT\DATN\api\04-GIAO-DICH.md
  </read_first>
  <action>
1. Tạo `TransactionByDateResponse` (record) khớp cấu trúc mẫu ở `<interfaces>`: `PeriodSummaryDto
   periodSummary` (nested: `String periodLabel, Long totalIncome, Long totalExpense, Long difference`)
   và `List<DayGroupDto> days` (nested: `LocalDate date, String dayNumber, String dayLabel, String
   monthYear, Long dayTotal, List<TransactionListItemResponse> transaction`).

2. Viết `TransactionService.listByDate(UUID userId, TransactionFilterParams filters)`: dùng lại
   query `search`/summary đã viết ở Task 1 (nhận cùng bộ tham số lọc), sau đó nhóm kết quả theo
   `date` bằng `Collectors.groupingBy` ở tầng Java (KHÔNG cần GROUP BY SQL vì danh sách đã đủ nhỏ sau
   khi lọc theo kỳ). Với mỗi ngày, tính `dayTotal = sum(income) - sum(expense)` của các giao dịch
   TRONG ngày đó, loại `transfer` khỏi `dayTotal` (cùng nguyên tắc CLAUDE.md §2).
   `dayLabel`: so sánh `date` với `LocalDate.now(ZoneId.of("Asia/Ho_Chi_Minh"))` (KHÔNG dùng
   `LocalDate.now()` trần trụi — xem giải thích múi giờ ở `<interfaces>`): bằng hôm nay →
   `"Hôm nay"`; bằng hôm qua → `"Hôm qua"`; khác → tính thứ bằng
   `date.getDayOfWeek()` map sang tiếng Việt (`"Thứ hai"`.."Thứ bảy"`, `"Chủ nhật"`).
   `monthYear` format `"tháng {M} {yyyy}"` bằng tiếng Việt thường (không viết hoa).

3. Thêm `GET /transactions/by-date` vào `TransactionController`, nhận cùng bộ `@RequestParam` như
   `list()` (Task 1) — không phân trang (`by-date` không có `page`/`page_size` theo đặc tả).

4. Viết test trong `TransactionListQueryIntegrationTest`: `listByDate_groupsTransactionsByDateWithCorrectLabels`
   — tạo giao dịch hôm nay và hôm qua (dùng `LocalDate.now(ZoneId.of("Asia/Ho_Chi_Minh"))` trong test
   để tránh flaky theo múi giờ máy CI), kiểm `day_label` đúng `"Hôm nay"`/`"Hôm qua"`, `day_total` đúng
   dấu (âm cho ngày chỉ có expense).
  </action>
  <verify>
    <automated>mvn test -Dtest=TransactionListQueryIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Asia/Ho_Chi_Minh" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` có kết quả
    - `mvn test -Dtest=TransactionListQueryIntegrationTest` thoát mã 0
  </acceptance_criteria>
  <done>GET /transactions/by-date trả đúng cấu trúc gom nhóm theo ngày, day_label tính đúng theo giờ Việt Nam, day_total loại trừ transfer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → `GET /transactions`, `GET /transactions/by-date`, `GET /transactions/{id}` | Query param lọc là input không tin cậy — `search` phải qua bind parameter (không nối chuỗi SQL trực tiếp), `category_id`/`wallet_id` phải kiểm quyền |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering (SQL Injection) | `TransactionRepository.search` | mitigate | Toàn bộ tham số dùng `@Param` bind qua JPA native query — `search` filter dùng `CONCAT('%',:search,'%')` bên trong SQL, không nối chuỗi Java |
| T-03-09 | Information Disclosure / IDOR | `list()`/`detail()`/`listByDate()` | mitigate | Điều kiện `t.user_id = :userId` nằm ngay trong query, không lọc ở tầng Java; `detail()` không tìm thấy → 404 |
| T-03-10 | Information Disclosure | `GET /transactions/{id}` trả `ai_drafts.raw_input` | accept | Đây là dữ liệu của chính user (câu họ đã gõ), không phải của người khác — rủi ro chỉ tồn tại nếu T-03-09 thất bại, đã mitigate riêng |
</threat_model>

<verification>
- `mvn test -Dtest=TransactionCategoryTreeFilterIntegrationTest,TransactionListQueryIntegrationTest` thoát mã 0
- `grep -n "findCategoryTree" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` xuất hiện ở đúng 1 điểm dùng chung (không lặp điều kiện lọc cây ở nhiều method)
</verification>

<success_criteria>
- TXN-01: GET /transactions lọc đủ tham số, phân trang, summary loại transfer
- TXN-02: GET /transactions/by-date và GET /transactions/{id} hoạt động đúng
- TXN-08: mọi lọc/thống kê theo category_id cộng gộp danh mục con qua fn_category_tree, dùng đúng một hàm chung
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/03-giao-dich/03-03-SUMMARY.md`
</output>
</content>
