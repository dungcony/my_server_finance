---
phase: 03-giao-dich
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/main/java/com/datn/financeapp/transaction/dto/CreateTransactionRequest.java
  - src/main/java/com/datn/financeapp/transaction/dto/UpdateTransactionRequest.java
  - src/main/java/com/datn/financeapp/transaction/dto/DuplicateTransactionRequest.java
  - src/main/java/com/datn/financeapp/transaction/dto/TransactionResponse.java
  - src/main/java/com/datn/financeapp/transaction/dto/CreateTransactionResponse.java
  - src/main/java/com/datn/financeapp/transaction/dto/DeleteTransactionResponse.java
  - src/main/java/com/datn/financeapp/transaction/service/TransactionService.java
  - src/main/java/com/datn/financeapp/transaction/controller/TransactionController.java
  - src/test/java/com/datn/financeapp/common/exception/GlobalExceptionHandlerTest.java
  - src/test/java/com/datn/financeapp/transaction/TransactionCrudIntegrationTest.java
  - src/test/java/com/datn/financeapp/transaction/TransactionUpdateAtomicityIntegrationTest.java
  - src/test/java/com/datn/financeapp/transaction/TransactionDeleteDebtLinkIntegrationTest.java
  - ../../api/04-GIAO-DICH.md
autonomous: true
requirements: [TXN-03, TXN-05, TXN-06, TXN-07]

must_haves:
  truths:
    - "User tạo giao dịch expense/income/transfer với validate đúng type (category bắt buộc/rỗng, destination bắt buộc/rỗng), ngày tương lai được chấp nhận, số dư ví cập nhật trong 1 DB transaction"
    - "User sửa giao dịch đổi cả amount lẫn wallet — số dư ví CŨ và ví MỚI đều đúng tuyệt đối, không cộng/trừ chênh lệch trực tiếp"
    - "User xoá giao dịch gắn debt_payments nhận 409 TRANSACTION_LINKED_TO_DEBT, không tự sửa paid_amount"
    - "User nhân bản giao dịch sang hôm nay, có thể ghi đè date/amount"
  artifacts:
    - path: "src/main/java/com/datn/financeapp/transaction/service/TransactionService.java"
      provides: "create/update/delete/duplicate — trình tự 3 bước sửa giao dịch, khoá tối đa 4 ví theo UUID.compareTo(), chặn xoá gắn debt"
    - path: "src/main/java/com/datn/financeapp/transaction/controller/TransactionController.java"
      provides: "POST/PUT/DELETE/POST duplicate /transactions"
  key_links:
    - from: "src/main/java/com/datn/financeapp/transaction/service/TransactionService.java"
      to: "src/main/java/com/datn/financeapp/transaction/service/TransactionWriter.java"
      via: "create()/duplicate() gọi transactionWriter.write() để ghi bản ghi mới + cập nhật số dư"
      pattern: "transactionWriter\\.write"
    - from: "src/main/java/com/datn/financeapp/transaction/service/TransactionService.java"
      to: "src/main/java/com/datn/financeapp/transaction/repository/TransactionRepository.java"
      via: "delete() gọi existsDebtPaymentLink(id) trước khi xoá mềm"
      pattern: "existsDebtPaymentLink"
---

<objective>
Xây CRUD giao dịch đơn lẻ: tạo (TXN-03), sửa đúng 3 bước bất di bất dịch (TXN-05), xoá có chặn liên
kết sổ nợ (TXN-06), nhân bản (TXN-07). Đây là luồng rủi ro cao nhất của toàn dự án — mọi request sửa
giao dịch đổi cả amount lẫn ví phải để lại đúng số dư ở cả ví cũ và ví mới, không có ngoại lệ.

Purpose: hiện thực hoá 4 endpoint ghi giao dịch, dùng lại `TransactionWriter` từ 03-01 làm điểm ghi
duy nhất, mở rộng khoá ví từ 2 lên tối đa 4 ví (D-33).
Output: `TransactionService` (create/update/delete/duplicate), `TransactionController`, DTO liên quan,
3 bộ test tích hợp theo luồng rủi ro ưu tiên #1 và #3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-CONTEXT.md
@D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-PATTERNS.md
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\api\04-GIAO-DICH.md
@D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md
@D:\PTIT\DATN\db\migration\V2__giao_dich.sql
@D:\PTIT\DATN\db\migration\V4__so_no_muc_tieu.sql

<interfaces>
`TransactionWriter` (từ plan 03-01, đã tồn tại — không viết lại):
```java
public record TransactionWriteCommand(
        UUID id, UUID userId, UUID walletId, UUID destinationWalletId, UUID categoryId,
        String type, long amount, LocalDate date, String note, String displayName,
        String source, boolean countsInReport, UUID recurringId, UUID draftId, String receiptUrl) {}

public record WriteResult(UUID transactionId, long walletNewBalance, Long destinationWalletNewBalance) {}

@Transactional
public WriteResult write(TransactionWriteCommand cmd);
```

`TransactionRepository` (từ 03-01):
```java
Optional<Transaction> findByIdAndUserIdAndIsDeletedFalse(UUID id, UUID userId);
boolean existsDebtPaymentLink(UUID id);
```

`WalletTransferService.lockAndCheckOwnership()` — pattern khoá ví D-33 cần mở rộng (nguyên văn, KHÔNG
đổi thuật toán, chỉ mở rộng số lượng ví từ 2 lên tối đa 4):
```java
UUID first = sourceId.compareTo(destinationId) < 0 ? sourceId : destinationId;
UUID second = sourceId.compareTo(destinationId) < 0 ? destinationId : sourceId;
Wallet firstWallet = lockAndCheckOwnership(first, userId);
Wallet secondWallet = lockAndCheckOwnership(second, userId);
```
```java
private Wallet lockAndCheckOwnership(UUID walletId, UUID userId) {
    Wallet wallet = walletRepository.findByIdForUpdate(walletId)
            .orElseThrow(() -> new BusinessException("NOT_FOUND", HttpStatus.NOT_FOUND.value(), "Không tìm thấy ví."));
    if (!userId.equals(wallet.getUserId())) {
        throw new BusinessException("NOT_FOUND", HttpStatus.NOT_FOUND.value(), "Không tìm thấy ví.");
    }
    return wallet;
}
```
`WalletRepository.findByIdForUpdate(UUID id)` dùng `@Lock(LockModeType.PESSIMISTIC_WRITE)` — dùng
lại nguyên, KHÔNG viết method khoá mới.

Ràng buộc theo `type` (api/04-GIAO-DICH.md mục 4, đối chiếu đúng `ck_txn_shape` ở V2):

| `type` | `category_id` | `destination_wallet_id` |
|---|---|---|
| `expense` | Bắt buộc, phải là danh mục loại `expense` | Phải rỗng |
| `income` | Bắt buộc, phải là danh mục loại `income` | Phải rỗng |
| `transfer` | Phải rỗng | Bắt buộc, khác `wallet_id` |

Mã lỗi riêng của POST /transactions (api/04-GIAO-DICH.md, KHÔNG có `FUTURE_DATE_NOT_ALLOWED` — D-36
bỏ hẳn luật này):
```
INVALID_AMOUNT (400), CATEGORY_REQUIRED (400), DESTINATION_WALLET_REQUIRED (400),
CATEGORY_NOT_ALLOWED (400), CATEGORY_TYPE_MISMATCH (400), SAME_SOURCE_AND_DESTINATION (400)
```
Mã lỗi bảng tóm tắt (api/04-GIAO-DICH.md cuối file), thêm cho PUT/DELETE:
```
TOO_MANY_ROWS (400 — bulk, không dùng ở plan này)
NOT_FOUND (404) — giao dịch không tồn tại/đã xoá/của người khác — KHÔNG BAO GIỜ trả 403
TRANSACTION_LINKED_TO_DEBT (409) — D-32
```

Trình tự 3 bước sửa giao dịch (CLAUDE.md §4, api/04-GIAO-DICH.md mục 8 — bất di bất dịch):
```
Bước 1 — HOÀN TÁC ảnh hưởng CŨ lên (các) ví CŨ:
    expense cũ  → cộng lại amount cũ vào wallet_id cũ
    income cũ   → trừ  amount cũ khỏi wallet_id cũ
    transfer cũ → cộng lại vào ví nguồn cũ, trừ khỏi ví đích cũ
Bước 2 — GHI giá trị MỚI vào bản ghi transactions
Bước 3 — ÁP DỤNG ảnh hưởng MỚI lên (các) ví MỚI
```
Ca kinh điển bắt buộc có test chặn: đổi khoản chi 100.000 ở ví A thành 80.000 ở ví B — nếu chỉ trừ
chênh lệch 20.000 vào ví B thì ví A vẫn bị trừ oan 100.000; đúng là ví A được CỘNG TRẢ 100.000, ví B
bị TRỪ 80.000.

Xoá giao dịch (api/04-GIAO-DICH.md mục 9):
```
1. EXISTS(SELECT 1 FROM debt_payments WHERE transaction_id = :id) → true → 409 TRANSACTION_LINKED_TO_DEBT, KHÔNG xoá gì
2. Hoàn tác ảnh hưởng ví (như bước 1 sửa) trong 1 @Transactional
3. is_deleted = true (xoá mềm, idempotent theo CORE-06 — gọi lại vẫn 200, không ném lỗi nếu đã xoá)
```

`GlobalExceptionHandlerTest` — excludeFilters hiện tại, PHẢI thêm `TransactionController.class`:
```java
@WebMvcTest(excludeFilters = @ComponentScan.Filter(type = FilterType.ASSIGNABLE_TYPE, classes = {
        SecurityConfig.class, JwtAuthFilter.class, RateLimitFilter.class,
        AuthController.class, WalletController.class, CategoryController.class
        // Phase 3 plan 03-02: TransactionController mới thêm — cùng lý do loại trừ.
}))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DTO giao dịch + TransactionService.create() (TXN-03, D-30 dùng TransactionWriter)</name>
  <files>
    src/main/java/com/datn/financeapp/transaction/dto/CreateTransactionRequest.java
    src/main/java/com/datn/financeapp/transaction/dto/TransactionResponse.java
    src/main/java/com/datn/financeapp/transaction/dto/CreateTransactionResponse.java
    src/main/java/com/datn/financeapp/transaction/service/TransactionService.java
    src/main/java/com/datn/financeapp/transaction/controller/TransactionController.java
    src/test/java/com/datn/financeapp/common/exception/GlobalExceptionHandlerTest.java
    src/test/java/com/datn/financeapp/transaction/TransactionCrudIntegrationTest.java
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\dto\CreateWalletRequest.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletTransferService.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionWriter.java
    D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\common\exception\GlobalExceptionHandlerTest.java
    D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-PATTERNS.md
    D:\PTIT\DATN\api\04-GIAO-DICH.md
  </read_first>
  <action>
1. Tạo `CreateTransactionRequest` (record, theo khuôn `CreateWalletRequest`):
   ```java
   public record CreateTransactionRequest(
           @NotNull @Pattern(regexp = "^(expense|income|transfer)$") String type,
           @NotNull Long amount,
           @NotNull UUID walletId,
           UUID destinationWalletId,
           UUID categoryId,
           LocalDate date,
           @Size(max = 150) String displayName,
           @Size(max = 500) String note,
           String source,
           Boolean countsInReport,
           String receiptUrl,
           UUID draftId) {}
   ```
   Validation chéo-field (category bắt buộc/rỗng theo type, destination bắt buộc/rỗng theo type) KHÔNG
   viết bằng annotation — xử lý bằng `if` tường minh trong Service (Bean Validation không validate
   chéo field được, đúng note ở PATTERNS.md).

2. Tạo `TransactionResponse` (record) map các field cần cho GET list/detail (dùng lại ở plan 03-03,
   viết ở đây vì `create()` cũng trả về): `UUID id, String type, Long amount, LocalDate date, String
   displayName, String note, String source, WalletRef wallet, WalletRef destinationWallet,
   CategoryRef category, String receiptUrl, UUID recurringId, UUID draftId, Instant createdAt,
   Instant updatedAt` với 2 nested record `WalletRef(UUID id, String name, String type)` và
   `CategoryRef(UUID id, String name, String type, UUID parentId)`.

3. Tạo `CreateTransactionResponse` (record) khớp `api/04-GIAO-DICH.md` mục 4 phản hồi 201:
   `TransactionResponse transaction, NewBalance newBalance` với nested `record NewBalance(UUID
   walletId, Long balance)`. KHÔNG bịa `affectedBudgets` — đó thuộc Phase 4 (module ngân sách chưa
   tồn tại); nếu muốn giữ field cho tương thích JSON tương lai thì để `List<Object> affectedBudgets =
   List.of()` cố định rỗng, ghi rõ comment "Phase 4 sẽ điền dữ liệu thật, Phase 3 luôn trả rỗng".

4. Viết `TransactionService.create(UUID userId, CreateTransactionRequest req)`:
   - Validate `amount > 0` → nếu không, `throw new BusinessException("INVALID_AMOUNT",
     HttpStatus.BAD_REQUEST.value(), "Số tiền phải lớn hơn 0.")`.
   - Validate theo `type`:
     - `expense`/`income`: nếu `categoryId == null` → ném `BusinessException("CATEGORY_REQUIRED",
       HttpStatus.BAD_REQUEST.value(), "Chi/thu phải có danh mục.")`; nếu `destinationWalletId != null`
       → ném `BusinessException("DESTINATION_WALLET_NOT_ALLOWED", HttpStatus.BAD_REQUEST.value(),
       "Giao dịch chi/thu không được có ví đích.")`. `DESTINATION_WALLET_NOT_ALLOWED` là mã lỗi MỚI,
       đối xứng với `CATEGORY_NOT_ALLOWED` đã có sẵn cho chiều ngược lại (transfer có category) —
       cùng cặp ràng buộc `ck_txn_shape`, cùng khuôn `BusinessException` mã lỗi nghiệp vụ như 5 mã
       còn lại trong bảng, KHÔNG dùng `error.fields`/`VALIDATION_ERROR` của CORE-01. Validate này
       chạy Ở TẦNG SERVICE TRƯỚC KHI chạm CSDL — không dựa vào `DataIntegrityViolationException` từ
       `ck_txn_shape` để báo lỗi cho người dùng (constraint DB là lưới an toàn cuối, không phải
       cơ chế báo lỗi nghiệp vụ).
     - `transfer`: nếu `destinationWalletId == null` → `DESTINATION_WALLET_REQUIRED` (400); nếu
       `categoryId != null` → `CATEGORY_NOT_ALLOWED` (400); nếu
       `walletId.equals(destinationWalletId)` → `SAME_SOURCE_AND_DESTINATION` (400).
   - Khoá ví: `transfer` khoá cả 2 ví theo `UUID.compareTo()` (copy nguyên khuôn
     `WalletTransferService.transfer()`, gọi `walletRepository.findByIdForUpdate` + kiểm tra
     `userId.equals(wallet.getUserId())` → 404 nếu sai); `expense`/`income` chỉ khoá 1 ví.
   - Nếu `categoryId != null`: kiểm tra `category.getType().equals(type)` — sai thì
     `CATEGORY_TYPE_MISMATCH` (400). Dùng `CategoryRepository.findByIdAndVisibleToUser` (đã có từ
     Phase 2) — không có/không thuộc quyền → coi như không hợp lệ, KHÔNG BAO GIỜ để lọt xuống DB rồi
     dựa vào trigger `fn_transactions_validate` bắt lỗi (trigger đó bắt exception dạng Postgres, khó
     map về đúng mã lỗi nghiệp vụ).
   - Gọi `transactionWriter.write(new TransactionWriteCommand(UUID.randomUUID(), userId,
     req.walletId(), req.destinationWalletId(), req.categoryId(), req.type(), req.amount(),
     req.date() != null ? req.date() : LocalDate.now(), req.note(), req.displayName(),
     req.source() != null ? req.source() : "manual", req.countsInReport() == null ? true :
     req.countsInReport(), null, req.draftId(), req.receiptUrl()))`.
   - KHÔNG chặn ngày tương lai — không viết bất kỳ `if (date.isAfter(LocalDate.now()))` nào (D-36).
   - Trả `CreateTransactionResponse` build từ `WriteResult` + entity vừa ghi (query lại qua
     `transactionRepository.findById` để build `TransactionResponse` đầy đủ wallet/category info,
     hoặc build tay từ dữ liệu đã có trong tay — chọn cách nào đơn giản hơn, không bắt buộc).

5. Tạo `TransactionController`:
   ```java
   @RestController
   @RequestMapping("/transactions")
   @RequiredArgsConstructor
   public class TransactionController {
       private final TransactionService transactionService;

       @PostMapping
       @Idempotent
       @ResponseStatus(HttpStatus.CREATED)
       public ApiResponse<CreateTransactionResponse> create(@Valid @RequestBody CreateTransactionRequest req) {
           UUID userId = SecurityContextUtil.currentUserId();
           return ApiResponse.of(transactionService.create(userId, req));
       }
   }
   ```
   (PUT/DELETE/duplicate thêm ở Task 2).

6. Sửa `GlobalExceptionHandlerTest.java`: thêm
   `com.datn.financeapp.transaction.controller.TransactionController.class` vào mảng `classes = {...}`
   của `excludeFilters`, kèm comment `// Phase 3 plan 03-02: TransactionController mới thêm — cùng lý do loại trừ.`
   theo đúng khuôn 2 dòng trước đó.

7. Viết `TransactionCrudIntegrationTest` (Testcontainers, theo khuôn `WalletCrudIntegrationTest`):
   test method `createExpenseTransaction_deductsWalletBalance_returns201`, `createTransferTransaction_deductsSourceAddsDestination_returns201`,
   `createExpenseWithoutCategory_returns400CategoryRequired`, `createTransferWithCategory_returns400CategoryNotAllowed`,
   `createTransactionWithFutureDate_isAccepted_returns201` (chứng minh D-36 — KHÔNG có
   `FUTURE_DATE_NOT_ALLOWED`), `createExpenseWithMismatchedCategoryType_returns400CategoryTypeMismatch`,
   `createExpense_withDestinationWalletId_returnsDestinationWalletNotAllowed` (gửi `type=expense` kèm
   `destinationWalletId` khác null → kỳ vọng HTTP 400, `error.code == "DESTINATION_WALLET_NOT_ALLOWED"`).
  </action>
  <verify>
    <automated>mvn test -Dtest=GlobalExceptionHandlerTest,TransactionCrudIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FUTURE_DATE_NOT_ALLOWED" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` KHÔNG có kết quả (không tồn tại mã lỗi này ở đâu trong code)
    - `grep -n "com.datn.financeapp.transaction.controller.TransactionController.class" src/test/java/com/datn/financeapp/common/exception/GlobalExceptionHandlerTest.java` có kết quả
    - `grep -n "transactionWriter.write" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` có kết quả
    - `grep -n "DESTINATION_WALLET_NOT_ALLOWED" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` có kết quả
    - `grep -c "VALIDATION_ERROR\" 400 chuẩn không phải mã nghiệp vụ riêng" .planning/phases/03-giao-dich/03-02-PLAN.md` trả về 0 (không còn câu văn xuôi tự bịa mã lỗi cũ trong action — kiểm bằng cụm từ đặc trưng của đoạn văn đã xoá, không kiểm chuỗi `VALIDATION_ERROR` trần trụi vì chuỗi đó còn xuất hiện hợp lệ ở 2 chỗ khác trong plan: 1 lần đối chiếu "KHÔNG dùng error.fields/VALIDATION_ERROR của CORE-01" ở action, 1 lần mô tả bảng mã lỗi chung api/00 trong Task 3 — cả hai đều đúng ngữ cảnh, không phải tàn dư lỗi)
    - `mvn test -Dtest=GlobalExceptionHandlerTest` thoát mã 0
    - `mvn test -Dtest=TransactionCrudIntegrationTest` thoát mã 0 (bao gồm test `createExpense_withDestinationWalletId_returnsDestinationWalletNotAllowed`)
  </acceptance_criteria>
  <done>POST /transactions tạo đúng expense/income/transfer, validate đầy đủ theo type (bao gồm DESTINATION_WALLET_NOT_ALLOWED cho chi/thu có ví đích), chấp nhận ngày tương lai, cập nhật số dư ví qua TransactionWriter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: update() 3 bước + delete() chặn debt + duplicate() (TXN-05, TXN-06, TXN-07, D-32, D-33)</name>
  <files>
    src/main/java/com/datn/financeapp/transaction/dto/UpdateTransactionRequest.java
    src/main/java/com/datn/financeapp/transaction/dto/DuplicateTransactionRequest.java
    src/main/java/com/datn/financeapp/transaction/dto/DeleteTransactionResponse.java
    src/main/java/com/datn/financeapp/transaction/service/TransactionService.java
    src/main/java/com/datn/financeapp/transaction/controller/TransactionController.java
    src/test/java/com/datn/financeapp/transaction/TransactionUpdateAtomicityIntegrationTest.java
    src/test/java/com/datn/financeapp/transaction/TransactionDeleteDebtLinkIntegrationTest.java
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionService.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletTransferService.java
    D:\PTIT\DATN\db\migration\V4__so_no_muc_tieu.sql
    D:\PTIT\DATN\api\04-GIAO-DICH.md
    D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1 (`TransactionUpdateAtomicityIntegrationTest.updateChangesAmountAndWallet_bothOldAndNewWalletBalancesAreExactlyCorrect`):
      ví A `initial_balance=1000000`, ví B `initial_balance=500000`. Tạo giao dịch expense 100000 ở
      ví A. Gọi `PUT /transactions/{id}` đổi thành expense 80000 ở ví B. Kỳ vọng: ví A quay lại
      `1000000` (được cộng trả nguyên 100000), ví B còn `500000 - 80000 = 420000` (KHÔNG phải
      `500000 - 20000 = 480000` — đây là ca sai kinh điển bắt buộc chặn).
    - Test 2 (`updateTransferChangingBothWallets_allFourWalletsEndUpCorrect`): giao dịch transfer
      A→B 200000, sửa thành transfer C→D 150000. Kỳ vọng: A quay lại số dư gốc (+200000), B quay lại
      số dư gốc (-200000 hoàn tác), C bị trừ 150000, D được cộng 150000 — đúng D-33 khoá tối đa 4 ví.
    - Test 3 (`TransactionDeleteDebtLinkIntegrationTest.deleteTransactionLinkedToDebtPayment_returns409`):
      insert thủ công 1 bản ghi `debt_payments` trỏ `transaction_id` tới giao dịch vừa tạo (dùng
      `jdbcTemplate` trực tiếp trong test — bảng `debts`/`debt_payments` đã tồn tại từ V4 dù Phase 4
      chưa code nghiệp vụ). Gọi `DELETE /transactions/{id}` → kỳ vọng 409, mã lỗi
      `TRANSACTION_LINKED_TO_DEBT`, và xác nhận `transactions.is_deleted` VẪN LÀ `false` sau lời gọi
      (không xoá gì cả).
  </behavior>
  <action>
1. Tạo `UpdateTransactionRequest` (record) — cùng field như `CreateTransactionRequest` nhưng tất cả
   optional trừ khi test cần bắt buộc (theo api/04: "Nhận cùng bộ trường như tạo mới" — PUT là replace
   toàn phần, không phải PATCH từng phần, nên giữ các field bắt buộc giống Create: `type`, `amount`,
   `walletId` bắt buộc).

2. Viết `TransactionService.update(UUID userId, UUID transactionId, UpdateTransactionRequest req)`
   trong MỘT `@Transactional`:
   - Bước 0: `Transaction old = transactionRepository.findByIdAndUserIdAndIsDeletedFalse(transactionId,
     userId).orElseThrow(() -> new BusinessException("NOT_FOUND", HttpStatus.NOT_FOUND.value(), "Không tìm thấy giao dịch."))`.
   - Bước "khoá ví D-33": gom toàn bộ UUID liên quan (ví cũ nguồn, ví cũ đích nếu transfer, ví mới
     nguồn, ví mới đích nếu transfer) vào `Set<UUID>` để loại trùng (VD sửa transfer A→B thành A→C
     chỉ cần khoá 3 ví, không phải 4), rồi `List<UUID> sortedIds = new
     ArrayList<>(idSet); sortedIds.sort(UUID::compareTo);` và khoá tuần tự qua
     `walletRepository.findByIdForUpdate(id)` — build `Map<UUID, Wallet> lockedWallets` để tra lại
     theo id. Method riêng `private List<Wallet> lockWalletsInOrder(Set<UUID> walletIds, UUID userId)`
     tái dùng logic `lockAndCheckOwnership` (copy hoặc gọi lại nếu đặt chung 1 nơi — Discretion, nhưng
     PHẢI giữ đúng thuật toán khoá theo `UUID.compareTo()` tăng dần, không đổi).
   - Bước 1 — hoàn tác CŨ: dựa vào `old.getType()`:
     - `expense` cũ → `walletRepository.adjustBalance(old.getWalletId(), +old.getAmount())`
     - `income` cũ → `walletRepository.adjustBalance(old.getWalletId(), -old.getAmount())`
     - `transfer` cũ → `adjustBalance(old.getWalletId(), +old.getAmount())` VÀ
       `adjustBalance(old.getDestinationWalletId(), -old.getAmount())`
   - Bước 2 — ghi giá trị mới: validate `req` giống hệt luồng validate ở `create()` (tái dùng method
     validate chung khai PACKAGE-PRIVATE (không modifier, KHÔNG `private`):
     `void validateShape(String type, UUID categoryId, UUID destinationWalletId, UUID walletId)` —
     KHÔNG `private` vì plan 03-04 (`TransactionBulkService`, cùng package `transaction.service`)
     cần gọi lại method này cho từng dòng bulk; package-private đủ phạm vi truy cập cho lời gọi
     cross-class trong cùng package mà không mở rộng thành `public` không cần thiết. Thêm comment kỹ
     thuật (tiếng Anh) ngay trên method: `// package-private: also called by TransactionBulkService
     (plan 03-04) to validate each bulk row without duplicating logic`. KHÔNG lặp lại code validate ở
     `update()`. Set toàn bộ field mới lên entity `old` (không tạo entity mới — cùng `id`), gọi
     `transactionRepository.save(old)`.
   - Bước 3 — áp dụng MỚI: dựa vào `req.type()` mới, gọi `adjustBalance` với dấu tương ứng (giống
     bảng ở `create()`, KHÔNG dấu ngược).
   - Toàn bộ 3 bước nằm trong CÙNG method `@Transactional` — KHÔNG gọi qua `TransactionWriter` ở đây
     (vì `TransactionWriter.write()` luôn INSERT bản ghi mới, không phù hợp cho UPDATE tại chỗ; gọi
     trực tiếp `transactionRepository.save()` + `walletRepository.adjustBalance()` ngay trong
     `TransactionService`, đã là bean khác `WalletTransferService`/`TransactionWriter` nên không có
     rủi ro self-invocation).
   - Trả về response chứa số dư MỚI của mọi ví liên quan (đọc lại qua
     `walletRepository.findByIdForUpdate(id).get().getCurrentBalance()` sau bước 3, không tự cộng
     tay trong Java).

3. Viết `TransactionService.delete(UUID userId, UUID transactionId)`:
   - `Transaction txn = transactionRepository.findByIdAndUserIdAndIsDeletedFalse(transactionId, userId)`
     — nếu rỗng: kiểm tra thêm `findById` KHÔNG lọc `isDeleted` để phân biệt "đã xoá idempotent" (200,
     không lỗi) với "không tồn tại/của người khác" (404) — copy đúng khuôn CORE-06 của
     `WalletService.delete()`. Cần thêm method
     `Optional<Transaction> findByIdAndUserId(UUID id, UUID userId)` (không lọc isDeleted) vào
     `TransactionRepository` nếu chưa có.
   - Nếu tồn tại và CHƯA xoá: `if (transactionRepository.existsDebtPaymentLink(transactionId)) throw
     new BusinessException("TRANSACTION_LINKED_TO_DEBT", HttpStatus.CONFLICT.value(), "Giao dịch này
     là một lần trả nợ — huỷ ở sổ nợ, không xoá trực tiếp.")` — kiểm tra TRƯỚC khi hoàn tác số dư.
   - Hoàn tác số dư (đúng bước 1 của update, dựa trên `txn.getType()`), rồi `txn.setIsDeleted(true);
     transactionRepository.save(txn)`. Nếu đã xoá từ trước → return luôn, không làm gì (idempotent).
   - **TUYỆT ĐỐI KHÔNG** ghi `debts.paid_amount`/`debts.status` — không đụng bảng `debts`/`debt_payments`
     ở bất kỳ dòng code nào trong method này ngoài câu `EXISTS` đọc.

4. Viết `TransactionService.duplicate(UUID userId, UUID transactionId, DuplicateTransactionRequest req)`:
   tạo `DuplicateTransactionRequest(Long amount, LocalDate date)` (cả 2 optional — ghi đè nếu có).
   Đọc giao dịch gốc qua `findByIdAndUserIdAndIsDeletedFalse`, gọi `transactionWriter.write(...)` với
   `date = req.date() != null ? req.date() : LocalDate.now()` (mặc định HÔM NAY theo api/04 mục 10,
   KHÔNG phải ngày gốc), `amount = req.amount() != null ? req.amount() : original.getAmount()`,
   `source = "manual"` (LUÔN, không kế thừa source gốc — theo đặc tả "source = 'manual'"), các field
   còn lại (`walletId`, `destinationWalletId`, `categoryId`, `note`, `type`) copy nguyên từ bản gốc.
   Trả `201` với cấu trúc giống `create()`.

5. Thêm vào `TransactionController`:
   ```java
   @PutMapping("/{id}")
   public ApiResponse<?> update(@PathVariable UUID id, @Valid @RequestBody UpdateTransactionRequest req) { ... }

   @DeleteMapping("/{id}")
   public ApiResponse<DeleteTransactionResponse> delete(@PathVariable UUID id) { ... }

   @PostMapping("/{id}/duplicate")
   @Idempotent
   @ResponseStatus(HttpStatus.CREATED)
   public ApiResponse<CreateTransactionResponse> duplicate(@PathVariable UUID id,
           @RequestBody(required = false) DuplicateTransactionRequest req) { ... }
   ```
   PUT/DELETE KHÔNG gắn `@Idempotent` (đúng lý do D-11: PUT là ghi đè, DELETE tự idempotent theo
   CORE-06 — cùng khuôn `WalletController.update`/`delete`). `duplicate` LÀ POST-tạo-mới thật sự →
   gắn `@Idempotent`.

6. Viết 2 test file theo `<behavior>`.
  </action>
  <verify>
    <automated>mvn test -Dtest=TransactionUpdateAtomicityIntegrationTest,TransactionDeleteDebtLinkIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "debts.paid_amount\|SET paid_amount\|debt.setPaidAmount" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` KHÔNG có kết quả (chưa từng ghi cột do trigger sở hữu)
    - `grep -n "existsDebtPaymentLink" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` có kết quả
    - `grep -n "UUID::compareTo\|compareTo(" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` có kết quả
    - `mvn test -Dtest=TransactionUpdateAtomicityIntegrationTest` thoát mã 0
    - `mvn test -Dtest=TransactionDeleteDebtLinkIntegrationTest` thoát mã 0
    - `mvn test -Dtest=TransactionCrudIntegrationTest,TransactionUpdateAtomicityIntegrationTest,TransactionDeleteDebtLinkIntegrationTest` thoát mã 0
  </acceptance_criteria>
  <done>PUT sửa giao dịch đúng 3 bước cho cả 2 và 4 ví liên quan, DELETE chặn đúng giao dịch gắn debt_payments (409, không tự sửa paid_amount), POST duplicate tạo bản sao ngày hôm nay.</done>
</task>

<task type="auto">
  <name>Task 3: Cập nhật api/04-GIAO-DICH.md bổ sung mã lỗi DESTINATION_WALLET_NOT_ALLOWED</name>
  <files>
    ../../api/04-GIAO-DICH.md
  </files>
  <read_first>
    D:\PTIT\DATN\api\04-GIAO-DICH.md
    D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md
  </read_first>
  <action>
Tài liệu `api/04-GIAO-DICH.md` hiện thiếu mã lỗi cho ca "chi/thu có kèm ví đích" — CSDL đã chặn cứng
ca này qua `ck_txn_shape` (V2), nhưng bảng mã lỗi chỉ có `CATEGORY_NOT_ALLOWED` cho chiều ngược lại
(transfer có category). Đây là lỗ hổng thật của tài liệu, phải vá theo đúng quy tắc CLAUDE.md "khi
thay đổi nghiệp vụ làm lệch tài liệu thiết kế, phải cập nhật tài liệu tương ứng trong cùng lần thay
đổi".

**LƯU Ý QUAN TRỌNG:** `api/` nằm ở REPO GỐC (`DATN/`, đường dẫn tuyệt đối
`D:\PTIT\DATN\api\04-GIAO-DICH.md`), KHÔNG phải trong repo `source/server/` đang làm việc. Đây là
BA repo git độc lập (xem CLAUDE.md gốc mục "Ba repo git độc lập"). Sau khi sửa file, PHẢI `cd` vào
`D:\PTIT\DATN` (repo gốc) để `git add`/`git commit` riêng — KHÔNG commit chung với thay đổi ở
`source/server/`.

1. Mở `D:\PTIT\DATN\api\04-GIAO-DICH.md`, tìm bảng mã lỗi ở mục 4 "Mã lỗi riêng" (quanh dòng
   299-304, ngay sau tiêu đề "### Mã lỗi riêng" của `POST /transactions`). Thêm dòng MỚI ngay sau
   dòng `| `CATEGORY_NOT_ALLOWED` | 400 | Giao dịch chuyển không được có danh mục |`:
   ```
   | `DESTINATION_WALLET_NOT_ALLOWED` | 400 | Giao dịch chi/thu không được có ví đích |
   ```

2. Tìm bảng "Tóm tắt mã lỗi" ở cuối file (quanh dòng 506-510). Thêm dòng MỚI ngay sau dòng
   `| `CATEGORY_NOT_ALLOWED` | 400 | Chuyển tiền không có danh mục |`:
   ```
   | `DESTINATION_WALLET_NOT_ALLOWED` | 400 | Chi/thu không được có ví đích |
   ```

3. Đối chiếu `D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md` mục 6 "Mã lỗi dùng chung": bảng đó CHỈ liệt kê
   mã lỗi tổng quát dùng cho MỌI nghiệp vụ (401/403/404/409/500 và `VALIDATION_ERROR` cho lỗi định
   dạng chung) — KHÔNG liệt kê các mã 400 nghiệp vụ riêng như `CATEGORY_NOT_ALLOWED`,
   `CATEGORY_REQUIRED`. `DESTINATION_WALLET_NOT_ALLOWED` cùng nhóm với các mã 400 nghiệp vụ đó —
   KHÔNG thêm vào `api/00-QUY-UOC-CHUNG.md`. Chỉ sửa `api/04-GIAO-DICH.md` như bước 1 và 2.

4. Commit riêng ở repo gốc (KHÔNG dùng `gsd-sdk query commit` — lệnh đó ghi vào `.planning/` của
   repo hiện tại, sai chỗ): chạy `cd D:\PTIT\DATN && git add api/04-GIAO-DICH.md && git commit -m
   "docs(api): bổ sung mã lỗi DESTINATION_WALLET_NOT_ALLOWED cho giao dịch chi/thu có ví đích"`.
  </action>
  <verify>
    <automated>grep -c "DESTINATION_WALLET_NOT_ALLOWED" D:\PTIT\DATN\api\04-GIAO-DICH.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DESTINATION_WALLET_NOT_ALLOWED" ../../api/04-GIAO-DICH.md` trả về ít nhất 2 (xuất hiện ở cả bảng mã lỗi riêng mục 4 và bảng tóm tắt cuối file)
    - `grep -n "DESTINATION_WALLET_NOT_ALLOWED" ../../api/00-QUY-UOC-CHUNG.md` KHÔNG có kết quả (không thêm nhầm vào bảng mã lỗi chung)
    - `cd ../../ && git log --oneline -1 -- api/04-GIAO-DICH.md` cho thấy commit mới nhất là của thay đổi này, thực hiện ở repo gốc DATN, không lẫn vào repo source/server
  </acceptance_criteria>
  <done>api/04-GIAO-DICH.md có mã lỗi DESTINATION_WALLET_NOT_ALLOWED ở cả 2 bảng mã lỗi, đã commit riêng ở repo gốc DATN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → `POST/PUT/DELETE /transactions` | Input trực tiếp từ app — walletId/categoryId/destinationWalletId phải được xác minh thuộc quyền user trước khi dùng, không tin `type` gửi lên khớp với ràng buộc DB mà không validate trước |
| `TransactionService.delete()` → `debts`/`debt_payments` | Chỉ đọc (`EXISTS`), không ghi — biên giới quyền sở hữu cột giữa backend và trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Elevation of Privilege / IDOR | `TransactionService.update()`/`delete()` | mitigate | `findByIdAndUserIdAndIsDeletedFalse` lọc `user_id` NGAY trong câu JPQL/native query — không load rồi so sánh Java; không thuộc quyền → `NOT_FOUND` 404, không phải 403, không lộ giao dịch có tồn tại |
| T-03-05 | Elevation of Privilege / IDOR | `TransactionService.update()` khoá ví ngoài quyền user | mitigate | `lockWalletsInOrder` gọi `lockAndCheckOwnership`-style check cho MỌI ví liên quan (cũ lẫn mới) — user không thể sửa giao dịch để chuyển tiền vào/ra ví không thuộc mình |
| T-03-06 | Denial of Service (race) | `update()` sửa cùng giao dịch đồng thời 2 request | mitigate | Khoá `PESSIMISTIC_WRITE` trên toàn bộ ví liên quan theo thứ tự `UUID.compareTo()` cố định — chống deadlock, request thứ hai chờ tới khi request đầu commit |
| T-03-07 | Tampering | `delete()` xoá giao dịch gắn debt_payments để né trigger | mitigate | `EXISTS` check chạy TRƯỚC bất kỳ thay đổi nào lên số dư/is_deleted — không có đường nào bỏ qua kiểm tra này |
</threat_model>

<verification>
- `mvn test -Dtest=TransactionCrudIntegrationTest,TransactionUpdateAtomicityIntegrationTest,TransactionDeleteDebtLinkIntegrationTest` thoát mã 0
- `mvn test -Dtest=GlobalExceptionHandlerTest` thoát mã 0 (TransactionController đã loại khỏi slice test)
- `grep -c "INSERT INTO transactions" src/main/java/com/datn/financeapp/transaction/service/TransactionService.java` trả về 0 (update() dùng save() qua JPA, không native INSERT)
</verification>

<success_criteria>
- TXN-03: tạo giao dịch đúng validate theo type, chấp nhận ngày tương lai
- TXN-05: sửa giao dịch đổi cả amount và ví — cả ví cũ và ví mới đều đúng tuyệt đối (test ca kinh điển 100k/80k xanh)
- TXN-06: xoá giao dịch gắn debt_payments trả 409, không tự ghi paid_amount
- TXN-07: nhân bản giao dịch sang hôm nay, cho phép ghi đè date/amount
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/03-giao-dich/03-02-SUMMARY.md`
</output>
</content>
