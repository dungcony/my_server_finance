---
phase: 03-giao-dich
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/datn/financeapp/transaction/entity/Transaction.java
  - src/main/java/com/datn/financeapp/transaction/repository/TransactionRepository.java
  - src/main/java/com/datn/financeapp/transaction/service/TransactionWriter.java
  - src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java
  - src/main/java/com/datn/financeapp/wallet/dto/WalletResponse.java
  - src/main/java/com/datn/financeapp/wallet/dto/WalletDetailResponse.java
  - src/main/java/com/datn/financeapp/wallet/service/WalletService.java
  - src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java
  - src/test/java/com/datn/financeapp/wallet/WalletFutureBalanceIntegrationTest.java
autonomous: true
requirements: [TXN-09]

must_haves:
  truths:
    - "Toàn bộ INSERT giao dịch trong hệ thống đi qua đúng một đường ghi (TransactionWriter), không còn jdbcTemplate.update(\"INSERT INTO transactions...\") nằm rải rác ở WalletTransferService"
    - "Sau khi gộp, 100% test Phase 1+2 hiện có vẫn xanh — không có test nào bị sửa logic nghiệp vụ để né lỗi, chỉ được sửa import/khai báo bean nếu thật sự cần"
    - "API GET /wallets và GET /wallets/{id} trả current_balance là tiền thật đến hết hôm nay (đã trừ ngược giao dịch tương lai), và chỉ trả projected_balance khi ví có giao dịch date > hôm nay"
  artifacts:
    - path: "src/main/java/com/datn/financeapp/transaction/entity/Transaction.java"
      provides: "Entity JPA ánh xạ đầy đủ cột bảng transactions theo V2/V8"
    - path: "src/main/java/com/datn/financeapp/transaction/service/TransactionWriter.java"
      provides: "Bean riêng ghi transaction + cập nhật số dư ví, dùng chung cho TransactionService/WalletTransferService/Phase 4"
    - path: "src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java"
      provides: "Method native query trừ ngược số dư tại mốc thời gian (D-36) dùng chung"
  key_links:
    - from: "src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java"
      to: "src/main/java/com/datn/financeapp/transaction/service/TransactionWriter.java"
      via: "gọi transactionWriter.write(...) thay cho jdbcTemplate.update(\"INSERT...\") trực tiếp"
      pattern: "transactionWriter\\.write"
    - from: "src/main/java/com/datn/financeapp/wallet/service/WalletService.java"
      to: "src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java"
      via: "toResponse()/detail() gọi method trừ ngược để lấy current_balance/projected_balance đúng nghĩa API"
      pattern: "findBalanceAsOf|computeBalanceAsOf"
---

<objective>
Gộp hai đường ghi giao dịch thành một (D-30), tạo nền tảng `Transaction`/`TransactionWriter` mà mọi
task sau của Phase 3 và cả Phase 4 sẽ gọi lại, đồng thời sửa đúng lệch nghĩa cột-CSDL-vs-trường-API
cho số dư ví (D-37/TXN-09).

Đây là plan bắt buộc chạy TRƯỚC mọi plan khác của Phase 3 — nếu không gộp trước, `WalletTransferService`
sẽ tiếp tục tự `INSERT INTO transactions` song song với đường ghi mới của `TransactionService` (plan
sau), khiến logic "trừ ngược" phải viết hai lần hoặc chỉ đúng một chỗ.

Purpose: dựng đúng MỘT đường ghi giao dịch duy nhất trong toàn hệ thống, sửa 2 DTO ví đang map sai
nghĩa cột/trường số dư.
Output: `Transaction` entity, `TransactionRepository`, `TransactionWriter` (bean ghi dùng chung),
`WalletTransferService` không còn tự INSERT, `WalletResponse`/`WalletDetailResponse` trả đúng
`current_balance`/`projected_balance` theo D-37.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-CONTEXT.md
@D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-PATTERNS.md
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\api\04-GIAO-DICH.md
@D:\PTIT\DATN\api\02-VI.md
@D:\PTIT\DATN\db\migration\V2__giao_dich.sql
@D:\PTIT\DATN\db\migration\V8__dieu_chinh_so_du.sql
@D:\PTIT\DATN\db\README.md

<interfaces>
Cột bảng `transactions` thật (trích `V2__giao_dich.sql`, đã đối chiếu — thắng mọi mô tả trong `api/*.md`
nếu lệch, theo CORE-11):
```sql
CREATE TABLE transactions (
    id                    UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id               UUID         NOT NULL,
    wallet_id             UUID         NOT NULL,
    destination_wallet_id UUID,
    category_id           UUID,
    type                  VARCHAR(10)  NOT NULL,   -- 'expense'|'income'|'transfer'
    amount                BIGINT       NOT NULL,   -- luôn dương
    date                  DATE         NOT NULL,   -- KHÔNG timezone
    note                  TEXT,
    display_name          VARCHAR(150),
    source                VARCHAR(20)  NOT NULL DEFAULT 'manual', -- 'manual'|'text'|'ocr'|'auto'|'adjustment'
    recurring_id          UUID,
    draft_id              UUID,
    receipt_url           TEXT,
    is_deleted            BOOLEAN      NOT NULL DEFAULT FALSE,
    created_at            TIMESTAMPTZ  NOT NULL DEFAULT now(),
    updated_at            TIMESTAMPTZ  NOT NULL DEFAULT now(), -- trigger fn_transactions_validate tự set
    counts_in_report      BOOLEAN      NOT NULL DEFAULT TRUE   -- thêm bởi V8
);
```
Ràng buộc `ck_txn_shape`: `transfer` bắt buộc `destination_wallet_id` khác `wallet_id`, `category_id
NULL`; `expense`/`income` bắt buộc `category_id`, `destination_wallet_id` phải NULL. Trigger
`trg_transactions_validate` tự gán `updated_at := now()` và chặn `CATEGORY_TYPE_MISMATCH` — không tự
set `updated_at` từ tầng Java khi insert/update qua Hibernate `save()`.

`WalletRepository.adjustBalance` hiện có (dùng lại nguyên, không viết UPDATE mới):
```java
@Modifying
@Query("UPDATE Wallet w SET w.currentBalance = w.currentBalance + :delta WHERE w.id = :id")
int adjustBalance(@Param("id") UUID id, @Param("delta") long delta);
```

`IdempotencyTransactionHelper` — mẫu bean riêng chống self-invocation, TransactionWriter phải theo
đúng khuôn này (mỗi method `@Transactional` riêng, không method nào gọi `this.xxx()` của bean khác
public method cùng class):
```java
@Component
@RequiredArgsConstructor
public class IdempotencyTransactionHelper {
    private final IdempotencyKeyRepository repository;

    @Transactional
    public int tryInsertProcessing(String key, UUID userId, String endpoint) {
        return repository.tryInsertProcessing(key, userId, endpoint);
    }
}
```

2 đoạn `WalletTransferService` PHẢI sửa (nguyên văn hiện tại):

Đoạn 1 — `transfer()`:
```java
LocalDate date = req.date() != null ? req.date() : LocalDate.now();
UUID transactionId = UUID.randomUUID();

jdbcTemplate.update(
        "INSERT INTO transactions (id, user_id, wallet_id, destination_wallet_id, category_id, "
                + "type, amount, date, note, source, counts_in_report, is_deleted, created_at, updated_at) "
                + "VALUES (?, ?, ?, ?, NULL, 'transfer', ?, ?, ?, 'manual', TRUE, FALSE, now(), now())",
        transactionId, userId, sourceId, destinationId, amount, date, req.note());

walletRepository.adjustBalance(sourceId, -amount);
walletRepository.adjustBalance(destinationId, amount);
```

Đoạn 2 — `adjustBalance()`:
```java
UUID categoryId = jdbcTemplate.queryForObject(
        "SELECT c.id FROM categories c "
                + "JOIN category_groups g ON g.id = c.category_group_id "
                + "JOIN icons i ON i.id = c.icon_id "
                + "WHERE g.name = 'Khác' AND i.code = 'vi_tien' AND c.type = ? AND c.user_id IS NULL",
        UUID.class, type);
if (categoryId == null) {
    throw new IllegalStateException(
            "Thiếu danh mục hệ thống 'Cập nhật số dư' (type=" + type + ") — kiểm tra seed V8.");
}

UUID transactionId = UUID.randomUUID();
jdbcTemplate.update(
        "INSERT INTO transactions (id, user_id, wallet_id, destination_wallet_id, category_id, "
                + "type, amount, date, note, source, counts_in_report, is_deleted, created_at, updated_at) "
                + "VALUES (?, ?, ?, NULL, ?, ?, ?, CURRENT_DATE, ?, 'adjustment', ?, FALSE, now(), now())",
        transactionId, userId, walletId, categoryId, type, amount, req.note(), countsInReport);

walletRepository.adjustBalance(walletId, difference);
```

SQL trừ ngược (D-36, trích nguyên văn `db/README.md` mục "Suy ra số dư ví theo mốc thời gian"):
```sql
SELECT w.current_balance
       - COALESCE(SUM(CASE
             WHEN t.type = 'income'                          THEN  t.amount
             WHEN t.type = 'expense'                         THEN -t.amount
             WHEN t.type = 'transfer' AND t.wallet_id = w.id THEN -t.amount
         END), 0)
       - COALESCE((SELECT SUM(amount) FROM transactions
                   WHERE destination_wallet_id = w.id
                     AND date > :as_of AND NOT is_deleted), 0) AS balance_as_of
FROM wallets w
LEFT JOIN transactions t
       ON t.wallet_id = w.id AND t.date > :as_of AND NOT t.is_deleted
WHERE w.id = :wallet_id
GROUP BY w.id, w.current_balance;
```

`WalletResponse`/`WalletDetailResponse` hiện tại (đang map thẳng, phải sửa) và `WalletService.toResponse()`/`detail()`
— xem `03-PATTERNS.md` mục "File PHẢI SỬA (di sản Phase 2)" để có nguyên văn đầy đủ 2 record và 2 method mapping.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo entity Transaction + TransactionRepository + TransactionWriter (bean ghi dùng chung)</name>
  <files>
    src/main/java/com/datn/financeapp/transaction/entity/Transaction.java
    src/main/java/com/datn/financeapp/transaction/repository/TransactionRepository.java
    src/main/java/com/datn/financeapp/transaction/service/TransactionWriter.java
    src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java
  </files>
  <read_first>
    D:\PTIT\DATN\db\migration\V2__giao_dich.sql
    D:\PTIT\DATN\db\migration\V8__dieu_chinh_so_du.sql
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\entity\Wallet.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\repository\WalletRepository.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\common\idempotency\IdempotencyTransactionHelper.java
    D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-PATTERNS.md
  </read_first>
  <action>
1. Tạo `Transaction.java` theo đúng khuôn `Wallet.java` (`@Entity @Table(name="transactions") @Getter
   @Setter @Builder @NoArgsConstructor @AllArgsConstructor`, `@Id` kiểu `UUID` không `@GeneratedValue`
   — sinh ở tầng service bằng `UUID.randomUUID()`). Map đúng 17 cột theo DDL trích ở `<interfaces>`:
   `id`(UUID), `userId`→`user_id`, `walletId`→`wallet_id`, `destinationWalletId`→`destination_wallet_id`,
   `categoryId`→`category_id`, `type`(String), `amount`(Long), `date`(LocalDate — cột `DATE`, KHÔNG
   convert timezone), `note`(String), `displayName`→`display_name`, `source`(String), `recurringId`→
   `recurring_id`, `draftId`→`draft_id`, `receiptUrl`→`receipt_url`, `isDeleted`→`is_deleted`(Boolean),
   `createdAt`→`created_at`(Instant), `updatedAt`→`updated_at`(Instant), `countsInReport`→
   `counts_in_report`(Boolean). Đặt Javadoc trên class ghi rõ: "`updated_at` do trigger
   `trg_transactions_validate` tự set khi INSERT/UPDATE — set tường minh ở tầng Java khi gọi
   `save()`/insert thủ công vẫn an toàn vì trigger sẽ ghi đè, nhưng KHÔNG dựa vào giá trị Java set để
   đọc lại ngay trong cùng transaction trước khi flush."

2. Tạo `TransactionRepository extends JpaRepository<Transaction, UUID>` theo khuôn
   `WalletRepository`. Thêm tối thiểu:
   - `Optional<Transaction> findByIdAndUserIdAndIsDeletedFalse(UUID id, UUID userId)` — quyền D-27
     (transaction không có group_id trực tiếp, quyền suy thẳng từ `user_id`, KHÔNG áp vế nhóm — giống
     `CategoryRepository`, ghi rõ trong Javadoc lý do đối chiếu schema V2 không có cột `group_id` ở
     bảng `transactions`).
   - `@Query(value = "SELECT EXISTS(SELECT 1 FROM debt_payments WHERE transaction_id = :id)",
     nativeQuery = true) boolean existsDebtPaymentLink(@Param("id") UUID id)` — dùng cho D-32 ở plan
     sau, viết sẵn ở đây vì thuộc "điểm nối" của entity.

3. Tạo `TransactionWriter` là `@Component` (không phải `@Service` — bean hạ tầng, không phải
   nghiệp vụ, theo D-31) `@RequiredArgsConstructor`, field `private final TransactionRepository
   transactionRepository; private final WalletRepository walletRepository;`. Viết đúng 1 public
   method:
   ```java
   @Transactional
   public WriteResult write(TransactionWriteCommand cmd) { ... }
   ```
   Tạo thêm 2 record trong cùng package `transaction/service/`:
   - `TransactionWriteCommand` (record) chứa đủ tham số nguyên thuỷ KHÔNG phải DTO nghiệp vụ:
     `UUID id, UUID userId, UUID walletId, UUID destinationWalletId, UUID categoryId, String type,
     long amount, LocalDate date, String note, String displayName, String source,
     boolean countsInReport, UUID recurringId, UUID draftId, String receiptUrl`. Nếu `id == null`
     thì `write()` tự sinh `UUID.randomUUID()`.
   - `WriteResult` (record): `UUID transactionId, long walletNewBalance, Long destinationWalletNewBalance`
     (null nếu `type != transfer`).
   Bên trong `write()`: build `Transaction` entity từ command (set `isDeleted=false`,
   `createdAt=Instant.now()`, để `updatedAt=null` — trigger tự set), `transactionRepository.save(entity)`,
   rồi gọi `walletRepository.adjustBalance(...)` theo đúng bảng chiều tiền của `api/04-GIAO-DICH.md`
   mục "Cách số dư thay đổi": `expense` → `adjustBalance(walletId, -amount)`; `income` → `adjustBalance(walletId, amount)`;
   `transfer` → `adjustBalance(walletId, -amount)` VÀ `adjustBalance(destinationWalletId, amount)`.
   Đọc lại số dư mới bằng `walletRepository.findByIdForUpdate(walletId).get().getCurrentBalance()`
   SAU khi `adjustBalance` (vì `adjustBalance` là `@Modifying` UPDATE trực tiếp, không tự đồng bộ
   persistence context — phải query lại, không cộng tay trong Java để tránh lệch nếu có nghiệp vụ
   khác chen giữa). Không method nào khác trong class này gọi `this.write(...)` (chống self-invocation).

4. Thêm vào `WalletRepository.java` (file đã có, chỉ thêm method, KHÔNG xoá gì) native query trừ
   ngược D-36 — copy nguyên văn SQL ở `<interfaces>` mục "SQL trừ ngược", đặt tên method
   `findBalanceAsOf`:
   ```java
   @Query(value = "SELECT w.current_balance "
           + "- COALESCE(SUM(CASE "
           + "WHEN t.type = 'income' THEN t.amount "
           + "WHEN t.type = 'expense' THEN -t.amount "
           + "WHEN t.type = 'transfer' AND t.wallet_id = w.id THEN -t.amount END), 0) "
           + "- COALESCE((SELECT SUM(amount) FROM transactions "
           + "WHERE destination_wallet_id = w.id AND date > :asOf AND NOT is_deleted), 0) AS balance_as_of "
           + "FROM wallets w "
           + "LEFT JOIN transactions t ON t.wallet_id = w.id AND t.date > :asOf AND NOT t.is_deleted "
           + "WHERE w.id = :walletId "
           + "GROUP BY w.id, w.current_balance", nativeQuery = true)
   Optional<Long> findBalanceAsOf(@Param("walletId") UUID walletId, @Param("asOf") LocalDate asOf);
   ```
   Thêm thêm `@Query(value = "SELECT EXISTS(SELECT 1 FROM transactions WHERE wallet_id = :walletId
   AND date > CURRENT_DATE AND NOT is_deleted)", nativeQuery = true) boolean
   hasFutureTransactions(@Param("walletId") UUID walletId)` — dùng để quyết định có trả
   `projectedBalance` hay không (D-37: "chỉ trả khi ví CÓ giao dịch tương lai").
   Nếu `findBalanceAsOf` trả rỗng (ví không có `current_balance` — không xảy ra vì cột NOT NULL,
   nhưng `Optional` vì `GROUP BY` có thể không khớp `wallet_id` sai) thì caller phải coi là lỗi
   `NOT_FOUND`, không catch âm thầm.
  </action>
  <verify>
    <automated>mvn -q compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private UUID id" src/main/java/com/datn/financeapp/transaction/entity/Transaction.java` trả về ít nhất 1
    - `grep -n "counts_in_report" src/main/java/com/datn/financeapp/transaction/entity/Transaction.java` có kết quả (cột V8 không bị bỏ sót)
    - `grep -c "@Transactional" src/main/java/com/datn/financeapp/transaction/service/TransactionWriter.java` trả về đúng 1 (chỉ 1 public method, không đặt `@Transactional` cấp class)
    - `grep -n "@Component" src/main/java/com/datn/financeapp/transaction/service/TransactionWriter.java` có kết quả (không phải `@Service`)
    - `grep -n "findBalanceAsOf" src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java` có kết quả
    - `grep -n "hasFutureTransactions" src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java` có kết quả
    - `mvn -q compile` thoát mã 0
  </acceptance_criteria>
  <done>Entity/repository/writer biên dịch sạch, TransactionWriter là bean riêng độc lập không bị nghiệp vụ chi phối, WalletRepository có sẵn 2 method mới cho D-36/D-37.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gộp WalletTransferService qua TransactionWriter (D-30) + sửa 2 trường số dư ví (D-37/TXN-09) + verify 100% test cũ xanh</name>
  <files>
    src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java
    src/main/java/com/datn/financeapp/wallet/dto/WalletResponse.java
    src/main/java/com/datn/financeapp/wallet/dto/WalletDetailResponse.java
    src/main/java/com/datn/financeapp/wallet/service/WalletService.java
    src/test/java/com/datn/financeapp/wallet/WalletFutureBalanceIntegrationTest.java
  </files>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletTransferService.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\dto\WalletResponse.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\dto\WalletDetailResponse.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletService.java
    D:\PTIT\DATN\source\server\.planning\phases\03-giao-dich\03-PATTERNS.md
    D:\PTIT\DATN\api\02-VI.md
    D:\PTIT\DATN\db\README.md
  </read_first>
  <behavior>
    - Test 1 (`WalletFutureBalanceIntegrationTest.walletWithFutureTransaction_returnsBothCurrentAndProjectedBalance`):
      tạo ví "Tiền mặt" `initial_balance=1000000`, ghi 1 giao dịch `expense` hôm nay 200000 và 1 giao
      dịch `income` ngày mai (`LocalDate.now().plusDays(1)`) 500000 qua `TransactionWriter` trực tiếp
      trong `@BeforeEach`/test setup (không qua HTTP — chỉ cần verify tầng Service/Repository ở test
      này). Gọi `GET /wallets/{id}` → kỳ vọng `current_balance = 800000` (1000000-200000, KHÔNG cộng
      khoản mai), `projected_balance = 1300000` (800000+500000).
    - Test 2 (`walletWithoutFutureTransaction_omitsProjectedBalanceField`): ví chỉ có giao dịch quá khứ/hôm
      nay → `GET /wallets/{id}` JSON KHÔNG có field `projected_balance` (dùng `jsonPath("$.data.projected_balance").doesNotExist()`,
      không phải kiểm `isNull()` — vì `isNull()` vẫn pass khi field tồn tại với giá trị null, sai mục tiêu D-37).
  </behavior>
  <action>
1. Sửa `WalletTransferService`:
   - Thêm field `private final com.datn.financeapp.transaction.service.TransactionWriter transactionWriter;`
     vào constructor (Lombok `@RequiredArgsConstructor` tự thêm, chỉ cần khai field).
   - Trong `transfer()`: xoá toàn bộ đoạn `jdbcTemplate.update("INSERT INTO transactions...")` (đoạn 1
     ở `<interfaces>`). Thay bằng gọi:
     ```java
     transactionWriter.write(new TransactionWriteCommand(
             UUID.randomUUID(), userId, sourceId, destinationId, null, "transfer", amount, date,
             req.note(), null, "manual", true, null, null, null));
     ```
     rồi dùng `WriteResult` trả về để lấy `newSourceBalance`/`newDestinationBalance` thay vì tự cộng
     trừ tay bằng `sourceWallet.getCurrentBalance() - amount` như hiện tại (đọc trực tiếp từ
     `WriteResult.walletNewBalance()`/`destinationWalletNewBalance()` — chính xác hơn vì đã qua
     `adjustBalance` thật, không suy diễn).
   - Trong `adjustBalance()`: GIỮ NGUYÊN đoạn tra `categoryId` danh mục hệ thống (không chuyển vào
     TransactionWriter — theo Discretion ở PATTERNS.md, giữ tại `WalletTransferService` vì đây là
     logic đặc thù nghiệp vụ điều chỉnh số dư, TransactionWriter "không biết gì về nghiệp vụ gọi nó"
     theo D-31). Xoá đoạn `jdbcTemplate.update("INSERT INTO transactions...")` (đoạn 2), thay bằng:
     ```java
     TransactionWriter.WriteResult result = transactionWriter.write(new TransactionWriteCommand(
             UUID.randomUUID(), userId, walletId, null, categoryId, type, amount, LocalDate.now(),
             req.note(), null, "adjustment", countsInReport, null, null, null));
     ```
     Dùng `result.transactionId()` và `result.walletNewBalance()` thay cho biến `transactionId` tự
     sinh + gọi `adjustBalance` trực tiếp như hiện tại.
   - SAU khi sửa xong: `grep -c "INSERT INTO transactions" src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java`
     PHẢI trả về 0.

2. Sửa `WalletResponse.java`: thêm field cuối cùng `Long projectedBalance` vào record (giữ nguyên
   11 field cũ, thêm field thứ 12). Sửa `WalletDetailResponse.java`: thêm cùng field `Long
   projectedBalance` (chèn sau `currentBalance`, trước `includeInTotal` hoặc cuối record — miễn nhất
   quán 2 DTO).

3. Sửa `WalletService.toResponse(Wallet wallet)` và `detail(...)`: KHÔNG map thẳng
   `wallet.getCurrentBalance()` vào field `currentBalance` của DTO nữa. Thay bằng gọi
   `walletRepository.findBalanceAsOf(wallet.getId(), LocalDate.now())` để lấy `current_balance` thật
   (tiền thật đến hết hôm nay), và `walletRepository.hasFutureTransactions(wallet.getId())` để quyết
   định `projectedBalance`: nếu `true` thì `projectedBalance = wallet.getCurrentBalance()` (cột thô,
   đã gồm tương lai), nếu `false` thì `projectedBalance = null`. Áp dụng cho CẢ `list()` (dùng trong
   `toResponse`) và `detail()` — cả hai đường đang map thẳng cột đều phải sửa.
   Kiểm tra cấu hình Jackson: chạy `grep -rn "JsonInclude" src/main/java/com/datn/financeapp/` — nếu
   KHÔNG có cấu hình `NON_NULL` toàn cục nào, thêm `@com.fasterxml.jackson.annotation.JsonInclude(
   com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)` ngay trên record
   `WalletResponse`/`WalletDetailResponse` để trường `null` bị Jackson bỏ hẳn khỏi JSON (D-37: "không
   có thì bỏ hẳn trường", không phải trả `null`).

4. Tạo `WalletFutureBalanceIntegrationTest` theo khuôn Testcontainers của `WalletCrudIntegrationTest`
   (copy `@Testcontainers @SpringBootTest @AutoConfigureMockMvc @ActiveProfiles("test")` +
   `NoRateLimitConfig` + `@DynamicPropertySource` jwt.secret) — viết đúng 2 test theo `<behavior>`.

5. **Chạy toàn bộ test suite hiện có** (`mvn test`) và xác nhận **100% xanh** — đây là điều kiện
   bắt buộc trước khi coi Task 2 hoàn thành (D-30). Không sửa logic nghiệp vụ của bất kỳ test cũ nào
   để né lỗi; chỉ được sửa import/khai báo bean nếu một test cũ trực tiếp gọi
   `jdbcTemplate.update("INSERT INTO transactions...")` giả lập seed data (việc này KHÔNG áp dụng
   cho D-30, chỉ áp dụng cho code nghiệp vụ ở `WalletTransferService`).
  </action>
  <verify>
    <automated>mvn test -Dtest=WalletFutureBalanceIntegrationTest && mvn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "INSERT INTO transactions" src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java` trả về 0
    - `grep -n "projectedBalance" src/main/java/com/datn/financeapp/wallet/dto/WalletResponse.java` có kết quả
    - `grep -n "projectedBalance" src/main/java/com/datn/financeapp/wallet/dto/WalletDetailResponse.java` có kết quả
    - `grep -n "findBalanceAsOf" src/main/java/com/datn/financeapp/wallet/service/WalletService.java` có kết quả
    - `mvn test` thoát mã 0, và output tổng kết Surefire không có `Tests run: 0` (đảm bảo test thật sự chạy, không bị skip nhầm)
    - `grep -rn "@Test" src/test/java | wc -l` phải trả về >= 100 (baseline đo thực tế 2026-08-23; nếu executor sửa/xoá bớt @Test lẻ trong 1 class để né lỗi, tổng số này giảm và tiêu chí này bắt được, khác với chỉ kiểm `mvn test` exit 0 — không bắt được kiểu né lỗi này)
    - `mvn test -Dtest=WalletFutureBalanceIntegrationTest` thoát mã 0
  </acceptance_criteria>
  <done>WalletTransferService chỉ còn một đường ghi giao dịch (qua TransactionWriter), WalletResponse/WalletDetailResponse trả đúng current_balance (đã trừ ngược) và projected_balance (chỉ khi có giao dịch tương lai, không có thì bỏ hẳn trường), toàn bộ test Phase 1+2 xanh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → `GET /wallets`, `GET /wallets/{id}` | Response lộ số dư — phải đúng nghĩa "tiền thật" theo D-37, sai nghĩa không phải lỗ hổng bảo mật nhưng là lỗi nghiệp vụ nghiêm trọng gây mất niềm tin |
| `WalletTransferService` → `TransactionWriter` | Nội bộ backend, không phải input người dùng trực tiếp — nhưng nếu `TransactionWriter` nhận sai `userId` từ caller thì ghi giao dịch cho sai chủ sở hữu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `TransactionWriter.write()` | mitigate | Nhận `userId` từ tham số tường minh do caller (`WalletTransferService`) truyền vào — caller đã xác thực qua `SecurityContextUtil.currentUserId()` trước đó; `TransactionWriter` không tự đọc `SecurityContext` để tránh phụ thuộc ngữ cảnh HTTP khi Phase 4 gọi từ job nền |
| T-03-02 | Information Disclosure | `WalletResponse`/`WalletDetailResponse` | mitigate | `projectedBalance = null` bị Jackson lược bỏ hẳn (`@JsonInclude(NON_NULL)`) thay vì trả `null` tường minh — tránh app hiểu nhầm "có nhưng bằng 0" |
| T-03-03 | Repudiation | `Transaction.updatedAt` | accept | Trigger `trg_transactions_validate` sở hữu cột này, backend không kiểm soát — chấp nhận vì đây là thiết kế DB cố ý, không phải lỗ hổng |
</threat_model>

<verification>
- `mvn -q compile` không lỗi
- `mvn test` toàn bộ suite (Phase 1+2+3-01) thoát mã 0, không có test bị `@Disabled`
- `grep -rn "INSERT INTO transactions" src/main/java/` chỉ còn xuất hiện trong `TransactionWriter.java` (qua `transactionRepository.save()`, không phải native SQL) — không còn ở `WalletTransferService.java`
</verification>

<success_criteria>
- Một đường ghi giao dịch duy nhất (`TransactionWriter`) cho toàn hệ thống
- 100% test hiện có (Phase 1+2) xanh sau khi gộp
- `WalletResponse`/`WalletDetailResponse` đúng nghĩa D-37: `current_balance` = tiền thật đến hết hôm nay, `projected_balance` chỉ xuất hiện khi có giao dịch tương lai
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/03-giao-dich/03-01-SUMMARY.md`
</output>
</content>
