---
phase: 04-nghi-p-v-ph-i-sinh-b-o-c-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migration/V9__thong_bao.sql
  - db/migration/V10__export_jobs.sql
  - api/11-THONG-BAO.md
  - CLAUDE.md
  - api/00-QUY-UOC-CHUNG.md
  - source/server/src/main/java/com/datn/financeapp/notification/entity/Notification.java
  - source/server/src/main/java/com/datn/financeapp/notification/repository/NotificationRepository.java
  - source/server/src/main/java/com/datn/financeapp/notification/service/NotificationService.java
  - source/server/src/main/java/com/datn/financeapp/notification/controller/NotificationController.java
  - source/server/src/main/java/com/datn/financeapp/notification/dto/NotificationListItemResponse.java
  - source/server/src/main/java/com/datn/financeapp/notification/dto/NotificationListResponse.java
  - source/server/src/test/java/com/datn/financeapp/common/exception/GlobalExceptionHandlerTest.java
  - source/server/src/test/java/com/datn/financeapp/notification/NotificationIntegrationTest.java
autonomous: true
requirements: [BUDGET-06, JOB-04]
user_setup: []

must_haves:
  truths:
    - "User xem được danh sách thông báo của chính mình, phân trang theo api/00 mục 7.1"
    - "User đánh dấu một thông báo là đã đọc"
    - "User A không đọc được thông báo của user B (404, không phải 403)"
    - "Bảng export_jobs tồn tại sẵn cho Plan 06 dùng, không phải tạo lại migration"
  artifacts:
    - path: "db/migration/V9__thong_bao.sql"
      provides: "Bảng notifications với UNIQUE chống trùng cảnh báo ngân sách"
    - path: "db/migration/V10__export_jobs.sql"
      provides: "Bảng export_jobs cho REPORT-05 (Plan 06 dùng)"
    - path: "api/11-THONG-BAO.md"
      provides: "Hợp đồng API mới GET /notifications, PATCH /notifications/{id}/read"
    - path: "source/server/src/main/java/com/datn/financeapp/notification/repository/NotificationRepository.java"
      provides: "Query có điều kiện quyền user_id = :currentUser trong SQL"
  key_links:
    - from: "notification/controller/NotificationController.java"
      to: "notification/service/NotificationService.java"
      via: "constructor injection"
      pattern: "NotificationService"
    - from: "notification/repository/NotificationRepository.java"
      to: "db/migration/V9__thong_bao.sql"
      via: "native query trên bảng notifications"
      pattern: "FROM notifications"
---

<objective>
Xây hạ tầng thông báo — bảng `notifications` mới (chưa tồn tại trong schema V1-V8) và hai
endpoint đọc/đánh dấu đã đọc — làm nền cho JOB-04 (nhắc nợ) và BUDGET-06/D-41 (cảnh báo ngân
sách) ở các plan sau. Đồng thời tạo bảng `export_jobs` ngay trong migration cùng đợt (đỡ phải
mở thêm file migration ở Plan 06) và viết đặc tả `api/11-THONG-BAO.md` MỚI theo quy tắc
"không tự bịa API — phải đặc tả trước khi code" (D-40).

Purpose: Đây là plan đi đầu bắt buộc theo D-56 vì cả sổ nợ (Plan 03) lẫn ngân sách (Plan 02)
đều cần bảng `notifications` tồn tại trước khi ghi cảnh báo vào đó.
Output: Migration V9/V10, package `notification/` đầy đủ CRUD tối thiểu, tài liệu API mới,
CLAUDE.md gốc cập nhật bản đồ tài liệu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md
@D:\PTIT\DATN\source\server\.planning\phases\04-nghi-p-v-ph-i-sinh-b-o-c-o\04-CONTEXT.md
@D:\PTIT\DATN\db\migration\V1__nen_tang.sql
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\repository\WalletRepository.java
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletService.java
@D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\common\exception\GlobalExceptionHandlerTest.java
@D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\wallet\WalletAccessControlIntegrationTest.java

<interfaces>
Mẫu quyền D-27 (áp dụng y hệt, KHÔNG có vế group_id cho notifications — chỉ user_id):
```java
// WalletRepository.java dòng 36-41
@Query(
        value = "SELECT * FROM wallets w WHERE w.id = :id AND NOT w.is_deleted "
                + "AND (w.user_id = :currentUser OR w.group_id IN "
                + "(SELECT group_id FROM group_members WHERE user_id = :currentUser AND is_active))",
        nativeQuery = true)
Optional<Wallet> findByIdForUser(@Param("id") UUID id, @Param("currentUser") UUID currentUser);
```

Mẫu response phân trang chuẩn (api/00 mục 4.2) — dùng lại đúng khung `{success, data, pagination}`
đã có ở `common/response/`. Đọc `WalletListResponse`-tương tự (nếu có) hoặc bám sát
`TransactionListResponse` (Phase 3) cho cấu trúc `pagination`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration V9 (notifications) + V10 (export_jobs) + đặc tả api/11-THONG-BAO.md</name>
  <read_first>
    D:\PTIT\DATN\db\migration\V1__nen_tang.sql (mẫu FK tới users, mẫu is_deleted/created_at)
    D:\PTIT\DATN\db\migration\V8__dieu_chinh_so_du.sql (mẫu comment giải thích lý do thiết kế)
    D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md (mục 4.2 phân trang, mục 6 bảng mã lỗi dùng chung, mục 14 quy ước đường dẫn)
    D:\PTIT\DATN\CLAUDE.md (bảng "Bản đồ tài liệu" — mục cần sửa "api/01 đến api/10")
  </read_first>
  <action>
    Tạo `db/migration/V9__thong_bao.sql` với bảng `notifications`:
    ```sql
    CREATE TABLE notifications (
        id         UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
        user_id    UUID        NOT NULL,
        type       VARCHAR(30) NOT NULL,
        title      VARCHAR(150) NOT NULL,
        content    TEXT        NOT NULL,
        reference_id UUID,
        is_read    BOOLEAN     NOT NULL DEFAULT FALSE,
        created_at TIMESTAMPTZ NOT NULL DEFAULT now(),

        CONSTRAINT fk_notif_user FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE,
        CONSTRAINT ck_notif_type CHECK (type IN ('budget_alert', 'debt_reminder', 'recurring_generated', 'budget_renewed', 'goal_completed'))
    );
    CREATE INDEX idx_notif_user ON notifications (user_id, created_at DESC);
    -- Chống trùng cảnh báo ngân sách khi bulk 50 dòng bắn nhiều event cùng ngân sách/ngày/mức độ (D-41).
    -- reference_id lưu budget_id hoặc debt_id tuỳ type.
    CREATE UNIQUE INDEX uq_notif_budget_alert
        ON notifications (user_id, reference_id, (created_at::date), type)
        WHERE type = 'budget_alert';
    ```
    Đặt `type` đủ 5 giá trị cần cho Phase 4: `budget_alert` (D-41), `debt_reminder` (JOB-04),
    `recurring_generated`, `budget_renewed`, `goal_completed` — dù Plan này chỉ ghi
    `budget_alert`/`debt_reminder`, các giá trị còn lại dự phòng cho service ở plan sau insert
    (không phải sửa lại CHECK constraint giữa phase).

    Tạo `db/migration/V10__export_jobs.sql`:
    ```sql
    CREATE TABLE export_jobs (
        id           UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
        user_id      UUID        NOT NULL,
        format       VARCHAR(10) NOT NULL,
        status       VARCHAR(20) NOT NULL DEFAULT 'processing',
        file_path    TEXT,
        error_message TEXT,
        expires_at   TIMESTAMPTZ,
        created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),

        CONSTRAINT fk_export_user FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE,
        CONSTRAINT ck_export_format CHECK (format IN ('csv', 'pdf', 'excel')),
        CONSTRAINT ck_export_status CHECK (status IN ('processing', 'completed', 'failed'))
    );
    CREATE INDEX idx_export_user ON export_jobs (user_id, created_at DESC);
    -- Dọn job/tệp quá hạn hằng ngày (D-45) quét theo cột này.
    CREATE INDEX idx_export_expires ON export_jobs (expires_at) WHERE status = 'completed';
    ```
    Migration này KHÔNG dùng ngay ở Plan 01 (Plan 06 mới code `report/`) nhưng viết cùng đợt để
    tránh mở lại file migration giữa phase — đúng D-42.

    Tạo file mới `api/11-THONG-BAO.md` (repo gốc DATN, KHÔNG phải `source/server/`) theo đúng
    khung các file `api/*.md` khác (đọc `api/05-NGAN-SACH.md` làm mẫu cấu trúc heading). Nội
    dung bắt buộc:
    - Bảng CSDL liên quan: `notifications`
    - Hai điểm cuối: `GET /notifications` (phân trang chuẩn mục 7.1, tham số `is_read` lọc
      tuỳ chọn) và `PATCH /notifications/{id}/read` (đặt `is_read = true`, trả 200)
    - Cấu trúc response item: `id`, `type`, `title`, `content`, `reference_id`, `is_read`,
      `created_at`
    - Ghi rõ nguyên tắc D-41: "Bản ghi `notifications` là lịch sử tại thời điểm phát sinh cảnh
      báo/nhắc nhở, KHÔNG tự cập nhật lại khi dữ liệu gốc thay đổi sau đó (ví dụ xoá giao dịch
      khiến ngân sách không còn vượt nữa) — nguồn sự thật cho trạng thái HIỆN TẠI vẫn là
      `GET /budgets/alerts`."
    - Mã lỗi: không có mã riêng ngoài `NOT_FOUND` (404) khi thông báo không phải của user.
    - Ghi rõ phạm vi KHÔNG làm ở phase này (D-39): không có `DELETE /notifications`, không có
      `GET /notifications/unread-count`.

    Sửa `CLAUDE.md` gốc (D:\PTIT\DATN\CLAUDE.md) mục bảng "Bản đồ tài liệu": dòng đang ghi
    `api/01` đến `api/10` — tên file tự giải thích nghiệp vụ" đổi thành `api/01` đến `api/11`.

    Sửa `api/00-QUY-UOC-CHUNG.md` mục 1 "Danh sách file": thêm dòng
    `| [11-THONG-BAO.md](11-THONG-BAO.md) | Thông báo trong ứng dụng (ngân sách, nhắc nợ) |`
    vào cuối bảng.
  </action>
  <verify>
    <automated>test -f D:/PTIT/DATN/db/migration/V9__thong_bao.sql && test -f D:/PTIT/DATN/db/migration/V10__export_jobs.sql && test -f D:/PTIT/DATN/api/11-THONG-BAO.md && grep -q "GET /notifications" D:/PTIT/DATN/api/11-THONG-BAO.md</automated>
  </verify>
  <acceptance_criteria>
    - `db/migration/V9__thong_bao.sql` tồn tại, chứa chuỗi `CREATE TABLE notifications` và
      `uq_notif_budget_alert`
    - `db/migration/V10__export_jobs.sql` tồn tại, chứa chuỗi `CREATE TABLE export_jobs`
    - `api/11-THONG-BAO.md` tồn tại, chứa chuỗi `GET /notifications` và
      `PATCH /notifications/{id}/read`
    - `grep -c "api/01.*api/11" D:/PTIT/DATN/CLAUDE.md` trả về ít nhất 1
    - `grep -c "11-THONG-BAO" D:/PTIT/DATN/api/00-QUY-UOC-CHUNG.md` trả về ít nhất 1
  </acceptance_criteria>
  <done>Hai migration mới và hai file tài liệu tồn tại đúng nội dung yêu cầu, chưa cần code Java chạy được ở bước này.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Entity/Repository/Service/Controller notification + test tích hợp</name>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\entity\Wallet.java (mẫu entity @Builder)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\repository\WalletRepository.java (mẫu quyền D-27)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletService.java (mẫu CRUD @Transactional từng method)
    D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\wallet\WalletAccessControlIntegrationTest.java (mẫu Testcontainers + NoRateLimitConfig)
    D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\common\exception\GlobalExceptionHandlerTest.java (danh sách excludeFilters hiện có)
  </read_first>
  <behavior>
    - Test 1: `GET /notifications` trả danh sách thông báo của user hiện tại, phân trang đúng
      `page`/`page_size`, KHÔNG trả thông báo của user khác
    - Test 2: `PATCH /notifications/{id}/read` đặt `is_read = true`, gọi lại lần 2 vẫn trả 200
      (idempotent)
    - Test 3: User A gọi `PATCH /notifications/{idCuaB}/read` nhận `404 NOT_FOUND`
    - Test 4: `GET /notifications?is_read=false` chỉ trả thông báo chưa đọc
  </behavior>
  <action>
    Tạo `notification/entity/Notification.java` — `@Entity @Table(name = "notifications")`, các
    field `id`, `userId`, `type`, `title`, `content`, `referenceId`, `isRead` (Boolean),
    `createdAt` (Instant), theo đúng khuôn `Wallet.java` (`@Getter @Setter @Builder
    @NoArgsConstructor @AllArgsConstructor`).

    Tạo `notification/repository/NotificationRepository.java` — `JpaRepository<Notification,
    UUID>` với 3 method native query kèm điều kiện quyền `user_id = :currentUser` NGAY TRONG
    SQL (không lọc ở Java, theo mẫu `WalletRepository.findByIdForUser`):
    ```java
    @Query(value = "SELECT * FROM notifications WHERE user_id = :currentUser "
            + "AND (:isRead IS NULL OR is_read = :isRead) "
            + "ORDER BY created_at DESC LIMIT :limit OFFSET :offset", nativeQuery = true)
    List<Notification> findAllForUser(@Param("currentUser") UUID currentUser,
            @Param("isRead") Boolean isRead, @Param("limit") int limit, @Param("offset") int offset);

    @Query(value = "SELECT COUNT(*) FROM notifications WHERE user_id = :currentUser "
            + "AND (:isRead IS NULL OR is_read = :isRead)", nativeQuery = true)
    long countForUser(@Param("currentUser") UUID currentUser, @Param("isRead") Boolean isRead);

    @Query(value = "SELECT * FROM notifications WHERE id = :id AND user_id = :currentUser",
            nativeQuery = true)
    Optional<Notification> findByIdForUser(@Param("id") UUID id, @Param("currentUser") UUID currentUser);

    @Modifying
    @Query("UPDATE Notification n SET n.isRead = TRUE WHERE n.id = :id AND n.userId = :currentUser")
    int markRead(@Param("id") UUID id, @Param("currentUser") UUID currentUser);
    ```
    Thêm method `insertBudgetAlertIfNotExists` dùng `INSERT ... ON CONFLICT DO NOTHING` cho
    Plan 02 (`budget/`) gọi lại — KHÔNG code logic ngân sách ở đây, chỉ chuẩn bị sẵn method này
    vì Plan 02 phụ thuộc `notification/repository/NotificationRepository`:
    ```java
    @Modifying
    @Query(value = "INSERT INTO notifications (user_id, type, title, content, reference_id) "
            + "VALUES (:userId, 'budget_alert', :title, :content, :budgetId) "
            + "ON CONFLICT (user_id, reference_id, (created_at::date), type) DO NOTHING "
            + "WHERE type = 'budget_alert'", nativeQuery = true)
    ```
    Lưu ý cú pháp: mệnh đề `WHERE` gắn vào partial unique index không hợp lệ trong câu INSERT
    thường — nếu Postgres báo lỗi cú pháp khi build, đổi tên constraint thành UNIQUE đầy đủ
    không có mệnh đề `WHERE` (bỏ điều kiện `WHERE type = 'budget_alert'` khỏi
    `uq_notif_budget_alert` ở Task 1 nếu cần, ưu tiên chọn phương án chạy được thay vì giữ
    nguyên cú pháp ban đầu) — ghi log quyết định thực tế vào SUMMARY.

    Tạo `notification/service/NotificationService.java` — `@Service @RequiredArgsConstructor`,
    hai method `@Transactional(readOnly = true) list(...)` và `@Transactional markRead(...)`.
    `markRead` ném `BusinessException("NOT_FOUND", 404, "Không tìm thấy thông báo.")` khi
    `findByIdForUser` rỗng — dùng để phân biệt không tồn tại/không có quyền, KHÔNG cần logic
    idempotent phức tạp vì `UPDATE ... WHERE is_read = TRUE` không đổi gì nếu gọi lại, vẫn trả
    200.

    Tạo `notification/controller/NotificationController.java` — `@RestController
    @RequestMapping("/notifications")`, `GET` trả `NotificationListResponse` (khung phân trang
    `{success, data, pagination}` theo api/00 mục 4.2), `PATCH /{id}/read` trả
    `{success: true, data: null}` hoặc `NotificationListItemResponse` cập nhật.

    Tạo `NotificationListItemResponse` (record: id, type, title, content, referenceId, isRead,
    createdAt) và `NotificationListResponse` (danh sách + pagination) trong `notification/dto/`.

    Sửa `GlobalExceptionHandlerTest.java` — thêm
    `com.datn.financeapp.notification.controller.NotificationController.class` vào mảng
    `classes = {...}` của `excludeFilters`.

    Tạo `src/test/java/com/datn/financeapp/notification/NotificationIntegrationTest.java` theo
    đúng khuôn Testcontainers của `WalletAccessControlIntegrationTest.java` (copy
    `@Testcontainers`, `@SpringBootTest`, `@AutoConfigureMockMvc`, `NoRateLimitConfig`, JWT
    secret giả). Viết 4 test case ở `<behavior>` — tên method tiếng Anh, ví dụ
    `getNotifications_returnsOnlyOwnNotifications`, `markRead_isIdempotent`,
    `markRead_otherUsersNotification_returns404`, `getNotifications_filterByIsRead`. Insert dữ
    liệu test trực tiếp bằng `JdbcTemplate` (bảng `notifications` mới, chưa có endpoint tạo —
    test tự chèn dữ liệu giả để kiểm GET/PATCH).
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn test -Dtest=NotificationIntegrationTest -Dtest=GlobalExceptionHandlerTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=NotificationIntegrationTest` thoát mã 0, cả 4 test case pass
    - `mvn test -Dtest=GlobalExceptionHandlerTest` vẫn xanh sau khi thêm exclude filter
    - `NotificationRepository.java` chứa chuỗi `user_id = :currentUser` ở mọi method SELECT/UPDATE theo id
    - `NotificationController.java` có route `GET` và `PATCH /{id}/read`
  </acceptance_criteria>
  <done>Hai endpoint GET/PATCH notifications chạy được qua Testcontainers thật, quyền kiểm tra ngay trong SQL, controller đã khai báo trong GlobalExceptionHandlerTest excludeFilters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET/PATCH /notifications | Người dùng đã xác thực JWT truy vấn thông báo của chính mình |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | NotificationRepository | mitigate | Mọi query kèm `user_id = :currentUser` ngay trong SQL, không lọc ở Java (CLAUDE.md §7) |
| T-04-02 | Information Disclosure | NotificationController PATCH /{id}/read | mitigate | 404 khi không có quyền (không phải 403), đúng quy tắc api/00 mục 9 |
| T-04-03 | Denial of Service | Bulk 50 giao dịch bắn 50 event cảnh báo ngân sách (chuẩn bị cho Plan 02) | accept | UNIQUE index chặn trùng ở tầng CSDL, không phải tầng ứng dụng — rủi ro thấp vì đã có lớp bảo vệ DB |
</threat_model>

<verification>
- `mvn test -Dtest=NotificationIntegrationTest,GlobalExceptionHandlerTest` xanh
- `mvn spring-boot:run` với Flyway trỏ `db/migration/` gốc chạy sạch V1→V10, không lỗi checksum/thứ tự
- `grep -rn "AT TIME ZONE\|giờ Việt Nam" D:/PTIT/DATN/api/11-THONG-BAO.md` trả về rỗng (tài liệu mới không dính lỗi múi giờ)
</verification>

<success_criteria>
- Bảng `notifications`/`export_jobs` tồn tại, migration chạy sạch
- `GET /notifications` và `PATCH /notifications/{id}/read` hoạt động đúng quyền D-27
- `api/11-THONG-BAO.md` tồn tại, `CLAUDE.md` gốc và `api/00-QUY-UOC-CHUNG.md` đã cập nhật bản đồ tài liệu
</success_criteria>

<output>
After completion, create `.planning/phases/04-nghi-p-v-ph-i-sinh-b-o-c-o/04-01-SUMMARY.md`
</output>
</content>
