---
phase: 04-nghi-p-v-ph-i-sinh-b-o-c-o
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - source/server/src/main/java/com/datn/financeapp/debt/entity/Debt.java
  - source/server/src/main/java/com/datn/financeapp/debt/entity/DebtPayment.java
  - source/server/src/main/java/com/datn/financeapp/debt/repository/DebtRepository.java
  - source/server/src/main/java/com/datn/financeapp/debt/repository/DebtPaymentRepository.java
  - source/server/src/main/java/com/datn/financeapp/debt/service/DebtService.java
  - source/server/src/main/java/com/datn/financeapp/debt/controller/DebtController.java
  - source/server/src/main/java/com/datn/financeapp/debt/dto/CreateDebtRequest.java
  - source/server/src/main/java/com/datn/financeapp/debt/dto/CreateDebtResponse.java
  - source/server/src/main/java/com/datn/financeapp/debt/dto/DebtListItemResponse.java
  - source/server/src/main/java/com/datn/financeapp/debt/dto/DebtDetailResponse.java
  - source/server/src/main/java/com/datn/financeapp/debt/dto/DebtSummaryResponse.java
  - source/server/src/main/java/com/datn/financeapp/debt/dto/CreatePaymentRequest.java
  - source/server/src/main/java/com/datn/financeapp/debt/dto/CreatePaymentResponse.java
  - source/server/src/main/java/com/datn/financeapp/debt/dto/UpdateDebtRequest.java
  - source/server/src/main/java/com/datn/financeapp/debt/dto/WriteOffRequest.java
  - source/server/src/test/java/com/datn/financeapp/debt/DebtLifecycleIntegrationTest.java
  - source/server/src/test/java/com/datn/financeapp/debt/DebtPaymentIntegrationTest.java
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05, DEBT-06, DEBT-07]
user_setup: []

must_haves:
  truths:
    - "User tạo khoản nợ sinh đúng một giao dịch thật, số dư ví cập nhật đúng"
    - "User ghi một lần trả nợ cộng dồn paid_amount qua trigger, chuyển status=settled khi trả đủ"
    - "User huỷ một lần trả nợ mở lại status=outstanding từ settled, số dư ví hoàn tác đúng"
    - "Trả vượt số còn nợ bị chặn ở tầng service với thông điệp gợi ý cách xử lý (D-47)"
    - "User xoá khoản nợ hoàn tác toàn bộ giao dịch gốc và mọi giao dịch trả nợ"
    - "Backend không bao giờ tự ghi debts.paid_amount hoặc debts.status (trừ write-off)"
  artifacts:
    - path: "source/server/src/main/java/com/datn/financeapp/debt/service/DebtService.java"
      provides: "Toàn bộ nghiệp vụ sổ nợ, gọi lại TransactionWriter, không viết lại logic ví"
    - path: "source/server/src/main/java/com/datn/financeapp/debt/repository/DebtRepository.java"
      provides: "Đọc lại debt sau khi trigger UPDATE bằng native query hoặc entityManager.refresh"
  key_links:
    - from: "debt/service/DebtService.java"
      to: "transaction/service/TransactionWriter.java"
      via: "transactionWriter.write(new TransactionWriteCommand(...))"
      pattern: "transactionWriter.write"
    - from: "debt/service/DebtService.java"
      to: "db/migration/V4__so_no_muc_tieu.sql"
      via: "chỉ INSERT/DELETE debt_payments, không ghi paid_amount/status"
      pattern: "debtPaymentRepository.(save|delete)"
---

<objective>
Xây module sổ nợ hoàn chỉnh (DEBT-01..07) — tạo khoản nợ/trả nợ/huỷ trả/write-off/xoá đều gọi
lại `TransactionWriter` (D-31) cho phần ghi giao dịch thật, và tuân thủ tuyệt đối ranh giới
trigger V4: backend chỉ chèn/xoá bản ghi `debt_payments`, KHÔNG BAO GIỜ ghi `paid_amount`/
`status` (trừ ngoại lệ write-off). Nhắc nợ đến hạn (DEBT-07/JOB-04) chỉ code phần TRUY VẤN ở
đây — job `@Scheduled` thật gộp chung ở Plan 07 theo D-57.

Purpose: Đây là module rủi ro nhất Phase 4 về mặt ranh giới dữ liệu — trigger ghi đè âm thầm
nếu backend cũng ghi. Đặt ngay sau ngân sách theo D-56.
Output: CRUD sổ nợ đầy đủ, API huỷ trả nợ (điểm D-32 Phase 3 trỏ sang), test xác nhận trigger
tự mở lại settled → outstanding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\api\08-SO-NO.md
@D:\PTIT\DATN\source\server\.planning\phases\04-nghi-p-v-ph-i-sinh-b-o-c-o\04-CONTEXT.md
@D:\PTIT\DATN\db\migration\V4__so_no_muc_tieu.sql
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionWriter.java
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionWriteCommand.java
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletTransferService.java
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\repository\WalletRepository.java

<interfaces>
`TransactionWriteCommand` (KHÔNG đổi, chỉ dùng):
```java
public record TransactionWriteCommand(
        UUID id, UUID userId, UUID walletId, UUID destinationWalletId, UUID categoryId,
        String type, long amount, LocalDate date, String note, String displayName,
        String source, boolean countsInReport, UUID recurringId, UUID draftId, String receiptUrl) {}
```
`id` truyền `null` để tự sinh. `source` = `"manual"` KHÔNG phù hợp cho giao dịch debt — kiểm
tra `ck_txn_source` (V2 + V8: `manual|text|ocr|auto|adjustment`) chỉ cho phép 5 giá trị này,
KHÔNG có `"debt"`. Vì vậy giao dịch debt vẫn phải dùng `source = "manual"` (người dùng chủ động
tạo qua form sổ nợ) — KHÔNG tự đặt giá trị `source` mới ngoài 5 giá trị CHECK constraint cho
phép. `recurringId` = `null`, `draftId` = `null`, `receiptUrl` = `null`, `countsInReport` =
`true` (giao dịch debt vẫn tính vào báo cáo — api/08 không nói loại trừ, chỉ danh mục riêng
"Cho vay/Thu nợ/Đi vay/Trả nợ" đã tách khỏi nhóm sinh hoạt để không lẫn vào thống kê chi tiêu
thường ngày).

`fn_debt_payments_sync` hành vi thật (V4 dòng 78-110): `AFTER INSERT OR UPDATE OR DELETE ON
debt_payments FOR EACH ROW`, tự `SELECT ... FOR UPDATE` khoá `debts`, tự `SUM()` lại
`debt_payments`, `RAISE EXCEPTION` nếu vượt `principal_amount` (ERRCODE `check_violation`), tự
chuyển `status` (giữ nguyên `written_off`, `settled` khi đủ, `outstanding` khi chưa đủ).
`RETURN NULL` — không sửa `NEW`, chạy như AFTER trigger thuần UPDATE bảng khác.

Bốn danh mục hệ thống theo api/08 mục "Cần danh mục riêng cho sổ nợ": "Cho vay" (expense),
"Thu nợ" (income), "Đi vay" (income), "Trả nợ" (expense) — **các danh mục này CHƯA tồn tại
trong migration V1-V10 hiện có** (không giống "Cập nhật số dư" đã insert sẵn ở V8). Task 1 của
plan này PHẢI kiểm tra bằng `SELECT * FROM categories WHERE name IN ('Cho vay','Thu nợ','Đi
vay','Trả nợ')` trên Testcontainers trước khi code — nếu không tồn tại, tạo migration mới
`V11__danh_muc_so_no.sql` chèn 4 danh mục hệ thống này theo đúng khuôn V8 mục 3 (JOIN
`category_groups`/`icons`, `user_id = NULL`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify/tạo 4 danh mục hệ thống sổ nợ + Debt/DebtPayment entity + repository</name>
  <read_first>
    D:\PTIT\DATN\db\migration\V4__so_no_muc_tieu.sql (toàn bộ — bảng debts/debt_payments, trigger)
    D:\PTIT\DATN\db\migration\V5__du_lieu_he_thong.sql (danh mục hệ thống đã có sẵn, mẫu category_groups/icons)
    D:\PTIT\DATN\db\migration\V8__dieu_chinh_so_du.sql (mẫu insert 2 danh mục hệ thống mục 3)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\entity\Wallet.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\repository\WalletRepository.java
  </read_first>
  <action>
    Đọc `db/migration/V5__du_lieu_he_thong.sql` để xác nhận DANH SÁCH ĐẦY ĐỦ các danh mục hệ
    thống đã insert sẵn (grep `INSERT INTO categories`). Nếu 4 danh mục "Cho vay"/"Thu nợ"/
    "Đi vay"/"Trả nợ" CHƯA có trong V5, tạo `db/migration/V11__danh_muc_so_no.sql` theo đúng
    khuôn V8 mục 3 (JOIN `category_groups` nhóm "Khác", `icons` chọn icon phù hợp có sẵn, ví dụ
    tái dùng code icon `vi_tien` như V8 nếu không có icon riêng, `user_id = NULL`, đặt
    `sort_order` cuối danh sách để không xáo trộn thứ tự hiện có).

    Tạo `debt/entity/Debt.java` — field: id, userId, walletId, type, counterpartyName,
    principalAmount, paidAmount, issuedDate, dueDate, note, status, originTransactionId,
    createdAt (theo đúng cột `debts` V4).

    Tạo `debt/entity/DebtPayment.java` — field: id, debtId, transactionId, amount, paidDate,
    note, createdAt (theo đúng cột `debt_payments` V4).

    Tạo `debt/repository/DebtRepository.java`:
    ```java
    @Query(value = "SELECT * FROM debts WHERE id = :id AND user_id = :currentUser", nativeQuery = true)
    Optional<Debt> findByIdAndUserId(@Param("id") UUID id, @Param("currentUser") UUID currentUser);

    @Query(value = "SELECT * FROM debts WHERE user_id = :currentUser "
            + "AND (:type IS NULL OR type = :type) AND (:status IS NULL OR status = :status) "
            + "ORDER BY issued_date DESC", nativeQuery = true)
    List<Debt> findAllForUser(@Param("currentUser") UUID currentUser, @Param("type") String type, @Param("status") String status);

    // Đọc lại SAU khi trigger UPDATE bảng cha — bypass Hibernate identity map, đúng mẫu
    // WalletRepository.findCurrentBalanceNative().
    @Query(value = "SELECT * FROM debts WHERE id = :id", nativeQuery = true)
    Optional<Debt> findByIdNative(@Param("id") UUID id);

    @Query(value = "SELECT * FROM debts WHERE user_id = :currentUser AND status = 'outstanding' "
            + "AND due_date IS NOT NULL ORDER BY due_date", nativeQuery = true)
    List<Debt> findOutstandingWithDueDate(@Param("currentUser") UUID currentUser);
    ```

    Tạo `debt/repository/DebtPaymentRepository.java`:
    ```java
    List<DebtPayment> findByDebtIdOrderByPaidDateDesc(UUID debtId);
    Optional<DebtPayment> findByIdAndDebtId(UUID id, UUID debtId);
    List<DebtPayment> findByDebtId(UUID debtId); // dùng khi xoá khoản nợ, lấy danh sách transaction_id TRƯỚC khi cascade xoá
    ```
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn compile -q</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT * FROM categories WHERE name IN ('Cho vay','Thu nợ','Đi vay','Trả nợ') AND user_id IS NULL` trả đúng 4 dòng sau khi chạy Flyway đầy đủ (kiểm bằng test Testcontainers ở Task 2)
    - `Debt.java` có đủ 12 field khớp cột bảng `debts`
    - `DebtRepository.java` có method `findByIdNative` tách biệt với `findByIdAndUserId` (một có điều kiện quyền, một không — dùng đúng ngữ cảnh)
  </acceptance_criteria>
  <done>Danh mục hệ thống sổ nợ tồn tại, entity + repository đọc/ghi đúng schema thật, có sẵn method đọc lại sau trigger.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DebtService — tạo khoản nợ + ghi/huỷ trả nợ + write-off + xoá</name>
  <read_first>
    D:\PTIT\DATN\api\08-SO-NO.md (toàn bộ)
    D:\PTIT\DATN\db\migration\V4__so_no_muc_tieu.sql (dòng 78-110 fn_debt_payments_sync)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletTransferService.java (mẫu validate trước + gọi TransactionWriter)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionService.java (method delete() — mẫu 3 bước hoàn tác, dòng 208+)
    D:\PTIT\DATN\source\server\.planning\phases\04-nghi-p-v-ph-i-sinh-b-o-c-o\04-RESEARCH.md (Pattern 1, dòng 221-266 — đọc lại bản ghi sau trigger)
  </read_first>
  <behavior>
    - Test 1 (DEBT-01): tạo debt `type=lending` sinh đúng 1 giao dịch `expense` danh mục
      "Cho vay", số dư ví giảm đúng `principal_amount`
    - Test 2 (DEBT-03): ghi 2 lần trả nợ 1.000.000 mỗi lần cho khoản nợ 3.000.000 → sau lần 2,
      `GET /debts/{id}` trả `paid_amount=2.000.000`, `status=outstanding`
    - Test 3 (D-47): ghi lần trả vượt số còn lại → 400 `EXCEEDS_REMAINING_AMOUNT`, message chứa
      số tiền còn nợ chính xác
    - Test 4 (D-46, ưu tiên D-58 mục 2): trả đủ 3.000.000 → `status=settled`; huỷ MỘT lần trả
      1.000.000 → `status` tự MỞ LẠI `outstanding`, `paid_amount` giảm đúng, số dư ví hoàn tác
      đúng phần đã huỷ
    - Test 5 (DEBT-05): write-off khoản nợ outstanding → `status=written_off`, KHÔNG sinh giao
      dịch mới, số dư ví không đổi
    - Test 6 (DEBT-06): xoá khoản nợ có 2 lần trả → cả giao dịch gốc VÀ 2 giao dịch trả nợ đều
      bị xoá mềm, số dư ví hoàn tác đúng tổng của cả 3 giao dịch, bản ghi `debts` biến mất hoàn
      toàn (xoá cứng), `debt_payments` cascade xoá theo
  </behavior>
  <action>
    Tạo `debt/service/DebtService.java` — `@Service @RequiredArgsConstructor`:

    **`create(userId, req)` (DEBT-01):** validate `principal_amount > 0`, `due_date >=
    issued_date` nếu có. Xác định `type`/`categoryName`: `lending` → `expense`/"Cho vay",
    `borrowing` → `income`/"Đi vay". Tra `category_id` qua
    `categoryRepository.findByIdAndVisibleToUser`-style nhưng theo TÊN (thêm method
    `findSystemCategoryByName(name, type)` vào `CategoryRepository` nếu chưa có — native query
    `WHERE name = :name AND type = :type AND user_id IS NULL`). Gọi
    `transactionWriter.write(new TransactionWriteCommand(null, userId, walletId, null,
    categoryId, txnType, principalAmount, issuedDate, note, null, "manual", true, null, null,
    null))`. Tạo `Debt` với `paidAmount=0, status="outstanding",
    originTransactionId=result.transactionId()`, `debtRepository.save(...)`. Toàn bộ trong MỘT
    `@Transactional`.

    **`addPayment(userId, debtId, req)` (DEBT-03, D-47):** load debt qua `findByIdAndUserId`
    (404 nếu không có quyền). Nếu `status == "written_off"` → 409 `DEBT_WRITTEN_OFF`. Nếu
    `status == "settled"` → 409 `DEBT_ALREADY_SETTLED`. **Validate D-47 TRƯỚC khi chạm
    trigger:**
    ```java
    long remaining = debt.getPrincipalAmount() - debt.getPaidAmount();
    if (req.amount() > remaining) {
        throw new BusinessException("EXCEEDS_REMAINING_AMOUNT", 400,
                String.format("%s chỉ còn nợ %,d đ. Ghi trả %,d đ, phần dư ghi thành khoản thu riêng.",
                        debt.getCounterpartyName(), remaining, remaining));
    }
    ```
    Xác định chiều giao dịch: `lending` được trả → `income`/"Thu nợ"; `borrowing` mình trả →
    `expense`/"Trả nợ". `walletId` = `req.walletId()` nếu có, else `debt.getWalletId()`. Gọi
    `transactionWriter.write(...)`. `debtPaymentRepository.save(DebtPayment.builder()
    .debtId(debtId).transactionId(result.transactionId()).amount(req.amount())
    .paidDate(req.paidDate() != null ? req.paidDate() : LocalDate.now()).note(req.note())
    .build())`. **BẮT BUỘC đọc lại `debtRepository.findByIdNative(debtId)`** ngay sau save để
    lấy `paidAmount`/`status` MỚI (trigger đã chạy trong cùng transaction PostgreSQL nhưng
    Hibernate identity map giữ instance cũ) — dùng kết quả đọc lại để build response, KHÔNG
    dùng biến `debt` đã load trước đó.

    **`cancelPayment(userId, debtId, paymentId)` (DEBT-04, D-46 — thứ tự BẮT BUỘC):**
    1. Load debt (quyền), load payment qua `debtPaymentRepository.findByIdAndDebtId` (404 nếu
       không tồn tại/sai debt)
    2. `debtPaymentRepository.delete(payment)` — trigger tự SUM lại, tự mở `settled →
       outstanding` nếu cần
    3. Xoá mềm giao dịch `payment.getTransactionId()` **qua đúng luồng 3 bước của
       `TransactionService`** (inject `TransactionService`, gọi lại `transactionService.delete(
       userId, payment.getTransactionId())` — KHÔNG viết lại logic hoàn tác ví). Lưu ý
       `TransactionService.delete()` tự kiểm tra `existsDebtPaymentLink` (D-32 Phase 3) — vì
       `debt_payments` ĐÃ bị xoá ở bước 2 (trước bước này), kiểm tra đó sẽ trả `false`, xoá mềm
       thành công bình thường, không bị chặn 409.
    Tất cả trong MỘT `@Transactional`.

    **`writeOff(userId, debtId, req)` (DEBT-05):** load debt (quyền), chặn nếu đã
    `written_off`/`settled` (theo api/08 không nói rõ có cho write-off khoản đã settled hay
    không — MẶC ĐỊNH cho phép write-off bất kỳ trạng thái nào TRỪ đã `written_off` rồi, vì
    "không đòi nữa" vẫn có nghĩa với khoản đã trả một phần rồi ngừng đòi phần còn lại). Đây là
    **ngoại lệ DUY NHẤT** được ghi trực tiếp `debt.setStatus("written_off")` rồi
    `debtRepository.save(debt)` — trigger có nhánh `WHEN status = 'written_off' THEN
    'written_off'` giữ nguyên, không ghi đè. KHÔNG sinh giao dịch mới.

    **`delete(userId, debtId)` (DEBT-06, D-48):**
    1. Load debt (quyền)
    2. `List<DebtPayment> payments = debtPaymentRepository.findByDebtId(debtId)` — lấy TRƯỚC
       khi xoá, vì `ON DELETE CASCADE` sẽ tự xoá `debt_payments` khi xoá `debts`
    3. `debtRepository.delete(debt)` — xoá CỨNG (bảng `debts` không có `is_deleted`, D-48)
    4. Với MỖI `payment` trong danh sách đã lấy ở bước 2, gọi
       `transactionService.delete(userId, payment.getTransactionId())` để xoá mềm + hoàn tác ví
    5. Cuối cùng, `transactionService.delete(userId, debt.getOriginTransactionId())` cho giao
       dịch gốc
    Toàn bộ trong MỘT `@Transactional`.

    **`list`/`detail`/`summary`** — theo cấu trúc response api/08 mục 1/2/3 (tính
    `remaining_amount = principal - paid`, `paid_ratio`, `days_remaining`, `is_overdue` bằng
    Java từ dữ liệu đã đọc, không cần view riêng vì số bản ghi nợ của một người nhỏ).

    Tạo `debt/controller/DebtController.java` — đủ 9 route theo bảng api/08. Gắn `@Idempotent`
    cho `POST /debts` và `POST /debts/{id}/payments`.

    Tạo các DTO cần thiết trong `debt/dto/` theo response mẫu api/08 (mục 1, 2, 3, 4, 5).

    **KHÔNG sửa `GlobalExceptionHandlerTest.java` ở plan này** — tránh xung đột merge giữa 4 plan wave 2 chạy song song. Việc thêm `com.datn.financeapp.debt.controller.DebtController.class` vào `excludeFilters` được GOM LẠI ở Plan 06 Task 1 — xem 04-06-PLAN.md.

    Tạo 2 file test:
    - `DebtLifecycleIntegrationTest.java` — Test 1, 5, 6 ở `<behavior>`
    - `DebtPaymentIntegrationTest.java` — Test 2, 3, 4 ở `<behavior>` (ưu tiên D-58 mục 2, đặc
      biệt xác nhận **mở lại** `settled → outstanding`)
    Tên method tiếng Anh, ví dụ `createLendingDebt_createsExpenseTransaction_reducesBalance`,
    `cancelPayment_reopensSettledToOutstanding`, `addPayment_exceedsRemaining_returns400`.
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn test -Dtest=DebtLifecycleIntegrationTest,DebtPaymentIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=DebtLifecycleIntegrationTest,DebtPaymentIntegrationTest` thoát mã 0, đủ 6 test case ở `<behavior>` pass
    - `grep -n "setPaidAmount\|setStatus" DebtService.java` chỉ xuất hiện ĐÚNG MỘT lần, trong nhánh `writeOff` — không có chỗ nào khác
    - `DebtService.java` chứa `findByIdNative` ngay sau mọi lời gọi `debtPaymentRepository.save` hoặc `.delete`
    - `mvn test` full suite vẫn xanh
  </acceptance_criteria>
  <done>Sổ nợ hoạt động đầy đủ vòng đời tạo/trả/huỷ trả/write-off/xoá, ranh giới trigger được tôn trọng tuyệt đối, D-47 chặn vượt nợ với thông điệp rõ ràng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /debts/** | Người dùng thao tác sổ nợ của chính mình |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | DebtService ghi paid_amount/status thủ công | mitigate | Code review checklist: grep setPaidAmount/setStatus chỉ được xuất hiện ở write-off |
| T-04-09 | Information Disclosure | DebtRepository.findByIdAndUserId | mitigate | Điều kiện user_id ngay trong SQL, 404 khi không có quyền |
| T-04-10 | Repudiation | Xoá khoản nợ (DEBT-06) | accept | Xoá cứng debts theo thiết kế cố ý (D-48), giao dịch liên quan vẫn xoá mềm giữ audit trail một phần |
</threat_model>

<verification>
- `mvn test -Dtest=DebtLifecycleIntegrationTest,DebtPaymentIntegrationTest` xanh
- `mvn test` full suite xanh
- `grep -rn "setPaidAmount\|debt.setStatus" source/server/src/main/java/com/datn/financeapp/debt/` chỉ xuất hiện trong nhánh write-off
</verification>

<success_criteria>
- DEBT-01..06 hoạt động đầy đủ qua TransactionWriter, không viết lại logic ví
- DEBT-07 có đủ truy vấn `findOutstandingWithDueDate` sẵn sàng cho job ở Plan 07
- Trigger tự mở lại settled → outstanding khi huỷ trả nợ, test xác nhận
</success_criteria>

<output>
After completion, create `.planning/phases/04-nghi-p-v-ph-i-sinh-b-o-c-o/04-03-SUMMARY.md`
</output>
</content>
