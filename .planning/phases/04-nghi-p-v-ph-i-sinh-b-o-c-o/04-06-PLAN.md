---
phase: 04-nghi-p-v-ph-i-sinh-b-o-c-o
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - source/server/CLAUDE.md
  - api/00-QUY-UOC-CHUNG.md
  - api/06-BAO-CAO.md
  - source/server/src/main/java/com/datn/financeapp/FinanceAppApplication.java
  - source/server/src/main/java/com/datn/financeapp/report/repository/ReportQueryFragments.java
  - source/server/src/main/java/com/datn/financeapp/report/repository/ReportRepository.java
  - source/server/src/main/java/com/datn/financeapp/report/service/ReportQueryService.java
  - source/server/src/main/java/com/datn/financeapp/report/controller/ReportController.java
  - source/server/src/main/java/com/datn/financeapp/report/dto/ReportHomeResponse.java
  - source/server/src/main/java/com/datn/financeapp/report/dto/ReportSummaryResponse.java
  - source/server/src/main/java/com/datn/financeapp/report/dto/CategoryGroupBreakdownResponse.java
  - source/server/src/main/java/com/datn/financeapp/report/dto/CategoryBreakdownResponse.java
  - source/server/src/main/java/com/datn/financeapp/report/dto/DailyTrendResponse.java
  - source/server/src/main/java/com/datn/financeapp/report/dto/MonthlyTrendResponse.java
  - source/server/src/main/java/com/datn/financeapp/report/entity/ExportJob.java
  - source/server/src/main/java/com/datn/financeapp/report/repository/ExportJobRepository.java
  - source/server/src/main/java/com/datn/financeapp/report/service/ExportService.java
  - source/server/src/main/java/com/datn/financeapp/report/export/ExportAsyncRunner.java
  - source/server/src/main/java/com/datn/financeapp/report/export/CsvReportWriter.java
  - source/server/src/main/java/com/datn/financeapp/report/dto/ExportRequest.java
  - source/server/src/main/java/com/datn/financeapp/report/dto/ExportJobResponse.java
  - source/server/src/test/java/com/datn/financeapp/common/exception/GlobalExceptionHandlerTest.java
  - source/server/src/test/java/com/datn/financeapp/report/ReportSummaryIntegrationTest.java
  - source/server/src/test/java/com/datn/financeapp/report/ExportJobIntegrationTest.java
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-03, REPORT-04, REPORT-05]
user_setup: []

must_haves:
  truths:
    - "Mọi endpoint báo cáo loại type=transfer, cộng gộp danh mục con, và lọc counts_in_report=TRUE"
    - "Gom nhóm theo ngày/tháng dùng thẳng transactions.date, không chuyển đổi múi giờ nào"
    - "GET /reports/home trả đủ dữ liệu màn Tổng quan trong một lần gọi"
    - "POST /reports/export trả 202 kèm job_id, hỗ trợ Idempotency-Key"
    - "GET /reports/export/{job_id}/download kiểm tra quyền sở hữu và expires_at trước khi trả file"
    - "Định dạng khác csv trả 501 NOT_IMPLEMENTED"
    - "Ba chỗ tài liệu múi giờ sai đã sửa dứt điểm (D-55, D-60)"
  artifacts:
    - path: "source/server/src/main/java/com/datn/financeapp/report/repository/ReportQueryFragments.java"
      provides: "Hằng số SQL REPORT_ELIGIBLE = type <> 'transfer' AND counts_in_report = TRUE AND NOT is_deleted"
    - path: "source/server/src/main/java/com/datn/financeapp/report/export/ExportAsyncRunner.java"
      provides: "Bean riêng @Async, tránh self-invocation mất proxy"
  key_links:
    - from: "report/service/ExportService.java"
      to: "report/export/ExportAsyncRunner.java"
      via: "gọi bean khác để giữ @Async proxy"
      pattern: "exportAsyncRunner.runExport"
    - from: "report/repository/ReportRepository.java"
      to: "category/repository/CategoryRepository.java"
      via: "findCategoryTree cho cộng gộp danh mục con"
      pattern: "findCategoryTree"
---

<objective>
Xây module báo cáo hoàn chỉnh (REPORT-01..05) — 6 endpoint đọc dữ liệu tổng hợp dùng chung ba
điều kiện bắt buộc (`type != 'transfer'`, `counts_in_report = TRUE`, `NOT is_deleted`, D-61) và
cộng gộp danh mục con qua `fn_category_tree`, KHÔNG cache (D-54). Xuất báo cáo CSV bất đồng bộ
qua bảng `export_jobs` (đã tạo ở Plan 01) — `POST /reports/export` (D-59, sửa từ GET), chỉ hỗ
trợ csv (D-44, pdf/excel trả 501). Đồng thời sửa dứt điểm BA chỗ tài liệu múi giờ sai (D-55,
D-60) trong cùng lần đổi model báo cáo.

Purpose: Đọc dữ liệu từ mọi module đã xây (ngân sách, sổ nợ, mục tiêu, định kỳ đều tạo giao
dịch qua TransactionWriter), nên đặt cuối theo D-56 mục 6, sau khi tất cả các plan tạo giao
dịch đã hoàn thành.
Output: 6 endpoint báo cáo + export async, 3 file tài liệu múi giờ đã sửa, `api/06-BAO-CAO.md`
cập nhật đúng hợp đồng D-44/D-59.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\api\06-BAO-CAO.md
@D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md
@D:\PTIT\DATN\source\server\.planning\phases\04-nghi-p-v-ph-i-sinh-b-o-c-o\04-CONTEXT.md
@D:\PTIT\DATN\db\migration\V2__giao_dich.sql
@D:\PTIT\DATN\db\migration\V6__va_loi_bao_mat.sql
@D:\PTIT\DATN\db\migration\V8__dieu_chinh_so_du.sql
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\repository\TransactionRepository.java
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\category\repository\CategoryRepository.java
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\FinanceAppApplication.java

<interfaces>
Ba điều kiện bắt buộc hard-code (D-61, KHÔNG nhận tham số cho phép tắt — khác
`TransactionRepository` ở `transaction/` nơi `counts_in_report` là tham số lọc người dùng tự
chọn):
```java
// report/repository/ReportQueryFragments.java
public final class ReportQueryFragments {
    private ReportQueryFragments() {}
    public static final String REPORT_ELIGIBLE =
            "t.type <> 'transfer' AND t.counts_in_report = TRUE AND NOT t.is_deleted";
}
```

Cộng gộp danh mục con dùng lại `CategoryRepository.findCategoryTree(categoryId)` (đã có từ
Phase 3) — không viết lại điều kiện lọc cây.

`ApplicationEventPublisher`/`@EnableAsync` — `FinanceAppApplication.java` hiện CHỈ có
`@EnableScheduling`, PHẢI thêm `@EnableAsync` trong plan này (cần cho `ExportAsyncRunner`).

`export_jobs` bảng (V10, Plan 01): `id, user_id, format, status, file_path, error_message,
expires_at, created_at`. `ck_export_format CHECK (format IN ('csv','pdf','excel'))`,
`ck_export_status CHECK (status IN ('processing','completed','failed'))`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sửa 3 file tài liệu múi giờ (D-55/D-60) + api/06 mục 7 (D-44/D-59) + @EnableAsync + gom exclude filters</name>
  <read_first>
    D:\PTIT\DATN\source\server\CLAUDE.md (dòng 75 — câu sai)
    D:\PTIT\DATN\api\00-QUY-UOC-CHUNG.md (mục 13, dòng 292-298)
    D:\PTIT\DATN\api\06-BAO-CAO.md (mục "Ba nguyên tắc chung", nguyên tắc 3; mục 7 "Xuất báo cáo")
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\FinanceAppApplication.java
    D:\PTIT\DATN\source\server\src\test\java\com\datn\financeapp\common\exception\GlobalExceptionHandlerTest.java (danh sách excludeFilters hiện có)
  </read_first>
  <action>
    **Sửa `source/server/CLAUDE.md` dòng 75** (mục "Đối chiếu `db/migration/V*.sql` trước khi
    tin `api/*.md`" hoặc dòng liên quan tới `AT TIME ZONE`): tìm chuỗi
    `AT TIME ZONE 'Asia/Ho_Chi_Minh'` hoặc câu diễn giải tương tự về múi giờ báo cáo, thay bằng:
    "Gom nhóm báo cáo theo ngày/tháng dùng thẳng cột `transactions.date` (kiểu `DATE` thuần,
    không có thành phần giờ) — KHÔNG chuyển đổi múi giờ dưới bất kỳ hình thức nào. Đây là thiết
    kế cố ý để tránh hẳn lớp bug múi giờ."

    **Sửa `api/00-QUY-UOC-CHUNG.md` mục 13** (dòng ~298, câu "Nếu tính theo UTC thì giao dịch
    lúc 6 giờ sáng ngày 7 sẽ bị xếp vào ngày 6"): thay đoạn "**Lưu ý cho các điểm cuối báo cáo:**
    khi gom nhóm theo ngày hoặc tháng, máy chủ tính theo **giờ Việt Nam**, không theo UTC..."
    thành: "**Lưu ý cho các điểm cuối báo cáo:** cột `transactions.date` là kiểu `DATE` thuần,
    KHÔNG có thành phần giờ và KHÔNG có múi giờ để chuyển đổi. Gom nhóm theo ngày/tháng dùng
    thẳng cột này, không tính toán múi giờ nào cả — đây là thiết kế cố ý."

    **Sửa `api/06-BAO-CAO.md` mục "Ba nguyên tắc chung của nhóm này" nguyên tắc số 3** (dòng
    43-45, hiện ghi "Luôn tính theo giờ Việt Nam... Gom nhóm theo ngày, tháng, kỳ đều dùng múi
    giờ UTC+7"): thay bằng nguyên tắc thứ ba MỚI theo D-61 (phát hiện của RESEARCH.md, chưa có
    trong CONTEXT gốc):
    ```markdown
    ### 3. Luôn loại bỏ giao dịch không tính vào báo cáo

    ```
    Mọi truy vấn báo cáo đều phải có: counts_in_report = true
    ```

    Giao dịch điều chỉnh số dư (`source = 'adjustment'`, sinh từ kiểm kê ví) có thể được người
    dùng chọn KHÔNG tính vào báo cáo thu-chi. Bỏ sót điều kiện này sẽ khiến các khoản điều
    chỉnh âm thầm lẫn vào thu/chi thật.

    ### 4. Gom nhóm theo ngày dùng thẳng cột `date`

    Cột `transactions.date` là kiểu `DATE` thuần, không có thành phần giờ và không có múi giờ
    để chuyển đổi. Gom nhóm theo ngày/tháng/kỳ dùng thẳng giá trị này — KHÔNG tính toán múi giờ
    nào.
    ```
    (đổi từ "Ba nguyên tắc" thành 4 nguyên tắc — sửa luôn câu giới thiệu "Ba điều dưới đây" →
    "Bốn điều dưới đây").

    **Sửa `api/06-BAO-CAO.md` mục 7 "Xuất báo cáo"** (D-44 + D-59) — thay toàn bộ nội dung mục
    7 hiện tại bằng:
    ```markdown
    ## 7. Xuất báo cáo

    ```
    POST /reports/export
    ```

    | Tham số | Kiểu | Bắt buộc | Mô tả |
    |---|---|---|---|
    | `format` | Chuỗi | ● | `csv` — **phiên bản này chỉ hỗ trợ csv**, `pdf`/`excel` trả `501 NOT_IMPLEMENTED` |
    | `period` | Chuỗi | ○ | |
    | `from_date` / `to_date` | Ngày | ○ | |
    | `include` | Chuỗi | ○ | Danh sách ngăn cách dấu phẩy: `transaction,bieu_do,budget` |

    Hỗ trợ header `Idempotency-Key` (api/00 mục 11) — gửi lại cùng khoá trong 24h trả về đúng
    job đã tạo, không tạo job trùng.

    ### Phản hồi — 202

    Vì tệp có thể lớn và mất thời gian, xử lý **bất đồng bộ**:

    ```json
    {
      "success": true,
      "data": { "job_id": "x1000000-...", "status": "processing" }
    }
    ```

    Gọi `GET /reports/export/{job_id}` để kiểm tra:

    ```json
    {
      "success": true,
      "data": {
        "status": "completed",
        "download_url": "/reports/export/x1000000-.../download",
        "expires_at": "2026-08-09T09:41:00Z"
      }
    }
    ```

    Gọi `GET /reports/export/{job_id}/download` (kèm `Authorization: Bearer` như mọi điểm cuối
    khác) để tải tệp — máy chủ kiểm tra quyền sở hữu job và `expires_at` trước khi trả. Hết hạn
    trả `410 GONE`.

    Đường dẫn tải có hạn **24 giờ** kể từ lúc hoàn tất.

    | `status` | Nghĩa |
    |---|---|
    | `processing` | Đang tạo tệp |
    | `completed` | Sẵn sàng tải |
    | `failed` | Lỗi, có kèm `error_message` |

    | Mã | HTTP | Nghĩa |
    |---|---|---|
    | `FORMAT_NOT_SUPPORTED` | 501 | Định dạng `pdf`/`excel` chưa hỗ trợ ở phiên bản này |
    | `EXPORT_LINK_EXPIRED` | 410 | Đường dẫn tải đã hết hạn |
    ```

    Sửa `FinanceAppApplication.java` — thêm `@EnableAsync` cạnh `@EnableScheduling`, import
    `org.springframework.scheduling.annotation.EnableAsync`.

    **Gom sửa `GlobalExceptionHandlerTest.java` (dồn từ Plan 02/03/04/05, D-56 tránh xung đột
    merge song song wave 2)** — thêm CẢ BỐN class sau vào mảng `classes = {...}` của
    `excludeFilters` (cùng lúc với `ReportController.class` ở Task 2):
    `com.datn.financeapp.budget.controller.BudgetController.class`,
    `com.datn.financeapp.debt.controller.DebtController.class`,
    `com.datn.financeapp.goal.controller.GoalController.class`,
    `com.datn.financeapp.recurring.controller.RecurringController.class`. Đọc lại danh sách
    hiện có trong file trước khi thêm — không trùng lặp entry đã có từ Phase 1-3.
  </action>
  <verify>
    <automated>grep -c "AT TIME ZONE" D:/PTIT/DATN/source/server/CLAUDE.md D:/PTIT/DATN/api/00-QUY-UOC-CHUNG.md D:/PTIT/DATN/api/06-BAO-CAO.md | awk -F: "{s+=$2} END{exit s!=0}"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AT TIME ZONE" D:/PTIT/DATN/source/server/CLAUDE.md` trả về 0
    - `grep -c "AT TIME ZONE\|giờ Việt Nam" D:/PTIT/DATN/api/00-QUY-UOC-CHUNG.md` trả về 0
    - `grep -c "AT TIME ZONE\|giờ Việt Nam\|múi giờ UTC+7" D:/PTIT/DATN/api/06-BAO-CAO.md` trả về 0
    - `grep -c "POST /reports/export" D:/PTIT/DATN/api/06-BAO-CAO.md` trả về ít nhất 1
    - `grep -c "counts_in_report" D:/PTIT/DATN/api/06-BAO-CAO.md` trả về ít nhất 1
    - `grep -c "@EnableAsync" D:/PTIT/DATN/source/server/src/main/java/com/datn/financeapp/FinanceAppApplication.java` trả về 1
    - `grep -c "BudgetController.class|DebtController.class|GoalController.class|RecurringController.class" D:/PTIT/DATN/source/server/src/test/java/com/datn/financeapp/common/exception/GlobalExceptionHandlerTest.java` trả về 4
  </acceptance_criteria>
  <done>Ba chỗ tài liệu múi giờ sửa dứt điểm, api/06 khớp hợp đồng POST/csv-only đã chốt, @EnableAsync sẵn sàng cho Task 3, GlobalExceptionHandlerTest gom đủ 4 controller mới từ wave 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReportQueryFragments + ReportRepository + ReportQueryService + ReportController</name>
  <read_first>
    D:\PTIT\DATN\api\06-BAO-CAO.md (đã sửa ở Task 1 — đọc lại bản mới)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\repository\TransactionRepository.java (mẫu native query + SummaryProjection)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\category\repository\CategoryRepository.java (findCategoryTree)
    D:\PTIT\DATN\db\migration\V8__dieu_chinh_so_du.sql (counts_in_report)
  </read_first>
  <behavior>
    - Test 1 (REPORT-01, ưu tiên D-58 mục 5): tạo 1 giao dịch `expense` 100.000, 1 giao dịch
      `transfer` 2.000.000, 1 giao dịch `expense` `counts_in_report=false` 50.000 (mô phỏng
      adjustment) → `GET /reports/summary` trả `total_expense=100.000` (loại cả transfer lẫn
      adjustment không tính báo cáo)
    - Test 2 (REPORT-01, cộng gộp danh mục con): giao dịch gán vào danh mục CON của "Ăn uống"
      → `GET /reports/by-category?level=parent` cộng đúng vào danh mục cha "Ăn uống"
    - Test 3 (REPORT-02): `GET /reports/home` trả đủ các khối `balance`, `top_wallets`,
      `period_summary`, `daily_trend`, `top_spending`, `recent_transactions` trong MỘT lần gọi
    - Test 4 (REPORT-04): `GET /reports/daily-trend` — `current_line` CHỈ có điểm dữ liệu tới
      hôm nay, không kéo dài hết tháng
  </behavior>
  <action>
    Tạo `report/repository/ReportQueryFragments.java` — hằng số `REPORT_ELIGIBLE` đúng nội
    dung ở `<interfaces>`.

    Tạo `report/repository/ReportRepository.java` — native query cho từng endpoint, MỌI query
    nối chuỗi `ReportQueryFragments.REPORT_ELIGIBLE` và điều kiện `t.category_id IN (SELECT *
    FROM fn_category_tree(:categoryId))` khi cần cộng gộp:
    - `summary(userId, fromDate, toDate, walletId)` → tổng `income`/`expense`/`count` trong kỳ,
      projection
    - `byCategoryGroup(userId, fromDate, toDate, type)` → nhóm theo `category_group_id`, JOIN
      `categories`/`category_groups`
    - `byCategory(userId, fromDate, toDate, level, limit)` → tổng theo từng danh mục cha (cộng
      gộp con) hoặc tách riêng con tuỳ `level`
    - `dailyTrend(userId, month)` → tổng chi từng ngày trong tháng, CHỈ tới hôm nay nếu tháng
      hiện tại (lọc `date <= LEAST(:endOfMonth, CURRENT_DATE)` ở tầng SQL hoặc lọc ở Java sau
      khi query đủ tháng — chọn lọc SQL để tránh trả dư dữ liệu)
    - `monthlyTrend(userId, monthsCount)` → tổng theo từng tháng, tối đa 24 tháng
    - `topWallets(userId, limit)` → dùng lại `WalletRepository.findAllForUser` sắp theo
      `sort_order`, lấy 3 đầu (KHÔNG viết query riêng, tái dùng repository đã có)

    Tạo `report/service/ReportQueryService.java` — `@Service @RequiredArgsConstructor
    @Transactional(readOnly = true)` cấp class (mọi method đều chỉ đọc, D-54 không cache):
    - `home(userId, period)` — gộp gọi các method khác trong CÙNG service (KHÔNG phải cross-bean
      self-invocation vì đây là gọi private/package method thuần Java trả DTO, không mang
      annotation `@Transactional` RIÊNG trên từng method con — chỉ class-level `readOnly=true`
      là đủ, không có nguy cơ self-invocation vì không có method nào cần transaction MỚI)
    - `summary`, `byCategoryGroup`, `byCategory`, `dailyTrend`, `monthlyTrend` — map trực tiếp
      từ `ReportRepository`, tính thêm các trường phái sinh (`opening_balance = closing_balance
      - total_income + total_expense` theo công thức api/06 mục 2; `avg_daily_expense`;
      `vs_previous_period` so kỳ trước)

    Tạo `report/controller/ReportController.java` — 6 route GET theo bảng api/06 (trừ export,
    làm ở Task 3): `/reports/home`, `/reports/summary`, `/reports/by-category-group`,
    `/reports/by-category`, `/reports/daily-trend`, `/reports/monthly-trend`.

    Tạo DTO cần thiết trong `report/dto/` theo response mẫu api/06 mục 1-6.

    Sửa `GlobalExceptionHandlerTest.java` — thêm
    `com.datn.financeapp.report.controller.ReportController.class` vào `excludeFilters`.

    Tạo `src/test/java/com/datn/financeapp/report/ReportSummaryIntegrationTest.java` — 4 test
    case ở `<behavior>`. Tên method tiếng Anh, ví dụ
    `summary_excludesTransferAndNonReportingAdjustment`,
    `byCategory_parentLevel_aggregatesChildTransactions`,
    `home_returnsAllRequiredBlocks_inOneCall`, `dailyTrend_currentLine_stopsAtToday`.
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn test -Dtest=ReportSummaryIntegrationTest,GlobalExceptionHandlerTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=ReportSummaryIntegrationTest` thoát mã 0, đủ 4 test case pass
    - `grep -rn "ReportQueryFragments.REPORT_ELIGIBLE" ReportRepository.java` xuất hiện ở MỌI method truy vấn tổng hợp thu-chi
    - `ReportRepository.java` không chứa cache annotation nào (`@Cacheable` v.v. — xác nhận D-54)
  </acceptance_criteria>
  <done>6 endpoint báo cáo hoạt động đúng ba nguyên tắc bắt buộc, tính tại chỗ không cache, home gộp đủ dữ liệu trong một lần gọi.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ExportJob entity/repository + ExportService + ExportAsyncRunner + CsvReportWriter</name>
  <read_first>
    D:\PTIT\DATN\api\06-BAO-CAO.md (mục 7 đã sửa ở Task 1)
    D:\PTIT\DATN\db\migration\V10__export_jobs.sql (Plan 01)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionWriter.java (mẫu bean riêng tránh self-invocation)
    D:\PTIT\DATN\source\server\.planning\phases\04-nghi-p-v-ph-i-sinh-b-o-c-o\04-RESEARCH.md (Pattern 4, dòng 375-443)
  </read_first>
  <behavior>
    - Test 1 (REPORT-05): `POST /reports/export {format: "csv"}` → 202, body chứa `job_id` và
      `status=processing`
    - Test 2 (REPORT-05): poll `GET /reports/export/{job_id}` tới khi `status=completed`, có
      `download_url` và `expires_at`
    - Test 3 (D-44): `POST /reports/export {format: "pdf"}` → 501 `FORMAT_NOT_SUPPORTED`,
      KHÔNG tạo bản ghi `export_jobs` nào
    - Test 4 (D-43, IDOR): user A tạo job, user B gọi `GET /reports/export/{jobCuaA}/download`
      → 404 `NOT_FOUND`
    - Test 5 (D-43, hết hạn): job có `expires_at` trong quá khứ (test tự set qua JdbcTemplate)
      → `GET .../download` trả 410 `EXPORT_LINK_EXPIRED`
  </behavior>
  <action>
    Tạo `report/entity/ExportJob.java` — field: id, userId, format, status, filePath,
    errorMessage, expiresAt, createdAt (theo đúng cột `export_jobs` V10).

    Tạo `report/repository/ExportJobRepository.java`:
    ```java
    @Query(value = "SELECT * FROM export_jobs WHERE id = :id AND user_id = :currentUser", nativeQuery = true)
    Optional<ExportJob> findByIdAndUserId(@Param("id") UUID id, @Param("currentUser") UUID currentUser);

    @Modifying
    @Query("UPDATE ExportJob e SET e.status = 'completed', e.filePath = :filePath, e.expiresAt = :expiresAt WHERE e.id = :id")
    void markCompleted(@Param("id") UUID id, @Param("filePath") String filePath, @Param("expiresAt") java.time.Instant expiresAt);

    @Modifying
    @Query("UPDATE ExportJob e SET e.status = 'failed', e.errorMessage = :error WHERE e.id = :id")
    void markFailed(@Param("id") UUID id, @Param("error") String error);

    // Dùng cho job dọn quá hạn (D-45, Plan 07)
    @Query(value = "SELECT * FROM export_jobs WHERE status = 'completed' AND expires_at < :now", nativeQuery = true)
    List<ExportJob> findExpired(@Param("now") java.time.Instant now);
    ```

    Tạo `report/export/CsvReportWriter.java` — utility thuần JDK, `BufferedWriter`, ghi UTF-8
    kèm BOM (`\uFEFF` ở đầu file để Excel mở đúng tiếng Việt có dấu). Escape RFC 4180 thủ công:
    field chứa dấu phẩy/dấu ngoặc kép/xuống dòng thì bọc trong `"..."`, escape `"` thành `""`.
    Signature: `public static Path write(Path targetDir, UUID jobId, List<TransactionExportRow>
    rows) throws IOException`. Cột: `date, type, amount, category_name, wallet_name, note`. Thư
    mục đích lấy từ property cấu hình `app.export.storage-dir` (mặc định
    `${java.io.tmpdir}/finance-exports`), đọc qua `@Value` trong `ExportAsyncRunner`.

    Tạo `report/service/ExportService.java` — `@Service @RequiredArgsConstructor`:
    ```java
    @Transactional
    public ExportJobResponse createJob(UUID userId, ExportRequest req) {
        if (!"csv".equals(req.format())) {
            throw new BusinessException("FORMAT_NOT_SUPPORTED", 501,
                    "Định dạng " + req.format() + " chưa hỗ trợ, chỉ hỗ trợ csv ở phiên bản này.");
        }
        UUID jobId = UUID.randomUUID();
        exportJobRepository.save(ExportJob.builder().id(jobId).userId(userId).format("csv")
                .status("processing").createdAt(Instant.now()).build());
        exportAsyncRunner.runExport(jobId, userId, req);
        return new ExportJobResponse(jobId, "processing");
    }

    public ExportStatusResponse getStatus(UUID userId, UUID jobId) {
        ExportJob job = exportJobRepository.findByIdAndUserId(jobId, userId)
                .orElseThrow(() -> new BusinessException("NOT_FOUND", 404, "Không tìm thấy tệp xuất."));
        String downloadUrl = "completed".equals(job.getStatus())
                ? "/reports/export/" + jobId + "/download" : null;
        return new ExportStatusResponse(job.getStatus(), downloadUrl, job.getExpiresAt(), job.getErrorMessage());
    }

    public Resource download(UUID userId, UUID jobId) {
        ExportJob job = exportJobRepository.findByIdAndUserId(jobId, userId)
                .orElseThrow(() -> new BusinessException("NOT_FOUND", 404, "Không tìm thấy tệp xuất."));
        if (job.getExpiresAt() == null || job.getExpiresAt().isBefore(Instant.now())) {
            throw new BusinessException("EXPORT_LINK_EXPIRED", 410, "Đường dẫn tải đã hết hạn.");
        }
        return new FileSystemResource(job.getFilePath()); // đường dẫn từ CSDL, KHÔNG nối input người dùng
    }
    ```

    Tạo `report/export/ExportAsyncRunner.java` — bean `@Component` RIÊNG (tránh self-invocation
    mất `@Async` proxy — cùng lý do `TransactionWriter`), method `@Async("exportTaskExecutor")
    public void runExport(UUID jobId, UUID userId, ExportRequest req)`: gọi
    `reportQueryService` để lấy danh sách giao dịch trong kỳ (áp dụng đúng `REPORT_ELIGIBLE` —
    export dùng CÙNG bộ lọc báo cáo, không phải toàn bộ giao dịch thô), gọi
    `CsvReportWriter.write(...)`, `exportJobRepository.markCompleted(jobId, path.toString(),
    Instant.now().plus(24, ChronoUnit.HOURS))`. Bọc try/catch toàn method, lỗi gọi
    `markFailed`.

    Thêm bean `TaskExecutor` tên `exportTaskExecutor` (core=2, max=4, queue=50) trong
    `@Configuration` mới `report/config/AsyncConfig.java` hoặc thêm vào
    `FinanceAppApplication.java` nếu ngắn gọn hơn — ưu tiên tạo `@Configuration` riêng để không
    làm phình class khởi động.

    Tạo `report/controller/ReportController.java` (BỔ SUNG vào file đã tạo ở Task 2, không tạo
    file mới) — thêm 3 route: `POST /reports/export` (gắn `@Idempotent`), `GET
    /reports/export/{jobId}`, `GET /reports/export/{jobId}/download` (trả
    `ResponseEntity<Resource>` với header `Content-Disposition: attachment`).

    Tạo `src/test/java/com/datn/financeapp/report/ExportJobIntegrationTest.java` — 5 test case
    ở `<behavior>`. **Lưu ý poll async trong test:** dùng
    `org.awaitility.Awaitility.await().atMost(Duration.ofSeconds(5)).until(...)` nếu thư viện
    `awaitility` đã có trong `pom.xml` (kiểm tra trước — nếu chưa có, dùng vòng lặp
    `Thread.sleep(200)` tối đa 20 lần thay thế, KHÔNG thêm dependency mới nếu không thật cần
    thiết theo Claude's Discretion "không thêm dependency mới"). Tên method tiếng Anh, ví dụ
    `createExportJob_returns202WithJobId`, `pollExportJob_eventuallyCompletes`,
    `createExportJob_pdfFormat_returns501WithoutCreatingJob`,
    `downloadExport_otherUsersJob_returns404`, `downloadExport_expiredJob_returns410`.
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn test -Dtest=ExportJobIntegrationTest,GlobalExceptionHandlerTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=ExportJobIntegrationTest` thoát mã 0, đủ 5 test case pass
    - `ExportAsyncRunner.java` là `@Component` riêng biệt với `ExportService.java`, có `@Async`
    - `ExportService.createJob` KHÔNG tạo bản ghi `export_jobs` khi format là pdf/excel (grep xác nhận check format nằm TRƯỚC dòng `exportJobRepository.save`)
    - `mvn test` full suite vẫn xanh
  </acceptance_criteria>
  <done>Xuất báo cáo CSV bất đồng bộ hoạt động đầy đủ vòng đời 202→processing→completed, IDOR và hết hạn đều chặn đúng, pdf/excel trả 501 sạch sẽ không tạo rác dữ liệu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET /reports/** | Người dùng đọc báo cáo tổng hợp của chính mình |
| Client → GET /reports/export/{id}/download | Tải file — biên giới nhạy cảm nhất (toàn bộ dữ liệu tài chính trong 1 file) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Information Disclosure | ExportJobRepository.findByIdAndUserId | mitigate | findByIdAndUserId (không findById trần), đúng mẫu TransactionRepository |
| T-04-17 | Tampering | Path traversal khi ghi/đọc file export | mitigate | Tên file sinh từ UUID jobId, không dùng input người dùng, lưu thư mục cấu hình cố định |
| T-04-18 | Information Disclosure | download_url dạng CDN placeholder trong tài liệu cũ | mitigate | D-43 đã đổi thành đường dẫn tương đối backend, xác thực JWT sẵn có, không sinh token tải riêng |
| T-04-19 | Denial of Service | Nhiều request export đồng thời tạo quá nhiều thread | mitigate | TaskExecutor giới hạn pool core=2 max=4 queue=50, không dùng executor mặc định không giới hạn |
</threat_model>

<verification>
- `mvn test -Dtest=ReportSummaryIntegrationTest,ExportJobIntegrationTest` xanh
- `mvn test` full suite xanh
- `grep -rn "AT TIME ZONE" D:/PTIT/DATN/api/*.md D:/PTIT/DATN/source/server/CLAUDE.md` trả về rỗng
</verification>

<success_criteria>
- REPORT-01..05 hoạt động đúng ba nguyên tắc bắt buộc, không cache
- Export CSV async hoạt động đầy đủ, pdf/excel trả 501 sạch
- Tài liệu múi giờ nhất quán ở cả 3 vị trí, api/06 khớp hợp đồng D-44/D-59
</success_criteria>

<output>
After completion, create `.planning/phases/04-nghi-p-v-ph-i-sinh-b-o-c-o/04-06-SUMMARY.md`
</output>
</content>
