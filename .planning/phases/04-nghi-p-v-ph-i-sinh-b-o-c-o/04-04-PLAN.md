---
phase: 04-nghi-p-v-ph-i-sinh-b-o-c-o
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - source/server/src/main/java/com/datn/financeapp/goal/entity/SavingsGoal.java
  - source/server/src/main/java/com/datn/financeapp/goal/entity/GoalContribution.java
  - source/server/src/main/java/com/datn/financeapp/goal/repository/SavingsGoalRepository.java
  - source/server/src/main/java/com/datn/financeapp/goal/repository/GoalContributionRepository.java
  - source/server/src/main/java/com/datn/financeapp/goal/service/GoalService.java
  - source/server/src/main/java/com/datn/financeapp/goal/controller/GoalController.java
  - source/server/src/main/java/com/datn/financeapp/goal/dto/CreateGoalRequest.java
  - source/server/src/main/java/com/datn/financeapp/goal/dto/UpdateGoalRequest.java
  - source/server/src/main/java/com/datn/financeapp/goal/dto/GoalListItemResponse.java
  - source/server/src/main/java/com/datn/financeapp/goal/dto/CreateContributionRequest.java
  - source/server/src/main/java/com/datn/financeapp/goal/dto/CreateContributionResponse.java
  - source/server/src/test/java/com/datn/financeapp/goal/GoalContributionIntegrationTest.java
autonomous: true
requirements: [GOAL-01, GOAL-02, GOAL-03, GOAL-04]
user_setup: []

must_haves:
  truths:
    - "User tạo mục tiêu với initial_amount không sinh giao dịch nào"
    - "User nạp tiền với create_transaction=true sinh giao dịch transfer thật, yêu cầu goal có wallet_id"
    - "User nạp tiền với create_transaction=false chỉ ghi nhận tiến độ, không đụng ví nào"
    - "User rút lại một lần nạp mở lại status=in_progress từ completed khi không còn đủ"
    - "Backend không bao giờ tự ghi savings_goals.saved_amount hoặc status"
  artifacts:
    - path: "source/server/src/main/java/com/datn/financeapp/goal/service/GoalService.java"
      provides: "Hai chế độ nạp theo create_transaction, gọi TransactionWriter khi cần"
  key_links:
    - from: "goal/service/GoalService.java"
      to: "transaction/service/TransactionWriter.java"
      via: "transactionWriter.write khi create_transaction=true"
      pattern: "transactionWriter.write"
    - from: "goal/service/GoalService.java"
      to: "db/migration/V4__so_no_muc_tieu.sql"
      via: "chỉ INSERT/DELETE goal_contributions, đọc lại sau trigger"
      pattern: "goalContributionRepository.(save|delete)"
---

<objective>
Xây module mục tiêu tiết kiệm hoàn chỉnh (GOAL-01..04) — cùng kiểu ranh giới trigger như sổ nợ
(Plan 03) nhưng thêm điểm khác biệt: hai chế độ nạp tiền (`create_transaction` true/false) theo
D-49, và `goal_contributions.transaction_id` NULLABLE (khác `debt_payments` bắt buộc).

Purpose: Cùng pattern kỹ thuật với Plan 03 (đọc lại sau trigger, không viết lại logic ví) nên
đặt song song wave 2, độc lập file với Plan 03.
Output: CRUD mục tiêu, nạp/rút tiền hai chế độ, test xác nhận trigger tự mở lại
`completed → in_progress`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\api\09-DINH-KY-MUC-TIEU.md
@D:\PTIT\DATN\source\server\.planning\phases\04-nghi-p-v-ph-i-sinh-b-o-c-o\04-CONTEXT.md
@D:\PTIT\DATN\db\migration\V4__so_no_muc_tieu.sql
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionWriter.java
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletTransferService.java

<interfaces>
`fn_goal_contributions_sync` (V4 dòng 163-188): `AFTER INSERT OR UPDATE OR DELETE ON
goal_contributions FOR EACH ROW`, tự `SUM()` lại, tự chuyển `status` (giữ nguyên `cancelled`,
`completed` khi `saved_amount >= target_amount`, else `in_progress`). Không có RAISE EXCEPTION
nào (khác debt) — mục tiêu KHÔNG chặn nạp vượt target, chỉ tự đánh dấu `completed`.

`goal_contributions` (V4 dòng 143-159): `transaction_id` **nullable + ON DELETE SET NULL** —
khác hẳn `debt_payments.transaction_id` (NOT NULL + RESTRICT). Khi `create_transaction=false`,
`transactionId = null` khi insert.

`savings_goals.wallet_id` — nullable, `ON DELETE SET NULL` (V4 dòng 129). GOAL-03 yêu cầu
`create_transaction=true` phải có `wallet_id` trên goal, KHÔNG suy đoán từ có/không có wallet_id
mà đọc tường minh field request `create_transaction` (D-49).

Giao dịch nạp tiền (`create_transaction=true`) LUÔN là `type=transfer`: `wallet_id =
req.sourceWalletId()`, `destination_wallet_id = goal.getWalletId()`, `category_id = null` (theo
`ck_txn_shape` — transfer không có category). `source = "manual"`, `countsInReport = true`
(transfer tự động bị loại khỏi báo cáo thu-chi qua `type != 'transfer'`, không cần đặt false).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SavingsGoal/GoalContribution entity + repository</name>
  <read_first>
    D:\PTIT\DATN\db\migration\V4__so_no_muc_tieu.sql (dòng 113-188 — savings_goals, goal_contributions, trigger)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\entity\Wallet.java
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\repository\WalletRepository.java
  </read_first>
  <action>
    Tạo `goal/entity/SavingsGoal.java` — field: id, userId, walletId, name, targetAmount,
    savedAmount, targetDate, iconId, status, createdAt (theo đúng cột `savings_goals` V4).

    Tạo `goal/entity/GoalContribution.java` — field: id, goalId, transactionId (nullable),
    amount, contributedDate, createdAt (theo đúng cột `goal_contributions` V4).

    Tạo `goal/repository/SavingsGoalRepository.java`:
    ```java
    @Query(value = "SELECT * FROM savings_goals WHERE id = :id AND user_id = :currentUser", nativeQuery = true)
    Optional<SavingsGoal> findByIdAndUserId(@Param("id") UUID id, @Param("currentUser") UUID currentUser);

    @Query(value = "SELECT * FROM savings_goals WHERE user_id = :currentUser ORDER BY created_at DESC", nativeQuery = true)
    List<SavingsGoal> findAllForUser(@Param("currentUser") UUID currentUser);

    // Đọc lại SAU trigger UPDATE, bypass Hibernate identity map (đúng mẫu WalletRepository.findCurrentBalanceNative)
    @Query(value = "SELECT * FROM savings_goals WHERE id = :id", nativeQuery = true)
    Optional<SavingsGoal> findByIdNative(@Param("id") UUID id);
    ```

    Tạo `goal/repository/GoalContributionRepository.java`:
    ```java
    List<GoalContribution> findByGoalIdOrderByContributedDateDesc(UUID goalId);
    Optional<GoalContribution> findByIdAndGoalId(UUID id, UUID goalId);
    List<GoalContribution> findByGoalId(UUID goalId); // dùng khi xoá goal, lấy transactionId TRƯỚC cascade
    ```
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn compile -q</automated>
  </verify>
  <acceptance_criteria>
    - `SavingsGoal.java` có đủ 9 field khớp cột bảng `savings_goals`
    - `GoalContribution.java` có `transactionId` khai báo KHÔNG có `nullable = false` (khác DebtPayment)
    - `SavingsGoalRepository.java` có method `findByIdNative` tách biệt `findByIdAndUserId`
  </acceptance_criteria>
  <done>Entity + repository sẵn sàng cho GoalService, đúng ràng buộc nullable của schema V4 thật.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GoalService — tạo/nạp/rút hai chế độ + test trigger mở lại</name>
  <read_first>
    D:\PTIT\DATN\api\09-DINH-KY-MUC-TIEU.md (Phần B, toàn bộ)
    D:\PTIT\DATN\db\migration\V4__so_no_muc_tieu.sql (dòng 163-188 fn_goal_contributions_sync)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletTransferService.java (mẫu nhánh có/không sinh giao dịch, method adjustBalance dòng 104-151)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionService.java (method delete() — mẫu 3 bước hoàn tác)
  </read_first>
  <behavior>
    - Test 1 (GOAL-01): tạo goal với `initial_amount=2.000.000` → `GET /goals` trả
      `saved_amount=0` (initial_amount KHÔNG sinh giao dịch nào, chỉ ghi nhận điểm xuất phát —
      xác nhận KHÔNG có bản ghi `goal_contributions` nào được tạo tự động lúc tạo goal, và
      `saved_amount` từ trigger vẫn là 0 vì chưa có contribution nào)
    - Test 2 (GOAL-03, `create_transaction=true`): goal có `wallet_id`, nạp 3.000.000 từ ví
      nguồn → giao dịch `transfer` thật được tạo, số dư 2 ví thay đổi đúng, `goal_contributions
      .transaction_id` khác NULL
    - Test 3 (GOAL-03, `create_transaction=false`): nạp 3.000.000 không kèm ví nguồn → KHÔNG
      giao dịch nào được tạo, `goal_contributions.transaction_id IS NULL`, số dư mọi ví không
      đổi, nhưng `saved_amount` (qua trigger) vẫn tăng đúng
    - Test 4 (GOAL-03): `create_transaction=true` nhưng goal KHÔNG có `wallet_id` → 400
      `GOAL_WALLET_REQUIRED`
    - Test 5 (GOAL-04, ưu tiên D-58 mục 2): nạp đủ target → `status=completed`; rút lại MỘT lần
      nạp khiến `saved_amount < target_amount` → `status` tự MỞ LẠI `in_progress`
  </behavior>
  <action>
    Tạo `goal/service/GoalService.java` — `@Service @RequiredArgsConstructor`:

    **`create(userId, req)` (GOAL-01):** validate `target_amount > 0`, `target_date` sau hôm
    nay nếu có. Tạo `SavingsGoal` với `savedAmount = 0, status = "in_progress"`. **KHÔNG** tạo
    bất kỳ `GoalContribution` nào cho `initial_amount` — theo đúng api/09 "`initial_amount`
    không sinh giao dịch — đó là số tiền người dùng đã tích được từ trước, chỉ ghi nhận điểm
    xuất phát". Lưu ý: điều này có nghĩa `initial_amount` gửi lên KHÔNG được cộng vào
    `saved_amount` qua trigger (trigger chỉ tính từ `goal_contributions`) — nếu response cần
    hiển thị `initial_amount` đã cộng, PHẢI làm ở tầng tính READ (ví dụ field riêng
    `initial_amount` trong response, cộng với `saved_amount` từ trigger để ra tổng hiển thị cho
    người dùng) — nhưng vì `savings_goals` KHÔNG có cột `initial_amount` trong schema V4 thật,
    và api/09 mục B2 liệt kê `initial_amount` là TRƯỜNG REQUEST không phải trường lưu trữ, xử lý
    đơn giản nhất là: nếu `req.initialAmount() > 0`, tạo NGAY một `GoalContribution` với
    `transactionId = null, amount = req.initialAmount(), contributedDate = LocalDate.now()` (chế
    độ `create_transaction=false` mặc định cho initial_amount, vì initial_amount theo định nghĩa
    "tiền đã có sẵn từ trước", không sinh giao dịch mới, nhưng VẪN cần một bản ghi
    `goal_contributions` để trigger cộng vào `saved_amount`, nếu không `saved_amount` sẽ mãi là
    0 và goal không bao giờ phản ánh đúng tiến độ ban đầu người dùng nhập). Đọc lại
    `savingsGoalRepository.findByIdNative` sau khi INSERT `goal_contributions` để lấy
    `saved_amount` đúng cho response.

    **`addContribution(userId, goalId, req)` (GOAL-03):** load goal (quyền), chặn nếu `status
    != "in_progress"` (409 `GOAL_ALREADY_COMPLETED` nếu `completed`, 409 `GOAL_CANCELLED` nếu
    `cancelled`). Đọc TƯỜNG MINH `req.createTransaction()` (mặc định `true` nếu null — theo
    api/09 "Mặc định `true`"):
    ```java
    UUID transactionId = null;
    if (Boolean.TRUE.equals(req.createTransaction()) || req.createTransaction() == null) {
        if (goal.getWalletId() == null) {
            throw new BusinessException("GOAL_WALLET_REQUIRED", 400, "Mục tiêu chưa gắn ví, không thể tạo giao dịch thật.");
        }
        if (req.sourceWalletId() == null) {
            throw new BusinessException("VALIDATION_ERROR", 400, "Thiếu ví nguồn để chuyển tiền.");
        }
        var result = transactionWriter.write(new TransactionWriteCommand(
                null, userId, req.sourceWalletId(), goal.getWalletId(), null,
                "transfer", req.amount(), date, req.note(), null, "manual", true, null, null, null));
        transactionId = result.transactionId();
    }
    goalContributionRepository.save(GoalContribution.builder()
            .goalId(goalId).transactionId(transactionId).amount(req.amount())
            .contributedDate(date).build());
    SavingsGoal reloaded = savingsGoalRepository.findByIdNative(goalId).orElseThrow();
    ```
    **KHÔNG** suy đoán chế độ từ `goal.getWalletId() != null` — luôn đọc field request.

    **`cancelContribution(userId, goalId, contributionId)` (GOAL-04):** load goal (quyền), load
    contribution qua `findByIdAndGoalId` (404 nếu không tồn tại). Nếu
    `contribution.getTransactionId() != null` → gọi `transactionService.delete(userId,
    contribution.getTransactionId())` (xoá mềm + hoàn tác ví qua luồng 3 bước có sẵn) TRƯỚC.
    Sau đó `goalContributionRepository.delete(contribution)` — trigger tự SUM lại, tự mở
    `completed → in_progress` nếu không còn đủ. Đọc lại `findByIdNative` sau cùng.
    **Thứ tự ngược với Debt (D-46 xoá `debt_payments` trước, xoá giao dịch sau)** vì
    `goal_contributions.transaction_id` là `ON DELETE SET NULL` (không `RESTRICT` như debt) —
    xoá giao dịch trước không vi phạm ràng buộc nào, và tránh trường hợp xoá contribution trước
    rồi mất tham chiếu để tìm `transaction_id` cần hoàn tác.

    **`update`/`delete`** theo api/09 mục B5 — sửa được `name/target_amount/target_date/
    icon_id/status`; xoá goal nhận tham số `revert_transactions` (mặc định `false`) — nếu
    `true`, lặp qua mọi `goal_contributions` của goal gọi `transactionService.delete` trước khi
    xoá goal; nếu `false`, chỉ xoá bản ghi `savings_goals` (cascade tự dọn `goal_contributions`,
    giao dịch liên quan giữ nguyên vì `transaction_id` chỉ `SET NULL`, không bị xoá).

    Tạo `goal/controller/GoalController.java` — 7 route theo bảng api/09 Phần B. Gắn
    `@Idempotent` cho `POST /goals` và `POST /goals/{id}/contributions`.

    Tạo DTO cần thiết trong `goal/dto/` theo response mẫu api/09 mục B1-B4.

    **KHÔNG sửa `GlobalExceptionHandlerTest.java` ở plan này** — tránh xung đột merge giữa 4 plan wave 2 chạy song song. Việc thêm `com.datn.financeapp.goal.controller.GoalController.class` vào `excludeFilters` được GOM LẠI ở Plan 06 Task 1 — xem 04-06-PLAN.md.

    Tạo `src/test/java/com/datn/financeapp/goal/GoalContributionIntegrationTest.java` — 5 test
    case ở `<behavior>`. Tên method tiếng Anh, ví dụ
    `createGoal_withInitialAmount_doesNotCreateTransactionForInitialAmountItself` (lưu ý tên
    phản ánh đúng: initial_amount tự nó không sinh GIAO DỊCH, dù có sinh một
    `goal_contribution` nội bộ để cộng `saved_amount` — assert trong test là KHÔNG có giao dịch
    mới nào trong bảng `transactions` sau khi tạo goal, CHỈ kiểm `saved_amount` đúng),
    `addContribution_createTransactionTrue_createsTransferTransaction`,
    `addContribution_createTransactionFalse_noWalletChange`,
    `addContribution_createTransactionTrueWithoutGoalWallet_returns400`,
    `cancelContribution_reopensCompletedToInProgress`.
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn test -Dtest=GoalContributionIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=GoalContributionIntegrationTest` thoát mã 0, đủ 5 test case pass
    - `grep -n "setSavedAmount\|goal.setStatus" GoalService.java` trả về rỗng (không có chỗ nào ghi tay hai cột này)
    - `GoalService.java` chứa `findByIdNative` ngay sau mọi lời gọi `goalContributionRepository.save/.delete`
    - `mvn test` full suite vẫn xanh
  </acceptance_criteria>
  <done>Mục tiêu tiết kiệm hoạt động đầy đủ hai chế độ nạp, trigger tự đồng bộ saved_amount/status, backend không ghi tay hai cột đó dù trong bất kỳ tình huống nào.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /goals/** | Người dùng thao tác mục tiêu tiết kiệm của chính mình |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | GoalService ghi saved_amount/status thủ công | mitigate | Code review: grep setSavedAmount/setStatus phải trả về rỗng tuyệt đối (không có ngoại lệ như debt write-off) |
| T-04-12 | Information Disclosure | SavingsGoalRepository.findByIdAndUserId | mitigate | Điều kiện user_id ngay trong SQL |
</threat_model>

<verification>
- `mvn test -Dtest=GoalContributionIntegrationTest` xanh
- `mvn test` full suite xanh
- `grep -rn "setSavedAmount\|\.setStatus" source/server/src/main/java/com/datn/financeapp/goal/` trả về rỗng
</verification>

<success_criteria>
- GOAL-01..04 hoạt động đầy đủ, hai chế độ nạp phân biệt rõ theo field request
- Trigger tự mở lại completed → in_progress khi rút nạp, test xác nhận
</success_criteria>

<output>
After completion, create `.planning/phases/04-nghi-p-v-ph-i-sinh-b-o-c-o/04-04-SUMMARY.md`
</output>
</content>
