---
phase: 04-nghi-p-v-ph-i-sinh-b-o-c-o
plan: 07
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-05", "04-06"]
files_modified:
  - source/server/src/main/java/com/datn/financeapp/scheduler/WalletReconciliationJob.java
  - source/server/src/main/java/com/datn/financeapp/scheduler/BudgetRenewalJob.java
  - source/server/src/main/java/com/datn/financeapp/scheduler/RecurringTransactionJob.java
  - source/server/src/main/java/com/datn/financeapp/scheduler/DebtReminderJob.java
  - source/server/src/main/java/com/datn/financeapp/scheduler/ExportCleanupJob.java
  - source/server/src/main/java/com/datn/financeapp/budget/service/BudgetService.java
  - source/server/src/main/java/com/datn/financeapp/debt/service/DebtService.java
  - source/server/src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java
  - source/server/src/test/java/com/datn/financeapp/scheduler/BudgetRenewalJobIntegrationTest.java
  - source/server/src/test/java/com/datn/financeapp/scheduler/DebtReminderJobIntegrationTest.java
  - source/server/src/test/java/com/datn/financeapp/scheduler/WalletReconciliationJobIntegrationTest.java
autonomous: true
requirements: [BUDGET-07, JOB-01, JOB-02, JOB-03, JOB-04]
user_setup: []

must_haves:
  truths:
    - "Job đối chiếu số dư ví chạy hằng ngày, tự sửa và ghi log khi lệch"
    - "Job lặp kỳ ngân sách hằng ngày cho auto_renew=true đã hết kỳ, chống tạo trùng"
    - "Job sinh giao dịch định kỳ hằng ngày gọi lại RecurringRunnerService đã có từ Plan 05"
    - "Job nhắc nợ đến hạn đúng lịch: còn 7 ngày, còn 1 ngày, quá hạn nhắc lại mỗi 7 ngày"
    - "Mỗi job bọc try/catch riêng, một job lỗi không crash scheduler pool"
    - "Job dọn export_jobs quá hạn xoá file đĩa trước khi xoá bản ghi CSDL"
  artifacts:
    - path: "source/server/src/main/java/com/datn/financeapp/scheduler/BudgetRenewalJob.java"
      provides: "@Scheduled cron riêng, gọi BudgetService.renewExpiredBudgets()"
    - path: "source/server/src/main/java/com/datn/financeapp/scheduler/DebtReminderJob.java"
      provides: "@Scheduled cron riêng, gọi DebtService + NotificationRepository ghi nhắc nợ"
  key_links:
    - from: "scheduler/RecurringTransactionJob.java"
      to: "recurring/service/RecurringRunnerService.java"
      via: "recurringRunnerService.runDueRecurring()"
      pattern: "runDueRecurring"
    - from: "scheduler/BudgetRenewalJob.java"
      to: "budget/service/BudgetService.java"
      via: "budgetService.renewExpiredBudgets()"
      pattern: "renewExpiredBudgets"
    - from: "scheduler/WalletReconciliationJob.java"
      to: "wallet/service/WalletTransferService.java"
      via: "gọi reconcile(autoFix=true) cho từng ví"
      pattern: "reconcile"
---

<objective>
Gom cả bốn tác vụ nền hằng ngày (JOB-01..04) + job dọn `export_jobs` quá hạn (D-45) vào MỘT plan
cuối phase theo D-56/D-57 — mỗi job dùng chung khuôn `IdempotencyCleanupJob` (Phase 1):
`@Scheduled(cron = "...", zone = "Asia/Ho_Chi_Minh")` đặt giờ khác nhau trong đêm, bọc try/catch
riêng để một job lỗi không crash scheduler pool. Bổ sung 3 method service còn thiếu
(`BudgetService.renewExpiredBudgets`, `DebtService`/notification cho nhắc nợ,
`WalletTransferService` cho đối chiếu hằng loạt) mà các plan trước chưa cần tới.

Purpose: Cần TOÀN BỘ module trước hoàn thành (Plan 02 ngân sách, Plan 03 sổ nợ, Plan 05 định
kỳ, Plan 06 export) mới gọi đúng — đặt cuối cùng theo D-56/D-57.
Output: 5 job `@Scheduled` hoạt động đầy đủ, test xác nhận chống tạo trùng và bọc lỗi đúng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\api\05-NGAN-SACH.md
@D:\PTIT\DATN\api\08-SO-NO.md
@D:\PTIT\DATN\source\server\.planning\phases\04-nghi-p-v-ph-i-sinh-b-o-c-o\04-CONTEXT.md
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\scheduler\IdempotencyCleanupJob.java
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletTransferService.java

<interfaces>
Khuôn duy nhất hiện có — copy nguyên cấu trúc cho cả 5 job (không đổi):
```java
@Component
@RequiredArgsConstructor
@Slf4j
public class XxxJob {
    private final XxxService xxxService;

    @Scheduled(cron = "0 M H * * *", zone = "Asia/Ho_Chi_Minh")
    public void run() {
        try {
            xxxService.doWork();
            log.info("Đã chạy xong tác vụ ...");
        } catch (Exception e) {
            log.error("Lỗi khi chạy tác vụ ...", e);
        }
    }
}
```
Chọn giờ cron KHÁC NHAU trong đêm (scheduler mặc định 1 thread — 2 job trùng giờ sẽ đợi nhau):
- `WalletReconciliationJob` — `0 0 1 * * *` (1h sáng)
- `BudgetRenewalJob` — `0 0 2 * * *` (2h sáng)
- `IdempotencyCleanupJob` (đã có, Phase 1) — `0 0 3 * * *` (3h sáng, KHÔNG đổi)
- `RecurringTransactionJob` — `0 30 3 * * *` (3h30)
- `DebtReminderJob` — `0 0 4 * * *` (4h sáng)
- `ExportCleanupJob` — `0 30 4 * * *` (4h30)

**Lưu ý quan trọng cho `RecurringTransactionJob` và `BudgetRenewalJob`:** service bên trong
được gọi (`RecurringRunnerService.runDueRecurring()`, `BudgetService.renewExpiredBudgets()`)
KHÔNG được mang `@Transactional` ở method top-level nếu bên trong nó tự lặp qua nhiều bản ghi
độc lập (giữ nguyên tinh thần D-51/D-52 — một bản ghi lỗi không cuốn bản ghi khác). Method
`@Scheduled` gọi service này CŨNG không tự thêm `@Transactional` bọc ngoài — chỉ log kết quả.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bổ sung method service còn thiếu (renewExpiredBudgets, reminder queries, reconcileAll)</name>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\budget\service\BudgetService.java (Plan 02)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\debt\service\DebtService.java (Plan 03)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\service\WalletTransferService.java (method reconcile hiện có, dòng 158+)
    D:\PTIT\DATN\api\05-NGAN-SACH.md (mục 8 "Tự động lặp lại kỳ mới")
    D:\PTIT\DATN\api\08-SO-NO.md (mục 9 "Nhắc nhở đến hạn")
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\notification\repository\NotificationRepository.java (Plan 01)
  </read_first>
  <action>
    **Sửa `BudgetService.java`** — thêm method mới (KHÔNG `@Transactional` ở method top-level,
    mỗi ngân sách xử lý độc lập theo đúng tinh thần D-51/D-52 dù CONTEXT không nói rõ nhưng cùng
    nguyên tắc "một bản ghi lỗi không chặn bản ghi khác"):
    ```java
    public void renewExpiredBudgets() {
        List<Budget> toRenew = budgetRepository.findAutoRenewExpired(LocalDate.now());
        for (Budget old : toRenew) {
            try {
                renewOneBudget(old.getId());
            } catch (Exception e) {
                log.error("Lỗi khi tự động lặp kỳ ngân sách {}", old.getId(), e);
            }
        }
    }

    @Transactional
    public void renewOneBudget(UUID oldBudgetId) {
        Budget old = budgetRepository.findById(oldBudgetId).orElseThrow();
        // Chống trùng: kiểm tra kỳ mới CHƯA tồn tại trước khi tạo (api/05 mục 8)
        LocalDate newStart = old.getEndDate().plusDays(1);
        LocalDate newEnd = calculateEndDate(newStart, old.getPeriodType());
        boolean exists = budgetRepository.existsOverlapping(old.getUserId(), old.getCategoryId(),
                old.getWalletId(), newStart, newEnd);
        if (exists) return; // đã lặp rồi (job chạy lại), bỏ qua im lặng
        old.setIsActive(false);
        budgetRepository.save(old);
        Budget renewed = Budget.builder().id(UUID.randomUUID()).userId(old.getUserId())
                .groupId(old.getGroupId()).categoryId(old.getCategoryId()).walletId(old.getWalletId())
                .limitAmount(old.getLimitAmount()).periodType(old.getPeriodType())
                .startDate(newStart).endDate(newEnd).autoRenew(true).isActive(true)
                .createdAt(Instant.now()).build();
        budgetRepository.save(renewed);
        notificationRepository.insertGenericNotification(old.getUserId(), "budget_renewed",
                "Ngân sách đã bắt đầu kỳ mới", buildRenewalContent(old), renewed.getId());
    }
    ```
    Thêm `NotificationRepository` vào constructor injection của `BudgetService` nếu chưa có.
    Thêm method `BudgetRepository.findAutoRenewExpired(LocalDate today)` — native query
    `WHERE auto_renew = TRUE AND is_active = TRUE AND end_date < :today`. Thêm method
    `existsOverlapping` nếu chưa có — dùng để kiểm tra chống trùng TRƯỚC khi tạo (lớp bảo vệ
    Java, cộng thêm `ex_bud_no_overlap` là lớp bảo vệ CSDL cuối cùng nếu Java race).

    Nếu `NotificationRepository` (Plan 01) chưa có method insert thông báo dạng chung (không
    phải `budget_alert` UNIQUE), thêm method mới:
    ```java
    @Modifying
    @Query(value = "INSERT INTO notifications (user_id, type, title, content, reference_id) "
            + "VALUES (:userId, :type, :title, :content, :referenceId)", nativeQuery = true)
    void insertGenericNotification(@Param("userId") UUID userId, @Param("type") String type,
            @Param("title") String title, @Param("content") String content, @Param("referenceId") UUID referenceId);
    ```
    (chữ ký 5 tham số — điều chỉnh lời gọi ở trên nếu cần thêm `referenceId`).

    **Sửa `DebtService.java`** — thêm method dùng cho JOB-04 (đọc `findOutstandingWithDueDate`
    đã có từ Plan 03, tính và ghi nhắc nợ):
    ```java
    public void sendDueReminders() {
        // Gom TẤT CẢ user có nợ outstanding — cần query khác findOutstandingWithDueDate
        // (method đó nhận userId, ở đây job quét TOÀN HỆ THỐNG). Thêm method repository mới:
        // DebtRepository.findAllOutstandingWithDueDate() — không lọc user, dùng riêng cho job.
        List<Debt> debts = debtRepository.findAllOutstandingWithDueDate();
        for (Debt debt : debts) {
            try {
                evaluateAndNotify(debt);
            } catch (Exception e) {
                log.error("Lỗi khi gửi nhắc nợ cho khoản {}", debt.getId(), e);
            }
        }
    }

    private void evaluateAndNotify(Debt debt) {
        long daysUntilDue = ChronoUnit.DAYS.between(LocalDate.now(), debt.getDueDate());
        String content;
        if (daysUntilDue == 7) {
            content = "Còn 7 ngày đến hạn khoản nợ với " + debt.getCounterpartyName();
        } else if (daysUntilDue == 1) {
            content = "Ngày mai đến hạn khoản nợ với " + debt.getCounterpartyName();
        } else if (daysUntilDue < 0 && daysUntilDue % 7 == 0) {
            content = "Đã quá hạn khoản nợ với " + debt.getCounterpartyName();
        } else {
            return; // không phải mốc nhắc nào trong ba mốc trên
        }
        notificationRepository.insertGenericNotification(debt.getUserId(), "debt_reminder",
                "Nhắc nợ đến hạn", content, debt.getId());
    }
    ```
    Thêm `NotificationRepository` vào constructor injection của `DebtService` nếu chưa có. Thêm
    `DebtRepository.findAllOutstandingWithDueDate()` — native query `WHERE status =
    'outstanding' AND due_date IS NOT NULL`.

    **Sửa `WalletTransferService.java`** — thêm method quét TOÀN BỘ ví cho JOB-01 (khác
    `reconcile(userId, walletId, autoFix)` hiện có chỉ xử lý 1 ví theo yêu cầu người dùng):
    ```java
    public void reconcileAllWallets() {
        List<UUID> allWalletIds = walletRepository.findAllActiveWalletIds(); // thêm method mới, SELECT id FROM wallets WHERE NOT is_deleted
        for (UUID walletId : allWalletIds) {
            try {
                reconcileOneWalletAutoFix(walletId);
            } catch (Exception e) {
                log.error("Lỗi khi đối chiếu ví {}", walletId, e);
            }
        }
    }

    @Transactional
    public void reconcileOneWalletAutoFix(UUID walletId) {
        // Tái dùng logic reconcile() hiện có nhưng KHÔNG cần userId (job hệ thống, không phải
        // request người dùng) — đọc Wallet trực tiếp để lấy userId nội bộ, gọi lại đúng công
        // thức đối chiếu đã có ở method reconcile(), autoFix luôn true.
    }
    ```
    **QUAN TRỌNG:** đọc kỹ method `reconcile()` hiện tại (dòng 158+) trước khi viết —
    KHÔNG viết lại công thức đối chiếu, chỉ bọc lại để không cần tham số `userId` từ request.
    Nếu `reconcile()` hiện tại validate quyền qua `lockAndCheckOwnership(walletId, userId)`,
    tạo nhánh nội bộ đọc `walletRepository.findById` trực tiếp (bỏ qua bước kiểm tra quyền vì
    đây là job hệ thống không có "người dùng đang gọi").
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn compile -q</automated>
  </verify>
  <acceptance_criteria>
    - `BudgetService.java` chứa method `renewExpiredBudgets` và `renewOneBudget`
    - `DebtService.java` chứa method `sendDueReminders`
    - `WalletTransferService.java` chứa method `reconcileAllWallets`
    - `mvn compile` biên dịch thành công không lỗi
  </acceptance_criteria>
  <done>Ba service có đủ method top-level cho scheduler gọi ở Task 2, tái dùng logic đã có, không viết lại công thức nghiệp vụ.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5 job @Scheduled + test chống trùng/bọc lỗi</name>
  <read_first>
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\scheduler\IdempotencyCleanupJob.java (khuôn duy nhất)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\recurring\service\RecurringRunnerService.java (Plan 05, method runDueRecurring)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\report\repository\ExportJobRepository.java (Plan 06, method findExpired)
  </read_first>
  <behavior>
    - Test 1 (JOB-02, chống trùng): 1 ngân sách `auto_renew=true`, `end_date` hôm qua → chạy
      `BudgetRenewalJob.run()` HAI LẦN liên tiếp → chỉ có ĐÚNG 1 ngân sách kỳ mới được tạo
    - Test 2 (JOB-04): khoản nợ `due_date` = hôm nay + 7 ngày → chạy `DebtReminderJob.run()` →
      bảng `notifications` có đúng 1 bản ghi `type=debt_reminder`; khoản nợ `due_date` = hôm nay
      + 3 ngày → KHÔNG có bản ghi nào (không rơi vào 1 trong 3 mốc)
    - Test 3 (JOB-01): ví có `current_balance` bị chỉnh sai thủ công qua JdbcTemplate (mô phỏng
      lệch dữ liệu) → chạy `WalletReconciliationJob.run()` → số dư tự sửa đúng về công thức
      `initial_balance + thu - chi - chuyển đi + chuyển đến`
    - Test 4 (bọc lỗi): một trong nhiều ví bị lỗi giả lập (ví dụ id không tồn tại chèn thủ công
      vào danh sách quét) → job vẫn hoàn tất, các ví hợp lệ khác vẫn được xử lý, không ném
      exception ra ngoài `run()`
  </behavior>
  <action>
    Tạo `scheduler/WalletReconciliationJob.java` — copy khuôn `IdempotencyCleanupJob`, gọi
    `walletTransferService.reconcileAllWallets()`, cron `0 0 1 * * *`.

    Tạo `scheduler/BudgetRenewalJob.java` — gọi `budgetService.renewExpiredBudgets()`, cron
    `0 0 2 * * *`.

    Tạo `scheduler/RecurringTransactionJob.java` — gọi
    `recurringRunnerService.runDueRecurring()` (method đã có từ Plan 05, KHÔNG cần sửa gì
    thêm), cron `0 30 3 * * *`.

    Tạo `scheduler/DebtReminderJob.java` — gọi `debtService.sendDueReminders()`, cron
    `0 0 4 * * *`.

    Tạo `scheduler/ExportCleanupJob.java` — quét `exportJobRepository.findExpired(Instant.now())`,
    với MỖI job: `Files.deleteIfExists(Path.of(job.getFilePath()))` TRƯỚC (xoá file đĩa trước
    khi xoá bản ghi CSDL — D-45, tránh mất bản ghi mà file vẫn còn trên đĩa; ngược lại chấp nhận
    được vì file rác không nguy hiểm bằng bản ghi trỏ tới file không tồn tại), rồi
    `exportJobRepository.delete(job)`, bọc try/catch từng job riêng (một file lỗi xoá không
    chặn file khác). Cron `0 30 4 * * *`.

    Mỗi job đều: `@Component @RequiredArgsConstructor @Slf4j`, method `run()` mang
    `@Scheduled(cron = "...", zone = "Asia/Ho_Chi_Minh")`, bọc try/catch toàn bộ nội dung, log
    tiếng Việt kết quả (số bản ghi xử lý nếu service trả về, hoặc câu cố định).

    Tạo 3 test file (không cần test riêng cho `RecurringTransactionJob`/`ExportCleanupJob` vì
    logic cốt lõi đã test ở Plan 05/06 — job ở đây chỉ là lớp `@Scheduled` mỏng gọi lại, theo
    nguyên tắc test theo luồng rủi ro D-58 không phủ đều mọi endpoint):
    - `BudgetRenewalJobIntegrationTest.java` — Test 1 ở `<behavior>`. Gọi trực tiếp
      `budgetRenewalJob.run()` (KHÔNG đợi cron thật) hai lần liên tiếp qua
      `@Autowired BudgetRenewalJob`.
    - `DebtReminderJobIntegrationTest.java` — Test 2 ở `<behavior>`.
    - `WalletReconciliationJobIntegrationTest.java` — Test 3, 4 ở `<behavior>`.
    Copy khuôn Testcontainers `@SpringBootTest` (KHÔNG cần `@AutoConfigureMockMvc` vì test gọi
    thẳng bean Java, không qua HTTP). Tên method tiếng Anh, ví dụ
    `renewExpiredBudgets_calledTwice_createsOnlyOneNewPeriod`,
    `sendDueReminders_sevenDaysBeforeDue_createsNotification`,
    `sendDueReminders_threeDaysBeforeDue_createsNoNotification`,
    `reconcileAllWallets_fixesDriftedBalance`,
    `reconcileAllWallets_oneWalletFails_othersStillProcessed`.
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn test -Dtest=BudgetRenewalJobIntegrationTest,DebtReminderJobIntegrationTest,WalletReconciliationJobIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=BudgetRenewalJobIntegrationTest,DebtReminderJobIntegrationTest,WalletReconciliationJobIntegrationTest` thoát mã 0, đủ 5 test case ở `<behavior>` pass
    - Cả 5 job (`WalletReconciliationJob`, `BudgetRenewalJob`, `RecurringTransactionJob`, `DebtReminderJob`, `ExportCleanupJob`) đều có `@Scheduled(cron = ..., zone = "Asia/Ho_Chi_Minh")` với giờ cron KHÁC NHAU
    - Mỗi job chứa khối `try { ... } catch (Exception e) { log.error(...); }` bọc TOÀN BỘ method `run()`
    - `mvn test` full suite (toàn bộ Phase 1-4) thoát mã 0 — đây là bài test cuối cùng của phase theo D-58 mục 6
  </acceptance_criteria>
  <done>Năm tác vụ nền chạy hằng ngày tự động, chống tạo trùng đúng, một job/bản ghi lỗi không crash scheduler pool, toàn bộ test tích luỹ từ Phase 1 vẫn xanh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler nội bộ → toàn bộ dữ liệu hệ thống | Job quét TOÀN BỘ user, không có "người dùng đang gọi" — khác mọi endpoint khác trong dự án |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Denial of Service | Một ví/ngân sách/khoản nợ lỗi trong vòng lặp job | mitigate | try/catch từng bản ghi độc lập, log và tiếp tục, không crash scheduler pool |
| T-04-21 | Tampering | ExportCleanupJob xoá nhầm file đang được tải | accept | expires_at đã qua nghĩa là download_url đã trả 410 từ trước, rủi ro race cực thấp ở quy mô đồ án 1 instance |
</threat_model>

<verification>
- `mvn test -Dtest=BudgetRenewalJobIntegrationTest,DebtReminderJobIntegrationTest,WalletReconciliationJobIntegrationTest` xanh
- `mvn test` full suite toàn bộ Phase 1-4 xanh — bài kiểm cuối cùng trước khi đóng phase
</verification>

<success_criteria>
- BUDGET-07, JOB-01..04 hoạt động đầy đủ qua @Scheduled, giờ cron không trùng nhau
- Toàn bộ 31 requirement ID của Phase 4 đã có plan triển khai
- Test tích luỹ Phase 1-4 xanh toàn bộ, sẵn sàng cho /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/04-nghi-p-v-ph-i-sinh-b-o-c-o/04-07-SUMMARY.md`
</output>
</content>
