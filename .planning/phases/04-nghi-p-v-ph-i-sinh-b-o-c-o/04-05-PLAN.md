---
phase: 04-nghi-p-v-ph-i-sinh-b-o-c-o
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - source/server/src/main/java/com/datn/financeapp/recurring/entity/RecurringTransaction.java
  - source/server/src/main/java/com/datn/financeapp/recurring/repository/RecurringTransactionRepository.java
  - source/server/src/main/java/com/datn/financeapp/recurring/service/RecurringService.java
  - source/server/src/main/java/com/datn/financeapp/recurring/service/RecurringRunnerService.java
  - source/server/src/main/java/com/datn/financeapp/recurring/service/RecurringPeriodWriter.java
  - source/server/src/main/java/com/datn/financeapp/recurring/util/RecurringDateCalculator.java
  - source/server/src/main/java/com/datn/financeapp/recurring/controller/RecurringController.java
  - source/server/src/main/java/com/datn/financeapp/recurring/dto/CreateRecurringRequest.java
  - source/server/src/main/java/com/datn/financeapp/recurring/dto/UpdateRecurringRequest.java
  - source/server/src/main/java/com/datn/financeapp/recurring/dto/RecurringListItemResponse.java
  - source/server/src/main/java/com/datn/financeapp/recurring/dto/PauseRecurringRequest.java
  - source/server/src/test/java/com/datn/financeapp/recurring/RecurringCrudIntegrationTest.java
  - source/server/src/test/java/com/datn/financeapp/recurring/RecurringDateCalculatorTest.java
  - source/server/src/test/java/com/datn/financeapp/recurring/RecurringRunnerIntegrationTest.java
autonomous: true
requirements: [RECUR-01, RECUR-02, RECUR-03]
user_setup: []

must_haves:
  truths:
    - "User tạo khoản định kỳ đặt next_run_date=start_date, không sinh giao dịch ngay"
    - "User tạm dừng/bật lại khoản định kỳ, run-now không đổi next_run_date"
    - "Tác vụ quét sinh đúng giao dịch cho mỗi kỳ bị bỏ lỡ, giữ ngày đáng lẽ phải chạy (D-50)"
    - "Ngày 31 rơi vào tháng 2 chạy ngày cuối tháng nhưng kỳ sau vẫn quay lại ngày 31 gốc"
    - "Kỳ lỗi giữa chừng không cuốn các kỳ trước đã ghi thành công (D-51/D-52)"
    - "Chống trùng qua uq_txn_recurring_date, không phụ thuộc check-then-act ở Java"
  artifacts:
    - path: "source/server/src/main/java/com/datn/financeapp/recurring/service/RecurringPeriodWriter.java"
      provides: "Bean riêng @Transactional, mỗi kỳ một transaction thật, tránh self-invocation"
    - path: "source/server/src/main/java/com/datn/financeapp/recurring/util/RecurringDateCalculator.java"
      provides: "Logic thuần Java tính next_run_date, xử lý ngày 29/30/31"
  key_links:
    - from: "recurring/service/RecurringRunnerService.java"
      to: "recurring/service/RecurringPeriodWriter.java"
      via: "gọi bean khác trong vòng lặp, method KHÔNG @Transactional ở tầng quét"
      pattern: "periodWriter.generateOnePeriod"
    - from: "recurring/service/RecurringPeriodWriter.java"
      to: "transaction/service/TransactionWriter.java"
      via: "transactionWriter.write, bắt DataIntegrityViolationException từ uq_txn_recurring_date"
      pattern: "transactionWriter.write"
---

<objective>
Xây module giao dịch định kỳ (RECUR-01..03) — CRUD/tạm dừng/chạy ngay ở phần request-response,
và cấu trúc 2-bean bắt buộc (`RecurringRunnerService` không `@Transactional` + `RecurringPeriodWriter`
bean riêng `@Transactional`) để đúng nghĩa "mỗi kỳ một transaction riêng" (D-51), xử lý đúng
ngày 29/30/31 (D-50) và bắt kịp kỳ bỏ lỡ. Job `@Scheduled` thật gọi `RecurringRunnerService`
gộp chung ở Plan 07 theo D-57 — plan này chỉ code phần service, không code `@Scheduled`.

Purpose: Cùng nhóm nghiệp vụ sinh giao dịch phái sinh, độc lập file với ngân sách/sổ nợ/mục
tiêu nên chạy song song wave 2.
Output: CRUD định kỳ đầy đủ, `RecurringRunnerService.runDueRecurring()` sẵn sàng cho scheduler
gọi ở Plan 07, test xác nhận ngày 31→28/29 và bắt kịp nhiều kỳ.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\PTIT\DATN\CLAUDE.md
@D:\PTIT\DATN\source\server\CLAUDE.md
@D:\PTIT\DATN\api\09-DINH-KY-MUC-TIEU.md
@D:\PTIT\DATN\source\server\.planning\phases\04-nghi-p-v-ph-i-sinh-b-o-c-o\04-CONTEXT.md
@D:\PTIT\DATN\db\migration\V2__giao_dich.sql
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionWriter.java
@D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionBulkService.java

<interfaces>
`recurring_transactions` bảng (V2 dòng 50-83): `id, user_id, wallet_id, category_id, type,
amount, display_name, note, frequency, interval, start_date, end_date, next_run_date,
last_run_date, is_enabled, created_at`. `ck_rec_type CHECK (type IN ('expense', 'income'))` —
KHÔNG có `transfer`. `idx_rec_due` trên `next_run_date WHERE is_enabled`.

`uq_txn_recurring_date` UNIQUE `(recurring_id, date) WHERE recurring_id IS NOT NULL AND NOT
is_deleted` (V2 dòng 153-155) — lớp bảo vệ CUỐI cùng ở tầng CSDL.

Mẫu 2-bean bắt buộc từ 04-RESEARCH.md Pattern 3 (dòng 325-373) và 04-PATTERNS.md (dòng
184-224) — sao chép cấu trúc, không tự sáng tạo cách khác:
```java
// RecurringRunnerService — KHÔNG @Transactional ở method quét
@Service @RequiredArgsConstructor @Slf4j
public class RecurringRunnerService {
    private final RecurringTransactionRepository recurringRepository;
    private final RecurringPeriodWriter periodWriter;

    public void runDueRecurring() {
        List<RecurringTransaction> due = recurringRepository.findDue(LocalDate.now());
        for (RecurringTransaction rec : due) {
            try {
                periodWriter.runOneRecurring(rec.getId()); // bean khác — giữ AOP proxy
            } catch (Exception e) {
                log.error("Lỗi khi sinh giao dịch định kỳ {}", rec.getId(), e); // D-52: log, bỏ qua, KHÔNG tắt is_enabled
            }
        }
    }
}

// RecurringPeriodWriter — bean RIÊNG, @Transactional CẤP KHOẢN ĐỊNH KỲ (bao mọi kỳ bị bỏ lỡ
// của MỘT khoản, KHÔNG bao khoản khác — mỗi khoản độc lập theo D-52)
@Component @RequiredArgsConstructor
public class RecurringPeriodWriter {
    private final TransactionWriter transactionWriter;
    private final RecurringTransactionRepository recurringRepository;

    @Transactional
    public void runOneRecurring(UUID recurringId) {
        RecurringTransaction rec = recurringRepository.findById(recurringId).orElseThrow();
        LocalDate cursor = rec.getNextRunDate();
        LocalDate today = LocalDate.now();
        while (!cursor.isAfter(today) && (rec.getEndDate() == null || !cursor.isAfter(rec.getEndDate()))) {
            try {
                transactionWriter.write(new TransactionWriteCommand(null, rec.getUserId(), rec.getWalletId(),
                        null, rec.getCategoryId(), rec.getType(), rec.getAmount(), cursor,
                        rec.getNote(), rec.getDisplayName(), "auto", true, rec.getId(), null, null));
            } catch (DataIntegrityViolationException dup) {
                // uq_txn_recurring_date bắt được — đã tồn tại, bỏ qua, KHÔNG rollback cả loop
            }
            cursor = RecurringDateCalculator.nextRunDate(rec.getStartDate(), cursor, rec.getFrequency(), rec.getInterval());
        }
        rec.setNextRunDate(cursor);
        rec.setLastRunDate(today);
        recurringRepository.save(rec);
    }
}
```
**Lưu ý quan trọng:** đoạn code mẫu trên gói TOÀN BỘ catch-up loop của MỘT khoản trong MỘT
`@Transactional` — nghĩa là nếu kỳ 3 của 5 kỳ bị bỏ lỡ ném lỗi KHÔNG PHẢI
`DataIntegrityViolationException` (ví dụ lỗi hệ thống khác), toàn bộ 5 kỳ của khoản NÀY sẽ
rollback cùng nhau (nhưng KHÔNG ảnh hưởng khoản định kỳ KHÁC — đó là ranh giới D-51/D-52 thật
sự: "mỗi kỳ một transaction riêng" áp dụng CHÉO GIỮA CÁC KHOẢN ĐỊNH KỲ khác nhau, còn bên trong
CÙNG một khoản, các kỳ share một transaction để đảm bảo `next_run_date`/`last_run_date` luôn
nhất quán với các giao dịch đã sinh của chính khoản đó). Việc `catch
DataIntegrityViolationException` NGAY TRONG vòng lặp (không phải bọc toàn bộ method) mới là chỗ
thật sự cho phép "một kỳ trùng không chặn kỳ khác của CÙNG khoản". Giữ đúng cấu trúc này —
ĐỪNG bọc `@Transactional` ở cấp mỗi kỳ riêng lẻ bằng gọi thêm một bean thứ ba, vì `next_run_date`
phải cập nhật atomic cùng các giao dịch nó đại diện.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecurringDateCalculator (ngày 29/30/31) + RecurringTransaction entity/repository</name>
  <read_first>
    D:\PTIT\DATN\api\09-DINH-KY-MUC-TIEU.md (mục A2 "Xử lý ngày không tồn tại", dòng 117-125)
    D:\PTIT\DATN\db\migration\V2__giao_dich.sql (dòng 50-83)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\wallet\entity\Wallet.java
  </read_first>
  <behavior>
    - Test 1: `nextRunDate` với `startDate=2026-01-31, frequency=month, interval=1` từ cursor
      `2026-01-31` → trả `2026-02-28` (2026 không nhuận)
    - Test 2: cùng cấu hình, từ cursor `2026-02-28` → trả `2026-03-31` (QUAY LẠI ngày gốc 31,
      không giữ 28 — đây là điểm cốt lõi D-50/api/09 "ngày gốc không đổi")
    - Test 3: `frequency=year, interval=1, startDate=2024-02-29` (năm nhuận), cursor
      `2024-02-29` → `2025-02-28` (2025 không nhuận)
    - Test 4: `frequency=day, interval=3` → cộng đúng 3 ngày, không có ca ngày không tồn tại
    - Test 5: `frequency=week, interval=2` → cộng đúng 14 ngày
  </behavior>
  <action>
    Tạo `recurring/util/RecurringDateCalculator.java` — class utility thuần Java (không Spring
    bean, static method), signature:
    ```java
    public final class RecurringDateCalculator {
        private RecurringDateCalculator() {}

        public static LocalDate nextRunDate(LocalDate originalStartDate, LocalDate currentCursor,
                String frequency, int interval) {
            int originalDay = originalStartDate.getDayOfMonth();
            return switch (frequency) {
                case "day" -> currentCursor.plusDays(interval);
                case "week" -> currentCursor.plusWeeks(interval);
                case "month" -> applyOriginalDay(currentCursor.plusMonths(interval), originalDay);
                case "year" -> applyOriginalDay(currentCursor.plusYears(interval), originalDay);
                default -> throw new IllegalArgumentException("Tần suất không hợp lệ: " + frequency);
            };
        }

        private static LocalDate applyOriginalDay(LocalDate target, int originalDay) {
            int lastDayOfTargetMonth = target.lengthOfMonth();
            int dayToUse = Math.min(originalDay, lastDayOfTargetMonth);
            return target.withDayOfMonth(dayToUse);
        }
    }
    ```
    **Giải thích logic:** `currentCursor.plusMonths(interval)` của `java.time.LocalDate` tự làm
    tròn xuống ngày cuối tháng nếu tháng đích không có đủ ngày (ví dụ `2026-01-31.plusMonths(1)`
    → tự động `2026-02-28`, JDK không ném lỗi) — NHƯNG hành vi JDK áp dụng LUÔN từ `currentCursor`
    (đã có thể đã bị làm tròn ở kỳ trước), không phải từ `originalStartDate`. Vì vậy PHẢI dùng
    `applyOriginalDay` để ép lại đúng ngày gốc mỗi lần tính — nếu chỉ gọi `plusMonths` liên tiếp
    từ cursor đã bị làm tròn, ngày sẽ "trôi dần" xuống 28 vĩnh viễn (đúng lỗi D-50 cảnh báo).

    Tạo `recurring/entity/RecurringTransaction.java` — field: id, userId, walletId, categoryId,
    type, amount, displayName, note, frequency, interval, startDate, endDate, nextRunDate,
    lastRunDate, isEnabled, createdAt.

    Tạo `recurring/repository/RecurringTransactionRepository.java`:
    ```java
    @Query(value = "SELECT * FROM recurring_transactions WHERE id = :id AND user_id = :currentUser", nativeQuery = true)
    Optional<RecurringTransaction> findByIdAndUserId(@Param("id") UUID id, @Param("currentUser") UUID currentUser);

    @Query(value = "SELECT * FROM recurring_transactions WHERE user_id = :currentUser "
            + "AND (:isEnabled IS NULL OR is_enabled = :isEnabled) AND (:type IS NULL OR type = :type) "
            + "ORDER BY next_run_date", nativeQuery = true)
    List<RecurringTransaction> findAllForUser(@Param("currentUser") UUID currentUser, @Param("isEnabled") Boolean isEnabled, @Param("type") String type);

    @Query(value = "SELECT * FROM recurring_transactions WHERE is_enabled = TRUE "
            + "AND next_run_date <= :today AND (end_date IS NULL OR end_date >= :today)", nativeQuery = true)
    List<RecurringTransaction> findDue(@Param("today") LocalDate today);
    ```

    Tạo `src/test/java/com/datn/financeapp/recurring/RecurringDateCalculatorTest.java` — 5 test
    case ở `<behavior>`, JUnit thuần (không cần Spring/Testcontainers vì đây là pure utility).
    Tên method: `monthlyFrequency_day31_fallsBackToLastDayOfShortMonth`,
    `monthlyFrequency_afterShortMonth_returnsToOriginalDay31`,
    `yearlyFrequency_leapDay_fallsBackInNonLeapYear`, `dailyFrequency_addsExactInterval`,
    `weeklyFrequency_addsExactInterval`.
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn test -Dtest=RecurringDateCalculatorTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=RecurringDateCalculatorTest` thoát mã 0, đủ 5 test case pass
    - `RecurringDateCalculator.java` không phụ thuộc Spring (không import `org.springframework.*`)
    - `RecurringTransactionRepository.findDue` chứa điều kiện `is_enabled = TRUE` và `next_run_date <= :today`
  </acceptance_criteria>
  <done>Logic tính ngày kế tiếp xử lý đúng ca 29/30/31, giữ nguyên ngày gốc qua nhiều kỳ liên tiếp, có test JUnit thuần xác nhận.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RecurringService CRUD + RecurringRunnerService/RecurringPeriodWriter + test bắt kịp kỳ</name>
  <read_first>
    D:\PTIT\DATN\api\09-DINH-KY-MUC-TIEU.md (Phần A toàn bộ)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionBulkService.java (toàn bộ — mẫu 2-bean, KHÔNG @Transactional ở tầng quét)
    D:\PTIT\DATN\source\server\src\main\java\com\datn\financeapp\transaction\service\TransactionWriter.java
    D:\PTIT\DATN\source\server\.planning\phases\04-nghi-p-v-ph-i-sinh-b-o-c-o\04-PATTERNS.md (mục "recurring/service/RecurringRunnerService.java", dòng 178-228)
  </read_first>
  <behavior>
    - Test 1 (RECUR-01): tạo khoản định kỳ → `next_run_date == start_date`, `is_enabled=true`,
      `last_run_date=null`, KHÔNG có giao dịch nào trong `transactions` với `recurring_id` này
    - Test 2 (RECUR-02): `POST /recurring/{id}/pause` với `is_enabled=false` → sau đó
      `POST /recurring/{id}/run-now` vẫn sinh giao dịch NGAY (không phụ thuộc `is_enabled` vì
      run-now là hành động chủ động của người dùng) nhưng KHÔNG đổi `next_run_date`
    - Test 3 (RECUR-03, ưu tiên D-58 mục 3): khoản `frequency=month, interval=1,
      start_date=2026-01-31`, giả lập `next_run_date` đã đặt về quá khứ xa (test tự set qua
      JdbcTemplate để mô phỏng "vắng mặt nhiều tháng" thay vì đợi thời gian thật) — chạy
      `runDueRecurring()` → sinh đủ N giao dịch cho N kỳ bị bỏ lỡ, giao dịch tháng 2 có
      `date=2026-02-28`, giao dịch tháng 3 có `date=2026-03-31` (không phải 28)
    - Test 4 (D-51/D-52, ưu tiên D-58 mục 4): 2 khoản định kỳ cùng đến hạn, một khoản trỏ tới
      `wallet_id` đã bị xoá (giả lập lỗi FK khi ghi) — khoản lỗi bị bỏ qua (log, không throw ra
      ngoài `runDueRecurring`), khoản còn lại VẪN sinh giao dịch thành công, `is_enabled` của
      khoản lỗi KHÔNG bị tự tắt
    - Test 5 (chống trùng): chạy `runDueRecurring()` HAI LẦN liên tiếp cho cùng dữ liệu → lần
      thứ hai KHÔNG tạo thêm giao dịch nào (mỗi kỳ chỉ đúng 1 bản ghi nhờ
      `uq_txn_recurring_date`)
  </behavior>
  <action>
    Tạo `recurring/service/RecurringService.java` — `@Service @RequiredArgsConstructor`, CRUD
    request-response:
    - `create(userId, req)`: validate `category.type == req.type()` (400
      `CATEGORY_TYPE_MISMATCH`), `end_date >= start_date` nếu có (400 `INVALID_END_DATE`),
      `frequency` hợp lệ. Đặt `next_run_date = start_date`, `is_enabled = true`,
      `last_run_date = null`. **KHÔNG gọi `TransactionWriter`** — không sinh giao dịch ngay
      (api/09 mục "Việc máy chủ phải làm" #4).
    - `update(userId, id, req)`: sửa các field cho phép (không hạn chế đặc biệt theo api/09).
    - `delete(userId, id)`: xoá mềm... schema `recurring_transactions` (V2) **KHÔNG có cột
      `is_deleted`** — đối chiếu trước khi code (CORE-11). Xoá bằng cách đặt `is_enabled =
      false` KHÔNG đúng ngữ nghĩa DELETE (đó là pause) — vì bảng không có is_deleted, xoá thật
      sự đồng nghĩa `recurringRepository.delete(entity)` (xoá cứng bản ghi định kỳ; giao dịch
      ĐàSINH trước đó giữ nguyên trong `transactions`, không bị ảnh hưởng vì
      `fk_txn_recurring ON DELETE SET NULL`). Ghi rõ quyết định này trong SUMMARY.
    - `pause(userId, id, req)`: đặt `is_enabled = req.isEnabled()`.
    - `runNow(userId, id)`: gọi `periodWriter.runSinglePeriodNow(id)` — method MỚI khác
      `runOneRecurring` (không đụng `next_run_date`/`last_run_date`, chỉ ghi MỘT giao dịch với
      `date = LocalDate.now()`, bắt `DataIntegrityViolationException` nếu hôm nay đã có giao
      dịch từ khoản này rồi → ném `BusinessException("ALREADY_RUN_TODAY", 409, "Đã ghi giao
      dịch hôm nay cho khoản định kỳ này.")`).

    Tạo `recurring/service/RecurringPeriodWriter.java` — bean `@Component` RIÊNG (KHÔNG
    `@Service`, hạ tầng giống `TransactionWriter`), copy đúng cấu trúc mẫu ở `<interfaces>` của
    plan này (`runOneRecurring(UUID recurringId)` — `@Transactional` cấp khoản, vòng lặp
    catch-up bên trong, dùng `RecurringDateCalculator.nextRunDate`). Thêm method thứ hai
    `runSinglePeriodNow(UUID recurringId)` (`@Transactional` riêng) cho `run-now`.

    Tạo `recurring/service/RecurringRunnerService.java` — `@Service @RequiredArgsConstructor
    @Slf4j`, method `runDueRecurring()` KHÔNG `@Transactional`, vòng lặp gọi
    `periodWriter.runOneRecurring(rec.getId())` bọc try/catch log lỗi, KHÔNG tắt `is_enabled`
    (D-52). Đây là method Plan 07 sẽ gọi từ `@Scheduled` — không cần code `@Scheduled` ở plan
    này.

    Tạo `recurring/controller/RecurringController.java` — 7 route theo bảng api/09 Phần A. Gắn
    `@Idempotent` cho `POST /recurring`.

    Tạo DTO cần thiết trong `recurring/dto/`.

    **KHÔNG sửa `GlobalExceptionHandlerTest.java` ở plan này** — tránh xung đột merge giữa 4 plan wave 2 chạy song song. Việc thêm `com.datn.financeapp.recurring.controller.RecurringController.class` vào `excludeFilters` được GOM LẠI ở Plan 06 Task 1 — xem 04-06-PLAN.md.

    Tạo `RecurringCrudIntegrationTest.java` (Test 1, 2 ở `<behavior>`) và
    `RecurringRunnerIntegrationTest.java` (Test 3, 4, 5 ở `<behavior>` — dùng `JdbcTemplate` để
    tự set `next_run_date` về quá khứ mô phỏng "vắng mặt", copy khuôn Testcontainers từ
    `WalletTransferConcurrencyTest.java`). Tên method tiếng Anh, ví dụ
    `createRecurring_setsNextRunDateToStartDate_noImmediateTransaction`,
    `runNow_doesNotChangeNextRunDate`,
    `runDueRecurring_missedThreeMonths_generatesThreePeriodsWithCorrectDates`,
    `runDueRecurring_oneRecurringFails_othersStillSucceed`,
    `runDueRecurring_calledTwice_doesNotDuplicate`.
  </action>
  <verify>
    <automated>cd D:/PTIT/DATN/source/server && mvn test -Dtest=RecurringCrudIntegrationTest,RecurringRunnerIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test -Dtest=RecurringCrudIntegrationTest,RecurringRunnerIntegrationTest` thoát mã 0, đủ 7 test case ở `<behavior>` pass
    - `RecurringRunnerService.java` KHÔNG có annotation `@Transactional` ở method `runDueRecurring`
    - `RecurringPeriodWriter.java` là `@Component`, có `@Transactional` ở method `runOneRecurring`
    - `mvn test` full suite vẫn xanh
  </acceptance_criteria>
  <done>Định kỳ CRUD đầy đủ, RecurringRunnerService.runDueRecurring() sẵn sàng cho scheduler, bắt kịp kỳ bỏ lỡ đúng ngày, khoản lỗi không chặn khoản khác.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /recurring/** | Người dùng thao tác khoản định kỳ của chính mình |
| Scheduler (Plan 07) → RecurringRunnerService | Nội bộ, không qua HTTP, nhưng vẫn ghi log userId khi lỗi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | RecurringTransactionRepository.findByIdAndUserId | mitigate | Điều kiện user_id ngay trong SQL |
| T-04-14 | Denial of Service | Một khoản định kỳ lỗi liên tục | mitigate | try/catch mỗi khoản độc lập trong runDueRecurring, không crash toàn bộ tác vụ (D-52) |
| T-04-15 | Repudiation | Ghi trùng giao dịch định kỳ khi job chạy song song | mitigate | uq_txn_recurring_date UNIQUE ở tầng CSDL là lớp bảo vệ cuối, không phụ thuộc check-then-act |
</threat_model>

<verification>
- `mvn test -Dtest=RecurringDateCalculatorTest,RecurringCrudIntegrationTest,RecurringRunnerIntegrationTest` xanh
- `mvn test` full suite xanh
</verification>

<success_criteria>
- RECUR-01..03 hoạt động đầy đủ, RecurringRunnerService sẵn sàng cho @Scheduled ở Plan 07
- Ngày 29/30/31 xử lý đúng qua nhiều kỳ liên tiếp
- Bắt kịp kỳ bỏ lỡ đúng ngày từng kỳ, không dồn vào hôm nay
</success_criteria>

<output>
After completion, create `.planning/phases/04-nghi-p-v-ph-i-sinh-b-o-c-o/04-05-SUMMARY.md`
</output>
</content>
