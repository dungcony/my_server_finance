---
phase: 02-vi-danh-muc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/datn/financeapp/wallet/entity/Wallet.java
  - src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java
  - src/main/java/com/datn/financeapp/auth/service/AuthService.java
  - src/main/java/com/datn/financeapp/common/wallet/WalletMinimal.java
  - src/main/java/com/datn/financeapp/common/wallet/WalletMinimalRepository.java
  - src/test/java/com/datn/financeapp/auth/AuthRegisterLoginIntegrationTest.java
  - src/test/java/com/datn/financeapp/auth/AuthLoginLockoutIntegrationTest.java
  - src/test/java/com/datn/financeapp/auth/AuthProfilePasswordIntegrationTest.java
  - src/test/java/com/datn/financeapp/auth/AuthRefreshRotationIntegrationTest.java
  - src/test/java/com/datn/financeapp/auth/AuthIdempotencyRateLimitEndToEndTest.java
autonomous: true
requirements: []

must_haves:
  truths:
    - "Package common/wallet/ không còn tồn tại trên đĩa"
    - "AuthService.register() vẫn tạo đúng 1 ví Tiền mặt số dư 0 trong cùng transaction với insert User"
    - "Toàn bộ 35 test Phase 1 (5 file integration test auth) chạy xanh sau khi đổi entity"
  artifacts:
    - path: "src/main/java/com/datn/financeapp/wallet/entity/Wallet.java"
      provides: "Entity đầy đủ cột bảng wallets, thay thế WalletMinimal"
    - path: "src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java"
      provides: "Repository JPA cho Wallet, có method countByUserIdAndIsDeletedFalse dùng lại ở AuthService.getMe"
  key_links:
    - from: "src/main/java/com/datn/financeapp/auth/service/AuthService.java"
      to: "src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java"
      via: "field injection WalletRepository thay cho WalletMinimalRepository"
      pattern: "WalletRepository walletRepository"
---

<objective>
Thay thế hẳn `common/wallet/WalletMinimal` (entity tạm thời Phase 1) bằng `wallet/entity/Wallet` đầy đủ, theo D-26. Đây là việc bắt buộc đầu tiên của Phase 2 (wave 1) — mọi plan sau (wallet CRUD, transfer, category) đều xây trên entity `Wallet` mới, không phải `WalletMinimal`.

Purpose: Tránh tình trạng hai entity JPA cùng ánh xạ bảng `wallets` tồn tại song song — Hibernate có thể giữ hai bản sao cùng một ví trong persistence context, sửa qua entity này thì entity kia không thấy, gây lỗi âm thầm khó truy khi Phase 2 xây thêm nghiệp vụ lên trên.

Output: Package `wallet/` mới với `Wallet` entity + `WalletRepository` tối thiểu (đủ cho AuthService dùng lại), `AuthService` chuyển sang dùng entity mới, `common/wallet/` bị xoá hoàn toàn, 5 file test Phase 1 cập nhật import/field, toàn bộ 35 test Phase 1 xanh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-vi-danh-muc/02-CONTEXT.md
@.planning/phases/02-vi-danh-muc/02-PATTERNS.md

# File nguồn thật cần đọc trước khi sửa (không suy đoán)
@src/main/java/com/datn/financeapp/common/wallet/WalletMinimal.java
@src/main/java/com/datn/financeapp/common/wallet/WalletMinimalRepository.java
@src/main/java/com/datn/financeapp/auth/service/AuthService.java
@src/main/java/com/datn/financeapp/auth/entity/User.java
</context>

<interfaces>
<!-- Cấu trúc entity Wallet mới — copy nguyên trạng từ WalletMinimal.java, đổi tên class,
     giữ đúng annotation bpchar(7) cho color (cạm bẫy Hibernate ddl-auto=validate đã xác nhận
     ở Phase 1). Cột đối chiếu từ db/migration/V1__nen_tang.sql dòng 87-113: -->

```java
// wallet/entity/Wallet.java — cấu trúc đích
@Entity
@Table(name = "wallets")
@Getter @Setter @Builder @NoArgsConstructor @AllArgsConstructor
public class Wallet {
    @Id
    private UUID id;

    @Column(name = "user_id")
    private UUID userId;

    @Column(name = "group_id")
    private UUID groupId;

    @Column(name = "name", nullable = false)
    private String name;

    @Column(name = "type", nullable = false)
    private String type;

    @Column(name = "initial_balance", nullable = false)
    private Long initialBalance;

    @Column(name = "current_balance", nullable = false)
    private Long currentBalance;

    @Column(name = "include_in_total", nullable = false)
    private Boolean includeInTotal;

    @Column(name = "icon")
    private String icon;

    @Column(name = "color", columnDefinition = "bpchar(7)")
    private String color;

    @Column(name = "sort_order", nullable = false)
    private Integer sortOrder;

    @Column(name = "is_deleted", nullable = false)
    private Boolean isDeleted;

    @Column(name = "created_at", nullable = false)
    private Instant createdAt;
}
```

`WalletRepository` tối thiểu ở plan này chỉ cần `extends JpaRepository<Wallet, UUID>` cộng
`long countByUserIdAndIsDeletedFalse(UUID userId)` (dùng lại y hệt tên method cũ ở
`WalletMinimalRepository`, để `AuthService.getMe()` chỉ đổi type khai báo, không đổi logic).
Plan 02-02 (wave 2) sẽ MỞ RỘNG interface này thêm các method CRUD/quyền — không tạo file mới,
chỉ thêm method vào file đã có ở plan này.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tạo entity Wallet + WalletRepository tối thiểu, xoá common/wallet/</name>
  <read_first>
    - src/main/java/com/datn/financeapp/common/wallet/WalletMinimal.java (copy nguyên trạng, đổi tên class)
    - src/main/java/com/datn/financeapp/common/wallet/WalletMinimalRepository.java (copy method countByUserIdAndIsDeletedFalse)
    - db/migration/V1__nen_tang.sql dòng 87-113 (đối chiếu đúng cột, đúng kiểu, đúng CHECK)
    - src/main/java/com/datn/financeapp/auth/entity/User.java (mẫu cấu trúc entity chuẩn dự án: @Getter @Setter @Builder @NoArgsConstructor @AllArgsConstructor, id set ở tầng Service bằng UUID.randomUUID())
  </read_first>
  <action>
    Tạo `src/main/java/com/datn/financeapp/wallet/entity/Wallet.java`: copy toàn bộ nội dung
    `WalletMinimal.java`, đổi `package com.datn.financeapp.common.wallet;` thành
    `package com.datn.financeapp.wallet.entity;`, đổi tên class `WalletMinimal` → `Wallet`. Giữ
    nguyên annotation `@Column(name = "color", columnDefinition = "bpchar(7)")` — không được bỏ
    `columnDefinition`, nếu không Hibernate `ddl-auto=validate` sẽ báo lệch kiểu cột lúc khởi
    động (Postgres báo physical type `bpchar` không phải `char`/`varchar`). Giữ đủ 12 field:
    `id, userId, groupId, name, type, initialBalance, currentBalance, includeInTotal, icon,
    color, sortOrder, isDeleted, createdAt` — đúng khớp `WalletMinimal` hiện tại (D-26 nói "bổ
    sung sortOrder" nhưng field này đã tồn tại sẵn trong `WalletMinimal`, chỉ cần giữ lại, không
    cần thêm field mới nào ở task này).

    Tạo `src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java`:
    ```java
    package com.datn.financeapp.wallet.repository;

    import com.datn.financeapp.wallet.entity.Wallet;
    import java.util.UUID;
    import org.springframework.data.jpa.repository.JpaRepository;

    public interface WalletRepository extends JpaRepository<Wallet, UUID> {

        long countByUserIdAndIsDeletedFalse(UUID userId);
    }
    ```
    (Interface này sẽ được MỞ RỘNG thêm method ở plan 02-02 — không cần tạo thêm gì khác ở đây.)

    Xoá hẳn hai file `src/main/java/com/datn/financeapp/common/wallet/WalletMinimal.java` và
    `src/main/java/com/datn/financeapp/common/wallet/WalletMinimalRepository.java`. Sau khi xoá
    thư mục `common/wallet/` phải rỗng — xoá luôn thư mục nếu hệ điều hành để lại thư mục rỗng.
  </action>
  <acceptance_criteria>
    - `find src/main/java/com/datn/financeapp/common/wallet -type f` không trả về file nào (thư mục không còn tồn tại hoặc rỗng)
    - `grep -c "columnDefinition = \"bpchar(7)\"" src/main/java/com/datn/financeapp/wallet/entity/Wallet.java` trả về `1`
    - `grep -c "class Wallet " src/main/java/com/datn/financeapp/wallet/entity/Wallet.java` trả về `1`, `grep -rn "class WalletMinimal" src/` trả về rỗng
    - `grep -c "countByUserIdAndIsDeletedFalse" src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java` trả về ít nhất `1`
  </acceptance_criteria>
  <done>File `Wallet.java` tồn tại với đủ 12 field khớp bảng `wallets`, `WalletRepository.java` tồn tại với method `countByUserIdAndIsDeletedFalse`, `common/wallet/` không còn file nào.</done>
</task>

<task type="auto">
  <name>Task 2: Sửa AuthService + 5 file test Phase 1, chạy lại toàn bộ 35 test xanh</name>
  <read_first>
    - src/main/java/com/datn/financeapp/auth/service/AuthService.java (toàn bộ file — sửa dòng 25-26 import, dòng 62 field, dòng 94-107 register(), dòng 239 getMe())
    - src/test/java/com/datn/financeapp/auth/AuthRegisterLoginIntegrationTest.java (import dòng 11, field dòng 92, deleteAll() dòng 100, assertion dòng 127-133 dùng walletMinimalRepository.findAll())
    - src/test/java/com/datn/financeapp/auth/AuthLoginLockoutIntegrationTest.java (import dòng 12, field dòng 91, deleteAll() dòng 103)
    - src/test/java/com/datn/financeapp/auth/AuthProfilePasswordIntegrationTest.java (import dòng 20, field dòng 63, deleteAll() dòng 78)
    - src/test/java/com/datn/financeapp/auth/AuthRefreshRotationIntegrationTest.java (import dòng 12, field dòng 96, deleteAll() dòng 104)
    - src/test/java/com/datn/financeapp/auth/AuthIdempotencyRateLimitEndToEndTest.java (import dòng 8, field dòng 66, deleteAll() dòng 78)
  </read_first>
  <action>
    Trong `AuthService.java`:
    - Xoá 2 dòng import `com.datn.financeapp.common.wallet.WalletMinimalRepository` và
      `com.datn.financeapp.common.wallet.WalletMinimal` (dòng 25-26). Thêm import
      `com.datn.financeapp.wallet.entity.Wallet` và `com.datn.financeapp.wallet.repository.WalletRepository`.
    - Đổi field `private final WalletMinimalRepository walletMinimalRepository;` (dòng 62) thành
      `private final WalletRepository walletRepository;`.
    - Trong `register()` (dòng 94-107): đổi `WalletMinimal cashWallet = WalletMinimal.builder()`
      thành `Wallet cashWallet = Wallet.builder()`, giữ nguyên toàn bộ các field đã set
      (`id, userId, groupId(null), name("Tiền mặt"), type("cash"), initialBalance(0L),
      currentBalance(0L), includeInTotal(true), sortOrder(0), isDeleted(false), createdAt(now)`).
      Đổi `walletMinimalRepository.save(cashWallet)` thành `walletRepository.save(cashWallet)`.
    - Trong `getMe()` (dòng 239): đổi
      `walletMinimalRepository.countByUserIdAndIsDeletedFalse(userId)` thành
      `walletRepository.countByUserIdAndIsDeletedFalse(userId)`.

    Trong cả 5 file test, áp dụng đúng 3 chỗ đổi (chỉ đổi type khai báo/import, KHÔNG đổi logic
    assertion hay thứ tự dọn bảng):
    - Đổi `import com.datn.financeapp.common.wallet.WalletMinimalRepository;` thành
      `import com.datn.financeapp.wallet.repository.WalletRepository;`.
    - Đổi field `private WalletMinimalRepository walletMinimalRepository;` thành
      `private WalletRepository walletRepository;`.
    - Đổi mọi lời gọi `walletMinimalRepository.` thành `walletRepository.` (bao gồm
      `walletMinimalRepository.deleteAll()` trong `cleanTables()`, và ở
      `AuthRegisterLoginIntegrationTest.java` cả đoạn assertion
      `walletMinimalRepository.findAll().stream()...getUserId()/getName()/getCurrentBalance()`
      dòng 127-133 — các getter này tồn tại y hệt trên `Wallet` nên không cần đổi logic, chỉ đổi
      tên biến/type).

    Sau khi sửa xong toàn bộ, chạy `mvn test` (Testcontainers PostgreSQL thật, không mock) và
    xác nhận đủ 35 test của Phase 1 (5 file integration test này cộng các test khác đã có từ
    Plan 01-06) đều PASS, không có test nào bị bỏ qua hay lỗi biên dịch.
  </action>
  <acceptance_criteria>
    - `grep -rn "WalletMinimal" src/` trả về rỗng (không còn tham chiếu nào trong toàn bộ codebase)
    - `grep -c "walletRepository" src/main/java/com/datn/financeapp/auth/service/AuthService.java` trả về ít nhất `3` (field + register + getMe)
    - `mvn -q test` exit code 0
    - Tổng số test chạy trong báo cáo Surefire (`target/surefire-reports/*.txt` tổng dòng `Tests run`) khớp hoặc lớn hơn 35, với `Failures: 0, Errors: 0` trên toàn bộ
  </acceptance_criteria>
  <done>AuthService và 5 file test biên dịch thành công, không còn tham chiếu WalletMinimal ở bất kỳ đâu, `mvn test` chạy toàn bộ suite Phase 1 xanh hoàn toàn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Không có boundary mới ở plan này | Plan chỉ thay thế entity nội bộ (refactor thuần), không thêm endpoint, không thay đổi input người dùng xử lý |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `Wallet` entity thay `WalletMinimal` | accept | Không đổi ràng buộc CSDL (CHECK/FK ở V1 giữ nguyên), chỉ đổi tên class Java — không phát sinh bề mặt tấn công mới |
| T-02-02 | Repudiation/Integrity | Hai entity cùng ánh xạ `wallets` nếu sót sửa | mitigate | Task 2 bắt buộc `grep -rn "WalletMinimal" src/` trả rỗng trước khi coi plan hoàn thành — loại trừ khả năng Hibernate giữ hai persistence context khác nhau cho cùng bảng |
</threat_model>

<verification>
1. `find src/main/java/com/datn/financeapp/common/wallet -type f 2>/dev/null` → không có kết quả
2. `grep -rn "WalletMinimal" src/` → không có kết quả
3. `mvn -q test` → exit 0, toàn bộ test Phase 1 (5 file integration auth + các test khác) PASS
4. Đọc lại `AuthService.register()` xác nhận vẫn tạo đúng 1 `Wallet` "Tiền mặt" `currentBalance=0` trong cùng `@Transactional` với insert `User`
</verification>

<success_criteria>
- Package `common/wallet/` không còn tồn tại
- `wallet/entity/Wallet.java` và `wallet/repository/WalletRepository.java` tồn tại, đúng cấu trúc cột bảng `wallets`
- `AuthService` dùng `WalletRepository`/`Wallet`, không còn `WalletMinimal`
- 5 file test Phase 1 biên dịch và chạy xanh với entity mới
- `mvn test` toàn bộ suite Phase 1 xanh 100% (không giảm test, không lỗi biên dịch)
</success_criteria>

<output>
After completion, create `.planning/phases/02-vi-danh-muc/02-01-SUMMARY.md`
</output>
