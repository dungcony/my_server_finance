---
phase: 02-vi-danh-muc
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/main/java/com/datn/financeapp/category/entity/Category.java
  - src/main/java/com/datn/financeapp/category/entity/CategoryGroup.java
  - src/main/java/com/datn/financeapp/category/entity/Icon.java
  - src/main/java/com/datn/financeapp/category/repository/CategoryRepository.java
  - src/main/java/com/datn/financeapp/category/repository/CategoryGroupRepository.java
  - src/main/java/com/datn/financeapp/category/repository/IconRepository.java
  - src/main/java/com/datn/financeapp/category/service/CategoryService.java
  - src/main/java/com/datn/financeapp/category/controller/CategoryController.java
  - src/main/java/com/datn/financeapp/category/dto/CreateCategoryRequest.java
  - src/main/java/com/datn/financeapp/category/dto/UpdateCategoryRequest.java
  - src/main/java/com/datn/financeapp/category/dto/CategoryResponse.java
  - src/main/java/com/datn/financeapp/category/dto/CategoryDetailResponse.java
  - src/main/java/com/datn/financeapp/category/dto/CategoryGroupResponse.java
  - src/main/java/com/datn/financeapp/category/dto/IconResponse.java
  - src/main/java/com/datn/financeapp/category/dto/IconGroupResponse.java
  - src/main/java/com/datn/financeapp/category/dto/ReorderCategoriesRequest.java
  - src/test/java/com/datn/financeapp/category/CategoryTreeIntegrationTest.java
  - src/test/java/com/datn/financeapp/category/CategoryAccessControlIntegrationTest.java
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06]

must_haves:
  truths:
    - "User xem danh sách danh mục dạng cây 2 cấp, gồm cả danh mục hệ thống lẫn danh mục riêng của mình"
    - "User tạo danh mục con cấp ba bị từ chối với mã MAX_DEPTH_EXCEEDED"
    - "User tạo danh mục con khác type với cha bị từ chối với mã TYPE_MISMATCH_WITH_PARENT"
    - "User xoá danh mục còn con bị từ chối với mã CHILD_CATEGORIES_EXIST"
    - "Danh mục hệ thống (user_id null) không sửa/xoá được — SYSTEM_CATEGORY_NOT_EDITABLE/SYSTEM_CATEGORY_NOT_DELETABLE"
    - "User xem kho icon lọc theo icon_group/search, và danh sách category_groups cố định"
  artifacts:
    - path: "src/main/java/com/datn/financeapp/category/service/CategoryService.java"
      provides: "CRUD danh mục 2 cấp + validate 5 ràng buộc của api/03-DANH-MUC.md mục 3"
    - path: "src/main/java/com/datn/financeapp/category/repository/CategoryRepository.java"
      provides: "Repository gọi fn_category_tree qua native query, dùng lại ở Phase 3/4"
  key_links:
    - from: "src/main/java/com/datn/financeapp/category/repository/CategoryRepository.java"
      to: "fn_category_tree (V6__va_loi_bao_mat.sql)"
      via: "native @Query SELECT category_id FROM fn_category_tree(:categoryId)"
      pattern: "fn_category_tree"
---

<objective>
Xây dựng CRUD danh mục hai cấp gắn biểu tượng (CAT-01 đến CAT-06): xem cây danh mục, tạo cha/con, sửa, xoá (mềm), sắp xếp, xem nhóm lớn cố định và kho biểu tượng. Bọc sẵn `fn_category_tree` thành 1 repository method dùng chung ngay từ phase này để Phase 3 (cộng gộp danh mục con) và Phase 4 (ngân sách/báo cáo) dùng lại, không viết lại điều kiện lọc cây ở nơi khác.

Purpose: Danh mục là điều kiện tiên quyết của module giao dịch (Phase 3) — mọi giao dịch expense/income phải gán vào một danh mục hợp lệ thuộc đúng type.

Output: `category/` package đầy đủ — entity/repository/service/controller/dto cho `categories`, `category_groups`, `icons`; test cây 2 cấp (D-28 mục 4) + test quyền truy cập.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-vi-danh-muc/02-CONTEXT.md
@.planning/phases/02-vi-danh-muc/02-PATTERNS.md
@../../api/03-DANH-MUC.md

# Pattern nguồn (Phase 1, dùng lại nguyên vẹn)
@src/main/java/com/datn/financeapp/auth/controller/AuthController.java
@src/main/java/com/datn/financeapp/auth/service/AuthService.java
@src/main/java/com/datn/financeapp/common/response/ApiResponse.java
@src/main/java/com/datn/financeapp/common/exception/BusinessException.java
@src/main/java/com/datn/financeapp/common/security/SecurityContextUtil.java
@src/main/java/com/datn/financeapp/common/idempotency/Idempotent.java
</context>

<interfaces>
<!-- Schema thật — db/migration/V1__nen_tang.sql dòng 178-217 (categories), 134-154 (icons),
     160-172 (category_groups). fn_category_tree — V6__va_loi_bao_mat.sql dòng 96-108 (đã lọc
     is_deleted, dùng lại, KHÔNG tự viết điều kiện lọc cây khác). -->

```java
// category/repository/CategoryRepository.java — method bắt buộc, dùng lại ở Phase 3/4
@Query(value = "SELECT category_id FROM fn_category_tree(:categoryId)", nativeQuery = true)
List<UUID> findCategoryTree(@Param("categoryId") UUID categoryId);
```

<!-- Ràng buộc DB đã có sẵn (KHÔNG cần tự implement lại logic đệ quy ở Java) —
     db/migration/V1__nen_tang.sql dòng 229-292:
     - trigger fn_categories_validate: chặn 3 cấp, chặn con khác type cha, tự set
       category_group_id của con = category_group_id của cha
     - trigger fn_categories_block_demote: chặn danh mục đang có con bị đổi thành con
     Service NÊN kiểm tra trước (1 lần SELECT cha rồi so sánh) để trả đúng mã lỗi nghiệp vụ
     thay vì lộ lỗi CHECK/trigger thô, nhưng KHÔNG bắt buộc phải bắt mọi exception DB — vẫn nên
     có catch fallback nếu người dùng lách qua được validate Service (race condition hiếm). -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Category/CategoryGroup/Icon entity + repository + CategoryService</name>
  <read_first>
    - db/migration/V1__nen_tang.sql dòng 128-293 (bảng icons, category_groups, categories, 2 trigger validate)
    - db/migration/V6__va_loi_bao_mat.sql dòng 96-108 (fn_category_tree)
    - ../../api/03-DANH-MUC.md mục 1 (cây 2 cấp, filter type/as_tree/roots_only), mục 3 (5 ràng buộc tạo danh mục + mã lỗi), mục 4 (sửa — TYPE_NOT_EDITABLE, CATEGORY_HAS_CHILDREN khi đổi parent), mục 5 (xoá — 4 quy tắc + replacement_category_id), mục 6 (reorder theo phạm vi 1 cấp), mục 7 (category-groups chỉ đọc), mục 8 (icons — icon_group/search)
    - src/main/java/com/datn/financeapp/auth/entity/User.java (mẫu cấu trúc entity)
    - src/main/java/com/datn/financeapp/auth/service/AuthService.java dòng 75-110 (mẫu @Transactional, build entity, throw BusinessException)
  </read_first>
  <behavior>
    - Test 1: `POST /categories` tạo danh mục con của một danh mục đã là con (cấp 3) → 400 `MAX_DEPTH_EXCEEDED`
    - Test 2: `POST /categories` tạo danh mục con với `type` khác cha → 400 `TYPE_MISMATCH_WITH_PARENT`
    - Test 3: `POST /categories` tạo danh mục cha không kèm `category_group_id` → 400 `CATEGORY_GROUP_REQUIRED`
    - Test 4: `DELETE /categories/{id}` khi còn con → 409 `CHILD_CATEGORIES_EXIST`
    - Test 5: `PATCH /categories/{id}` trên danh mục hệ thống (`user_id` null) → 403 `SYSTEM_CATEGORY_NOT_EDITABLE`
    - Test 6: `DELETE /categories/{id}` trên danh mục hệ thống → 403 `SYSTEM_CATEGORY_NOT_DELETABLE`
  </behavior>
  <action>
    Tạo 3 entity theo mẫu `User.java` (`@Entity @Table @Getter @Setter @Builder
    @NoArgsConstructor @AllArgsConstructor`, id set ở Service bằng `UUID.randomUUID()`):
    - `category/entity/Category.java` — khớp bảng `categories`: `id, userId, parentCategoryId,
      categoryGroupId, name, type, iconId, color(columnDefinition="bpchar(7)"), sortOrder,
      isDeleted, createdAt`
    - `category/entity/CategoryGroup.java` — khớp bảng `category_groups`: `id, name, iconId,
      color(columnDefinition="bpchar(7)"), sortOrder`
    - `category/entity/Icon.java` — khớp bảng `icons`: `id, code, displayName, pathData,
      iconGroup, searchKeywords, sortOrder, isActive`

    Tạo `category/repository/CategoryRepository.java` (`extends JpaRepository<Category, UUID>`):
    - `findCategoryTree(UUID categoryId)` → `List<UUID>` — native query gọi `fn_category_tree`
      như mẫu ở `<interfaces>`, **bắt buộc viết method này** dù Phase 2 chưa dùng tới việc cộng
      gộp (Phase 3/4 sẽ gọi lại)
    - `findByIdAndVisibleToUser(UUID id, UUID currentUser)` → `Optional<Category>` — điều kiện
      `(user_id IS NULL OR user_id = :currentUser) AND NOT is_deleted` (danh mục không có khái
      niệm nhóm — không áp dụng D-27 vế group vì `categories.user_id` không liên kết `group_id`
      theo schema V1; danh mục hệ thống `user_id IS NULL` luôn hiển thị cho mọi người theo đúng
      `api/03-DANH-MUC.md` mục 9 "Danh mục mặc định, nhóm lớn, biểu tượng: Mọi người đã đăng nhập")
    - `findAllForUser(UUID currentUser, String type, Boolean rootsOnly)` → `List<Category>`
    - `existsByParentCategoryIdAndIsDeletedFalse(UUID parentId)` → `boolean` (cho
      `CHILD_CATEGORIES_EXIST`)
    - `existsByCategoryGroupIdAndIsDeletedFalse` không cần ở đây — dùng cho `CATEGORY_HAS_BUDGET`
      thuộc Phase 4, KHÔNG tự bịa thêm ở plan này
    - `countTransactionsByCategoryId` — dùng native `SELECT COUNT(*) FROM transactions WHERE
      category_id = :id AND NOT is_deleted` qua `JdbcTemplate` trong Service (giống mẫu
      `AuthService.getMe()`), không cần thêm method repository riêng cho việc này nếu dùng
      `JdbcTemplate` trực tiếp trong `CategoryService`

    Tạo `category/repository/CategoryGroupRepository.java`
    (`extends JpaRepository<CategoryGroup, UUID>`, thêm `List<CategoryGroup>
    findAllByOrderBySortOrderAsc()`), `category/repository/IconRepository.java`
    (`extends JpaRepository<Icon, UUID>`, thêm method lọc `icon_group`/`search`, ví dụ
    `List<Icon> findByIconGroupAndIsActiveTrue(String iconGroup)` và một `@Query` tìm theo
    `LOWER(display_name) LIKE %:search% OR LOWER(search_keywords) LIKE %:search%` khi có
    `search`).

    Tạo `category/service/CategoryService.java` (`@Service @RequiredArgsConstructor`,
    `@Transactional` từng public method):
    - `list(userId, type, asTree, rootsOnly)` → `findAllForUser`, nếu `asTree=true` (mặc định)
      thì group con vào cha ở tầng Java (danh sách phẳng đã có `parentCategoryId`, gom nhóm bằng
      `Collectors.groupingBy`), trả `is_system = (userId == null)` theo đúng field response
    - `detail(userId, categoryId)` → `findByIdAndVisibleToUser` không thấy → 404; kèm `stats`
      (`transaction_count, expense_this_month, has_budget` — `has_budget` Phase 2 luôn `false`
      vì bảng `budgets` chưa có service, không tự bịa logic, ghi rõ comment "Phase 4 sẽ nối
      thật")
    - `create(userId, req)`: validate đúng 5 ràng buộc theo thứ tự `api/03-DANH-MUC.md` mục 3 —
      (1) nếu có `parentCategoryId`: load cha, cha phải có `parentCategoryId == null` (nếu
      không → `MAX_DEPTH_EXCEEDED` 400), (2) cha.type phải bằng `req.type()` (nếu không →
      `TYPE_MISMATCH_WITH_PARENT` 400), (3) nếu KHÔNG có `parentCategoryId` (tạo cha) thì
      `req.categoryGroupId()` bắt buộc khác null (nếu null → `CATEGORY_GROUP_REQUIRED` 400),
      (4) check trùng tên cùng cấp/cùng type/cùng chủ sở hữu (409 `CATEGORY_NAME_EXISTS`), (5)
      `iconId` phải tồn tại và `isActive=true` (400 `INVALID_ICON`); nếu là con thì
      **sao chép `categoryGroupId` từ cha** (dù trigger DB `fn_categories_validate` cũng tự làm
      việc này, Service vẫn set tường minh để response trả đúng ngay không cần load lại); set
      `userId = currentUser`, `sortOrder = max hiện có trong cùng cấp + 1`; bọc insert trong
      try/catch bắt exception CSDL (trigger ném ra qua `ERRCODE = check_violation`) làm fallback
      nếu Service check bị lách (race condition hiếm), map sang `BusinessException` tương ứng
    - `update(userId, categoryId, req)`: load danh mục — không tìm thấy → 404; nếu
      `userId(category) == null` (hệ thống) → `SYSTEM_CATEGORY_NOT_EDITABLE` 403; nếu
      `userId(category) != currentUser` → 404 (không lộ tồn tại); DTO `UpdateCategoryRequest`
      KHÔNG có field `type` (chặn `TYPE_NOT_EDITABLE` ở tầng hợp đồng, tương tự cách
      `UpdateWalletRequest` chặn `current_balance`); khi đổi `parentCategoryId`: nếu danh mục
      đang có con (`existsByParentCategoryIdAndIsDeletedFalse`) → `CATEGORY_HAS_CHILDREN` 409;
      còn lại vẫn phải thoả ràng buộc 2 tầng như lúc tạo; khi cha đổi thì cập nhật lại
      `categoryGroupId` theo cha mới
    - `delete(userId, categoryId, replacementCategoryId)`: không tìm thấy → 404; hệ thống →
      `SYSTEM_CATEGORY_NOT_DELETABLE` 403; còn con → `CHILD_CATEGORIES_EXIST` 409; còn giao dịch
      mà không có `replacementCategoryId` → `CATEGORY_HAS_TRANSACTIONS` 409 (đếm qua
      `JdbcTemplate`); có `replacementCategoryId` thì validate cùng `type` rồi
      `UPDATE transactions SET category_id = :replacement WHERE category_id = :old` trước khi
      xoá mềm; **`has_budget`/`CATEGORY_HAS_BUDGET` Phase 2 bỏ qua** vì bảng `budgets` chưa có
      service — ghi rõ comment TODO cho Phase 4, không tự bịa kiểm tra rỗng
    - `reorder(userId, parentCategoryId, req)`: tương tự `WalletService.reorder`, chỉ update
      trong phạm vi 1 cấp (`parent_category_id = :parentCategoryId` hoặc `IS NULL` khi tham số
      null), chỉ áp dụng cho danh mục thuộc `currentUser` (không reorder được danh mục hệ thống)
    - `listCategoryGroups()` → `categoryGroupRepository.findAllByOrderBySortOrderAsc()`, map
      sang `CategoryGroupResponse`
    - `listIcons(iconGroup, search)` → gọi `IconRepository`, gom theo `icon_group` thành cấu
      trúc `IconGroupResponse` (`groupCode, groupName, items`) — `groupName` tra từ hằng số hoặc
      map cố định theo `api/03-DANH-MUC.md` mục 8 (8 nhóm: an_uong, di_lai, mua_sam, giai_tri,
      suc_khoe, hoc_tap, tai_chinh, khac — dùng tên tiếng Việt hiển thị tương ứng)

    Tạo DTO: `CreateCategoryRequest(name, type, iconId, color, parentCategoryId,
    categoryGroupId)`, `UpdateCategoryRequest(name, iconId, color, categoryGroupId,
    parentCategoryId)` (KHÔNG có `type`), `CategoryResponse` (record đệ quy có field
    `children: List<CategoryResponse>` cho dạng cây), `CategoryDetailResponse` (thêm `stats`),
    `CategoryGroupResponse(id, name, color, icon, sortOrder)`, `IconResponse(id, code,
    displayName, pathData)`, `IconGroupResponse(groupCode, groupName, items)`,
    `ReorderCategoriesRequest(parentCategoryId, sortOrder)`.
  </action>
  <acceptance_criteria>
    - `grep -c "fn_category_tree" src/main/java/com/datn/financeapp/category/repository/CategoryRepository.java` trả về ít nhất `1`
    - `grep -c "type" src/main/java/com/datn/financeapp/category/dto/UpdateCategoryRequest.java` trả về `0`
    - `grep -c "MAX_DEPTH_EXCEEDED\|TYPE_MISMATCH_WITH_PARENT\|CATEGORY_GROUP_REQUIRED\|CATEGORY_NAME_EXISTS\|INVALID_ICON\|SYSTEM_CATEGORY_NOT_EDITABLE\|SYSTEM_CATEGORY_NOT_DELETABLE\|CATEGORY_HAS_CHILDREN\|CHILD_CATEGORIES_EXIST\|CATEGORY_HAS_TRANSACTIONS" src/main/java/com/datn/financeapp/category/service/CategoryService.java` trả về ít nhất `10`
    - Test behavior 1-6 ở `<behavior>` PASS (viết ở Task 2)
  </acceptance_criteria>
  <done>CategoryService triển khai đủ list/detail/create/update/delete/reorder/listCategoryGroups/listIcons đúng validate api/03-DANH-MUC.md mục 1-8, fn_category_tree đã bọc thành repository method dùng lại được ở Phase 3/4.</done>
</task>

<task type="auto">
  <name>Task 2: CategoryController + test cây 2 cấp (D-28 mục 4) + test quyền truy cập</name>
  <read_first>
    - src/main/java/com/datn/financeapp/auth/controller/AuthController.java (mẫu controller)
    - src/test/java/com/datn/financeapp/auth/AuthRegisterLoginIntegrationTest.java (mẫu Testcontainers + MockMvc + NoRateLimitConfig)
    - ../../api/03-DANH-MUC.md mục 1-8 (toàn bộ endpoint)
    - .planning/phases/02-vi-danh-muc/02-CONTEXT.md phần D-28 mục 4 (test cây danh mục)
  </read_first>
  <action>
    Tạo `category/controller/CategoryController.java` (`@RestController
    @RequiredArgsConstructor`):
    - `GET /categories` (`type, as_tree, roots_only`) → `categoryService.list(...)`
    - `GET /categories/{id}` → `categoryService.detail(userId, id)`
    - `POST /categories` — gắn `@Idempotent` (POST-tạo-mới) → 201
    - `PATCH /categories/{id}` — KHÔNG gắn `@Idempotent`
    - `DELETE /categories/{id}` (`replacement_category_id`) — KHÔNG gắn `@Idempotent`
    - `PATCH /categories/reorder` — KHÔNG gắn `@Idempotent`
    - `GET /category-groups` → `categoryService.listCategoryGroups()`
    - `GET /icons` (`icon_group, search`) → `categoryService.listIcons(...)`

    Viết `test/java/com/datn/financeapp/category/CategoryTreeIntegrationTest.java` (D-28 mục 4):
    copy khung Testcontainers từ `AuthRegisterLoginIntegrationTest`. Test đủ 6 behavior của
    Task 1 cộng thêm:
    - `getCategoriesAsTree_returnsSystemAndUserCategoriesInTwoLevelTree()` — xác nhận response
      gồm cả danh mục hệ thống (đã seed sẵn từ V1/V8) lẫn danh mục user vừa tạo, đúng cấu trúc
      `children`
    - `createChildCategory_inheritsParentCategoryGroupId()` — tạo con, xác nhận
      `category_group_id` của con bằng của cha dù không truyền lên

    Viết `test/java/com/datn/financeapp/category/CategoryAccessControlIntegrationTest.java`:
    user A tạo 1 danh mục riêng, user B gọi `GET/PATCH/DELETE /categories/{a_category_id}` phải
    nhận **404** (không phải 403) — vì `findByIdAndVisibleToUser` không có bản ghi thoả điều
    kiện `user_id = B`. Đồng thời test cả hai user đều **thấy được** danh mục hệ thống (GET trả
    200) để xác nhận không chặn nhầm.
  </action>
  <acceptance_criteria>
    - `grep -c "@Idempotent" src/main/java/com/datn/financeapp/category/controller/CategoryController.java` trả về đúng `1`
    - `mvn -q test -Dtest=CategoryTreeIntegrationTest` exit code 0
    - `mvn -q test -Dtest=CategoryAccessControlIntegrationTest` exit code 0, case PATCH/DELETE danh mục người khác assert `status().isNotFound()`
  </acceptance_criteria>
  <done>CategoryController expose đủ 8 endpoint theo api/03-DANH-MUC.md, test cây 2 cấp và quyền truy cập PASS qua Testcontainers thật.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → CategoryController | Input `parent_category_id`, `type`, `category_group_id` chưa tin cậy — sai một trong số này có thể phá vỡ cây 2 cấp nếu chỉ dựa vào validate tầng ứng dụng |
| CategoryService → DB trigger | Validate tầng Service là lớp phòng thủ đầu; trigger `fn_categories_validate`/`fn_categories_block_demote` là lớp phòng thủ cuối cùng, không thể bỏ qua |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | `POST /categories` với `parent_category_id` trỏ tới danh mục cấp 2 | mitigate | Service kiểm tra `cha.parentCategoryId == null` trước khi insert (400 `MAX_DEPTH_EXCEEDED`); trigger DB `fn_categories_validate` là lớp chặn cuối nếu Service bị lách |
| T-02-08 | Tampering (mass-assignment) | `UpdateCategoryRequest` | mitigate | DTO không có field `type` — không thể đổi chi thành thu qua PATCH |
| T-02-09 | Information Disclosure | `CategoryRepository.findByIdAndVisibleToUser` | mitigate | User B gọi danh mục riêng của user A trả 404 (test `CategoryAccessControlIntegrationTest`); danh mục hệ thống (`user_id IS NULL`) cố ý hiển thị cho mọi người theo đúng `api/03-DANH-MUC.md` mục 9 |
| T-02-10 | Elevation of Privilege | Sửa/xoá danh mục hệ thống | mitigate | Kiểm tra `userId(category) == null` → 403 `SYSTEM_CATEGORY_NOT_EDITABLE`/`SYSTEM_CATEGORY_NOT_DELETABLE` trước khi cho phép ghi |
</threat_model>

<verification>
1. `mvn -q test -Dtest=CategoryTreeIntegrationTest,CategoryAccessControlIntegrationTest` → exit 0
2. Đọc `CategoryController.java` xác nhận chỉ `POST /categories` có `@Idempotent`
3. Xác nhận `fn_category_tree` được gọi qua repository, không có điều kiện lọc cây tự viết ở nơi khác trong `category/`
</verification>

<success_criteria>
- CAT-01 đến CAT-06 hoạt động đúng theo `api/03-DANH-MUC.md` mục 1-8
- Cây 2 cấp bị chặn đúng ở cả Service lẫn DB trigger, không thể tạo cấp 3
- Danh mục hệ thống không sửa/xoá được
- fn_category_tree bọc thành 1 repository method dùng lại được, không lặp điều kiện lọc
- Test cây 2 cấp (D-28 mục 4) và quyền truy cập PASS qua Testcontainers thật
</success_criteria>

<output>
After completion, create `.planning/phases/02-vi-danh-muc/02-03-SUMMARY.md`
</output>
