---
phase: 02-vi-danh-muc
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java
  - src/main/java/com/datn/financeapp/wallet/service/WalletService.java
  - src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java
  - src/main/java/com/datn/financeapp/wallet/controller/WalletController.java
  - src/main/java/com/datn/financeapp/wallet/dto/TransferRequest.java
  - src/main/java/com/datn/financeapp/wallet/dto/TransferResponse.java
  - src/main/java/com/datn/financeapp/wallet/dto/AdjustBalanceRequest.java
  - src/main/java/com/datn/financeapp/wallet/dto/AdjustBalanceResponse.java
  - src/main/java/com/datn/financeapp/wallet/dto/ReconcileResponse.java
  - src/test/java/com/datn/financeapp/wallet/WalletTransferConcurrencyTest.java
  - src/test/java/com/datn/financeapp/wallet/WalletAdjustBalanceIntegrationTest.java
  - src/test/java/com/datn/financeapp/wallet/WalletTransferIntegrationTest.java
autonomous: true
requirements: [WALLET-06, WALLET-07, WALLET-08]

must_haves:
  truths:
    - "Chuyển tiền giữa 2 ví trong 1 DB transaction tạo đúng một bản ghi type=transfer, trừ đúng ví nguồn cộng đúng ví đích"
    - "Mặc định vẫn cho chuyển dù ví nguồn không đủ số dư; chỉ chặn khi fail_if_insufficient=true"
    - "Hai luồng chuyển tiền đồng thời trên cùng một ví không làm mất cập nhật (lost-update) — số dư cuối đúng tuyệt đối"
    - "Điều chỉnh số dư không ghi đè current_balance — sinh đúng một giao dịch bù source=adjustment; chênh=0 không tạo giao dịch nào"
    - "Sau khi điều chỉnh số dư, gọi lại reconcile vẫn cho difference=0 (matches=true)"
    - "Đối chiếu số dư tính đúng công thức initial_balance + thu - chi - chuyển đi + chuyển đến, ghi log khi lệch, không tự động sửa số nếu auto_fix=false"
    - "Không có quyền trên ví nguồn/đích/ví điều chỉnh trả 404, không phải 403"
  artifacts:
    - path: "src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java"
      provides: "Chuyển tiền + điều chỉnh số dư + đối chiếu, atomic UPDATE, lock theo thứ tự cố định tránh deadlock"
    - path: "src/test/java/com/datn/financeapp/wallet/WalletTransferConcurrencyTest.java"
      provides: "Test D-28 mục 1 — hai luồng chuyển tiền song song, chứng minh không lost-update"
    - path: "src/test/java/com/datn/financeapp/wallet/WalletAdjustBalanceIntegrationTest.java"
      provides: "Test D-28 mục 2 — cả hai chiều điều chỉnh, chênh 0, đối chiếu vẫn khớp sau điều chỉnh"
  key_links:
    - from: "src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java"
      to: "src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java"
      via: "adjustBalance atomic UPDATE current_balance = current_balance + :delta"
      pattern: "current_balance = .*current_balance.*\\+.*:delta"
    - from: "src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java"
      to: "categories (Cập nhật số dư, seed ở V8__dieu_chinh_so_du.sql)"
      via: "tra category hệ thống theo category_group.name='Khác' + icon.code='vi_tien' + đúng type"
      pattern: "vi_tien"
---

<objective>
Hoàn thiện phần rủi ro cao nhất của Phase 2: chuyển tiền giữa hai ví (WALLET-06), điều chỉnh số dư theo kiểm kê thực tế (WALLET-08), và đối chiếu số dư thủ công (WALLET-07). Cả ba đều thao tác trực tiếp lên `current_balance` — phải atomic, đúng thứ tự lock để tránh deadlock, và chống lost-update khi có truy cập đồng thời.

Purpose: Đây là nền tảng nghiệp vụ tài chính cốt lõi của module ví — sai ở đây nghĩa là sai số dư, sai niềm tin của người dùng vào toàn bộ ứng dụng. D-28 yêu cầu tập trung test vào đúng các luồng rủi ro này thay vì phủ đều CRUD.

Output: `WalletTransferService.java` (transfer/adjust-balance/reconcile), 3 endpoint mới trên `WalletController`, test concurrency chứng minh không lost-update, test điều chỉnh số dư đủ 2 chiều + chênh 0 + đối chiếu vẫn khớp sau điều chỉnh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-vi-danh-muc/02-CONTEXT.md
@.planning/phases/02-vi-danh-muc/02-PATTERNS.md
@.planning/notes/2026-08-23-kiem-ke-so-du-vi.md
@../../api/02-VI.md

# Pattern nguồn (Phase 1 + plan 02-02, dùng lại nguyên vẹn)
@src/main/java/com/datn/financeapp/auth/repository/RefreshTokenRepository.java
@src/main/java/com/datn/financeapp/common/response/ApiResponse.java
@src/main/java/com/datn/financeapp/common/exception/BusinessException.java
@src/main/java/com/datn/financeapp/common/security/SecurityContextUtil.java
@src/main/java/com/datn/financeapp/common/idempotency/Idempotent.java
@src/main/java/com/datn/financeapp/wallet/entity/Wallet.java
@src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java
@src/main/java/com/datn/financeapp/wallet/service/WalletService.java
@src/main/java/com/datn/financeapp/wallet/controller/WalletController.java
</context>

<interfaces>
<!-- Schema thật — bắt buộc đối chiếu trước khi code (CORE-11):
     db/migration/V2__giao_dich.sql dòng 89-134 — bảng transactions:
       ck_txn_amount CHECK (amount > 0) — KHÔNG được insert amount=0, phải bỏ qua nhánh chênh=0
       ck_txn_shape  CHECK — expense/income bắt buộc category_id, destination_wallet_id NULL;
                              transfer bắt buộc destination_wallet_id, category_id NULL
       ck_txn_source CHECK (source IN ('manual','text','ocr','auto','adjustment')) — V8 đã thêm 'adjustment'
     db/migration/V8__dieu_chinh_so_du.sql dòng 40-44 — transactions.counts_in_report BOOLEAN
       NOT NULL DEFAULT TRUE; dòng 79-87 — 2 danh mục hệ thống "Cập nhật số dư" (category_group
       'Khác', icon 'vi_tien'), một type=expense một type=income, user_id NULL. -->

```sql
-- Tra category hệ thống điều chỉnh số dư theo đúng type cần dùng (chạy 1 lần lúc service khởi
-- tạo hoặc query trực tiếp mỗi lần — Claude's Discretion, KHÔNG hardcode UUID vì id sinh ngẫu
-- nhiên khi migration chạy):
SELECT c.id FROM categories c
JOIN category_groups g ON g.id = c.category_group_id
JOIN icons i ON i.id = c.icon_id
WHERE g.name = 'Khác' AND i.code = 'vi_tien' AND c.type = :type AND c.user_id IS NULL
```

<!-- WalletRepository đã có từ plan 02-02: findByIdForUser(id, currentUser) — dùng lại nguyên
     vẹn cho việc load + kiểm tra quyền trước khi transfer/adjust/reconcile. Plan này CHỈ THÊM
     2 method mới vào file đã có, không tạo file mới: -->
```java
// Lock bi quan theo mẫu RefreshTokenRepository.findActiveByTokenHashForUpdate — thêm vào
// WalletRepository.java đã có từ plan 02-02:
@Lock(LockModeType.PESSIMISTIC_WRITE)
@Query("SELECT w FROM Wallet w WHERE w.id = :id AND NOT w.isDeleted")
Optional<Wallet> findByIdForUpdate(@Param("id") UUID id);

// Atomic UPDATE số dư — KHÔNG load-modify-save (CLAUDE.md backend mục 3):
@Modifying
@Query("UPDATE Wallet w SET w.currentBalance = w.currentBalance + :delta WHERE w.id = :id")
int adjustBalance(@Param("id") UUID id, @Param("delta") long delta);
```

<!-- Công thức đối chiếu — api/02-VI.md mục 10, chỉ tính is_deleted=false:
computed_balance = initial_balance
    + SUM(amount WHERE type='income'  AND wallet_id=:id)
    - SUM(amount WHERE type='expense' AND wallet_id=:id)
    - SUM(amount WHERE type='transfer' AND wallet_id=:id)               -- chuyển ĐI
    + SUM(amount WHERE type='transfer' AND destination_wallet_id=:id)   -- chuyển ĐẾN
-->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WalletTransferService — chuyển tiền (WALLET-06) với lock chống deadlock + chống lost-update</name>
  <read_first>
    - src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java (file đã có từ plan 02-02, MỞ RỘNG thêm 2 method lock/atomic update, không tạo file mới)
    - src/main/java/com/datn/financeapp/wallet/entity/Wallet.java
    - src/main/java/com/datn/financeapp/auth/repository/RefreshTokenRepository.java (mẫu @Lock(PESSIMISTIC_WRITE) dòng 24-26, mẫu @Modifying @Query dòng 31-34)
    - ../../api/02-VI.md mục 8 (chuyển tiền — dữ liệu gửi lên, response 201, việc máy chủ phải làm, fail_if_insufficient, mã lỗi SAME_SOURCE_AND_DESTINATION/INSUFFICIENT_BALANCE)
    - db/migration/V2__giao_dich.sql dòng 89-134 (bảng transactions, ck_txn_shape bắt buộc destination_wallet_id + category_id NULL cho type=transfer)
    - .planning/phases/02-vi-danh-muc/02-CONTEXT.md phần D-27 (điều kiện quyền), D-28 mục 1 (test đồng thời)
  </read_first>
  <behavior>
    - Test 1: `POST /wallets/transfer` giữa 2 ví hợp lệ, đủ số dư → 201, đúng 1 bản ghi `transactions` với `type=transfer`, ví nguồn bị trừ đúng `amount`, ví đích được cộng đúng `amount`
    - Test 2: `source_wallet_id == destination_wallet_id` → 400 `SAME_SOURCE_AND_DESTINATION`
    - Test 3: Ví nguồn thiếu số dư, không gửi `fail_if_insufficient` → 201, vẫn chuyển thành công (số dư ví nguồn thành âm)
    - Test 4: Ví nguồn thiếu số dư, gửi `fail_if_insufficient=true` → 422 `INSUFFICIENT_BALANCE`
    - Test 5 (concurrency, D-28 mục 1): Hai luồng cùng gọi transfer 2 triệu từ ví A (số dư ban đầu 10 triệu) sang ví B, chạy song song qua `ExecutorService` 2 thread + `CountDownLatch` đồng bộ điểm bắt đầu → sau khi cả hai hoàn tất, `wallet_a.current_balance == 6_000_000` và `wallet_b.current_balance == 4_000_000` (10tr - 2×2tr, 0 + 2×2tr) — không được có kết quả nào lệch do lost-update
  </behavior>
  <action>
    Mở rộng `WalletRepository.java` (đã có từ plan 02-02) thêm đúng 2 method theo mẫu ở
    `<interfaces>`: `findByIdForUpdate(UUID id)` dùng `@Lock(LockModeType.PESSIMISTIC_WRITE)` và
    `adjustBalance(UUID id, long delta)` dùng `@Modifying @Query`. Import
    `jakarta.persistence.LockModeType`, `org.springframework.data.jpa.repository.Lock`,
    `org.springframework.data.jpa.repository.Modifying`.

    Tạo `wallet/service/WalletTransferService.java` (`@Service @RequiredArgsConstructor`,
    tách riêng khỏi `WalletService` để tránh transaction self-invocation nếu `WalletController`
    cần gọi cả hai — theo đúng cạm bẫy đã ghi ở `IdempotencyTransactionHelper` Phase 1: KHÔNG
    để method `@Transactional` này bị gọi từ một method `@Transactional` khác trong cùng class).

    Method `transfer(UUID userId, TransferRequest req)`, `@Transactional`:
    1. Nếu `req.sourceWalletId().equals(req.destinationWalletId())` → ném
       `BusinessException("SAME_SOURCE_AND_DESTINATION", 400, "Hai ví phải khác nhau.")` TRƯỚC
       khi lock (không cần khoá nếu request sai ngay từ đầu).
    2. **Khoá theo thứ tự cố định để tránh deadlock**: sắp 2 UUID theo `compareTo()` tăng dần,
       gọi `walletRepository.findByIdForUpdate(idNhỏHơn)` trước, rồi
       `findByIdForUpdate(idLớnHơn)` sau — áp dụng bất kể vai trò nguồn/đích. Đây là điểm mấu
       chốt chống deadlock khi 2 giao dịch A→B và B→A chạy đồng thời (nếu không sắp thứ tự, một
       giao dịch khoá A trước B trong khi giao dịch kia khoá B trước A, dẫn tới deadlock kinh
       điển).
    3. Cả hai `Optional` phải có giá trị VÀ thuộc quyền `userId` (áp D-27: sau khi lock, kiểm
       tra lại `wallet.getUserId().equals(userId) || (wallet.getGroupId() != null && ...)` —
       Phase 2 chỉ có nhánh cá nhân nên điều kiện thực chất là
       `wallet.getUserId().equals(userId)`) — nếu ví nào không thoả, ném
       `BusinessException("NOT_FOUND", 404, "Không tìm thấy ví.")` (không tiết lộ ví có tồn tại
       hay không, kể cả khi ví thuộc người khác).
    4. Nếu `req.failIfInsufficient() == true` và `sourceWallet.getCurrentBalance() < req.amount()`
       → ném `BusinessException("INSUFFICIENT_BALANCE", 422, "Số dư ví không đủ để thực hiện
       giao dịch này.", detail)` với `detail` chứa `current_balance`/`requested_amount` theo mẫu
       `api/00-QUY-UOC-CHUNG.md` mục 4.3. Mặc định (`failIfInsufficient` null hoặc `false`) VẪN
       CHO CHUYỂN dù âm.
    5. Tạo 1 bản ghi `Transaction` (dùng trực tiếp `JdbcTemplate.update` INSERT vào bảng
       `transactions` — module `transaction/` đầy đủ thuộc Phase 3, Phase 2 CHƯA có entity JPA
       cho `transactions`, dùng SQL thô là đúng ranh giới trách nhiệm hiện tại, KHÔNG tự tạo
       entity `Transaction` ở Phase 2): `id=gen_random_uuid() hoặc UUID.randomUUID(), user_id,
       wallet_id=sourceWalletId, destination_wallet_id=destinationWalletId, category_id=NULL,
       type='transfer', amount, date=COALESCE(req.date(), CURRENT_DATE), note, source='manual',
       counts_in_report=true, is_deleted=false, created_at=now(), updated_at=now()`.
    6. Atomic update: `walletRepository.adjustBalance(sourceWalletId, -amount)`,
       `walletRepository.adjustBalance(destinationWalletId, +amount)` — **không**
       `wallet.setCurrentBalance(x); walletRepository.save(wallet)`.
    7. Trả `TransferResponse` gồm `transaction` (id, type, amount, date, sourceWallet{id,name},
       destinationWallet{id,name}, note) và `newBalance{sourceWallet, destinationWallet}` — đọc
       lại số dư mới bằng cách cộng/trừ vào giá trị đã lock trong bước 2 (không cần SELECT lại,
       tránh round-trip thừa).

    Tạo DTO: `TransferRequest(sourceWalletId, destinationWalletId, amount, date, note,
    failIfInsufficient)` — `amount` kiểu `Long`, `@NotNull @Positive`. `TransferResponse` theo
    cấu trúc response mẫu `api/02-VI.md` mục 8.

    Trong `WalletController.java`, thêm `POST /wallets/transfer` — gắn `@Idempotent` (POST-tạo-
    mới theo D-11), `@ResponseStatus(HttpStatus.CREATED)`, gọi
    `walletTransferService.transfer(userId, req)`.

    Viết `test/java/com/datn/financeapp/wallet/WalletTransferIntegrationTest.java` cho Test
    1-4 (happy path + validate), và `test/java/com/datn/financeapp/wallet/
    WalletTransferConcurrencyTest.java` cho Test 5: dùng `ExecutorService.newFixedThreadPool(2)`
    + `CountDownLatch(1)` để hai thread cùng gọi `walletTransferService.transfer(...)` (gọi
    thẳng Service trong cùng JVM qua Spring context — KHÔNG cần qua MockMvc/HTTP — vì mục tiêu
    là chứng minh tầng DB lock hoạt động đúng, không phải test HTTP), mỗi thread `await()`
    latch trước khi gọi để tối đa hoá khả năng đụng độ thật; dùng `Future.get()` để đợi cả hai
    xong; assert số dư cuối bằng cách load lại `Wallet` từ `WalletRepository.findById`.
  </action>
  <acceptance_criteria>
    - `grep -c "PESSIMISTIC_WRITE" src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java` trả về ít nhất `1`
    - `grep -c "currentBalance = w.currentBalance + :delta\|currentBalance = w\.currentBalance +" src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java` trả về ít nhất `1`
    - `grep -c "compareTo" src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java` trả về ít nhất `1` (bằng chứng đã sắp thứ tự khoá)
    - `grep -c "setCurrentBalance" src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java` trả về `0` (không load-modify-save)
    - `grep -c "@Idempotent" src/main/java/com/datn/financeapp/wallet/controller/WalletController.java` (đếm tổng, đã có 1 từ plan 02-02 cho POST /wallets, cộng 1 cho POST /wallets/transfer) trả về ít nhất `2`
    - `mvn -q test -Dtest=WalletTransferIntegrationTest` exit code 0
    - `mvn -q test -Dtest=WalletTransferConcurrencyTest` exit code 0, chạy 5 lần liên tiếp đều PASS (không flaky — verify thủ công bằng `for i in 1 2 3 4 5; do mvn -q test -Dtest=WalletTransferConcurrencyTest || break; done`)
  </acceptance_criteria>
  <done>Chuyển tiền hoạt động atomic trong 1 DB transaction, khoá 2 ví theo thứ tự cố định tránh deadlock, atomic UPDATE số dư không load-modify-save, test đồng thời chứng minh không lost-update chạy ổn định qua nhiều lần.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Điều chỉnh số dư (WALLET-08) + đối chiếu (WALLET-07)</name>
  <read_first>
    - .planning/notes/2026-08-23-kiem-ke-so-du-vi.md (toàn bộ — bối cảnh nghiệp vụ, phân biệt adjust-balance vs reconcile, quyết định cột counts_in_report)
    - db/migration/V8__dieu_chinh_so_du.sql (toàn bộ — cột counts_in_report, giá trị 'adjustment', 2 danh mục hệ thống)
    - ../../api/02-VI.md mục 9 (điều chỉnh số dư — actual_balance, chiều suy từ chênh lệch, response, INVALID_AMOUNT) và mục 10 (đối chiếu — công thức, auto_fix, ghi log khi lệch)
    - src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java (file vừa tạo ở Task 1 — thêm 2 method mới vào cùng file, KHÔNG tạo service riêng vì cùng nhóm "thao tác trực tiếp current_balance")
  </read_first>
  <behavior>
    - Test 1: Ví có `current_balance=450000`, gọi `adjust-balance` với `actual_balance=300000` → 200, `difference=-150000`, `adjusted=true`, tạo đúng 1 giao dịch `type=expense, amount=150000, source=adjustment`, gán danh mục hệ thống "Cập nhật số dư" (type=expense), `current_balance` sau đó = 300000 (đúng bằng actual, đạt được nhờ atomic update -150000 chứ không phải ghi đè)
    - Test 2: Ví có `current_balance=450000`, gọi với `actual_balance=500000` → 200, `difference=50000`, tạo giao dịch `type=income, amount=50000`, `current_balance` sau đó = 500000
    - Test 3: Ví có `current_balance=450000`, gọi với `actual_balance=450000` → 200, `adjusted=false`, `transaction_id=null`, KHÔNG có bản ghi `transactions` mới nào được tạo
    - Test 4: `actual_balance` âm → 400 `INVALID_AMOUNT`
    - Test 5: Sau Test 1 (đã điều chỉnh), gọi `POST /wallets/{id}/reconcile` → `difference=0`, `matches=true` — chứng minh giao dịch bù không phá vỡ công thức đối chiếu
    - Test 6: `counts_in_report=false` được truyền lên → giao dịch bù tạo ra có `counts_in_report=false`; không truyền → mặc định `true`
    - Test 7 (đối chiếu phát hiện lệch): tạo dữ liệu giả lệch bằng cách UPDATE trực tiếp `current_balance` qua JdbcTemplate trong test (mô phỏng bug hệ thống, KHÔNG qua API), gọi reconcile với `auto_fix=false` → `matches=false`, `stored_balance != computed_balance`, và **`current_balance` trong DB KHÔNG bị đổi** (không âm thầm sửa số)
  </behavior>
  <action>
    Trong `WalletTransferService.java` (file đã tạo ở Task 1), thêm 2 method mới:

    `adjustBalance(UUID userId, UUID walletId, AdjustBalanceRequest req)`, `@Transactional`:
    1. `walletRepository.findByIdForUpdate(walletId)` — dùng lock để tránh đọc `current_balance`
       cũ trong lúc có giao dịch khác đang chạy song song trên cùng ví; kiểm tra quyền D-27 sau
       khi lock (`wallet.getUserId().equals(userId)`) — không khớp → `NOT_FOUND` 404.
    2. `req.actualBalance() < 0` → `BusinessException("INVALID_AMOUNT", 400, "Số dư thực tế
       không được âm.")`.
    3. `long difference = req.actualBalance() - wallet.getCurrentBalance();`
    4. Nếu `difference == 0`: trả ngay `AdjustBalanceResponse(walletId, previousBalance,
       actualBalance, 0, false, null, null, null)` — **không insert gì vào `transactions`**
       (bắt buộc vì `ck_txn_amount CHECK (amount > 0)` sẽ tự chặn amount=0, nhưng Service phải
       xử lý tường minh trước khi build câu SQL, không dựa vào DB reject).
    5. Xác định `type = difference < 0 ? "expense" : "income"`, `amount = Math.abs(difference)`.
    6. Tra category hệ thống theo đúng `type` bằng câu SQL ở `<interfaces>` (JOIN
       `category_groups`/`icons`, lọc `g.name = 'Khác' AND i.code = 'vi_tien' AND c.type =
       :type AND c.user_id IS NULL`) — nếu không tìm thấy (dữ liệu seed thiếu) ném
       `IllegalStateException` rõ ràng thay vì NPE âm thầm (đây là lỗi hạ tầng, không phải lỗi
       người dùng, không cần mã BusinessException riêng).
    7. Insert 1 bản ghi `transactions` qua `JdbcTemplate`: `type, amount, category_id=<tra được>,
       wallet_id=walletId, destination_wallet_id=NULL, source='adjustment',
       counts_in_report=COALESCE(req.countsInReport(), true), note=req.note(),
       date=CURRENT_DATE, is_deleted=false`.
    8. Atomic update: `walletRepository.adjustBalance(walletId, difference)` — cộng đúng phần
       chênh (dương hoặc âm), **không** set thẳng `actualBalance` vào `current_balance` (dù kết
       quả số học giống nhau, atomic UPDATE giữ đúng nguyên tắc "không load-modify-save" và
       không có race condition nếu ví vừa bị giao dịch khác chạm vào giữa lúc đọc và ghi — dù đã
       lock ở bước 1, giữ nhất quán cách viết code với `transfer()`).
    9. Trả `AdjustBalanceResponse(walletId, previousBalance, actualBalance, difference, true,
       transactionId, type, countsInReport)` đúng cấu trúc response mẫu `api/02-VI.md` mục 9.

    `reconcile(UUID userId, UUID walletId, boolean autoFix)`, `@Transactional`:
    1. `walletRepository.findByIdForUser(walletId, userId)` (method đã có từ plan 02-02, áp
       D-27) — không thấy → `NOT_FOUND` 404.
    2. Tính `computedBalance` bằng 1 câu `JdbcTemplate.queryForObject` native SQL theo đúng công
       thức ở `<interfaces>` (chỉ tính `is_deleted = false`):
       ```sql
       SELECT w.initial_balance
         + COALESCE(SUM(CASE WHEN t.type='income'  THEN t.amount END), 0)
         - COALESCE(SUM(CASE WHEN t.type='expense' THEN t.amount END), 0)
         - COALESCE(SUM(CASE WHEN t.type='transfer' AND t.wallet_id = w.id THEN t.amount END), 0)
         + COALESCE(SUM(CASE WHEN t.type='transfer' AND t.destination_wallet_id = w.id THEN t.amount END), 0)
       FROM wallets w
       LEFT JOIN transactions t ON (t.wallet_id = w.id OR t.destination_wallet_id = w.id) AND NOT t.is_deleted
       WHERE w.id = :walletId
       GROUP BY w.id, w.initial_balance
       ```
    3. `long difference = wallet.getCurrentBalance() - computedBalance;`
    4. Nếu `difference != 0`: **ghi log** (SLF4J `log.warn` — dùng `Logger`/`LoggerFactory` mẫu
       chuẩn Java, chưa có logger dùng sẵn ở Phase 1 nên tạo mới trong class này) kèm `walletId,
       storedBalance, computedBalance, difference, Instant.now()` — log là bắt buộc theo
       `api/02-VI.md` mục 10, đây là dấu hiệu có bug hệ thống cần điều tra, không chỉ âm thầm
       sửa số.
    5. Nếu `autoFix == true` VÀ `difference != 0`: `walletRepository.adjustBalance(walletId,
       -difference)` (đưa `current_balance` về đúng `computedBalance`), `wasFixed = true`.
       Ngược lại (`autoFix=false` hoặc `difference=0`): **không đổi gì trong DB**,
       `wasFixed = false`.
    6. Trả `ReconcileResponse(walletId, storedBalance, computedBalance, difference,
       difference == 0, wasFixed)` đúng cấu trúc `api/02-VI.md` mục 10. **KHÔNG** đăng ký
       `@Scheduled` job nào cho việc này — D-25 đã chốt bỏ job tự động, chỉ giữ endpoint thủ
       công.

    Tạo DTO: `AdjustBalanceRequest(actualBalance, countsInReport, note)` — `actualBalance` kiểu
    `Long @NotNull`. `AdjustBalanceResponse(walletId, previousBalance, actualBalance, difference,
    adjusted, transactionId, transactionType, countsInReport)`. `ReconcileResponse(walletId,
    storedBalance, computedBalance, difference, matches, wasFixed)`.

    Trong `WalletController.java`, thêm:
    - `POST /wallets/{id}/adjust-balance` — gắn `@Idempotent` (POST-tạo-mới theo D-11, vì có thể
      tạo 1 bản ghi `transactions` mới) → gọi `walletTransferService.adjustBalance(userId, id, req)`, HTTP 200 (không phải 201 — endpoint này không luôn tạo
      tài nguyên mới, `api/02-VI.md` mục 9 ghi rõ response 200).
    - `POST /wallets/{id}/reconcile` (query param `auto_fix` mặc định `false`) — **KHÔNG** gắn
      `@Idempotent` (đây là POST-hành-động dò lỗi, gọi lại nhiều lần phải luôn tính lại chứ
      không được trả cache — tương tự lý do D-11 loại `/auth/login` khỏi idempotency) → 200.

    Viết `test/java/com/datn/financeapp/wallet/WalletAdjustBalanceIntegrationTest.java` cho đủ
    7 behavior liệt kê ở trên, tên method tiếng Anh mẫu `<điều_kiện>_<hành_động>_<kết_quả>`:
    `actualBalanceLessThanCurrent_createsExpenseAdjustmentTransaction()`,
    `actualBalanceGreaterThanCurrent_createsIncomeAdjustmentTransaction()`,
    `actualBalanceEqualsCurrent_createsNoTransaction()`,
    `negativeActualBalance_returns400InvalidAmount()`,
    `afterAdjustment_reconcileStillMatches()`,
    `adjustmentTransaction_respectsCountsInReportFlag()`,
    `reconcile_detectsDrift_doesNotSilentlyFixWhenAutoFixFalse()`.
  </action>
  <acceptance_criteria>
    - `grep -c "ck_txn_amount\|difference == 0" src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java` trả về ít nhất `1` (bằng chứng có nhánh xử lý chênh=0 không insert)
    - `grep -c "'adjustment'\|adjustment" src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java` trả về ít nhất `1`
    - `grep -c "vi_tien" src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java` trả về ít nhất `1`
    - `grep -c "log.warn\|logger.warn\|LOG.warn" src/main/java/com/datn/financeapp/wallet/service/WalletTransferService.java` trả về ít nhất `1` (bằng chứng ghi log khi lệch)
    - `grep -n "reconcile" src/main/java/com/datn/financeapp/wallet/controller/WalletController.java` không có dòng `@Idempotent` ngay phía trên
    - `grep -rn "@Scheduled" src/main/java/com/datn/financeapp/wallet/` trả về rỗng (D-25 — không job tự động)
    - `mvn -q test -Dtest=WalletAdjustBalanceIntegrationTest` exit code 0, cả 7 test case PASS
  </acceptance_criteria>
  <done>Điều chỉnh số dư sinh đúng 1 giao dịch bù (hoặc không sinh gì khi chênh=0), không ghi đè current_balance, tôn trọng counts_in_report; đối chiếu tính đúng công thức, ghi log khi lệch, không tự sửa số khi auto_fix=false, không có @Scheduled job nào.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → `POST /wallets/transfer` | `source_wallet_id`/`destination_wallet_id` chưa tin cậy — client có thể gửi id ví của người khác để thử chuyển tiền vào/ra ví không thuộc quyền mình |
| Client → `POST /wallets/{id}/adjust-balance` | Endpoint tạo giao dịch tài chính thật (ảnh hưởng `current_balance`) từ input `actual_balance` do người dùng tự nhập — sai lệch ở đây trực tiếp sai số tiền |
| Concurrent request → cùng 1 ví | Hai request chuyển tiền/điều chỉnh số dư chạy song song trên cùng ví là kịch bản thực tế (người dùng bấm hai lần do mạng chậm, hoặc 2 thiết bị cùng thao tác) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering (race condition / lost-update) | `WalletTransferService.transfer` khi 2 request đồng thời trên cùng ví | mitigate | `@Lock(PESSIMISTIC_WRITE)` khoá cả 2 ví theo thứ tự `UUID.compareTo()` cố định trước khi đọc/ghi số dư; atomic `UPDATE ... SET current_balance = current_balance + :delta` thay vì load-modify-save; test `WalletTransferConcurrencyTest` (D-28 mục 1) chứng minh số dư cuối đúng tuyệt đối sau 2 luồng song song |
| T-02-12 | Tampering (deadlock tự gây ra) | Lock 2 ví không theo thứ tự nhất quán khi A→B và B→A chạy đồng thời | mitigate | Luôn sắp `sourceId`/`destinationId` theo `compareTo()` tăng dần trước khi gọi `findByIdForUpdate`, bất kể vai trò nguồn/đích trong request |
| T-02-13 | Information Disclosure / IDOR | `POST /wallets/transfer` chuyển tiền vào/từ ví không thuộc quyền `userId` | mitigate | Sau khi lock cả 2 ví, kiểm tra lại `wallet.getUserId().equals(userId)` cho CẢ HAI ví (không chỉ ví nguồn) — không khớp trả `NOT_FOUND` 404, không phải 403, không tiết lộ ví đích có tồn tại hay không |
| T-02-14 | Tampering (replay không idempotent) | `POST /wallets/{id}/adjust-balance` gọi lại nhiều lần do mạng đứt giữa chừng, client tự động retry | mitigate | Gắn `@Idempotent` (CORE-03) — cùng `Idempotency-Key` trong 24h trả lại đúng kết quả lần đầu, không tạo thêm giao dịch bù thứ hai; nếu client KHÔNG gửi `Idempotency-Key` thì đây là hành vi cố ý của client (kiểm kê lại lần 2 là hợp lệ về nghiệp vụ — số dư đã đổi sau lần 1 nên `difference` lần 2 sẽ khác, không phải lỗi trùng lặp) |
| T-02-15 | Repudiation | `reconcile` phát hiện lệch nhưng không log | mitigate | Bắt buộc `log.warn` kèm đủ `walletId, storedBalance, computedBalance, difference, timestamp` mỗi khi `difference != 0`, kể cả khi `autoFix=false` không sửa gì — đây là bằng chứng điều tra duy nhất, không được bỏ qua |
| T-02-16 | Elevation of Privilege | `reconcile` với `auto_fix=true` âm thầm ghi đè `current_balance` mà không qua kiểm tra quyền | mitigate | `reconcile` dùng `findByIdForUser` (đã áp D-27) trước khi cho phép `auto_fix`, không khớp quyền trả 404 trước khi chạm tới bất kỳ thao tác ghi nào |
</threat_model>

<verification>
1. `mvn -q test -Dtest=WalletTransferIntegrationTest,WalletTransferConcurrencyTest,WalletAdjustBalanceIntegrationTest` → exit 0
2. Chạy lại `WalletTransferConcurrencyTest` 5 lần liên tiếp, không flaky
3. Đọc `WalletController.java` xác nhận `POST /wallets/transfer` và `POST /wallets/{id}/adjust-balance` có `@Idempotent`, `POST /wallets/{id}/reconcile` KHÔNG có
4. `grep -rn "@Scheduled" src/main/java/com/datn/financeapp/wallet/` → rỗng (D-25)
5. Đọc lại `WalletTransferService.adjustBalance` xác nhận nhánh `difference == 0` return sớm, không có câu INSERT nào chạy qua nhánh đó
</verification>

<success_criteria>
- WALLET-06, WALLET-07, WALLET-08 hoạt động đúng theo `api/02-VI.md` mục 8-10
- Chuyển tiền atomic, khoá theo thứ tự cố định, test đồng thời chứng minh không lost-update
- Điều chỉnh số dư không ghi đè `current_balance`, tôn trọng `counts_in_report`, chênh=0 không tạo giao dịch
- Đối chiếu tính đúng công thức, ghi log khi lệch, không tự sửa số nếu `auto_fix=false`, không có job `@Scheduled` nào
- Mọi thao tác trên ví (nguồn/đích/điều chỉnh) kiểm tra quyền D-27, không có quyền trả 404
</success_criteria>

<output>
After completion, create `.planning/phases/02-vi-danh-muc/02-04-SUMMARY.md`
</output>
