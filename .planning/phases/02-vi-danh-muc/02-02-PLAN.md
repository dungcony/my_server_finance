---
phase: 02-vi-danh-muc
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java
  - src/main/java/com/datn/financeapp/wallet/service/WalletService.java
  - src/main/java/com/datn/financeapp/wallet/controller/WalletController.java
  - src/main/java/com/datn/financeapp/wallet/dto/CreateWalletRequest.java
  - src/main/java/com/datn/financeapp/wallet/dto/UpdateWalletRequest.java
  - src/main/java/com/datn/financeapp/wallet/dto/WalletResponse.java
  - src/main/java/com/datn/financeapp/wallet/dto/WalletSummaryResponse.java
  - src/main/java/com/datn/financeapp/wallet/dto/WalletDetailResponse.java
  - src/main/java/com/datn/financeapp/wallet/dto/WalletStatsDto.java
  - src/main/java/com/datn/financeapp/wallet/dto/ReorderWalletsRequest.java
  - src/test/java/com/datn/financeapp/wallet/WalletCrudIntegrationTest.java
  - src/test/java/com/datn/financeapp/wallet/WalletAccessControlIntegrationTest.java
autonomous: true
requirements: [WALLET-01, WALLET-02, WALLET-03, WALLET-04, WALLET-05]

must_haves:
  truths:
    - "User tạo ví mới, tên không trùng trong phạm vi của mình, current_balance = initial_balance lúc tạo, không sinh giao dịch nào"
    - "User sửa ví qua PATCH không thể đổi current_balance/type — cố gửi 2 trường này bị từ chối với mã BALANCE_NOT_EDITABLE"
    - "User xoá ví (mềm) bị chặn nếu ví còn giao dịch và không truyền delete_transactions=true, và không xoá được ví cuối cùng"
    - "User sắp xếp lại thứ tự ví qua PATCH /wallets/reorder"
    - "User A gọi GET/PATCH/DELETE ví của user B nhận 404, không phải 403"
  artifacts:
    - path: "src/main/java/com/datn/financeapp/wallet/service/WalletService.java"
      provides: "CRUD ví + validate business rule (tên trùng, không sửa balance/type, không xoá ví cuối)"
    - path: "src/main/java/com/datn/financeapp/wallet/controller/WalletController.java"
      provides: "Endpoint GET/POST/PATCH/DELETE /wallets, GET /wallets/summary, PATCH /wallets/reorder"
    - path: "src/test/java/com/datn/financeapp/wallet/WalletAccessControlIntegrationTest.java"
      provides: "Test D-28 mục 3 — user A gọi ví của user B trả 404"
  key_links:
    - from: "src/main/java/com/datn/financeapp/wallet/controller/WalletController.java"
      to: "src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java"
      via: "WalletService gọi WalletRepository với điều kiện quyền D-27 ngay trong @Query"
      pattern: "w.user_id = :currentUser OR w.group_id IN"
---

<objective>
Xây dựng CRUD ví đầy đủ (WALLET-01 đến WALLET-05): xem danh sách/tổng quan/chi tiết, tạo, sửa, xoá mềm, sắp xếp lại thứ tự. Không bao gồm chuyển tiền/điều chỉnh số dư/đối chiếu (thuộc plan 02-04, vì đó là nghiệp vụ có atomic balance update + concurrency, tách riêng theo nguyên tắc plan 2-3 task).

Purpose: Đây là phần API ví "đọc và cấu hình" — nền cho module giao dịch ở Phase 3 cần danh sách ví hợp lệ để gán vào giao dịch.

Output: `wallet/service/WalletService.java`, `wallet/controller/WalletController.java`, DTO đầy đủ, mở rộng `WalletRepository` với các query có điều kiện quyền D-27, test CRUD happy-path + test quyền truy cập 404 (D-28 mục 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-vi-danh-muc/02-CONTEXT.md
@.planning/phases/02-vi-danh-muc/02-PATTERNS.md
@../../api/02-VI.md

# Pattern nguồn (Phase 1, dùng lại nguyên vẹn)
@src/main/java/com/datn/financeapp/auth/controller/AuthController.java
@src/main/java/com/datn/financeapp/auth/service/AuthService.java
@src/main/java/com/datn/financeapp/auth/repository/RefreshTokenRepository.java
@src/main/java/com/datn/financeapp/common/response/ApiResponse.java
@src/main/java/com/datn/financeapp/common/exception/BusinessException.java
@src/main/java/com/datn/financeapp/common/security/SecurityContextUtil.java
@src/main/java/com/datn/financeapp/common/idempotency/Idempotent.java
@src/main/java/com/datn/financeapp/wallet/entity/Wallet.java
</context>

<interfaces>
<!-- Wallet entity đã tồn tại từ plan 02-01 — dùng thẳng, KHÔNG tạo lại:
     id, userId, groupId, name, type, initialBalance, currentBalance, includeInTotal,
     icon, color, sortOrder, isDeleted, createdAt -->

<!-- Điều kiện quyền D-27 — BẮT BUỘC trong mọi @Query select/update/delete 1 ví theo id.
     group_members chưa có entity JPA (Group thuộc Phase 5) — dùng native SQL subquery: -->
```java
@Query(value = "SELECT * FROM wallets w WHERE w.id = :id AND NOT w.is_deleted "
        + "AND (w.user_id = :currentUser OR w.group_id IN "
        + "(SELECT group_id FROM group_members WHERE user_id = :currentUser AND status = 'active'))",
        nativeQuery = true)
Optional<Wallet> findByIdForUser(@Param("id") UUID id, @Param("currentUser") UUID currentUser);
```
Vế nhóm luôn trả rỗng ở Phase 2 (chưa ai `INSERT` vào `group_members`) — không cần entity `Group`
tồn tại, bảng đã có sẵn từ V1. Không có quyền → BusinessException("NOT_FOUND", 404, ...).

<!-- Response envelope dùng lại nguyên vẹn: -->
```java
public record ApiResponse<T>(boolean success, T data) {
    public static <T> ApiResponse<T> of(T data) { return new ApiResponse<>(true, data); }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WalletRepository (mở rộng) + WalletService — CRUD + validate nghiệp vụ</name>
  <read_first>
    - src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java (file đã có từ plan 02-01, MỞ RỘNG thêm method, không tạo file mới)
    - src/main/java/com/datn/financeapp/wallet/entity/Wallet.java
    - ../../api/02-VI.md mục 1 (danh sách + filter type/only_in_total/include_shared), mục 2 (summary — công thức personal_total/shared_total KHÔNG cộng đôi), mục 4 (tạo ví — WALLET_NAME_EXISTS, current_balance=initial_balance, sort_order=max+1), mục 5 (sửa ví — BALANCE_NOT_EDITABLE khi cố sửa current_balance/type), mục 6 (xoá ví — WALLET_HAS_TRANSACTIONS, CANNOT_DELETE_LAST_WALLET), mục 7 (reorder)
    - db/migration/V1__nen_tang.sql dòng 87-125 (bảng wallets, unique index uq_wallets_user_name theo lower(name), ck_wallets_type)
    - src/main/java/com/datn/financeapp/auth/service/AuthService.java dòng 75-110 (mẫu @Transactional trên method, mẫu build entity + Instant.now() dùng chung, mẫu throw BusinessException khi trùng)
  </read_first>
  <behavior>
    - Test 1: `POST /wallets` với tên chưa trùng → 201, `current_balance == initial_balance`, không có bản ghi `transactions` nào được tạo
    - Test 2: `POST /wallets` với tên đã trùng (cùng user, không phân biệt hoa thường) → 409 `WALLET_NAME_EXISTS`
    - Test 3: `PATCH /wallets/{id}` gửi field `current_balance` hoặc `type` trong body → 400 `BALANCE_NOT_EDITABLE`
    - Test 4: `DELETE /wallets/{id}` khi ví còn giao dịch và không truyền `delete_transactions=true` → 409 `WALLET_HAS_TRANSACTIONS`
    - Test 5: `DELETE /wallets/{id}` khi đây là ví cuối cùng của user → 409 `CANNOT_DELETE_LAST_WALLET`
    - Test 6: `PATCH /wallets/reorder` với mảng id theo thứ tự mới → `sort_order` được gán đúng theo vị trí trong mảng
  </behavior>
  <action>
    Mở rộng `WalletRepository.java` (đã có từ plan 02-01) thêm các method sau, tất cả kèm điều
    kiện quyền D-27 (native query, `w.user_id = :currentUser OR w.group_id IN (SELECT group_id
    FROM group_members WHERE user_id = :currentUser AND status = 'active')`):
    - `findByIdForUser(UUID id, UUID currentUser)` → `Optional<Wallet>`, lọc `NOT is_deleted`
    - `findAllForUser(UUID currentUser, String type, Boolean onlyInTotal, boolean includeShared)`
      → `List<Wallet>`, sắp theo `sort_order` (dùng JPQL hoặc native tuỳ Claude's Discretion,
      nhưng vế quyền D-27 bắt buộc trong câu SQL)
    - `existsByUserIdAndNameIgnoreCaseAndIsDeletedFalse(UUID userId, String name)` → `boolean`
      (kiểm tra trùng tên, dùng cho `WALLET_NAME_EXISTS`)
    - `findMaxSortOrderByUserId(UUID userId)` → `Integer` (native `SELECT COALESCE(MAX(sort_order), -1) FROM wallets WHERE user_id = :userId AND NOT is_deleted`)
    - `countByUserIdAndIsDeletedFalse` đã có sẵn từ plan 02-01, dùng lại cho `CANNOT_DELETE_LAST_WALLET`

    Tạo `wallet/service/WalletService.java` (`@Service @RequiredArgsConstructor`,
    `@Transactional` trên từng public method theo mẫu `AuthService`):
    - `list(userId, type, onlyInTotal, includeShared)` → gọi `findAllForUser`, map sang
      `WalletResponse` (record: `id, name, type, currentBalance, includeInTotal, isShared` (suy
      từ `groupId != null`), `groupId, icon, color, sortOrder, createdAt`)
    - `summary(userId)` → tính `personal_total` (tổng `current_balance` các ví `user_id=userId`
      và `include_in_total=true` và chưa xoá), `personal_wallet_count`, `shared_total` (tính
      riêng ví có `group_id` mà user là thành viên — Phase 2 luôn = 0 vì chưa ai vào nhóm, vẫn
      viết đúng logic để không phải sửa lại ở Phase 5), `shared_wallet_count`, `by_type` (group
      theo `type`, JdbcTemplate hoặc query riêng — theo `api/02-VI.md` mục 2). **Không cộng đôi
      personal và shared** — đây là điểm dễ sai nhất của endpoint này
    - `detail(userId, walletId)` → `findByIdForUser` không thấy thì `BusinessException("NOT_FOUND",
      404, "Không tìm thấy ví.")`; map sang `WalletDetailResponse` kèm `stats` (dùng JdbcTemplate
      COUNT/SUM tương tự mẫu `AuthService.getMe()` dòng 240-245 — `transaction_count`,
      `income_this_month`, `expense_this_month`, `last_transaction_date`; Phase 2 chưa có bảng
      transactions ORM nên native SQL trực tiếp lên bảng `transactions` là hợp lý, transactions
      module thuộc Phase 3)
    - `create(userId, req)`: check `existsByUserIdAndNameIgnoreCaseAndIsDeletedFalse` → 409
      `WALLET_NAME_EXISTS`; nếu `req.groupId() != null` thì tạm thời luôn ném
      `BusinessException("NOT_GROUP_MEMBER", 403, "Chưa hỗ trợ tạo ví chung ở Phase 2 — Group
      thuộc Phase 5.")` (Group chưa tồn tại, không tự bịa logic kiểm tra thành viên); build
      `Wallet` với `id=UUID.randomUUID()`, `currentBalance = initialBalance`,
      `sortOrder = findMaxSortOrderByUserId(userId) + 1`, `isDeleted=false`,
      `createdAt=Instant.now()`; **không tạo bản ghi giao dịch nào**
    - `update(userId, walletId, req)`: `UpdateWalletRequest` KHÔNG có field `currentBalance`
      hay `type` trong DTO (chặn ở tầng hợp đồng, không phải chỉ validate) — nếu request JSON
      gửi thêm `current_balance` hoặc `type` mà DTO không có field tương ứng, Jackson mặc định
      bỏ qua field lạ; để đúng yêu cầu API trả lỗi tường minh `BALANCE_NOT_EDITABLE` khi client
      cố gửi 2 trường này, dùng `Map<String, Object> rawBody` hoặc `@JsonAnySetter` bắt field lạ
      trong DTO và kiểm tra key `current_balance`/`type` có mặt thì ném
      `BusinessException("BALANCE_NOT_EDITABLE", 400, "Không sửa được số dư và loại ví qua PATCH.")`
      trước khi áp update; các field cho phép: `name, includeInTotal, icon, color`, PATCH bán
      phần (null nghĩa là không đổi, theo mẫu `updateProfile` của `AuthService`)
    - `delete(userId, walletId, deleteTransactions)`: `findByIdForUser` không thấy → 404;
      `countByUserIdAndIsDeletedFalse(userId) <= 1` → 409 `CANNOT_DELETE_LAST_WALLET`; đếm giao
      dịch chưa xoá của ví (native COUNT lên bảng `transactions` — bảng đã tồn tại từ V2, dù
      Phase 3 mới xây service) — còn giao dịch mà `deleteTransactions != true` → 409
      `WALLET_HAS_TRANSACTIONS`; nếu `deleteTransactions=true` thì xoá mềm luôn giao dịch liên
      quan (`UPDATE transactions SET is_deleted=true WHERE wallet_id=:id OR
      destination_wallet_id=:id`) trước khi xoá mềm ví; **idempotent theo CORE-06** — gọi
      DELETE lần 2 trên ví đã `is_deleted=true` vẫn trả 200 (không ném lỗi 404 khi bản ghi tồn
      tại nhưng đã xoá mềm — chỉ 404 khi bản ghi không tồn tại hoặc không thuộc quyền)
    - `reorder(userId, req)`: với mỗi id trong `req.sortOrder()` theo vị trí index, gán
      `sort_order = index`; chỉ update các ví thuộc quyền user (bọc điều kiện D-27 trong
      `@Modifying @Query`, không load-modify-save từng cái một nếu tránh được — có thể dùng
      vòng lặp gọi `@Modifying @Query("UPDATE Wallet w SET w.sortOrder = :order WHERE w.id =
      :id AND w.userId = :currentUser") int updateSortOrder(...)` cho từng phần tử, chấp nhận
      N câu UPDATE nhỏ vì mảng ví của 1 user luôn nhỏ)

    Tạo DTO (record, theo mẫu `RegisterRequest`/`UserDetailDto`):
    `CreateWalletRequest(name, type, initialBalance, includeInTotal, icon, color, groupId)`,
    `UpdateWalletRequest(name, includeInTotal, icon, color)` (KHÔNG có currentBalance/type),
    `WalletResponse(id, name, type, currentBalance, includeInTotal, isShared, groupId, icon,
    color, sortOrder, createdAt)`, `WalletDetailResponse` (thêm `initialBalance, stats`),
    `WalletStatsDto(transactionCount, incomeThisMonth, expenseThisMonth, lastTransactionDate)`,
    `WalletSummaryResponse(personalTotal, personalWalletCount, sharedTotal, sharedWalletCount,
    byType)` với `ByTypeItem(type, total, walletCount)` là record lồng hoặc DTO riêng,
    `ReorderWalletsRequest(sortOrder)` (List&lt;UUID&gt;). `amount`/`initialBalance`/
    `currentBalance` luôn `Long`, không `Double`.
  </action>
  <acceptance_criteria>
    - `grep -c "w.user_id = :currentUser OR w.group_id IN" src/main/java/com/datn/financeapp/wallet/repository/WalletRepository.java` trả về ít nhất `1`
    - `grep -c "current_balance\|currentBalance" src/main/java/com/datn/financeapp/wallet/dto/UpdateWalletRequest.java` trả về `0` (DTO không có field này)
    - `grep -c "type" src/main/java/com/datn/financeapp/wallet/dto/UpdateWalletRequest.java` trả về `0`
    - `grep -c "WALLET_NAME_EXISTS\|BALANCE_NOT_EDITABLE\|WALLET_HAS_TRANSACTIONS\|CANNOT_DELETE_LAST_WALLET" src/main/java/com/datn/financeapp/wallet/service/WalletService.java` trả về ít nhất `4`
    - Test `WalletCrudIntegrationTest` (viết ở Task 2) PASS cho cả 6 behavior liệt kê ở trên
  </acceptance_criteria>
  <done>WalletService triển khai đủ list/summary/detail/create/update/delete/reorder đúng validate của api/02-VI.md mục 1-7, mọi query select ví theo id có điều kiện quyền D-27.</done>
</task>

<task type="auto">
  <name>Task 2: WalletController + test CRUD happy-path + test quyền truy cập 404</name>
  <read_first>
    - src/main/java/com/datn/financeapp/auth/controller/AuthController.java (mẫu @RestController, @RequestMapping, ResponseStatus, cách lấy userId qua SecurityContextUtil, cách gắn @Idempotent)
    - src/test/java/com/datn/financeapp/auth/AuthRegisterLoginIntegrationTest.java (toàn bộ file — mẫu Testcontainers, NoRateLimitConfig, MockMvc + jsonPath assertion, cleanTables thứ tự FK con trước cha)
    - .planning/phases/02-vi-danh-muc/02-CONTEXT.md phần D-28 mục 3 (test quyền truy cập)
  </read_first>
  <action>
    Tạo `wallet/controller/WalletController.java` (`@RestController @RequestMapping("/wallets")
    @RequiredArgsConstructor`):
    - `GET /wallets` (query params `type, only_in_total, include_shared`) → `walletService.list(...)`
    - `GET /wallets/summary` → `walletService.summary(userId)`
    - `GET /wallets/{id}` → `walletService.detail(userId, id)`
    - `POST /wallets` — gắn `@Idempotent` (POST-tạo-mới theo D-11/CONTEXT) →
      `@ResponseStatus(HttpStatus.CREATED)`, `walletService.create(userId, req)`
    - `PATCH /wallets/{id}` — KHÔNG gắn `@Idempotent` → `walletService.update(userId, id, req)`
    - `DELETE /wallets/{id}` (query param `delete_transactions` mặc định `false`) — KHÔNG gắn
      `@Idempotent` → `walletService.delete(userId, id, deleteTransactions)`, trả 200 rỗng hoặc
      `ApiResponse.of(null)` theo đúng CORE-06 (idempotent, gọi lại vẫn 200)
    - `PATCH /wallets/reorder` — KHÔNG gắn `@Idempotent` → `walletService.reorder(userId, req)`

    Mọi endpoint lấy `userId` qua `SecurityContextUtil.currentUserId()`, không nhận `user_id`
    từ body/param.

    Viết `test/java/com/datn/financeapp/wallet/WalletCrudIntegrationTest.java`: copy khung
    Testcontainers + NoRateLimitConfig từ `AuthRegisterLoginIntegrationTest.java` (đăng ký 1 user
    thật qua `POST /auth/register` lấy access token, dùng token đó gọi các endpoint ví qua
    header `Authorization: Bearer`). Viết đủ 6 test case theo `<behavior>` của Task 1 (tên method
    tiếng Anh, mẫu `<điều_kiện>_<hành_động>_<kết_quả>`, ví dụ
    `createWallet_duplicateName_returns409WalletNameExists()`,
    `patchWallet_withCurrentBalanceField_returns400BalanceNotEditable()`,
    `deleteWallet_lastWallet_returns409CannotDeleteLastWallet()`,
    `reorderWallets_returnsSortOrderMatchingArrayPosition()`).

    Viết `test/java/com/datn/financeapp/wallet/WalletAccessControlIntegrationTest.java` (D-28
    mục 3): tạo 2 user (A, B) qua `/auth/register`, B tạo 1 ví. A gọi `GET /wallets/{b_wallet_id}`,
    `PATCH /wallets/{b_wallet_id}`, `DELETE /wallets/{b_wallet_id}` bằng access token của A — cả
    3 case đều phải trả **404** `NOT_FOUND` (không phải 403). Tên method ví dụ:
    `userA_getWalletOfUserB_returns404NotFound()`.
  </action>
  <acceptance_criteria>
    - `grep -c "@Idempotent" src/main/java/com/datn/financeapp/wallet/controller/WalletController.java` trả về đúng `1` (chỉ POST tạo mới)
    - `grep -n "PATCH\|DELETE" src/main/java/com/datn/financeapp/wallet/controller/WalletController.java` không dòng nào có `@Idempotent` ở trên nó
    - `mvn -q test -Dtest=WalletCrudIntegrationTest` exit code 0
    - `mvn -q test -Dtest=WalletAccessControlIntegrationTest` exit code 0, và cả 3 case (GET/PATCH/DELETE ví người khác) assert `status().isNotFound()` + `jsonPath("$.error.code").value("NOT_FOUND")`
  </acceptance_criteria>
  <done>WalletController expose đủ 7 endpoint theo api/02-VI.md mục 1-7, test CRUD happy-path và test quyền truy cập 404 đều PASS qua Testcontainers PostgreSQL thật.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → WalletController | Toàn bộ input `name/type/initial_balance/include_in_total/icon/color/group_id` từ người dùng chưa tin cậy, phải validate ở DTO + Service |
| WalletController → WalletService (mass-assignment) | PATCH `/wallets/{id}` phải KHÔNG cho phép ghi `current_balance`/`type` dù bằng đường nào (DTO thiếu field + kiểm tra field lạ) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | `WalletRepository.findByIdForUser` | mitigate | Điều kiện quyền D-27 nằm ngay trong `@Query`, không có quyền trả `NOT_FOUND` (404) thay vì `403`, không lộ sự tồn tại của bản ghi (Task 1, Task 2 test `WalletAccessControlIntegrationTest`) |
| T-02-04 | Tampering (mass-assignment) | `UpdateWalletRequest` / `WalletService.update` | mitigate | DTO không có field `currentBalance`/`type`; nếu client cố gửi field lạ, phát hiện và trả `BALANCE_NOT_EDITABLE` (400) thay vì âm thầm bỏ qua — Task 1 |
| T-02-05 | Elevation of Privilege | `POST /wallets` với `group_id` khi Group chưa tồn tại | mitigate | Ném `NOT_GROUP_MEMBER` (403) tường minh thay vì tạo ví "chung" không kiểm chứng được thành viên — tránh tạo lỗ hổng ví chung không ai kiểm soát ở Phase 2 |
| T-02-06 | Denial of Service (xoá dữ liệu người khác) | `DELETE /wallets/{id}?delete_transactions=true` | accept | Đã kiểm tra quyền qua `findByIdForUser` trước khi xoá — nằm trong `disposition=mitigate` của T-02-03, không phải rủi ro riêng |
</threat_model>

<verification>
1. `mvn -q test -Dtest=WalletCrudIntegrationTest,WalletAccessControlIntegrationTest` → exit 0
2. Đọc `WalletController.java` xác nhận chỉ `POST /wallets` có `@Idempotent`
3. `curl` thủ công (hoặc test) xác nhận `GET /wallets/summary` không cộng đôi personal/shared khi test có ví chung (Phase 2 luôn 0 ví chung nên assert `shared_total == 0`)
</verification>

<success_criteria>
- WALLET-01 đến WALLET-05 hoạt động đúng theo `api/02-VI.md` mục 1-7
- Mọi query select/update/delete 1 ví có điều kiện quyền D-27, không có quyền trả 404
- PATCH không sửa được `current_balance`/`type`
- Test CRUD happy-path + test quyền truy cập 404 (D-28 mục 3) PASS qua Testcontainers thật
</success_criteria>

<output>
After completion, create `.planning/phases/02-vi-danh-muc/02-02-SUMMARY.md`
</output>
